"""graph — 쇼츠 파이프라인의 실행 엔진 (LangGraph).

이 패키지가 존재하는 이유는 하나다: **3시간짜리 영상화 단계 앞에 문을 세우는 것.**

    스틸 1장  =   9초
    영상 1컷  = 7분        (26컷이면 3시간, GPU 점유)

`docs/generative-shorts-pipeline.md`는 이미 "싼 단계에서 실패시켜라 — REGEN은
스틸(9초)에서, 영상(7분)에서 하지 않는다"고 적어두고 있다. 문제는 그게 **문서**라
사람이 매번 기억해서 지켜야 했다는 것이고, 한 번 건너뛰면 3시간이 날아갔다.

이 패키지는 그 규칙을 **코드**로 만든다. 전 샷이 임계 점수를 넘지 못하면
영상화 단계로 넘어가는 엣지 자체가 열리지 않는다.

부수 효과 두 가지:
  - 그래프 정의가 곧 구조도다 (`python -m graph.shorts_graph diagram`).
  - 체크포인트가 있어서 중간에 죽어도 남은 샷부터 이어서 돈다.
"""

__all__ = ["state", "tools", "nodes", "shorts_graph"]
