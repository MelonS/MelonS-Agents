# 세션 인계 — 2026-07-24 (아트 대전환일)

> 다음 세션 첫 읽기 문서. 정본 계층: `docs/GDD.md`(기획) → `docs/design-qa.md`(아트
> 프로세스) → 이 문서(당일 상태). 워룸(운영자 관전용 Artifact)은 메모리
> `reference-pawnsim-warroom` 참조 — 타 세션에서 갱신 시 url 파라미터 필수.

## 오늘 결론 한 줄

생성형 단독 아트는 장간 스타일 드리프트로 실패(스웨터→콜라주 연쇄 반려) →
**Tiny Swords 팩 하이브리드로 전환, 운영자 "대만족", 씬 통일 완성 단계 도달.**

## 확정 사항 (뒤집지 말 것)

1. **아트 노선 = B2 하이브리드**: TS 팩(지형·나무·자원·유닛·양) + 부족분은
   ①가구·구조물=**절차 드로잉**(`scripts/gen-ts-furniture.py`, `gen-ts-props.py` —
   TS 관찰 팔레트·웜브라운 아웃라인·3톤 셀셰이딩) ②유기물=**TS 스타일 LoRA** 정합 생성.
2. **팩 라이선스**: 상업OK·크레딧불요·재배포금지 → `ts_*` git 미추적,
   재현=`scripts/extract-tinyswords.py` (팩 원본 `G:\ai\_artpacks\TinySwords`).
   ATTRIBUTIONS.md 등재됨. **repo 공개 유지 시 ts_* 미추적 유지 필수.**
3. **아트 프로세스 = design-qa.md 5단계** (시안→TA→운영자 픽→인게임 재채점→커밋).
   TA 롤 `.claude/agents/ta.md` (다음 세션부터 Agent 타입으로 사용 가능).
4. **기획 정본 = GDD.md** — 컷 전 코드 재검증 룰 포함. 교역·socialMul 컷 취소됨
   (실동작 확인). goal(stop-hook)은 운영자가 세션 종료와 함께 취소.

## ✅ TS 스타일 LoRA — 훈련 완료 (2026-07-24 21:18, 2000스텝 완주)

- **설치됨**: `ComfyUI/models/loras/tswords_v1.safetensors` (원본+체크포인트
  500/1000/1500 은 `G:\ai\ai-toolkit\output\tswords_v1\`)
- 사용: `scripts/flux-still.py --lora tswords_v1.safetensors`, 트리거
  `"tswords style game sprite, a ..."` — 최종 샘플 4종 확인:
  `G:\ai\_pawnsim_시안\아트비교\54_LoRA_최종샘플_2000.png` (**멧돼지가 증거 —
  동물 3종 정합 생성 준비 완료.** 가구류는 색 드리프트 있음 → 가구는 절차
  드로잉 유지, LoRA 는 유기물 전담 권고. 과하면 1500 체크포인트 시도)
- HF 게이트 우회 = YuCollection 미러 (메모리 `reference-flux-hf-mirror`)
- 원단 소스 회수: `art-src/`(잔디 원단 3종 커밋) + 전체 작업본
  `G:\ai\_pawnsim_시안\_art_v3_작업본` (30MB)

## 남은 아트 큐 (우선순)

1. **화덕·연구대·벽·문·저장선반** — 절차 드로잉 (gen-ts-furniture.py 확장).
   벽은 W1/W2 오토타일(lookfeel 문서)과 묶어서 — TS Elevation 절벽타일 참고.
2. **동물 3종** — LoRA 완성 후 정합 생성 (양 스타일 앵커).
3. **풀 스캐터** — ScatterVarietyDriver 를 ts_deco_01~18 로 교체.
4. 곡물자루·간편식 등 잔여 아이템 (ItemArt32 TsOverride 확장).
5. 초기 메뉴 키아트 — TS 톤과 재조화 검토 (현 수채 키아트 유지 여부 운영자 픽).
6. WebGL 재빌드 + Pages 배포 (localhost:8931 은 구세대 아트).

## 아트 외 남은 큐 (GDD §5)

- **G3 무덤·매장 = 출하됨** (repro 시나리오 p2-grave-burial 은 미작성 — 후속).
- 멘탈브레이크 3티어 (승인·스펙 완비) → 밸런스 B군 소크 10건 (B1 무드 모델은
  단독 라운드) → C군 판단 8건 (운영자).
- G1 잡담 (픽 대기, 스펙 `design-social-2026-07-24.md` 완비).
- p1 플레이크 강건화 (PLAYTEST-TODO).

## 오늘 커밋 (전부 게이트 그린·푸시됨)

`2321744` GDD+밸런스조사 · `7be3142` TA/디자인QA · `5b03735` 밸런스A+NRE ·
`19a9508` TS 전환+G3 · `c799ab9` 절차 가구 · `1fa8f24` TS 유닛 · `372b5b6` 절차 자연물

## 함정 리마인더

- 게이트 중 소스 편집 금지 · cwd 항상 명시(`cd /g/ai/MelonS-Agents`) ·
  Mode enum append-only 계약 · rng 체인 소비 보존 · TS 타일 flip 금지 ·
  Resources 신규 png 는 베이크 임포트 블록(Entities.cs)에 PPU 등록 필수.
