> 출처: 게임루프 감사 워크플로 wf_0e04d4e2 (4축 병렬 코드 추적 + 합성).
> 운영자 평결 (2026-06-11): "레퍼런스 콜로니심는 생존게임+이벤트 대응+발전 시뮬+RPG 인데 이 요소들이 돌아가지 않는다"
> + "가만히 냅둬도 게임오버가 되지 않음. 가장 큰 문제점."
> 본 세션 선행 구현: 아사 데미지(TakeTrueDamage — 방어구 무시) + 전멸 오버레이 + p0-survival-pressure 재현.

# 콜로니심 게임루프 감사 합성 — 4축 통합 백로그\n\n## 1. 평결: "냅두면 게임오버"까지 끊긴 사슬\n\n엔진은 살아 있는데 죽음으로 가는 문이 전부 잠겨 있다. 소비·붕괴·전투·아사 데미지(food 0 → 12초당 1HP, 이번 세션 구현완료 — 재제안 금지)는 모두 실동작하지만, 그 사이를 잇는 고리 4개가 끊겨 "방치 → 1.5일차 mood 붕괴 → 좀비 콜로니 영구 생존"에서 게임이 멈춘다. 구체적으로: **(a)** 베리덤불 1개가 60실초/5베리 재생으로 림 16명분을 무한 공급해 식량 희소성이 0이고(농사·사냥·요리가 전부 선택 불요), **(b)** 정신붕괴 중에도 자율 섭취가 계속 돌아 mood가 생사 수치로 전이되지 않으며, **(c)** 유일한 외부 살상 압력인 습격은 1~2인 캡 + 비징집 자동반격으로 ~5초 자가 해결되거나 벽 한 겹(적의 구조물 공격 호출자 0)으로 영구 무효이고, **(d)** 그 습격마저 threatTier 미설정 버그로 경보 카드+카메라 팬 사슬이 통째로 죽어 플레이어가 인지조차 못 한다. 아래 TOP 10 중 #2(희소화)+#4(붕괴 섭취 금지)로 "방치 → 3~4일차 아사 전멸"이 성립하고, #1(threatTier)+#10(구조물 공격)으로 습격이 진짜 위협이 된다 — 신규 시스템 발명 0, 전부 기존 뼈대에 수치·호출을 연결하는 작업이다.\n\n---\n\n## 2. TOP 10 백로그 (임팩트÷노력 순)\n\n### 1. bandit_raid threatTier=2 — 끊긴 경보 사슬 복구 [버그픽스]\n- **근거(이벤트)**: SpawnRaid의 raidEv가 tier 기본 0 → AlertStackUI.cs:186의 `threatTier < 1` 필터가 습격만 걸러냄. 상인(tier2)은 영구 카드를 받는데 습격은 못 받는 신호 위계 역전.\n- **방법**: `AIDirector.cs:250-256` raidEv 초기화에 `threatTier = 2` **한 줄**. AlertStackUI 무수정 — tier≥2 영구 카드(245행)·클릭 시 강도 팬(ResolveThreatTarget 강도 우선)은 이미 완성.\n- **Effort: S** (1줄)\n\n### 2. 베리 재생 희소화 — 공짜 음식 무한 차단\n- **근거(생존)**: BerryBushEntity.cs:31 — 60실초/5베리 = 덤불 1개가 림 16명분 공급. 레퍼런스 콜로니심 생존 축의 전제("야생만으론 못 버팀")가 부재.\n- **방법**: `BerryBushEntity.cs` bushRegenSec 60 → ~1200실초(≈1.2게임일), initialBerries 5 → 3 → 덤불 1개 ≈ 림 0.7일치. **주의**: [SerializeField]라 씬에 구워진 60이 코드값을 덮어씀 — GameClock.cs:39 패턴대로 평 private 전환 또는 코드 clamp.\n- **Effort: S**\n\n### 3. 식사 영양 위계 — 조리 사슬을 흑자로 (생존+발전 중복 지적)\n- **근거(생존·발전 양축)**: eatRestore 30이 베리·raw·meal·fine 전원 동일(PawnNeeds.cs:57) + 요리 3raw→1meal(StoveEntity.cs:27) = 조리가 영양 -66% 가치 파괴. better_stove는 손실을 2배속 가속.\n- **방법**: `PawnNeeds.cs` ConsumeAtSource(599-673) 단일 eatRestore를 3필드 분리 — **raw(베리/생고기) 20 / meal 40 / fine 55**. `food += eatRestore` 지점 치환(615, 629, 639, 652, 668행). 한 파일.\n- **Effort: S**\n\n### 4. 정신붕괴 중 자율 섭취 금지 + 양성 thought 3종 배선 — 죽음 나선 완성 (동일 배치 필수)\n- **근거(생존)**: 붕괴 중에도 PawnNeeds 자율 섭취가 돌아 mood 0 콜로니도 영구 생존. mood 공급측(양성 thought)은 카탈로그만 있고 호출처 0 — 압력만 올리면 못 막는 일방 난이도가 되므로 둘을 한 배치로.\n- **방법**: (a) `PawnNeeds.cs:458` TryPhysicalEatTick() 앞 `if (isBreaking)` 게이트 1줄 → 방치 시 붕괴→섭취 정지→food 0→기존 아사 데미지로 3~4일차 전멸 성립. 회복 경로 보존: moodBreakRecoverAt(35)용 침대 MoodBonus는 계속 작동. (b) `PawnNeeds.cs` 기상 지점(319-323, 396)에서 bed!=null → AddThought("침대에서 잠"), sleep≥95 → "푹 잠"; `PawnThoughts.cs:116` 환류부에 IsOnFloor() → "따뜻한 실내". 기존 AddThought 패턴 복제만.\n- **Effort: S**\n\n### 5. 수면 자연 치유 — 부상→휴식→회복 루프 완성\n- **근거(RPG)**: 부위 HP 회복이 의사 tend(+5)뿐인데 의사는 다운/출혈만 잡음(PawnActions.cs:387) → 다리 5/20 림은 영구 37% 이속 불구. 침대의 회복 가치 0.\n- **방법**: `PawnNeeds.cs` IsSleeping 분기(~356-392, 이미 bed.RestMul 참조)에서 게임시간 1시간 주기 `GetComponent<PawnHealth>().HealAll(bed!=null ? 2 : 1)`. 기존 HealAll(PawnHealth.cs:190) 재사용, 신규 시스템 0.\n- **Effort: S**\n\n### 6. simple_bow 완료 시 활 지급 — 죽은 테크 보증 제거\n- **근거(발전)**: 발사에 활 장착 필요(PawnUtilityAI.cs:492)인데 장비는 스폰 랜덤 ~20%뿐, 획득 경로 0 → 활 없는 콜로니는 연구해도 변화 0.\n- **방법**: `ResearchManager.cs:144` 완료 블록에서 FindObjectsByType\\<PawnEntity\\> 순회, 원거리 무기 없는 폰에게 Catalog '활' Equip(PawnEquipment.Equip 재사용).\n- **Effort: S**\n\n### 7. XP 커브 하향 + 레벨업 피드백 — 성장 체감\n- **근거(RPG)**: 1000*L^1.5 커브 vs 수입률 = L8→9 건축 90분 연속, 한 세션 레벨업 0회. OnLevelUp 구독자 0 — 곱셈 배선은 살아있으나 성장이 결정에 닿지 않음.\n- **방법**: `PawnSkills.cs:78` 1000f → 200f(목표: 1세션 1~2렙). 같은 패치에서 레벨업 시 `FloatingText.Spawn(..., "건축 Lv9!")` 1줄(PawnDoctor.cs:89 패턴 그대로).\n- **Effort: S**\n\n### 8. 수면 박탈 대가 — 속도 페널티 + 밤잠의 시간 비용\n- **근거(생존)**: 회복 8/s vs 감쇠 0.12/s = 67:1, 밤잠이 하루의 ~10초. 저수면 페널티 훅 0개라 수면은 실패 불가능한 수치, 침대 RestMul이 경제에 안 닿음.\n- **방법**: `PawnUtilityAI.cs:184` MoveSpeedScale 단일 출처에 `needs.sleep<25 → ×0.5` 1줄. `PawnNeeds.cs:47` sleepRegenAtNight 8 → 3(밤잠 ~17실초, 바닥취침 0.6x면 ~28실초) → 침대 0.8/1.0/1.4가 처음으로 노동시간 경제에 닿음.\n- **Effort: S**\n\n### 9. 습격 상한을 생존 림 수에 연동 — 중반 위협 평탄화 해소\n- **근거(이벤트·발전 양축)**: MaxConcurrentGroups=2 영구 고정 — 콜로니는 계속 강해지는데 위협 불변 → 발전이 의무가 아닌 취향이 됨.\n- **방법**: `AIDirector.cs:234-236` clamp 상한 → `Mathf.Max(MaxConcurrentGroups, 생존 PawnEntity 수 - 1)` (FindAllNeeds 패턴 재사용). 3림 기본은 현행 동일(5/31 wipe 재발 없음), 5림이면 cap 4. 코드 3줄.\n- **Effort: S**\n\n### 10. 막힌 강도의 구조물 공격 — '벽 = 영구 무적' 제거 (이벤트+발전 중복 지적)\n- **근거(이벤트·발전 양축)**: WallEntity.TakeDamage 게임플레이 호출자 0(테스트만) → 벽 한 겹이면 습격 영구 무효, 석벽 HP 280 vs 목벽 100이 무의미(석벽 = 더 비싼 목벽).\n- **방법**: `BanditEnemy.cs` TryStep(153-175) 3-슬라이드 전부 실패 ~2s 누적 시 진행 방향 셀에 Physics2D.OverlapBox로 WallEntity/DoorEntity/BarricadeEntity 탐색 → 기존 contactDamage·damageInterval 스로틀로 TakeDamage(WallEntity.cs:113, HP tint 피드백 완비) 호출. 기존 피해 API 배선만, 기존 정지 동작은 비전투 폴백 유지. 벽이 '면제'→'시간 벌기'로, 자재 선택이 처음으로 결정이 됨.\n- **Effort: M**\n\n**차순위 (P1~P2/S, 위 10개 안정화 후)**: 식탁 배선(ConsumeAtSource에서 TableEntity.EatSpotAt 호출 — 호출자 0인 probe 1줄, 툴팁 거짓말 해소) · 시작 스킬 개인차(PawnTraits.cs:131 이름 해시 패턴 복제 ReRollFromName) · 팔 손상→작업속도(죽은 PawnHealth.WorkSpeedMultiplier를 워커 5지점에 곱) · 석공술/관개 테크 2개(연구 아크 연장, M) · 늑대 재활성+자동방어 대상 확장 · wanderer_arrival 실합류 · 폭풍 물리 비용(부패 ×4) · 강도 60s 무전투 퇴각 · 호전적 kill thought.\n\n---\n\n## 3. 이미 돌아가는 것 — 건드리지 말 것\n\n**생존/니즈**\n- 니즈 감쇠→물리 섭취 상태기계 (PawnNeeds.cs:415-489, 순간이동 차감 제거됨)\n- **아사 사슬: food 0 → 12초당 1HP → ~6실분 사망 (PawnNeeds.cs:424-438) — 이번 세션 구현, 재제안 금지**\n- 탈진(<15) 제자리 강제취침 자가복구 (PawnNeeds.cs:381-394)\n- 정신붕괴(<20)→전 워커 ClearTask+배회 (PawnUtilityAI.cs:289-307)\n- 부정 thought 델타 환류: 배고픔/수면부족/폭풍야외/부상 (PawnThoughts.cs:116-146)\n- 야외 고기더미 부패→haul 결정 연결 (MeatPileEntity.cs:59), 사냥 트리거(전체 식량<5, PawnActions.cs:143-147), 요리 3:1 카운터+물리 동기 차감 (StoveEntity.cs:24-32), 농사 harvestFood 14 물리 드롭 (CropEntity.cs:121)\n\n**이벤트/전투**\n- 습격 스케줄러: day≥2, 06:00, 3일 간격, 점증, save/load 복원 (AIDirector.cs:202-259, 147)\n- BanditEnemy 실추격·실피해→PawnHealth 부위피해·다운·사망 (BanditEnemy.cs:113-146)\n- 비징집 자동반격 (PawnEntity.cs:221-251), 징집 전투 (SetDrafted + HandleDraftedCombat), 포로 50% 합류 (BanditEnemy.TryCapture)\n- 이벤트 실효 3종(lucky_find/morale_dip/minor_disease, AIDirector.cs:442-480), storm 즉시 전환+야외 -6 (WeatherController.cs:65-68), trader 60실초 실거래 6종 (TraderEntity.cs:70-114)\n- 경보 3채널: 사이렌, ThreatAlertUI 근접 플래시, AlertStackUI tier 카드+카메라 팬 (#1 픽스로 습격에도 개통됨)\n- 동료 사망 -15×30분 전파→연쇄 붕괴 (PawnHealth.cs:298-305)\n\n**발전/RPG**\n- better_stove 조리 2배 (PawnCook.cs:101-103), 연구 루프 자체구동 (ResearchManager.cs:92-152)\n- 침대 RestMul/MoodBonus 실소비 (BedEntity.cs:43-50 → PawnNeeds), 바닥 이동 보너스 목 1.3x/석 1.5x (FloorEntity→PawnMovement.cs:581,626)\n- 스킬 곱셈 배선: 작업 +4%/lvl, 전투 +3%/lvl + save/load (워커 4종 + PawnUtilityAI.cs:537,562)\n- 특성 7/8 실배선 (workSpeedMul/moveSpeedMul/maxHpMul/미식가/무던/호전적XP)\n- 부위 건강 체인: 다리→이속, 머리≤30%→다운, 머리·몸통 0→사망, 새 상처→붕대 해제 (PawnHealth.cs:170,225,262-312)\n- 의사 tend 루프 (PawnActions.cs:361-394 → PawnDoctor.cs:71-92), 장비 실효(방어구 감소/무기 가산/원거리 게이트), 능력치 ±15% 곱, per-pawn 작업 우선순위 AI 준수 (PawnUtilityAI.cs:382-387)\n\n✅완료
