> **집행 현황 (2026-06-12)**: TOP 8 전부 ✅ + E(아사 임박 경고) ✅ + J 중 거래 개통/
> LowAlert 인구연동/ColonistBar 위기 테두리 ✅, keepOpen 은 조사 결과 이미 정상.
> 마퀴 플레이크 → 프로브 '선택 집합' 의미론으로 클로즈.  잔여 = 운영자 결정 항목
> (A 동물 산물 / B 사냥 지정 / C 여가 슬롯 / D 습격 예고 / F 테크 tier2 / G 늑대 /
> H 수리 / I 다중명령 / 상인 장비 2종) + 드래그 앵커 잔존(증거 미확장 — 차회 조사).

> 출처: 2차 감사 워크플로 wf_c63d9541 (4렌즈, 1차 16항목 출하 이후 스냅샷).

# 플레이어빌리티 2차 감사 — TOP 8 백로그\n\n정렬: 임팩트÷노력 (P0=3, P1=2, P2=1 ÷ S=1, M=2). 선정 원칙: WORKFLOW-V2 기능동결 하에서 **버그 수정·결정 보존·거짓 UI 교정·위기 신호**만 즉시 착수 대상. 신규 게임플레이/밸런스 변경은 전부 '결정 필요'로 분리. 핵심 evidence 3건(SaveLoadManager 직렬화 부재, AlertStackUI.Notify 부재, ClickSelector 게이트 누락) 코드 재확인 완료.\n\n---\n\n## TOP 8 — 즉시 착수\n\n### 1. F3 작업 우선순위 + F4 일정 슬롯 save/load 직렬화 [P0 · S]\n- **렌즈**: 플레이어 결정권\n- **근거**: `SaveLoadManager.cs:9-29` — PawnSave에 skillLevels/partHp는 있으나 workPriorities·scheduleSlots 필드 부재 (파일 전체 grep 'schedule|priorities' 0건 재확인). `PawnWorkSettings.cs:33-34` GetPriority/SetPriority, `PawnSchedule.cs:46-50` SetSlot 공개 API 기존재.\n- **구현**: PawnSave에 `public int[] workPriorities; public int[] scheduleSlots;` 추가 → Save() 폰 루프에서 PawnWorkSettings.AllKinds 순서로 GetPriority, PawnSchedule.slots 캐스팅 캡처 → Load에서 길이 가드 후 SetPriority/SetSlot 복원. 구세이브 빈배열 가드는 skillLevels 패턴(`SaveLoadManager.cs:16-19`) 그대로.\n- **이유**: 플레이어 결정 2대 표면(직업표·일정표)이 재시작마다 전부 리셋 — "결정해도 사라진다"는 최악의 반(反)결정 신호.\n\n### 2. 모드 해제 우클릭 이중발화 차단 + MarqueeSelector 게이트 3종 보강 [P0 · S] *(렌즈3 P0+P1 통합 — 동일 파일·동일 패턴)*\n- **렌즈**: 조작 마찰\n- **근거**: `ClickSelector.cs:96-101,162,193,215` — overUI·buildActive만 검사, 지정모드 7종·ContextMenuUI.IsOpen 게이트 0개 (재확인). `TreeChopDesignation.cs:95`, `MineDesignation.cs:179`, `DeconstructDesignation.cs:189`, `GrowZoneDesignation.cs:240`, `StockpileDesignation.cs:192`, `RoofDesignation.cs:256`, `BuildManager.cs:202` — 전부 우클릭=모드해제인데 같은 프레임 ClickSelector 핸들러도 발화 (Update 순서 비결정, 간헐 재현). `MarqueeSelector.cs:107-122` — InputBlocked가 TreeChop/Stockpile/Roof 3종 ModeActive 미검사.\n- **구현**: (a) ClickSelector.Update 좌/우클릭 분기에 `designationActive`(7종 ModeActive OR) + `ContextMenuUI.IsOpen`(우클릭만) 게이트 — buildActive 패턴 복제. (b) 각 지정 매니저 SetMode(false)·BuildManager.SetMode(Off)에 정적 `LastModeExitFrame = Time.frameCount` 스탬프 1줄(BuildManager:182 setModeTime 쿨다운 미러), ClickSelector·MarqueeSelector가 같은 프레임 마우스 입력 스킵. (c) MarqueeSelector.InputBlocked()에 TreeChop/Stockpile/Roof ModeActive 검사 3줄.\n- **이유**: 지정 취소 클릭이 이동/벌목 명령으로 새는 레이스 + 벌목 드래그 중 몰래 박스선택 → 그룹이동 변질.\n\n### 3. 정신붕괴 진입 알림 + AlertStackUI.Notify 공용 API [P0 · S] *(#4·#5의 선행 작업)*\n- **렌즈**: 정보/피드백 루프\n- **근거**: `PawnNeeds.cs:518-522` isBreaking=true 무음 전환, `PawnNeeds.cs:550-554` 붕괴 중 섭취 금지 → 붕괴=아사 나선 입구. `PawnNameLabel.cs:155,194` 라벨은 선택+줌인 시에만. AlertStackUI에 정적 Notify 부재(인스턴스 PushCard:201만 — 재확인).\n- **구현**: AlertStackUI에 `public static Notify(string title, int tier)` 추가 — FindExisting() 인스턴스의 기존 PushCard/TierColor 재사용(~8줄, 디렉터 외 발신자용 단일 진입점). PawnNeeds.cs:520 붕괴 진입에서 `Notify($"{PawnName} 정신붕괴!", 1)` + FloatingText(502행 '굶주림!' 패턴 복제). 회복 지점(525행)에 FloatingText '진정함' 1줄.\n- **이유**: 백로그 #4로 붕괴가 생사 분기점이 됐는데 신호 0 — 죽음 나선의 입구가 완전 무음.\n\n### 4. 콜로니스트 다운/사망 개별 알림 [P0 · S]\n- **렌즈**: 정보/피드백 루프\n- **근거**: `PawnHealth.cs:278` 사망 시 Debug.Log만, `PawnHealth.cs:310-313` IsDowned 전환 무음(StateVersion++만), `ColonistBar.cs:129-133` 죽은 림은 상단 바에서 조용히 소멸.\n- **구현**: PawnHealth.CheckDeath 사망 블록(278행)에 `AlertStackUI.Notify($"{이름} 사망", 3)`, IsDowned false→true 에지(312행, prev 비교 변수 기존재)에 `Notify($"{이름} 의식불명 — 치료 필요", 2)` + FloatingText. tier2+ 영속 카드는 기존 규칙(`AlertStackUI.cs:245`)이라 추가 작업 0. #3의 Notify API 의존.\n- **이유**: 화면 밖 출혈사를 모름. 의사도 다운이면 플레이어 개입이 유일한데 인지 경로가 월드 스프라이트 포즈뿐.\n\n### 5. '콜로니에 먹을 것 없음' 신호 [P0 · S]\n- **렌즈**: 정보/피드백 루프\n- **근거**: `PawnNeeds.cs:617-618` BeginEatWalk 음식원 전무 분기가 주석만 달고 무음 return — 림이 조용히 굶다 food 0. `PawnNeeds.cs:499-504` 첫 가시 신호 '굶주림!'은 이미 아사 데미지 시작 후.\n- **구현**: 618행 전무 분기에 static float 스로틀 1개(실초 30s, lastStarvText 패턴 502행 복제)로 FloatingText '먹을 것 없음!' + `Notify("식량 전무 — 굶는 림 발생", 2)`. #3의 Notify API 의존.\n- **이유**: '배고픈데 먹을 게 없다'는 아사 ~1게임일 전 마지막 행동 가능 시점. 운영자 의도(3~4일차 전멸 페이싱)가 '예고된 죽음'이 되려면 필수.\n\n### 6. 식량 잔여일 표시 보정 — 소비율 ~3배 과소평가 + stale 갱신 [P1 · S]\n- **렌즈**: 정보/피드백 루프\n- **근거**: `ResourceCounterUI.cs:80-94` '~N일치'가 meals/fineMeals 변화 게이트 안에만 있어 rm.food·림 수 변화 시 미갱신. `:91` 제수 pawnCount*3f(1림 3유닛/일 가정) vs 실소비 `PawnNeeds.cs:42,65` 게임일당 need ~130 ÷ meal 40 ≈ 3.3식/일 × 3유닛 ≈ 9~10유닛/일.\n- **구현**: 한 파일 — (a) days 문자열 계산을 meals 게이트 밖으로, lastFood·cachedPawnCount 변화에도 재조립(lastMeals=-1 강제 트리거 패턴 55행 재사용), (b) 제수 `const float FoodUnitsPerPawnDay = 9f`.\n- **이유**: '3일치'로 읽히는 재고가 실제 1일치 — 카운트다운이 위기 경보가 되려면 수치가 진실이어야 함.\n\n### 7. 시체 클릭 우선순위 강등 [P1 · S]\n- **렌즈**: 조작 마찰\n- **근거**: `ClickSelector.cs:55-67` _actionableTypes에 PawnEntity/AnimalEntity/BanditEnemy 무조건 actionable, `:106-111` IsDead 무관 Select+선택음+카메라 포커스, `:310-315` IsDead 가드 없이 TryTame. `PawnEntity.cs:330,346` 사망 시 컴포넌트만 disable — 콜라이더 잔존. `PawnHealth.cs:270-288` 시체 영구 잔존.\n- **구현**: ClickSelector 한 파일 — (a) IsActionable/PickEntityAt 루프에서 IsDead면 act=false 강등(시체 밑 MeatPile/생존 림이 승자), (b) 좌클릭 분기 `pawn != null && !pawn.IsDead`일 때만 Select(죽은 폰은 비-pawn inspect 경로), (c) L310 동물 우클릭에 `!animalC.IsDead` 가드(BuildContextMenu L608 기존 가드와 통일).\n- **이유**: 죽은 림/동물이 클릭 1순위로 아이템·생존 림을 가로채고 시체에 길들이기 시도 가능.\n\n### 8. 합류 림 컴포넌트 패리티 [P1 · S]\n- **렌즈**: 5~10일차 콘텐츠 깊이\n- **근거**: `AIDirector.cs:480-491` wanderer 조립에 PawnBuilder/PawnMiner/PawnHarvester/PawnDoctor/PawnEquipment/PawnAbilities/PawnTraits/PawnThoughts/PawnSchedule 전부 누락. `BanditEnemy.cs:53-64` 포로 합류도 동일 누락 세트.\n- **구현**: 정적 헬퍼 `PawnEntity.AttachColonistComponents(GameObject)` 하나로 시작 림(Editor/SceneSetup) 조립 세트를 추출, wanderer·포로 양쪽이 호출하도록 두 지점 교체.\n- **이유**: 합류 인구가 건축·채광·수확·치료 영구 불가 + 장비/특성/무드 무사고 — 주 보상(인구)이 절반 가치. 습격 상한이 림 수 연동이라 위협만 키우는 역전도 해소. 신규 기능 아닌 기존 조립 경로 패리티 수리.\n\n---\n\n## 결정 필요 — 운영자 승인 후 착수 (기능동결 예외 심사 대상)\n\n전부 신규 게임플레이/밸런스/난이도 변경이라 WORKFLOW-V2 기능동결 하에서 운영자 결정 필요. 임팩트÷노력 순.\n\n| # | 항목 | 임팩트·노력 | 결정 포인트 | 핵심 근거 |\n|---|---|---|---|---|\n| A | **길들인 동물 1게임일 주기 산물 드롭** (렌즈1+렌즈4 중복 지목 — 단일 작업으로 통합) | P1·S | 식량 유입 밸런스: 닭·토끼 1 / 사슴·멧돼지 2(=foodDrop/2) 수치 승인. 베리 희소화(덤불 0.7일치)를 되돌리지 않는 보수 범위 주장 검증 | `AnimalEntity.cs:84-104` TryTame 보상 0(음수 EV 함정 결정), `:206-209` MeatPileEntity.Spawn 드롭 경로 기존재 |\n| B | **좌클릭 '사냥 지정'** | P1·S | 신규 플레이어 명령 추가 승인. 현 대안(영구 징집 마이크로)이 굶김 유발하는 건 사실 | `ClickSelector.cs:662-739` AnimalEntity 분기 부재, `PawnHunter.cs:44-57` SetAnimalTarget 완비, DispatchGatherToIdle(758-789행) 패턴 ~25줄 복제 |\n| C | **여가(Joy) 슬롯 실배선** | P1·S | **#126 회귀 이력**(슬롯 hard-block 림 정지) 있는 영역의 AI 행동 변경 — soft-skip 설계라 재현 불가 주장이나 운영자 확인 필수. F4 4색 중 3색 플라시보 해소 효과 | `PawnNeeds.cs:254-255` GetCurrentSlot 소비처 1곳, `PawnUtilityAI.cs:350-353` "UI+hint만", Decide() ~375행에 3줄 |\n| D | **습격 전야 예고 (hour 18 'raid_omen')** | P1·S | 난이도 하향 의도 여부 — 습격 서프라이즈를 죽이는 설계 결정. 스케줄 결정적(06:00·3일)이라 거짓 예고는 없음 | `AIDirector.cs:213-222` 예고 이벤트 부재, `AlertStackUI.cs:186,245` 카드·로그·사운드 사슬 기존 완비 |\n| E | **아사 임박 개인 경고 (food 15 하향 교차)** | P1·S | 신호 추가 자체는 저위험 — TOP 8 차순위로 자동 승격 권장 | `PawnNeeds.cs:476,490-506`, `PawnFloatingBars.cs:126-141` 허기 무표시 |\n| F | **연구 사다리 tier-2 3종 (석궁 300pt/의술 280pt/축산 320pt)** | P0·M | 콘텐츠 추가 + 포인트·효과 수치(tend +10, tame ×1.5) 승인. ~5일차 사다리 소진은 P0급 격차 | `ResearchManager.cs:75-84` 총 640pt 종료, `:152-167` 지급 패턴, `PawnEquipment.cs:51` 석궁 획득경로 0 |\n| G | **wolf_pack 재활성** | P1·M | `AIDirector.cs:620-630` WolvesEnabled=false가 **의도적 비활성**인지 확인 필수 (과거 사고 가능성). 자동반격 표적 확장(`PawnEntity.cs:253-268`) 동반 | 카드/화살/징집 사슬(`AlertStackUI.cs:291-306`, `ArrowProjectile.cs:78-82`)은 기존 배선 |\n| H | **구조물 수리 작업 (RepairAction)** | P1·M | 신규 작업 종류 + 자재 소모 룰(석재 벽=석재 1) 설계 승인 | `WallEntity.cs:113-131` 회복 API 0, `PawnActions.cs:366-407` TendPatientAction 복제 모델 |\n| I | **다중선택 우클릭 명령 확장** | P1·M | 다중선택 시 '가장 가까운 1명 배정' UX 룰 승인 | `MarqueeSelector.cs:208-249`, `ClickSelector.cs:416-449` 패턴 복제 |\n| J | **림 미선택 거래 개통 / 상인 장비 2종 / ResourceLowAlert 인구연동 / ColonistBar 위기시각화 / 드래그 앵커 잔존 / 저장고 keepOpen** | P2·S×6 | 거래 개통·LowAlert·ColonistBar·앵커 잔존·keepOpen은 사실상 버그/UX 수리라 일괄 승인 추천. 상인 장비 2종(식량25→체인메일, 목재20→석궁)만 경제 설계 결정 | 렌즈별 원본 evidence 유지 |\n\n**의존 순서**: #3(Notify API) → #4, #5. #1·#2·#6·#7·#8 상호 독립 병렬 가능. 결정 필요 A는 렌즈1·렌즈4 동일 수정의 중복 발견이므로 승인 시 1회 작업
