# 자율 세션 결정 로그 — 2026-06-14 야간 (운영자 부재 10~30h+)

운영자 지시(2026-06-14 저녁): "나가서 밤에 옴(최소 10h, 30h+ 가능). QA·퀄리티업·
폴리싱·사운드·ui/ux + 가능하면 rimworldwiki.com 을 RAG 로 연결·비교해 적용할 것
적용까지 혼자서 계속 진행."

## 운영 원칙 (이 세션 — 운영자 부재 동안 자가 게이트)
- **모든 변경은 게이트 그린 후에만 커밋·푸시.** fresh-build → repro 프로브 → **격리
  grader**(작성자≠검증자, rule 3/8) → **repro_all 15/15**(회귀 가드) → commit+push.
  빨간 게이트는 절대 커밋 안 함.
- **추측 수정 금지(rule 1).** 버그는 재현 먼저, 효과는 probe 로 어서트(rule 7).
- **무인 검증 가능성 우선.** 스샷·grader·카운트·로그로 보증되는 변경 위주.
  사운드는 와이어링·무예외·자산존재까지만 보증(실제 청취 검증 불가 — 명시).
- **거대 신규 시스템 무인 추가 자제.** 검증 가능한 증분 위주. 위키 갭은 값/소형
  행동/용어 정합부터, 대형 시스템은 "보류 후보"로 기록만.
- **매 배치 결정 로그 + playbook 환류**(아침 브리핑 가능하게).
- **빌드 비용 분산.** 관련 항목 2~4건 묶어 1회 빌드+게이트.

## 작업 큐 (우선순위, 동적 갱신)
1. [진행] 폴리싱 §3 잔여 (물 경계 림·구름그림자·답압길·나무 flip 등)
2. [백그라운드] rimworldwiki 갭 분석 → 적용 큐 (docs/wiki-ref/ + wiki-gap-analysis)
3. ui/ux 백로그 잔여 (ui-backlog / ui-overlap-backlog / 디자인 v3 잔여)
4. QA 버그헌트 (미재현 버그 재현 시도 → 회귀 시나리오 보존)
5. 사운드 이벤트별 누락 SFX (절차 생성, 로컬)
6. 퀄리티 = 위키 정합 값/밸런스 (harness 카운트로 검증)

## 진행 기록 (배치별 — append)

### R7 (직전, 운영자 부재 직전 출하 완료)
바위 타일 디그리딩 + 건축 제거버튼 라벨/✕ — grader PASS, gate 15/15, push 4610144.

### 위키 갭 분석 수신 (서브에이전트, docs/wiki-gap-analysis-2026-06-14.md)
RAG 노트 7종(docs/wiki-ref/) + TIER L 8 / M 4 / H 3. **위키 직접 fetch 403 → WebSearch 스니펫 정본.**

**무인 적용 리스크 자세 (밸런스 회귀 방지 우선):**
- ✅ 무인 적용 OK (저밸런스리스크·검증가능): #4 열정 XP배율, #5 채광 수율×스킬,
  #7 작물 밤 성장게이트 점검, #6 침대 RestMul 회귀가드.
- ⚠ 신중 적용 (full soak 회귀검증 동반): #1 섭취임계 50→35(식량/생존 밸런스 직격),
  #3 허기 thought 25→35(무드 연동), #8 raid wealth proxy 톤(난이도).
- ⛔ 무인 보류 (운영자 OK 필요 — 회귀/신규시스템): **#2 정신붕괴 Minor=35**
  (어렵게 만든 '정신붕괴 0건' 소크 회귀 위험 — D-A 무드모델 미해결), TIER M(#14~17),
  TIER H(#20 온도/계절·#21 passion 풀모델·#22 raid points 정본식).

근거: WORKFLOW-V2 의 수면축/정신붕괴 30→0 튜닝은 무인 회귀 시 손실 큼. "적용할 것
적용"의 합리적 해석 = 저위험 검증가능부터, 밸런스/신규는 검증·승인 게이트.

**배치 계획:** UI-1(가독성 4건, 진행중) → UI-2(ui-overlap/backlog 잔여) →
WIKI-L(#5·#7 → #4) → WIKI-M(#1·#3 full soak 검증) → 폴리싱 §3 → QA 버그헌트. 반복.

### QA 패스 — 전 패널 ui-tour 격리 리뷰 (격리 grader, 2026-06-14)
11패널 스샷 결함 리뷰. 직접 재확인 후 처리:
- ✅ 수정(배치): **F1** 나무 인스펙터 제목↔본문 1행 겹침("위치:"+제목 중첩, 깨진 글자) —
  엔티티 시 탭스트립 숨지만 title 은 스트립아래 정규화 위치라 본문(상단34px만 비움)과
  충돌 → 본문을 제목 실하단(-62)부터 시작. **F2** 나무 설명 영문 수종(Oak/Pine/Birch)→
  한글. **F8** 연구 "3~5 dmg"→"3~5 피해".
- ⏸ 보류(전용 재현 필요 — 규칙1): **F3** 인스펙터 "벌목 지정" 버튼 2개 보임(코드상
  EntityActionBtn 단일 → ui-tour 전환 잔상 의심, 정상상태 재현 후 판정). **F4** 폰
  상태/건강/기분 탭 하단에 파란 바 1조각 누출(폰 need바 클립 경계 — 폰선택 정상상태 재현).
- ❌ 기각(오판): **F5** 콜로니스트 클리핑 — 실제론 민지/서연/지훈 3개 정상 중앙정렬.
- 📋 폴리싱 후보(다음): F9 일정 색범례 부재, F7 직업탭 스킬레벨 첨자가 디버그처럼 보임,
  F6 징집버튼 좌여백. F10 스킬값 하드코딩 여부(저, 코드확인).

### 진행 (배치별 출하 — 2026-06-14 야간)
- ✅ **UI 가독성**(push 8a539cb): 탭 행이름 Pawn(Clone)→실명, 이모지 tofu 제거, 채굴/청사진 정렬.
- ✅ **QA F1/F2/F8**(push 4910612): 나무 인스펙터 제목겹침, 영문 수종, 연구 dmg 한글화.
- 🔄 **QA F3**(게이트중): 인스펙터 중복 '벌목 지정' 버튼 제거(플로트 메뉴만 정본). grader PASS.
- 📋 다음: **위키 #3**(허기 thought 3티어 — 극심한배고픔 -10 추가, 배고픔 임계 35 정렬).
  검증 = setNeed+hasThought 효과 프로브(food30→"배고픔"=구코드 "출출함"과 판별) + 게이트.
- ⛔ **위키 #5 보류**: 하네스에 setSkill op 없음 → 수율×스킬 효과 무인검증 불가. 운영자 빌드/OK 후.
- 결함 처리 누계: QA 발견 10건 중 4 수정(F1/F2/F8/F3)·2 보류(F4 재오판→호수terrain·실은 기각)·
  4 기각/기구현(F5 콜로니스트정상·F6 징집정상·F9 범례기구현·F10 스킬정상).

### 누적 검증 소크 (human-rhythm 4일, 2026-06-14) — 위키 #3 후
- **OVERALL PASS · 정신붕괴 0 · 바닥기상 0** → 위키 #3(허기 3티어) + 누적 UI/QA 변경이
  무드/수면 밸런스 무회귀 입증 (밸런스 항목 소크 posture 충족).
- ⚠ **기존 이슈 기록**: Player.log 에 TMP_SubMeshUI.OnDisable NullReferenceException 7건.
  우리 스크립트/씬에 TMP 사용 0(전부 legacy Text) + 5월 빌드 로그에도 존재 → 오늘 변경
  무관, 프레임워크(TMP) 레벨, 무해(빌드/소크 PASS). 범위 외(운영자 빌드 환경 점검 후보).
- 위키 TIER-L 종결 현황: #3 적용·검증·푸시(225cde0). #1(섭취임계)은 #3와 무드 상호작용
  (eat35 시 배고픔밴드 상주 → 회귀 위험)으로 **무인 보류**. #5(setSkill 부재)·#2(붕괴임계)
  ·#8(난이도 장기관측) 보류. → 위키 무인 적용분 종료, 운영자 OK 대기 목록 정리됨.

### ⚠ 정정 — stale 로그 오독 (2026-06-14)
앞선 "소크 체크포인트" 항목의 정신붕괴/exception 수치는 **틀렸다**.  LocalLow
Player.log(00:20 stale, 게임 Debug.Log 0건)를 읽었다.  실제 런타임 로그는 repro
하네스의 `-logFile` = `G:/ai/_repro_run__<scenario>.log`.  올바른 로그
(`_repro_run__showcase-human-rhythm.log`) 재확인 결과:
- **정신붕괴 = 1 이벤트** (지훈 진입 mood=18 → 카타르시스 복귀 mood=45), 바닥기상 0,
  **exception 0**.  (이전 오보: 정신붕괴 0 / exception 7 — 둘 다 stale 로그값.)
- 판정: 붕괴 1건은 임계 20 미만(mood 18)으로 떨어진 **기존 D-A 무드 적자(수면)** 로,
  소크 중 폰은 급식되어 #3(허기) 무관.  역대 범위(r11=2·r15=1) 내 → 위키 #3 회귀 아님.
- TMP exception 7건 주장도 철회 — 실제 소크 로그 exception 0.  (stale 로그 잔재였음.)
**함정**: 런타임 로그는 LocalLow Player.log 가 아니라 하네스 `-logFile`
(`_repro_run__<scenario>.log`) 을 읽어라.  playbook 에 증류.

### ✅ 수면포즈 회귀 진범 규명·수정 (2026-06-14) — QA 소크 발견 → 런타임 진단
소크 grader 가 "침대 위 직립"을 적발(커밋 41eaaea 의 78° 누움이 회귀).  정적 코드는
전부 정상이라 런타임 진단 사다리로 추적:
- PoseTop: PawnPoseDriver 부착·sleeping=True·bodyChild OK 확인.
- PoseSleep/PoseLate: roll=78·setZ=78·lateZ=78 → 회전은 정확히 적용·유지(클로버 아님).
- **PoseEnum(전 렌더러 열거)**: `Pawn(Clone) en=1,z=0` + `PawnSpriteBob en=1,z=78`
  → **루트 렌더러가 enabled=true 로 직립 몸통을 덧그림**(진범).  SceneSetup 이 빌드시
  sr.enabled=false 로 두지만 art-v2 파이프라인 이후 런타임에 유실.  깨어있을 땐 직립끼리
  겹쳐 안 보이다 수면 78° 회전 시 직립 루트가 노출.
- **수정**: PawnSpriteBob.MirrorRootTint 가 매 프레임 rootTintRenderer.enabled=false 강제
  (소유 컴포넌트가 "앵커는 두되 안 그린다"를 보증).  깨어있을 때 잠재 이중그림도 제거 = 순개선.
- 검증: 강제수면 프로브 — 격리 grader R1 PASS(단일 깔끔 누움)·R2(before 이중결함 확인).
  repro_all 게이트 진행.  진단 로그는 전량 제거(클린).
