# 게임 소개 및 설명 — PawnSim

<!--internal-->
> NAN 2026 사전과제 제출물 ③.  요강 요구: *게임 제목 및 한 줄 소개 / 게임 방법
> (목표·조작·종료 조건) / 실행 방법 / 플레이 링크 / 영상 링크.*
>
> **초안 (2026-07-29).**  PDF 변환 전 최종 검토 필요.  ⚠ §3 "종료 조건" 미비 항목 확인 요망.
> 이 블록은 `md2print.py` 가 인쇄본에서 제거한다 (심사자용 아님).

---
<!--/internal-->

## 제목

**PawnSim** — 작은 정착지의 하루하루

## 한 줄 소개

**콜로니스트에게 명령하지 않는다. 일감을 지정하면, 그들이 스스로 판단한다.**
간접 조작 콜로니 시뮬레이션.

---

## 1. 목표

맨땅에 떨어진 콜로니스트 3명으로 정착지를 세우고 유지한다.
나무를 베고 돌을 캐 집을 짓고, 농사를 지어 먹고, 밤에는 자고, 며칠 뒤 찾아오는
습격을 견딘다.

플레이어는 **무엇을 할지만 지정한다.**  누가·언제·어떤 순서로 할지는 각
콜로니스트의 **작업 우선순위·스킬·현재 니즈**가 결정한다.  이 게임의 재미는
명령이 아니라 **정책을 세우고 그 결과를 지켜보는 것**에 있다.

## 2. 조작

마우스만으로 전부 된다.  (WebGL 은 캔버스 포커스를 잃으면 키 입력을 놓치므로
**하단 버튼 사용을 권장**한다.)

| 하려는 것 | 조작 |
|---|---|
| 콜로니스트 선택 | 좌클릭, 또는 상단 초상화 클릭 |
| 벌목·채광 지정 | 대상에 **우클릭** |
| 건축 | 하단 `건축` → 분류 → 항목 → 맵 클릭 |
| 배치 회전 | 청사진 상태에서 `R` |
| 구역 지정(저장·경작·지붕) | `건축` → `구역` → 드래그 |
| 작업 우선순위 | 하단 `직업` — 3인 × 9직종, 좌클릭으로 0~4 변경 |
| 하루 일정 | 하단 `일정` — 24시간 슬롯 |
| 연구 | 하단 `연구` |
| 시간 배속 | 우하단 `멈춤 / 1x / 3x / 6x` |

**게임은 일시정지로 시작한다** (의도된 설계).  배속을 눌러야 시간이 흐른다.

## 3. 종료 조건

- **패배**: 콜로니스트 전멸 → 게임 오버.  이후 관전 모드로 세계는 계속 돌아간다.
- **승리**: 현재 **없음**.  무한 운영형이며, 상단 `가치` 수치는 점수가 아니라
  습격 난이도의 입력값이다.

<!--internal-->
> ⚠ **제출 전 판단 필요 (운영자)**: 요강이 "종료 조건"을 명시적으로 요구한다.
> 현재 승리 조건이 없다는 사실을 위와 같이 정직하게 적을지, 아니면 마감 전에
> 최소 승리 조건(예: 정착지 가치 N 달성 또는 X일 생존)을 넣을지는 운영자 판단.
> 넣는다면 비용은 작다 — 가치·경과일은 이미 집계된다.
>
> 요강 충족 자체는 **지금 상태로도 가능**하다.  요구는 "종료 조건을 기술하라"이지
> "승리 조건을 만들라"가 아니고, 위 §3 은 패배 조건과 무한 운영형이라는 사실을
> 이미 명시한다.  따라서 이 항목은 제출을 막지 않는다 — 게임을 더 좋게 만들지에
> 대한 판단만 남는다.
<!--/internal-->

## 4. 실행 방법

브라우저에서 링크를 열면 끝이다.  설치·계정·유료 라이선스 불필요.

**플레이**: https://melons.github.io/MelonS-Agents/play/

- 권장: Chrome / Edge (Unity WebGL 공식 지원 대상)
- 빌드 약 20MB — 첫 로딩에 시간이 걸릴 수 있다
- 소리는 브라우저 정책상 **첫 클릭 이후** 재생된다
- 우하단 전체화면 버튼 권장

**소스**: https://github.com/MelonS/MelonS-Agents
(전체 소스 + 커밋 기록 공개.  게임 코드는 `skills/game-prototype/`)

> 저장소를 클론해 직접 빌드할 경우: 외부 아트 팩은 재배포 금지라 포함돼 있지
> 않다.  팩 없이도 **절차 생성 폴백**으로 정상 빌드·실행된다.
> 팩을 쓰려면 `skills/game-prototype/scripts/extract-tinyswords.py` 참조.

## 5. 영상 링크

*(제출물 ② 촬영 후 기입 — 30~60초, 실제 플레이 화면)*

---

## 6. 처음 5분 권장 동선

심사자가 무엇을 하는 게임인지 가장 빨리 알 수 있는 순서.

1. `게임 시작` → `3x` 배속.  시작 저장구역이 깔려 있어 **30초 안에**
   목재·식량 수치가 오르는 것이 보인다 (콜로니스트가 알아서 나른다).
2. 나무를 **우클릭** → 벌목 지정.  지정한 만큼만 일한다.
3. `건축` → `구조` → `목재 벽` 을 몇 칸 배치.  자재 운반 → 건설이 자동으로 돈다.
4. 하단 `직업` — 3인의 우선순위가 **서로 다르다**.  각자 잘하는 일에 1이 붙어 있다.
   이 그리드가 이 게임의 핵심 조작면이다.
5. `6x` 로 하루를 돌린다.  시간대에 따라 화면 색조가 바뀌고 콜로니스트가
   먹고·자고·일한다.

---

## 7. 정직한 한계

프로토타입이다.  다음은 알고 있는 결손이며 숨기지 않는다.

- **콜로니스트 간 사회 상호작용(잡담·관계)이 없다.**  현재 최대 결손으로 기록돼 있다.
- 승리 조건이 없다 (§3).
- 전투는 기본 활·근접 자동공격 수준이며 무기 장착 시스템은 미완이다.
- 일부 시스템(멘탈브레이크 상위 티어 등)은 스펙만 있고 미구현이다.
