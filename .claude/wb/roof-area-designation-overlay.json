{
  "role": "game-programmer",
  "lane": "roof-area",
  "milestone": "M4-building",
  "task": "지붕 기능 + 지붕영역 지정 메뉴 (운영자: the reference sim처럼 지붕영역 설정 메뉴 따로 있음)",
  "status": "Done",
  "reference-sim_concept": "(1) RoofDesignation — Architect Zone(구역) 메뉴 '지붕 영역' 드래그-rect 지정(MineDesignation/Stockpile 드래그 패턴 clone), 지정 셀을 roofed 로 마크. (2) RoofOverlayRenderer(self-bootstrap, GameSceneGate) — roofed 셀 위 반투명 어두운 그늘 오버레이. (3) 벽으로 둘러싸인 방 auto-roof (flood-fill, 열린맵이면 bail → 수동만 OK). 효과 = 시각(그늘) + IsRoofed 플래그(향후 비/온도 hook).",
  "files_owned_and_changed": [
    "skills/game-prototype/unity-project/Assets/Scripts/RoofDesignation.cs (NEW)",
    "skills/game-prototype/unity-project/Assets/Scripts/RoofOverlayRenderer.cs (NEW)",
    "skills/game-prototype/unity-project/Assets/Scripts/ArchitectMenu.cs (EDIT — Zone(구역) 카테고리에 '지붕 영역 (U)' OrderItem 1줄 추가만)"
  ],
  "implementation": {
    "RoofDesignation": "MineDesignation.cs / GrowZoneDesignation.cs 를 EXACT clone — self-bootstrap [RuntimeInitializeOnLoadMethod(AfterSceneLoad)] behind GameSceneGate, idempotent FindFirstObjectByType, drag-rect MarkRect / single-click MarkCell, Time.unscaledTime only, 다른 designation 모드와 mutual-exclusion(Build/Deconstruct/Mine/Grow/Stockpile). roofed 셀은 HashSet<Vector2Int> 에 저장, Version int 가 변경마다 bump. 공개 API: IsRoofed(cell) (roofed 플래그 hook), Roofed (IReadOnlyCollection, 오버레이가 read-only enumerate), SetMode/Toggle. QA 진입점: SimulateClick/SimulateDragRect/DesignateCell.",
    "auto_roof": "TryAutoRoofEnclosedFrom — seed 에서 flood-fill, 벽/문/impassable terrain 을 경계로. region 이 autoRoofMaxCells(400) 넘으면 open map 으로 보고 bail (수동 지정만 유지). bounded 면 interior 전체 roof. 필수는 수동 지정, auto-roof 는 bonus.",
    "RoofOverlayRenderer": "self-bootstrap(GameSceneGate) renderer, RoofDesignation.Instance 를 매 프레임 read-only poll, Version 비교로 변경시에만 Rebuild. POOLED 그늘 쿼드(List<SpriteRenderer>, 재사용/성장만, 프레임당 new 없음 — GC firewall). 공유 1x1 white sprite 를 shadeTint(어두운 반투명)로 tint. sortingOrder 24 = pawns/structures 위, NightOverlay(25) 아래, UI Canvas 아래.",
    "architect": "ArchitectMenu Zone(구역) OrderItem 배열에 '지붕 영역 (U)' 1줄 추가만 — 기존 MakeBtn onClick 플러밍(▣ active 반영) 재사용, RoofDesignation.Instance.SetMode(true). 다른 카테고리/구조 미변경.",
    "hotkey": "U (roof Up) — 자유 키 (B/F/G/T/Y build, N/R, X deconstruct, M mine, P plant, O stockpile, K floor-stone, J table, H, L lamp, E fence, WASD camera 모두 taken)."
  },
  "bug_firewall_check": {
    "#4_audio_buzz": "PASS — AudioBank.PlaySelect() 는 셀당 1회 단발(single click) 또는 rect/auto-roof 전체당 1회만 호출(throttle inside AudioBank); 절대 per-cell tight loop 아님. idempotent 재-roof 는 silent.",
    "#5_GetInstanceID_race": "n/a — instance-ID 비교 없음.",
    "#6_OnCollisionEnter_only": "n/a — collision 핸들러 없음 (designation/overlay 둘 다 폴링 기반, IsWallCell 는 OverlapBox read-only probe).",
    "#7_singleton_subscription_race": "PASS — OnEnable 에서 singleton 구독/캡처 없음. RoofOverlayRenderer 는 RoofDesignation.Instance 를 Update 에서 read-only poll-find + 사라지면 re-find. RoofDesignation 은 다른 designation Instance 를 read-only 참조만.",
    "#8_justSpawned_default_true": "n/a — grace-period 필드 없음.",
    "#9_runInBackground_freeze": "PASS — 코루틴/WaitForSeconds/always-on background timer 전혀 없음. 모든 타이밍은 Update 내 Time.unscaledTime. overlay 는 Version 비교 1줄 + 변경시에만 rebuild."
  },
  "no_per_frame_alloc": "RoofOverlayRenderer 쿼드는 pool(List<SpriteRenderer>) 재사용 — 변경시에만 Rebuild, surplus 는 SetActive(false), 프레임당 new List/GameObject 없음. RoofDesignation 의 auto-roof 만 일시적 HashSet/Stack 할당(드래그-완료 이벤트 1회, per-frame 아님).",
  "syntactic_check": "RoofDesignation.cs braces 37/37 parens 221/221; RoofOverlayRenderer.cs braces 19/19 parens 83/83; ArchitectMenu.cs braces 44/44 parens 276/276. 참조 API 확인: ClickEffect.Spawn(Vector3,Color), PawnMovement.IsBlockedAt(Vector2), AudioBank.PlaySelect(), WallEntity/DoorEntity 존재, StockpileDesignation.ModeActive/SetMode/DumpingMode/SetDumpingMode 존재, GameSceneGate.RunWhenGameScene 존재. Span/stackalloc 위해 using System 추가.",
  "lane_isolation": "STRICT — 신규 RoofDesignation.cs + RoofOverlayRenderer.cs 만, ArchitectMenu.cs 는 Zone 항목 1줄 추가만(다른 카테고리/구조/MakeBtn/RefreshContent 미변경). SceneSetup* / PathGrid / 다른 designation / PawnMovement / NightOverlay / 어떤 Entity 도 미수정. 다른 designation Instance 는 read-only SetMode 호출만. Unity 실행 안함(QA verifies).",
  "flags_for_qa": [
    "SCENE-WIRING (zero): RoofDesignation + RoofOverlayRenderer 둘 다 self-bootstrap(GameSceneGate) — SceneSetup 미수정. MainMenu 에는 안 뜸(게이트), Game scene 에서만.",
    "CODE-GEN (no prefab/PNG): 그늘 오버레이는 공유 1x1 white sprite 를 어두운 반투명으로 tint 한 pooled 쿼드. QA: Architect Zone '지붕 영역' 행 클릭 또는 U 키 → 맵에 드래그-rect → 해당 셀에 어두운 그늘 오버레이가 pawn/구조물 위, 밤 오버레이 아래로 보이는지 확인.",
    "AUTO-ROOF: 벽으로 완전히 둘러싸인 방 내부 셀을 roof 하면 flood-fill 로 방 전체 자동 지붕. 열린 공간(벽 없음)을 roof 하면 region>400 으로 bail → 수동 지정 셀만 지붕(의도된 동작, 필수는 수동).",
    "HOTKEY: 지붕 = U (신규). HotkeyCheatSheet(별도 lane) 에서 충돌 없는지 확인 — lane 시점에 U 는 free 였음.",
    "FUTURE-HOOK: RoofDesignation.IsRoofed(cell) 가 roofed 플래그 — 향후 비/온도 시스템이 read-only 로 hook 가능(현재는 시각 그늘만)."
  ],
  "verify_lane_2026-05-31": {
    "trigger": "운영자: LongPlay 가 지붕 UI 를 안 써서 인게임 그늘 오버레이가 실제 렌더되는지 미확인. 지붕 지정 시 눈에 보이는 그늘 오버레이 확인 + 안 보이면 수정.",
    "lane_files": "RoofOverlayRenderer.cs + RoofDesignation.cs(미변경) + Tests/UIScenarioRunner.cs. ArchitectMenu 미변경(buildmenu lane).",
    "found_and_fixed": {
      "RoofOverlayRenderer.cs": "(1) sortingLayerName 미설정 버그 — NightOverlay 는 명시적으로 sr.sortingLayerName='Default' 를 박는데 roof 쿼드는 안 박았음. 런타임 생성 SpriteRenderer 의 기본 layer 가 Default 라 대부분 OK 이지만, shadeSortingLayer SerializeField('Default') 추가 + GetQuad/Rebuild 양쪽에서 sr.sortingLayerName 명시 설정(visibility firewall). (2) alpha 0.32 → 0.45 상향 — 한낮(NightOverlay alpha ~0) 밝은 잔디 위에서 0.32 는 너무 흐려 스샷에서 그늘이 안 보임. tint=(0.05,0.06,0.11,0.45).",
      "geometry_verified": "쿼드 = 공유 1x1 white sprite @ pixelsPerUnit 1 (1 world unit), pivot center, position=(cx+0.5,cy+0.5) → 셀 [cx,cx+1]x[cy,cy+1] 정확 커버. sortingOrder 24 = pawns/structures 위, NightOverlay(25) 아래 — 한낮엔 NightOverlay 가 거의 투명해 그늘이 비쳐 보임. 렌더 체인 정상."
    },
    "UIScenarioRunner_step8": "step 8 'designate_roof' 추가 — RoofDesignation.SetMode(true) → 콜로니스트 중심 ~5x5 셀 rect 를 SimulateDragRect(실제 drag-rect MarkRect 경로)로 지붕 지정(pawn 없으면 벽 셀 근처 fallback rect) → roof 모드 해제 → Step 하베스(WaitForEndOfFrame 후 캡처)가 _scn_08_designate_roof.png 저장. WaitForEndOfFrame 은 RoofOverlayRenderer.Update 의 Version-rebuild 이후라 스샷에 그늘 포함. DONE steps=7 → steps=8 갱신, 기존 step 이름/순서 보존.",
    "bug_firewall_recheck": "#9 PASS — step8 은 기존 Step() 코루틴 하베스 재사용(runInBackground=ON 이미 Start 에서 설정), 신규 always-on 타이머 없음. #4 PASS — SimulateDragRect 는 rect 전체당 PlaySelect 1회(기존 MarkRect throttle). #7 PASS — RoofDesignation.Instance read-only poll, OnEnable 구독 없음.",
    "syntactic_check": "RoofOverlayRenderer.cs braces 19/19; UIScenarioRunner.cs braces 98/98. Unity 미실행(QA 가 batchmode 컴파일/스샷 검증).",
    "qa_action": "-ui-scenario 플래그로 QA 실행 시 _scn_08_designate_roof.png 에 콜로니스트 영역 ~5x5 셀에 어두운 반투명 그늘 사각이 보여야 함. 안 보이면(빈 잔디) NightOverlay 가 그 시각에 어두운지 또는 sortingLayer 충돌 여부 확인."
  }
}
