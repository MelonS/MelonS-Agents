{
  "role": "Programmer (Lane C)",
  "wave": "W-M2-01",
  "cycle": "C-005",
  "milestone": "M4-building",
  "status": "Done",
  "file": "skills/game-prototype/unity-project/Assets/Scripts/BlueprintEntity.cs",
  "change": "Single additive SFX call in private void Complete(): AudioBank.Instance?.PlayBuild() inserted after the finished-prefab spawn/material/footprint block and before Destroy(gameObject). No build/material/footprint logic changed.",
  "inserted_call_line": 211,
  "inserted_call": "AudioBank.Instance?.PlayBuild();",
  "null_guarded": true,
  "wiki_acceptance_criterion": "reference-sim-comparison.md Dim2 backlog row #1 (L154): 'A wall finishing plays a construct sound once (throttled).' Also Dim2 table L56: Construct/build -> add sfxBuild, call from PawnBuilder/BlueprintEntity complete.",
  "qa_flag": {
    "verify_line": "skills/game-prototype/unity-project/Assets/Scripts/BlueprintEntity.cs L211 inside Complete(): AudioBank.Instance?.PlayBuild();",
    "expected_behavior": "When a blueprint (e.g. wall) finishes (Progress>=1 -> Complete()), the construct sound plays exactly once. PlayBuild() is throttled inside AudioBank, and Complete() runs once per blueprint, so one build = one sound.",
    "cross_lane_dependency": "PlayBuild() is a NEW method being added to AudioBank.cs by Lane A (Sound) THIS SAME WAVE (W-M2-01). It does not exist on disk until Lane A's edit lands. QA must compile the merged 4-lane set together; AudioBank.Instance?.PlayBuild() will not resolve until Lane A's AudioBank.cs change is present.",
    "no_op_if_lane_a_missing": "If Lane A merge is absent at compile, this is a missing-method compile error (not silent) — expected to resolve post-merge."
  },
  "constraints_honored": {
    "edited_only": "BlueprintEntity.cs",
    "did_not_edit_audiobank": true,
    "no_todo_comments": true,
    "no_logic_changes": true,
    "unity_not_run": true
  }
}
