{
  "role": "PM",
  "status": "Done",
  "cycle": "C-013",
  "wave": "W-M4-05",
  "wave_title": "W-M4-05 — drag-rect placement (#18) + stone/paved floor (#21) + lamp->Architect catalogue (QA flag #42) + deconstruct-refund verify (#15)",
  "prior_wave_status": "W-M4-04 (torch/lamp #19 / scatter Resources.Load fix / V3 research verify / V1 drafted verify) CLOSED: qa.json Status=PASS @c0ce5bf build cy-w2 (isolated 76/76 +V1/V3, integration 42/42, build-click 9/9, real-qa wood +65/30s, visual-diff 2.69%). No disk RED.",
  "qa_flags_carried": [
    "#42 LampEntity not yet in ArchitectMenu catalogue (L-hotkey only) -> Lane C actions it (one ArchitectMenu.Categories line for the EXISTING Mode.Lamp).",
    "#41 scatter PNGs still referenced at old Assets/Sprites/ paths in SceneSetup.cs ForceImport -> harmless '[SceneSetup] missing:' warnings; NOT actioned this wave (no SceneSetup hot-file budget spent) -> deferred to a final cleanup wave with #20.",
    "PawnInfoPanel tab strip lazy (low priority, SceneSetup.UI.cs) -> not actioned.",
    "V1DraftedTintTest cyan literal coupling -> optional DraftedTint accessor, not required."
  ],
  "rationale": "Non-gated wiki backlog is nearly exhausted (sound 1-9, motion 10-14, UI 22/23, M4 #15/#16/#17/#19, verify V1-V8 + #28 + #29 all shipped). Per wiki sequencing ('build-ops one at a time + #20-21 catalogue one new-buildable per wave; verification continuous as the gate') this wave advances #18 drag-rect placement (the biggest remaining design-a-base UX win, implemented WITHOUT a BuildManager edit by self-bootstrapping over the public TryPlaceAt API), the single BuildManager catalogue lane #21 stone-floor, the carried lamp-catalogue QA flag, and a standalone gated deconstruct-refund verify (#15 acceptance promoted to a single PASS line). #20 table+chair + the harmless scatter ForceImport cleanup are the only non-gated remainder after this -> one final wave, then the chain ENDS.",
  "lanes": {
    "A": "game-programmer / M4 #18 drag-rect blueprint placement — NEW BlueprintDragDesignation.cs, self-bootstrapping (DeconstructDesignation/MineDesignation/GrowZoneDesignation pattern: [RuntimeInitializeOnLoadMethod], hidden DontDestroyOnLoad GO, FindFirstObjectByType idempotency). While build mode is active, left-click-drag paints a rect/line; on release it loops BuildManager.TryPlaceAt(cx,cy) over the covered cells. BuildManager.cs READ-ONLY (no edit). ESC/right-click cancels. Zero SceneSetup edit.",
    "B": "game-programmer + game-artist / M4 #21 stone/paved floor — the SINGLE BuildManager.cs lane: add Mode.FloorStone + cost + place handler mirroring the Floor entry; free hotkey; higher move bonus than wood floor (reuse FloorEntity's existing bonus mechanism, read-only). NEW _gen_stone_floor.py generator + PNG (ROCK_DK/MD/LT + 1px OUTLINE_OBJ, palette.py read-only). NOT surfaced in ArchitectMenu this wave (that is a follow-up to keep ArchitectMenu.cs single-owner = Lane C).",
    "C": "game-designer / M6 + W-M4-04 QA flag #42 — ArchitectMenu.cs only: add the EXISTING BuildManager.Mode.Lamp to the Categories dictionary (one entry, Production or a new light row) so the lamp is reachable from the Architect menu, not just the L hotkey. No new enum, no BuildManager edit.",
    "D": "game-build-engineer / M5 verify #15 — NEW test .cs (test asmdef, different file from any other lane): build a wall -> designate deconstruct -> a pawn removes it -> ~50% material refund + the PathGrid cell becomes walkable, asserted as ONE PASS. Runtime untouched (read DeconstructDesignation/PawnBuilder/PathGrid/ResourceManager public state read-only the way V4/V7 do)."
  },
  "non_overlap_proof": "A=BlueprintDragDesignation.cs(new); B=BuildManager.cs(edit)+_gen_stone_floor.py(new)+stone_floor PNG(new); C=ArchitectMenu.cs(edit); D=new deconstruct-refund test .cs. No file in two lanes. ONLY Lane B touches a build hot file (BuildManager.cs). No lane touches a SceneSetup hot file (SceneSetup.cs / SceneSetup.Game.Entities.cs / SceneSetup.Pawn.cs) — FLAG if unavoidable in A or B. Lane A and Lane C both call BuildManager public APIs (TryPlaceAt / SetMode) read-only without editing it; Lane B is the only BuildManager editor.",
  "op_gated_excluded": ["work-priority", "mood/mental-break #30 (= V9 mood-break verify)", "per-terrain path-cost #24", "roof/collapse", "catalogue #20 table+chair (shared BuildManager catalogue hot zone -> own future wave)", "M4 breadth trading/power/taming/bills", "P5 detailed-pawn redraw (superseded by flat #202 — confirm with operator)"],
  "backlog_remaining_after_this_wave": "#20 table+chair (own final single-build-op wave, shared BuildManager catalogue) + the harmless scatter ForceImport SceneSetup.cs cleanup (#41). These become ONE final wave; after that the non-gated remainder is exhausted and the chain ENDS (done=true), leaving only OP-gated items (#24 terrain-cost, #30 mood/V9, roof/collapse, M4 breadth, P5 redraw).",
  "outputs": [".claude/whiteboard.json", ".claude/wb/pm.json"],
  "done": false
}
