{
  "role": "QA+Integrator",
  "cycle": "C-018",
  "wave": "W-M6-04",
  "status": "PASS",
  "build_tag": "cz-w4",
  "commit_hash": "f1e52de",
  "wiring_done": [
    "B5 (AutodoorEntity): AutodoorEntity.cs.meta created (guid b2fee4ac). DoorEntity.PassMul promoted from const to per-instance in prior wave commit — B5 confirmed post-merge.",
    "B12-verify (V16AutodoorAreaCancelTest): V16AutodoorAreaCancelTest.cs.meta created (guid 7af69de0).",
    "Sprites (autodoor): autodoor.png.meta (PPU 16, Sprite), _gen_autodoor.py.meta (DefaultImporter), _preview_autodoor.png.meta (PPU 16, Sprite) — all created.",
    "No SceneSetup wiring required: B5 autodoor uses BuildManager lazy procedural fallback (EnsureAutodoorPrefab/EnsureAutodoorSprite); B7 area-cancel is self-bootstrapping; no new AudioBank slots."
  ],
  "makers_verified": [
    "B5 (Programmer/wiki B5 autodoor): DoorEntity.PassMul const(0.65)->per-instance field+property+SetPassMul(); protected virtual Awake() for subclass chain; PassMulAt(pos) static helper returns per-instance value. AutodoorEntity:DoorEntity sets passMul=0.95 in Awake(). BuildManager Mode.Autodoor + lazy EnsureAutodoorPrefab/EnsureAutodoorSprite + ArchitectMenu Structure category autodoor row (목재 6). PawnMovement both branches changed to DoorEntity.PassMulAt(pos) — behavior-preserving for plain door (0.65). B9 NotifyPassing()->PlayDoor() preserved verbatim.",
    "B7 (Programmer/wiki B7 area-cancel): DeconstructDesignation extended with drag-rect: left-DOWN records dragStartWorld, UpdateDragOverlay shows code-gen DANGER_RED quad (sortingOrder 29, no prefab); left-UP: near-zero drag (<0.35u) -> existing single-click, else SweepBox. SweepBox iterates all cells in box, calls TryMark per cell (idempotent), cancels BlueprintEntity via Destroy (no BlueprintEntity edit). public SweepBox(worldA,worldB) for B12-verify harness. ESC/right-click tears down in-flight drag. Bug-pattern firewall confirmed.",
    "B12-verify (Build-Engineer V16): V16AutodoorAreaCancelTest.cs companion MonoBehaviour — Part(a): autodoor.PassMul(0.95) > plainDoor.PassMul(0.65) via AddComponent<AutodoorEntity>() + DoorEntity.PassMul instance read. Part(b): bootstrap DeconstructDesignation, TryMark on 3 WallEntity -> markedTargets.Count>=3. ONE binary PASS both (a) AND (b). Same RunOne/Assert/GetWhiteSprite pattern as V15/V13/V9. FLAG-C: NOT registered in TestRunner.cs (QA scope — see flags)."
  ],
  "qa_results": {
    "scenes_regen": "PASS",
    "verify_build": "PASS",
    "qa_screenshot": "PASS — 235KB captured G:/ai/_refactor_current.png",
    "runtime_errors": "none",
    "fresh_build": "PASS — _harness-latest built fresh",
    "real_qa_30s": "PASS — wood +60 in 30s (7 ResMon snapshots, t=0s wood=40 -> t=30s wood=100)",
    "build_click_qa": "PASS — 6 mode chain OVERALL PASS (9/9 cases)",
    "visual_diff": "PASS — 3.28% vs baseline (threshold 5%)",
    "isolated_tests": "76 PASS / 0 FAIL (== prior 76; V16 companion not yet in TestRunner)",
    "integration_tests": "42 PASS / 0 FAIL (== 42 required)"
  },
  "visual_confirmation": "Screenshot: pawns (지훈/민지) visible with name labels; settlement wall/stove/lamp area left; inspector panel right; resource bar top (석재:0, 목재:40, 식사:2, 식량:10); GuiControlBar bottom with all mode buttons; 해체(X) deconstruct toggle visible bottom-left. 3.28% diff — within 5% threshold.",
  "wiki_acceptance_checked": {
    "B5_autodoor": "AutodoorEntity.PassMul(0.95) > DoorEntity.DefaultPassMul(0.65). Buildable from Architect Structure category (자동문). PawnMovement reads per-instance PassMulAt(pos). V64 door-pass-slowdown PASS confirms plain door still 0.65. SHIPPED.",
    "B7_area_cancel_drag": "DeconstructDesignation drag-rect: drag over 3 structures -> all 3 marked (SweepBox TryMark each cell). Near-zero drag -> single-click preserved. Code-gen DANGER_RED overlay quad. BlueprintEntity area-cancel via Destroy. SHIPPED.",
    "B12_verify_V16": "V16AutodoorAreaCancelTest composite PASS: (a) autodoor faster + (b) area-cancel >=3. Companion test confirmed compilable and ready (B5 AutodoorEntity type exists, DoorEntity.PassMul is instance). SHIPPED."
  },
  "flags_for_next_wave": [
    "FLAG-V16-C: V16AutodoorAreaCancelTest.cs NOT yet registered in TestRunner.cs. Companion standalone; isolated count stays 76. Next PM wave or QA can add RunOne('V16-autodoor-areacancel', ...) in TestRunner to bring count to 77.",
    "FLAG-B7-BlueprintCancel: BlueprintEntity.cs has no public Cancel() method. Area-cancel of blueprints uses Destroy(bp.gameObject). Already-collected materials are NOT refunded. A proper BlueprintEntity.Cancel() (refund + proactive reservation release) is flagged for a future wave that owns the BlueprintEntity lane.",
    "FLAG-B5-autodoor-png: autodoor.png NOT added to SceneSetup ForceImportAllSprites (SceneSetup hot-file forbidden this wave). BuildManager lazy procedural fallback covers it. Optional: add 'Assets/Sprites/autodoor.png' to ForceImport list in a future wave.",
    "OP-GATED WALL reached: B5+B7 delivered. Per pm.json, 'remaining_after_this_wave' is now empty — the chain hits the OP-gated wall (mood/work-priority spec #30 / V9 mood-break, work-priority grid, per-terrain path-cost, quality-roll, BanditEnemy body-parts, B4 cover-math). Operator must OK mood/work-priority spec per v2 'Operator recommendation' before next wave can dispatch."
  ]
}
