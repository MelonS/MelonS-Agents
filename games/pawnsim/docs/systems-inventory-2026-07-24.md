# 시스템 as-built 전수 인벤토리 — 2026-07-24

> GDD §8 세부기획서 계층 문서. 코드 전수 조사 에이전트 산출(Scripts 184파일/약 41k줄)
> + 톱레벨 검증 정정. 원칙: 코드가 실제로 하는 것만 기술.

## ⚠ 검증 정정 — 게임성 오딧(2026-07-24)을 뒤집는 발견 2건

1. **교역은 작동한다** — 오딧 §3 R1 은 "교역 UI 없음·빈 껍데기"로 판정했으나 **오판**.
   `ClickSelector.cs:652` 우클릭 컨텍스트 메뉴가 `TraderEntity.TradeOptions` 6종
   (목재↔식량↔석재↔식사)을 노출하고 `TryTrade(idx)` 실행 — 근접 5u 체크(#168) +
   socialMul 가격 보정(#178) + 구매품 물리 더미 드롭(자원모델 단일화 Stage2c).
   **→ R1 컷(이벤트 가중치 0)의 전제 붕괴. 컷 취소, 유지 + 이벤트 카드의 스테일
   텍스트("실제 거래 시스템: 향후 Day")만 정직화.**
2. **socialMul 은 죽은 스탯이 아니다** — R2 "소비자 없음"도 오판. 거래 가격 receive
   측에 0.80~1.20 배수 실사용. **→ UI 숨김 취소, 유지.**

교훈: **오딧 단일 소스로 기능 삭제 결정 금지 — 컷 전 코드 레벨 재검증 의무.**

---

## 총괄

콜로니스트 3명 시작(일시정지), 맨땅에서 유틸리티 AI 가 우선순위로 일감 선택,
며칠 뒤 습격. 전멸=게임오버, **승리 조건 없음**.

## 1. 니즈·멘탈브레이크 — 완전동작
`PawnNeeds.cs`(1036줄), `PawnThoughts.cs`. food/sleep/mood 0~100 실시간 감소
(0.13/0.12/0.048 per s). 허기: 임계 50에서 물리적 음식원까지 걸어가 3초 식사
(raw 20/meal 40/fine 55 회복), food 0 지속 시 4.5초마다 3피해 아사. 수면: 침대
0.35/s×RestMul(0.8~1.4). 무드: thought 카탈로그 델타 합산, <20 확률 붕괴(20~45초
배회·노동정지) → 만료 시 "후련함"+8 카타르시스 복귀. 여가 need 게이지는 없음
(스케줄 Joy 슬롯+모닥불 +2 thought 근사만).

## 2. 작업 시스템 — 완전동작
WorkKind 9종: 벌목/채집/사냥/요리/연구/건축/채광/운반/의료. 생존 pre-pass(기아
채집·출혈 침대·자율취침·여가) 선행. 벌목=지정 나무만(HP 80~150→목재 20~30 드롭,
그루터기·묘목 ~120s 재생). 채광=지정 광맥만(HP 200×재질, 20/s, 돌 1~3). 건설=
청사진→물리 자재 운반→5초(바닥 2초). 조리=화덕 raw3→meal1(스킬≥1.10 fine,
better_stove 2배, 상한 림×5끼). 농사=경작존→파종→~90초 성장→수확 14. 운반=
더미→저장존(우선순위순), 받을 곳 없으면 미시작. 의료=접근 5초 tend(출혈0·+5HP).

## 3. 작업 우선순위/할당 AI — 완전동작
`PawnUtilityAI.cs`(641줄) 1.5초 Decide, WorkTab(F1) per-pawn×작업 0~4 우선순위,
`ReservationManager` 중앙 예약(중복 배정 방지), A*(PathGrid) 경로, `WorkGiveUp`
도달불가 포기. 유휴 배회 0.35×.

## 4. 전투 — 완전동작 (늑대만 비활성)
6부위 HP(분배 몸통35/다리20/팔20/머리5%), 머리·몸통 0=사망, 머리≤30%=다운,
다리→감속, 팔→작업감속, 출혈(붕대 정지). 장비 17종(셔츠4/바지3/모자3/무기7),
방어 합산 상한 85%, 근접=(5+무기)×meleeMul×(1+전투스킬3%). 습격: 유예 2일,
간격 ~3일±1, 1마리 시작 2습격당+1, 상한 max(2,림-1)+wealth 보너스, 밴딧
HP20/dmg2/4.2속, A* 추격+벽 공격(sapper), 60초 무전투 퇴각, 다운 밴딧 포섭
50%, 30% 확률 미친 멧돼지 떼 대체. 활: simple_bow 연구+장착 시 화살 3~5dmg.
**늑대(WolfEnemy)**: 코드 완비, `WolvesEnabled=false` 로 전면 차단.

## 5. 동물 — 부분구현
사슴(12/5/tame30%)/멧돼지(25/8/15%, 반격 돌진)/닭(4/2/60%)/토끼(3/1/50%).
배회+피격 도주(초식 2.5×). 길들이기 성공해도 **효과는 자동사냥 면제+파란 tint 뿐**
(추종/번식/생산 없음).

## 6. 아이템/재고/저장 — 완전동작
글로벌 카운터 5종(목재/식량/석재/meals/fineMeals). 자원=물리 더미 entity, 저장존
반입 시 적립(단일화 모델). 저장존 1×1 셀, 우선순위 5단(우클릭 순환), 상향-이주
재운반. 종류 필터 API 는 있으나 UI 토글 제거됨(반쪽). 옥외 부패(고기 빠름,
폭풍 4배 — ⚠ stale 스케일 버그 = balance-parity A1). 선반 없음(더미 ×N 합산).

## 7. 음식 체인 — 완전동작 (닫힌 루프)
채집(베리, ~1200s 재생)/수확(작물 0.011/s 성장, 관개 1.4배)/사냥(고기) → 더미
→ 운반 → 조리 → 식사(3초, 위계별 영양·무드). 부패: 고기 90s/작물 600s/간편식
3000s. 식탁 무드 보너스는 우연 적용(능동 탐색 안 함).

## 8. 건설 목록 — 완전동작
목벽5(HP100)/석벽 석재5(HP280)/문 목25(0.65×)/자동문 목6(0.95×)/울타리·문 목1
(차단 없음)/나무바닥 목1(1.30×)/석재바닥 석1(1.50×)/수면자리 0(0.8)/목침대 45
(1.0,+3)/고급침대 100(1.4,+8)/식탁+의자 40/화덕 80/연구대 75(2×1, r1.5)/램프 4/
바리케이드 5. 건설 5초(바닥 2초). 철강 벽은 데이터만(건설 불가).

## 9. 연구 — 완전동작 (평면 4항목)
원시 활 100pt(전원 활 지급)/개선 화덕 120(조리 2배)/석공술 180(채광+30%)/관개
240(작물+40%). 벤치 r1.5 내 림 manipulation 합×0.15/s. 선행관계 없음.

## 10. 이벤트 — 완전동작
디렉터 3모드(Steady/Calm/Chaos)+위협 tier, 실효 이벤트 36~72h 간격+분위기
텍스트 8~16h. 좋음: 방랑자 합류/행운 발견(목재25)/**상인 도착(실거래 — 위 정정
참조)**. 나쁨: 폭풍(60초, 부패 4배)/사기 저하(-10)/감기 기운. 분위기(무효과):
새들의 징조/여우/조용한 저녁/좋은 수확. ⚠ 상인 이벤트 카드 텍스트가 "실제 거래
시스템: 향후 Day"로 스테일(실제로는 작동) → 정직화 필요.

## 11. 시간/계절/날씨/밤낮 — 완전동작 (계절은 표시만)
1게임일≈1000실초(1x), 배속 1/3/6+일시정지. 밤낮 배경색 9스톱+NightOverlay 램프
라이트맵. 날씨 Clear/Storm 2종. 계절은 ClockUI 표시 전용(밸런스 영향 0).

## 12. 세이브/로드 — 완전동작 (광범위)
자원/시계/림(니즈·스킬·부위HP·출혈·장비·우선순위·일정)/나무 종/침대 품질/저장존
우선순위/벽 재질/작물 성장/지정/연구/구조물 전체(StructureTag 재구성)/레이드
스케줄러. 슬롯 1개(save.json). 손상 시 안전 취소.

## 13. UI — 완전동작 (ThreatAlertUI 만 비활성)
GuiControlBar/ArchitectMenu(F8)/WorkTabUI(F1)/ScheduleUI(F4)/PawnInfoPanel(4탭)/
ColonistBar/SkillUI/ResearchUI(N)/AlertStackUI/EventLogUI(45s)/ResourceCounterUI
(식량 N일치+정착지 가치)/ClockUI/ContextMenuUI/SelectionGizmoBar/SettingsMenu/
ResourceLowAlert/HotkeyCheatSheet(H)/MainMenu. ThreatAlertUI 는 부트스트랩
주석처리(AlertStack 과 중복).

## 14. 튜토리얼 — 완전동작
행동 기반 5단계(해제→저장존→집→농장→완료), 스킵 가능, 25초 딤.

## 15. 온도/실내외 — 온도 없음, 실내외(지붕) 완전동작
지붕 지정+밀폐 자동지붕 → "따뜻한 실내"+3, 무지붕 취침 -4, 폭풍 차단. 작물
주간(06~20시)만 성장. 온도 시스템 없음.

## 16. 사회 상호작용 — 없음
관계/대화/호감도 부재. 유일한 사회 반응 = 동료 사망 -15 thought.
socialMul 은 거래 가격에 사용(위 정정 — 죽은 스탯 아님).

## 17. 정착지 가치 — 완전동작
wealth 프록시(자원+구조물 가중+림×30) → 습격 규모 가산(500당+1, 초반 0.7 완충),
UI "가치: N" 노출. 점수/승리 아님(난이도 입력).

---

## 스텁·빈껍데기 (제거/보강 결정 필요)

1. **늑대** — 코드 완비·전면 비활성. 되살리기 or 정리 결정 필요.
2. **길들이기 결과 없음** — 보강(최소: 추종) or 축소(제거) 판단.
3. **상인 카드 스테일 텍스트** — 텍스트만 정직화 (기능은 정상 — 정정 참조).
4. **ThreatAlertUI** — 비활성 중복 UI, 제거 후보.
5. **철강 벽** — 데이터만 존재, dead 옵션.
6. **저장존 종류 필터** — API·hauler 로직 생존, UI 토글 제거된 반쪽.
7. **계절** — 표시 전용 (작물/온도/이벤트 영향 0).

## 구현돼 있는데 눈에 안 띄는 것 (숨은 자랑거리 — 소개서 재료)

정신붕괴 카타르시스 복귀 / 6부위 상해(다리 감속·팔 작업감속·머리 다운) / 트레잇
8종+배경사연(이름 해시 결정론) / 장비 17종 방어 수학 / 밴딧 sapper(벽 공격) /
포섭+방랑자 = 인구 경로 2개 / 미친 멧돼지 떼(습격 30% 대체) / 적응 난이도(전멸
방지 완충) / 저장존 상향-이주 운반 / 수면 중 자연치유 / 재시작 후에도 구조물
재구성 로드 / 드래그-사각 광역 지정(벽 줄 배치 포함).
