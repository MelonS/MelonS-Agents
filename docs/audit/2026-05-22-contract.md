# Audit report — 2026-05-22 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Forty-fifth contract audit (HEAD `bec0f3b`, 2026-05-22).  All twelve hard rules and the
Conventions block in `docs/operator-contract.md` were checked against code, configuration,
git history, and the pre-computed skill-activation drift report (0 findings — clean).
Four findings from the 44th-cycle report (HEAD `33e7f81`) are RESOLVED in the current HEAD:
(1) `scripts/lyric-extract.sh` §8 medium finding — inline `# §8 exception:` comment and
registry entry added by `bec0f3b`; (2) unreachable commit hash `9c8b69e` in roadmap Done
— replaced with `f5d909a` (verified reachable) by `e8b4162`; (3) three production scripts
undocumented (`music-video-doctor.sh`, `music-video-trim.sh`, `music-video-upload-meta.sh`)
— added to `docs/for-analysts.md` inventory by `e8b4162`; (4) `docs/for-analysts.md:94`
date claim "unchanged since 2026-05-17" — file now correctly reads "unchanged since
2026-05-15".  Twelve new commits have landed since `abb45d0` (the last covered Done entry),
growing the Done gap from 6 to 12.  §5 CLEAN — zero commits to `.claude/agents/*.md` or
`agents/*.md` since `8570a9c` (2026-05-15, pre-marker-convention carry-forward exempt).
§7 CLEAN.  §8 CLEAN — all 12 registered files confirmed ≥1 `# §8 exception:` comment;
no output artifacts under `agents/` or `scripts/`.  §12 CLEAN — secret scan returned only
NLP tokenization code, CI annotation strings, and audit-drift token variables.  §3 money
firewall intact.  All six subagent model assignments match the `docs/for-analysts.md` table.
Skill-activation drift: 0 findings (pre-computed).  One [info] new finding: `scripts/shot-plan.sh`
(added `9c4a081`, 2026-05-22) is present in a research doc but absent from the operational
inventory in `docs/for-analysts.md`; it is an opt-in scaffold (MUSIC_VIDEO_USE_SHOT_PLAN=1
not yet wired) so the absence is low-urgency.  Verdict DRIFT_DETECTED is driven solely by the
structural medium: §9 roadmap "Now" stale, 7th consecutive cycle, operator-gated.

## Findings

- **[medium]** §9 roadmap "Now" stale — 7th consecutive cycle — `docs/roadmap.md:16-41`
  Evidence: The "Now" section was last updated 2026-05-20 ~19:55 KST and describes
  "operator-activation of `job-hunt` v0.4.0" — an operator-only action (copy
  `operator-profile.md`, flip `JH_*_LIVE=1` flags).  Twelve commits have landed since
  `abb45d0` without touching the active subgoal.  The roadmap's inline note already
  acknowledges the structural nature ("stale is structural — the subgoal is operator-only,
  agent can't tick it").  Per audit dimension 2: 7 consecutive stale cycles constitutes
  confirmed drift regardless of cause.
  Suggested fix: Operator either completes the job-hunt activation steps (clears by definition)
  or rewrites "Now" to name agent-actionable follow-on work from the `<!-- suggest -->` block
  (B.1 Stage-2/3, C.1 time-windowed gating, A.3 QA tuning, shot-plan.sh pipeline wiring).

- **[low]** §9 roadmap Done gap — 12 commits without entries since `abb45d0` — `docs/roadmap.md:155+`
  Evidence: `git log abb45d0..HEAD --oneline` returns 12 commits: `8cd5b8e` (bulk-rec #2
  itself), `482500f` (idempotency test + leak fix), `33e7f81` (docs decisions), `ce7dd4d`
  (readme link CHANGELOG), `e33c488` (test log-decision), `dec4557` (test log-decision),
  `dbe2a01` (fix lyrics safe-band), `16332d1` (feat presets cut_density), `e8b4162` (audit
  clearing), `90f9abb` (feat LYRICS_POSITIONS), `9c4a081` (feat shot-plan.sh), `bec0f3b`
  (audit clearing).  Contract §9: "After work lands, append a one-line entry to roadmap Done
  with the commit hash and date."  The gap is larger than the 44th-cycle count (6 → 12);
  the six new commits are `dbe2a01`, `16332d1`, `e8b4162`, `90f9abb`, `9c4a081`, `bec0f3b`.
  Suggested fix: Append Done entries; the six net-new commits can be grouped into two lines:
  lyric / preset refinements (`dbe2a01`, `16332d1`, `90f9abb`) and scaffold + audit-clearing
  (`9c4a081`, `e8b4162`, `bec0f3b`).

- **[low]** Roadmap "Blocked" entry vague — 6th consecutive cycle — `docs/roadmap.md:158-168`
  Evidence: The "Iterative QA-feedback loop inside editor" blocked entry continues to read
  "Worth picking up when the coarse retry is observed to waste compute on a per-output basis"
  — an observation condition, not a user action.  The `To unblock:` paragraph names the
  approval needed but is buried in 4 lines of prose.  Unresolved since 40th cycle.
  Suggested fix: Condense to: "Blocked: operator must (1) observe a retry wasting compute
  on a real output, then (2) approve agent changes to `agents/lib/ffmpeg.sh` per §5."

- **[info]** `scripts/shot-plan.sh` absent from operational inventory — `docs/for-analysts.md`
  Evidence: `scripts/shot-plan.sh` was added in `9c4a081` (2026-05-22, 15:09 KST).  It is
  referenced in `docs/research/2026-05-22-music-video-director-methodology.md` (lines 246,
  460, 543), which satisfies the letter of audit dimension 1 ("mentioned somewhere in `docs/`
  or `README.md`").  However, it is absent from the operational inventory in
  `docs/for-analysts.md` and `docs/music-video-pipeline-reference.md`.  The script is an
  opt-in scaffold gated on `MUSIC_VIDEO_USE_SHOT_PLAN=1` (not yet wired to the pipeline),
  so low-urgency absence.
  Suggested fix: Add a row to the Operator Utilities table in `docs/music-video-pipeline-reference.md`
  once the flag is wired to the pipeline; add a note in `docs/for-analysts.md` at the same time.

- **[info]** Test scripts undocumented — `scripts/test-*.sh` — 3rd consecutive cycle
  Evidence: `test-first-touch.sh`, `test-lyric-extract.sh`, `test-music-video-batch.sh`,
  `test-music-video-trim.sh`, `test-music-video-upload-meta.sh`, `test-music-video-validate.sh`,
  `test-qa-anchor.sh`, `test-shader-gates.sh`, `test-thumbnail.sh`, `test-install-claude-local.sh`,
  `test-log-decision.sh` — none appear in any `docs/` file or README.  `scripts/test-all.sh`
  aggregates them but is itself unlisted in the operational docs.
  Suggested fix: Add a "Test suite" paragraph to `docs/for-analysts.md` CI section naming
  `scripts/test-all.sh` as the aggregator and the per-feature test scripts it covers.

## Drift between docs and code

- `docs/roadmap.md:16` "Now" describes a 2026-05-20 operator-only step while 12 consecutive
  commits address unrelated topics.  The roadmap's own note acknowledges the structural cause.
- `docs/roadmap.md:155+` Done section missing 12 commits since `abb45d0`.
- `scripts/shot-plan.sh` (added `9c4a081`) exists in `scripts/` but is absent from the
  operational `docs/for-analysts.md` inventory (present in research doc only).

## Stale items

- **Roadmap "Now"**: last updated 2026-05-20; stale through 7 consecutive audit cycles.
  Structurally gated on operator action; no agent commit will tick it without operator input.
- **Roadmap Done gap**: 12 commits without entries since `abb45d0`.
- **Roadmap "Blocked" vague**: 6th consecutive cycle unresolved.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✅ PASS | No user-directed terminal recipes in committed scripts |
| §2 Never pause | ✅ PASS (behavioral) | Not statically checkable |
| §3 Money firewall | ✅ PASS | `config/policies.yaml` present; `.env.example` schema-only (no real keys); paid API calls env-var-gated |
| §4 Auto-approve | ✅ PASS | `.claude/settings.json` gitignored (line 141 of `.gitignore`); settings rendered per-machine |
| §5 Logic changes need OK | ✅ PASS | Zero commits to `.claude/agents/*.md` or `agents/*.md` since `8570a9c` (2026-05-15, pre-marker-convention carry-forward exempt) |
| §6 Git workflow | ✅ PASS | `records/` gitignored (line 4); `.env` gitignored (line 69); `.claude/settings.json` gitignored; no output artifacts under `agents/` or `scripts/` |
| §7 Split commit + push | ✅ PASS | No `git commit … && git push` compound found in tracked scripts |
| §8 Code/data separation | ✅ PASS | All 12 registered files confirmed ≥1 `# §8 exception:` comment (lyric-extract.sh registration gap RESOLVED by `bec0f3b`); no output artifacts in `agents/` or `scripts/`; `outputs/publish/upload-meta-v2/*.json` under documented §8 operator-directed deviation |
| §9 Goal + roadmap | ⚠️ DRIFT | "Now" stale 7th cycle (medium, structural/operator-gated); 12 commits without Done entries (low); "Blocked" entry vague (low, 6th cycle) |
| §10 Session resume | ✅ PASS (behavioral) | `docs/daily/` trail current; `scripts/morning-brief.sh` committed and launchd-wired |
| §11 Shutdown protocol | ✅ PASS (behavioral) | Daily report trail intact in `docs/daily/` |
| §12 No PII / secrets | ✅ PASS | Secret scan: `yt-stats-collect.sh` reads OAuth credentials from `~/.config/youtubeuploader/` (gitignored external path, not committed values); all other matches are NLP tokens, CI annotation strings, or audit-drift variables; `.env` gitignored; `.env.example` schema-only |
| Conventions — doc style | ✅ PASS | README EN/KO split; center-aligned headers; for-the-badge badges; role-emoji table; no casual emojis |
| Conventions — README cadence | ✅ PASS | `ce7dd4d` (readme link CHANGELOG) on-trigger; cadence rule maintained |
| Skill-activation drift | ✅ PASS | Pre-computed: 0 findings; 1 manifest; manifests and gated scripts agree |

## Next audit hint

The sole DRIFT_DETECTED driver is structural (operator-only "Now" gate, 7th cycle) — once the
operator completes the job-hunt activation step or redirects "Now" to agent-actionable work,
the verdict will clear.  Next audit should also verify the `scripts/shot-plan.sh` scaffold
has been wired to the pipeline (MUSIC_VIDEO_USE_SHOT_PLAN=1 consumer integration) and added
to the operational inventory, and confirm the Done gap (12 commits) has been closed.
