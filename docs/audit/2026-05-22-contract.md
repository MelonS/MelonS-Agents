# Audit report — 2026-05-22 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Thirty-fifth contract audit (second pass today; prior run at 01:01 KST had HEAD
`a122f99` — this run is HEAD `c04d371`, one commit later).  Checked all twelve hard
rules and the Conventions block in `docs/operator-contract.md` against code,
configuration, and recent git history.  Seven findings: three medium (carry-forward
— `outputs/publish/upload-meta-v2/` committed while architecture table claims
otherwise; portability gap from `~/.claude/CLAUDE.md` split having no bootstrap path;
roadmap Done now 26 commits behind HEAD) and four low (carry-forward — §8 exception
comment absent in `scripts/ffmpeg-throttled.sh:33`, same comment absent in three
2026-05-21 music-video scripts, `docs/for-analysts.md:93` date label 5 days stale,
§8 exception registry line numbers for `agents/lib/env.sh` off by 2 lines).  §5
CLEAN — no commits to `.claude/agents/*.md` or `agents/*.md` since 2026-05-17.  §7
CLEAN — no `&&`-compound git calls in tracked scripts.  §12 CLEAN — secret scan
returned only comments and tokenization code.  New commit `c04d371`
(`feat(audit): skill-activation drift check`) adds `scripts/audit-skill-drift.sh`
with no hardcoded paths; the pre-flight skill-drift report passed with zero findings.
Subagent model assignments: all six agents match the `docs/for-analysts.md` table.

## Findings

- **[medium, carry-forward — 2026-05-20 / 2026-05-21 / 2026-05-22 audits]**
  `outputs/publish/upload-meta-v2/*.json` committed — architecture table claim
  stale — `docs/architecture.md:126`
  Evidence: `git ls-files outputs/publish/` returns `.gitkeep` plus
  `outputs/publish/upload-meta-v2/01-01-rain-lofi-v2.json` through
  `05-05-noir-detective-v2.json` (five files, committed `a182380`, 2026-05-21).
  `docs/architecture.md` Layers table at line 126 states the row has "only
  `.gitkeep` tracked; mp4/wav/jpg gitignored."  `.gitignore` excludes
  `outputs/publish/upload-meta-v[3-9]*/` (v3+) but not v2.  Fourth consecutive
  audit flagging; unresolved across three audit cycles.
  Suggested fix: Either (a) `git rm --cached outputs/publish/upload-meta-v2/*.json`
  and extend `.gitignore` to cover v2, or (b) update `docs/architecture.md:126` to
  document the v2 exception with an inline `<!-- §8 operator-authorized deviation -->`
  marker.

- **[medium, carry-forward — 2026-05-22 audit]** Portability gap —
  `~/.claude/CLAUDE.md` now canonical for operator-style conventions but has no
  repo-tracked template or bootstrap step — `docs/operator-contract.md:22–24`
  Evidence: Commit `9c10dc7` (2026-05-22 00:55 KST) moved Dual-stack reporting,
  Terminal/shell format, Batch execution, Writing tone, Idle-state signaling, and
  Scrum-master footer from `docs/operator-contract.md` to `~/.claude/CLAUDE.md`.
  That file is per-user and local-only.  §8 principle 2 states "Anything required
  to reconstruct the project on a fresh machine is tracked."  No template (e.g.,
  `config/claude-global.template.md`) exists in the repo; `scripts/bootstrap.sh`
  and `scripts/install-claude-local.sh` contain no step to restore it.  Second
  audit flagging; commit was the previous run's HEAD so this is the first
  post-split audit cycle to carry it over.
  Suggested fix: Add `config/claude-global.template.md` (a copy of the moved
  conventions block) and a step in `scripts/install-claude-local.sh` to install
  it to `~/.claude/CLAUDE.md` when absent, making operator-style conventions
  reproducible from a fresh clone.

- **[medium, carry-forward — 2026-05-22 audit]** Roadmap Done section 26 commits
  behind HEAD — `docs/roadmap.md`
  Evidence: Last Done update at `a05c12b` ("docs(roadmap): batch-append Done
  entries — 23 commits since 2026-05-21 01:15").  `git log --oneline a05c12b..HEAD`
  returns 26 commits, including `feat(skill-job-hunt)` plugins (`f9426ed`, `7b68a83`,
  `29e4363`, `0655866`), music-video presets (`82cb15d`, `d9c7c06`), contract split
  (`9c10dc7`), skill-goal-lock (`a122f99`), audit drift check (`c04d371`), and others.
  Contract §9 requires "After work lands, append a one-line entry to roadmap Done
  with the commit hash and date."
  Suggested fix: Batch-append Done entries for commits `f4881cf` through `c04d371`
  (26 commits) to the roadmap Done section.

- **[low, carry-forward — 31st / 32nd / 33rd / 34th audits]** §8 exception comment
  absent in `scripts/ffmpeg-throttled.sh:33` — `scripts/ffmpeg-throttled.sh:33`
  Evidence: Line 33 is `for c in /opt/homebrew/bin/ffmpeg /usr/local/bin/ffmpeg
  /usr/bin/ffmpeg; do` with no `# §8 exception:` inline comment and no entry in the
  §8 exception registry at `docs/operator-contract.md:337–345`.  The same probe
  pattern in `scripts/audit-run.sh:37–42` carries the required comment.  Fifth
  consecutive audit flagging; unresolved across four cycles.
  Suggested fix: Add `# §8 exception: probe-loop fallback — same documented pattern
  as agents/lib/env.sh` above line 33 in `ffmpeg-throttled.sh` and append an entry
  to the registry table in `docs/operator-contract.md`.

- **[low, carry-forward — 33rd / 34th audits]** §8 exception comments absent in
  three 2026-05-21 scripts — `scripts/music-video-lyrics.sh:63–64`,
  `scripts/music-video-stillzoom.sh:37`, `scripts/music-video-canvas.sh:42`
  Evidence: All three files use bare
  `${FFMPEG_BIN:-/opt/homebrew/bin/ffmpeg}` / `${FFPROBE_BIN:-/opt/homebrew/bin/ffprobe}`
  parameter-expansion defaults with no `# §8 exception:` comment.  The required
  comment template is established by `scripts/music-video-shaders.sh:57,64`.  Third
  consecutive audit flagging; unresolved since the 2026-05-21 audit.
  Suggested fix: Add one-line `# §8 exception:` comments following the
  `shaders.sh:57` template to each of the three files and add entries to the
  registry table.

- **[low, carry-forward — 33rd / 34th audits]** `docs/for-analysts.md:93` date
  label 5 days stale — `docs/for-analysts.md:93`
  Evidence: Line 93 reads "`.claude/agents/*.md` frontmatter sets the model.
  As of 2026-05-17:" — today is 2026-05-22.  The model-assignment table itself is
  accurate (all six agents confirmed against frontmatter), but the date anchor
  creates a false impression the table may be outdated.  Third consecutive audit
  flagging.
  Suggested fix: Replace "As of 2026-05-17:" with "As of 2026-05-22: (unchanged
  from 2026-05-17)" or remove the date anchor entirely.

- **[low, carry-forward — 33rd / 34th audits]** §8 exception registry line numbers
  stale for `agents/lib/env.sh` — `docs/operator-contract.md:337`
  Evidence: Registry says `agents/lib/env.sh:38,50` but `grep -n "§8"
  agents/lib/env.sh` shows the first §8 exception comment starts at line 36 and
  the second at line 54 (not 38 and 50).  The comments are present and correct;
  only the registry coordinates are off.
  Suggested fix: Update the registry line reference from `38,50` to `36,54` in
  `docs/operator-contract.md:337`.

## Drift between docs and code

- `docs/architecture.md:126` Layers table row for `outputs/publish/` claims
  "only `.gitkeep` tracked" — five JSON files under
  `outputs/publish/upload-meta-v2/` are committed (fourth cycle of drift).
- `docs/for-analysts.md:93` "As of 2026-05-17" date anchor is stale; model
  assignments themselves are accurate.
- `docs/operator-contract.md:337` §8 exception registry `agents/lib/env.sh:38,50`
  does not match the actual file's §8 comment positions (lines 36 and 54).

## Stale items

- Roadmap Done: 26 commits since last batch append (`a05c12b`), none recorded.
  Range: `f4881cf` (feat: region filter) through `c04d371` (feat: audit skill-drift).
- `CURRENT-ALERT.md` still reflects the 01:01 KST run (HEAD `a122f99`).  The
  underlying medium findings are unresolved so the alert correctly persists; the
  pointer will be refreshed when `scripts/audit-run.sh` next commits this report.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | CLEAN | No user-directed shell recipe found in committed scripts |
| §2 Never pause | CLEAN (behavioral) | Not code-checkable |
| §3 Money firewall | CLEAN | `config/policies.yaml` present; `.env.example` schema-only (no key values) |
| §4 Auto-approve | CLEAN | `.claude/settings.json` gitignored; template at `config/claude-settings.template.json` |
| §5 Logic changes need OK | CLEAN | Zero commits to `.claude/agents/*.md` or `agents/*.md` since 2026-05-17 marker convention; all prior agent-def commits predate 2026-05-17 (carry-forward exempt) |
| §6 Git workflow | CLEAN | `records/` gitignored at `.gitignore:4`; `.env` gitignored at `.gitignore:69`; origin/main in sync |
| §7 Split commit + push | CLEAN | No `&&`-compound `git commit … && git push` pattern found in tracked scripts |
| §8 Code/data separation | DRIFT | `outputs/publish/upload-meta-v2/*.json` committed while architecture table says otherwise (4th cycle); §8 exception comments absent in four scripts |
| §8 Portability | DRIFT (carry-forward) | `~/.claude/CLAUDE.md` conventions block has no repo-tracked template or bootstrap restoration path (2nd cycle) |
| §9 Goal + roadmap | DRIFT | Roadmap Done 26 commits stale; `CURRENT-ALERT.md` persists from prior unresolved findings |
| §10 Session resume | CLEAN (behavioral) | Not code-checkable |
| §11 Shutdown protocol | CLEAN (behavioral) | Not code-checkable |
| §12 No PII / secrets | CLEAN | Secret scan (`api[_-]?key\|secret\|token\|password` outside docs/example files) returned only comments, tokenization code, and GitHub workflow annotation strings; `.env.example` schema-only; no email addresses in committed non-doc files; `ToddStudio` references in `docs/` and `scripts/` are an intentionally public channel name; operator first name in `docs/daily/2026-05-17.md:40` is within a git-log citation in a daily report (immutable; noted in prior audits as [info]) |
| Conventions — doc style | CLEAN | README EN/KO split present; center-aligned headers, badge, role-emoji tables intact |
| Conventions — README cadence | CLEAN | README not edited per-commit since last cadence batch |

## Next audit hint

The two most persistent blockers are the four-cycle-old `outputs/publish/upload-meta-v2/` architecture table drift and the five-cycle-old `ffmpeg-throttled.sh` §8 comment gap; next audit should check whether either has been resolved before re-verifying the §8 exception registry coordinates, which are a lower-priority documentation accuracy issue.
