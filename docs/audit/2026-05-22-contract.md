# Audit report — 2026-05-22 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Forty-second contract audit (HEAD `2ca305c`, 2026-05-22).  All twelve hard rules and the
Conventions block in `docs/operator-contract.md` were checked against code, configuration,
git history, and the pre-computed skill-activation drift report (0 findings — clean).
Three findings from the 41st-cycle report are confirmed RESOLVED: `scripts/generate-intervention-chart.py`
hardcoded project-dir key (fixed via dynamic derivation from `ROOT` in commits `ae73973`/`f5d909a`,
verified at line 75), `docs/review-digest.md` untracked (added to `.gitignore` in `f5d909a`),
and `scripts/music-video-qa-anchor.sh` untracked (committed in `7e52ab8`, tuned in `63431b4`).
Seven findings persist: roadmap "Now" has reached 4th consecutive stale cycle (20+ commits
since its 2026-05-20 update, none referencing job-hunt activation); Done gap stands at ~10
commits; three low-severity portability/housekeeping items carry into their 3rd cycle
(`@@REPO_ROOT@@` unsubstituted, `.playwright-mcp/` ungitignored, `demo-mode-log.txt:1` machine
path); roadmap "Blocked" vague (3rd cycle).  One new low finding: `docs/audit/2026-05-22-all.md`
is untracked (§6 violation).  §5 CLEAN — no commits to `.claude/agents/*.md` or `agents/*.md`
since `8570a9c` (2026-05-15).  §7 CLEAN — no `&&`-compound git calls.  §12 CLEAN — secret scan
returned only tokenization code and comment lines.  §3 money firewall intact.  All six subagent
model assignments match the `docs/for-analysts.md` table.  §8 exception registry: all registered
files confirmed ≥1 `# §8 exception:` comment.  Skill-activation drift: 0 findings.

## Findings

- **[medium]** §9 roadmap "Now" stale — 4th consecutive cycle — `docs/roadmap.md`
  Evidence: The "Now" section was last updated 2026-05-20 ~19:55 KST and names
  "operator-activation of `job-hunt` v0.4.0" as the active subgoal.  Twenty commits have
  landed since that update (`77535c3` through `2ca305c`); none reference job-hunt activation.
  Recent commit subjects: `tune(broll): tighten vocal-genre keyword_pools`, `fix(lyrics):
  apostrophe`, `fix(audit): clear 41st cycle`, `docs(contract): session-start protocol` — all
  music-video quality-bar, audio, audit, or infra.  Per auditor dimension 2: "If the most
  recent 5 commits are about a different topic, the roadmap is stale."  First raised in the
  39th-cycle report; persists through the 42nd.
  Suggested fix: Operator updates "Now" to the music-video quality-bar follow-on phases
  from the `<!-- suggest -->` block (B.1 Stage-2/3, A.3 QA gate, C.1 full time-windowed
  gating), or inserts a note that no agent work is queued and the thread is parked.

- **[medium]** §9 roadmap Done — ~10 commits without entries — `docs/roadmap.md`
  Evidence: Commits not covered by any Done section entry (verified by hash search):
  `a701e41` (docs/daily: A.2 marker-leak), `bde9453` (docs/daily: C.1 Phase 2 note),
  `9ac5d22` (docs/roadmap: Done entries self), `b523c00` (docs/research: qb demo #2),
  `94190c2` (tune/broll: keyword_pools), `bc46a7f` (fix/lyrics: commas), `b5e61de`
  (fix/lyrics: apostrophe), `f5d909a` (fix/audit: 41st cycle), `63431b4` (tune/qa:
  qa-anchor regex), `2ca305c` (docs/contract: session-start protocol).  The partial
  cleanup in `9ac5d22` + `f5d909a` reduced the gap from 11 (41st cycle) to ~10 but
  did not fully close it.  Contract §9: "After work lands, append a one-line entry to
  roadmap Done with the commit hash and date."
  Suggested fix: Batch-append Done entries; daily-note commits (`a701e41`, `bde9453`)
  may group into one bullet; feature/fix commits warrant individual entries.

- **[low]** `@@REPO_ROOT@@` unsubstituted in global template write-paths (3rd cycle) —
  `scripts/install-claude-local.sh:159,167,179`
  Evidence: All three code paths that write `$GLOBAL_TEMPLATE` to `$GLOBAL_TARGET` use
  `cat "$GLOBAL_TEMPLATE"` (line 159, 179) or `getline line < tmpl` (awk block at line 167)
  without piping through the `sed -e "s|@@REPO_ROOT@@|$REPO_ROOT|g"` substitution that is
  present in the agent-defs block (line 84).  `config/claude-global.template.md:19` still
  contains the literal `` `@@REPO_ROOT@@/config/claude-global.template.md` ``.  Any fresh
  `install-claude-local.sh` run embeds the unresolved placeholder in `~/.claude/CLAUDE.md`.
  Unresolved since the 40th cycle.
  Suggested fix: Pipe each `cat "$GLOBAL_TEMPLATE"` through
  `sed -e "s|@@REPO_ROOT@@|$REPO_ROOT|g"` and apply the same substitution in the awk
  `getline` source (read into a variable and apply sed) before printing.

- **[low]** `.playwright-mcp/` runtime directory not in `.gitignore` (3rd cycle) —
  `.gitignore`
  Evidence: `git status --short` shows `?? .playwright-mcp/` (untracked, unignored).
  `grep "playwright" .gitignore` → no match.  Directory contains Playwright MCP runtime
  artifacts.  Unresolved since the 40th cycle.
  Suggested fix: Add `.playwright-mcp/` to `.gitignore` in the "Claude Code local state"
  section.

- **[low]** Machine path in committed evidence log (3rd cycle) —
  `docs/onboarding/demo-mode-log.txt:1`
  Evidence: Line 1 reads `2026-05-19 01:25:59 +0900  PASS  remote=/Users/melons/ai  …`.
  The local checkout path `/Users/melons/ai` is embedded in the committed log.  Lines 2–4
  correctly use the public GitHub URL.  §8 principle 4 prohibits machine-specific values in
  committed files.  Low severity: `melons` matches the public GitHub handle `MelonS`, adding
  no new identity surface.  Unresolved since the 40th cycle.
  Suggested fix: Replace `remote=/Users/melons/ai` with `remote=<local-clone-path>` in a
  one-line cleanup commit to anonymize while preserving the PASS evidence row.

- **[low]** Roadmap "Blocked" entry vague (3rd cycle) — `docs/roadmap.md` lines 148–152
  Evidence: The "Iterative QA-feedback loop inside editor" blocked entry does not name a
  specific user action to unblock.  The trigger phrase "Worth picking up when the coarse
  retry is observed to waste compute on a per-output basis" is an observation condition, not
  a user step.  Contract auditor dimension 2: blocked entries should name what the user must
  do to unblock.  Unresolved since the 40th cycle.
  Suggested fix: Add "To unblock: operator confirms a retry has been observed wasting
  compute on a per-output basis, then approves agent change to `agents/lib/ffmpeg.sh`
  (single-window re-cut helper + opt-in flag in mission retry loops)."

- **[low]** `docs/audit/2026-05-22-all.md` untracked — `docs/audit/2026-05-22-all.md`
  Evidence: `git status --short` shows `?? docs/audit/2026-05-22-all.md`.  The file is an
  audit report (all-focus) that was generated but not committed.  Per §6, every change under
  `docs/` (including audit reports) is committed and pushed on completion.  The `auditor`
  agent definition states reports go to `docs/audit/` "committed — never `records/`" because
  "Audit history must survive a machine swap."  An untracked report provides no durable trail.
  Suggested fix: Commit `docs/audit/2026-05-22-all.md` in a dedicated commit; if the report
  is incomplete or a work-in-progress, add `docs/audit/2026-05-22-all.md` to `.gitignore`
  under a "work-in-progress audit" note until it is finalized.

- **[info]** `docs/for-analysts.md:94` date claim imprecise (3rd cycle)
  Evidence: Line 93 reads "As of 2026-05-22 (unchanged since 2026-05-17):"
  `git log --format="%H %ai" -- ".claude/agents/*.md"` shows the last commit is `8570a9c`
  dated 2026-05-15 22:04:53 +0900 — two days earlier than the "unchanged since" claim.
  The agents are genuinely unchanged; only the date reference is off.  Unresolved since
  the 40th cycle.
  Suggested fix: Change "unchanged since 2026-05-17" to "unchanged since 2026-05-15"
  at `docs/for-analysts.md:94`.

## Drift between docs and code

- `scripts/install-claude-local.sh:159,167,179` renders `config/claude-global.template.md`
  verbatim without substituting `@@REPO_ROOT@@`, contradicting the template's self-referential
  canonical-path note at line 19.
- `docs/roadmap.md` "Now" describes a 2026-05-20 job-hunt activation task while 20 consecutive
  commits address music-video quality-bar, lyrics, metrics, audit, and contract work.
- `docs/onboarding/demo-mode-log.txt:1` embeds the operator's local checkout path
  `/Users/melons/ai` while lines 2–4 of the same file use the public GitHub URL.
- `docs/audit/2026-05-22-all.md` exists locally but is not committed; remote clone cannot
  see this audit report.

## Stale items

- **Roadmap "Now"**: last updated 2026-05-20; stale through 4 consecutive audit cycles with
  20+ commits on a different topic.
- **Roadmap Done**: ~10 commits (`a701e41` through `2ca305c`) without Done-section entries;
  gap partially reduced from 11 (41st cycle) but not closed.
- **`@@REPO_ROOT@@` substitution**: missing in `install-claude-local.sh` write-paths; carries
  into 3rd consecutive audit cycle.
- **`.playwright-mcp/` gitignore**: 3rd consecutive cycle unresolved.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✅ PASS | No user-directed terminal recipes in committed scripts |
| §2 Never pause | ✅ PASS (behavioral) | Not code-checkable from static audit |
| §3 Money firewall | ✅ PASS | `config/policies.yaml` present; `.env.example` schema-only; all paid API keys env-var-gated |
| §4 Auto-approve | ✅ PASS | `.claude/settings.json` gitignored; template at `config/claude-settings.template.json` |
| §5 Logic changes need OK | ✅ PASS | Zero commits to `.claude/agents/*.md` or `agents/*.md` since `8570a9c` (2026-05-15); all pre-2026-05-17 commits carry-forward exempt |
| §6 Git workflow | ⚠️ DRIFT | `docs/audit/2026-05-22-all.md` untracked (§6 violation — audit reports must be committed per auditor agent definition) |
| §7 Split commit + push | ✅ PASS | No `&&`-compound `git commit … && git push` found in tracked scripts |
| §8 Code/data separation | ⚠️ DRIFT | `scripts/install-claude-local.sh:159,167,179` renders global template without `@@REPO_ROOT@@` substitution (low, 3rd cycle); `docs/onboarding/demo-mode-log.txt:1` machine path (low, 3rd cycle) |
| §9 Goal + roadmap | ⚠️ DRIFT | "Now" stale since 2026-05-20 — 4th cycle (medium); ~10 commits without Done entries (medium, persisting); "Blocked" entry vague (low, 3rd cycle) |
| §10 Session resume | ✅ PASS (behavioral) | `docs/daily/` trail current; `scripts/morning-brief.sh` committed `4cd3aaa`; session-start protocol updated `2ca305c` |
| §11 Shutdown protocol | ✅ PASS (behavioral) | Daily report trail intact in `docs/daily/` |
| §12 No PII / secrets | ✅ PASS | Secret scan returned only tokenization code, CI annotation strings, and comment lines; `.env` gitignored; `.env.example` schema-only |
| Conventions — doc style | ✅ PASS | README EN/KO split; center-aligned headers; for-the-badge badges; role-emoji table; no casual emojis |
| Conventions — README cadence | ✅ PASS | Recent cadence triggers (`e415875` README+site refresh, `2ca305c` contract update) handled appropriately |
| Skill-activation drift | ✅ PASS | Pre-computed input: 0 findings; 1 manifest; manifests and gated scripts agree |

## Next audit hint

Focus on closing the three 3rd-cycle housekeeping lows in a single commit: `.playwright-mcp/`
gitignore, `@@REPO_ROOT@@` substitution in `install-claude-local.sh`, and the `demo-mode-log.txt`
machine-path scrub — all are one-line fixes that have persisted long enough to become noise.
