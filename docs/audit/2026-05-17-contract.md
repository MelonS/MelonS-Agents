# Audit report — 2026-05-17 (contract)

**Verdict**: CLEAN

## Summary

Seventh contract-focused audit of the session; HEAD = `605b569`
(2026-05-17 ~12:55 KST).  Supersedes the previous report at HEAD `ebce3a2`
(DRIFT_DETECTED).  Scope: all twelve hard rules in
`docs/operator-contract.md`, §5 commit-marker compliance for all commits
to `.claude/agents/*.md` and root-level `agents/*.md` after the convention
date (`7c6ff4f`), model-assignment parity between `.claude/agents/*.md`
frontmatter and `docs/for-analysts.md`, `.gitignore` coverage, secrets
scan, output-artifact placement, and documentation drift.

Since the prior DRIFT_DETECTED verdict at HEAD `ebce3a2`, one additional
commit landed: `605b569` ("fix(audit): clear two findings introduced this
session").  That commit resolved both remaining medium/low file-content
findings:

1. `scripts/disk-watch.sh:7` — operator first name replaced with
   "operator sees the toast even when the terminal is closed" (§12 fix).
2. `docs/architecture.md:181,186` — B-roll count "6" replaced with
   "8 × B-roll.mp4" and "per-window keyword extraction (8 narration
   windows by default, FACELESS_NUM_BROLL)" (architecture sync fix).

No §5-scope files (`.claude/agents/*.md`, root `agents/*.md`) were
modified in any post-convention commit.  No credentials or API keys found
in committed file content.  `.env` is gitignored and never been part of
any commit.  The CLEAN verdict reflects the absence of any critical, high,
or medium findings at the current HEAD.

## Findings

- **[low, carry-forward]** §8 — Four launchd plist files hardcode `/Users/melons/...` absolute paths — `scripts/com.melons.agents.auditor.plist:15,20,23,26,53`, `scripts/com.melons.agents.queue.plist:15,19,22,25`, `scripts/com.melons.agents.audit-poll.plist:15,19,22,25,46`, `scripts/com.melons.agents.disk-watch.plist:15,19,22,25`
  Evidence: launchd does not expand shell variables in plist XML values;
  absolute paths are structurally required.  All four plists carry an
  inline `<!-- §8 exception: launchd plist values do not expand shell
  variables -->` comment directing future readers to
  `scripts/install-scheduler.sh` for regeneration on machine swap.
  Unchanged finding across five prior audits; no new risk.
  Suggested fix: Generate all four plists from `*.plist.template` sources
  in `scripts/install-scheduler.sh` using `sed "s|@@HOME@@|$HOME|g"` at
  install time; commit only the templates; add rendered `*.plist` to
  `.gitignore`.

- **[info, carry-forward]** §11 — Two 2026-05-16 daily reports use `<ISO-date>-<topic>.md` naming, not `<ISO-date>.md` as specified — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`
  Evidence: `docs/operator-contract.md §11` step 3 specifies
  `docs/daily/<ISO-date>.md`.  Both existing files carry the required
  content sections; only the filename pattern deviates.  Session is
  still active; a 2026-05-17 shutdown report is expected at day-end.
  Unchanged finding across four prior audits.
  Suggested fix: Rename both files to `2026-05-16.md` (merging their
  content) or amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md`
  as an approved variant.

- **[info]** `for-analysts.md:89` — "As of 2026-05-16" subagent table date is one day stale — `docs/for-analysts.md:89`
  Evidence: Line 89 reads "`.claude/agents/*.md` frontmatter sets the
  model.  As of 2026-05-16:".  The table content is accurate; only the
  date annotation lags by one calendar day.  No functional drift.
  Suggested fix: Update the date annotation to 2026-05-17 on the next
  README maintenance pass (per the cadence rule — not worth a standalone
  commit for a date annotation).

- **[info]** Commit `1537ca6` body mentions the operator's first name in git history — `git log --format="%H %B" 1537ca6`
  Evidence: The body of commit `1537ca6` (feat(infra): disk-space watchdog)
  reads "fires a macOS Notification Center toast (osascript) so Todd sees
  the alert even when the terminal is closed."  This is commit metadata,
  not file content; `git grep` does not scan commit bodies.  The §12 rule
  targets file content ("any byte that may end up under `origin/main`");
  commit messages are git object metadata rather than tree-blob content.
  The violation was self-acknowledged in the fix commit (`605b569` body:
  "§12 PII violation introduced in `1537ca6`").  Cannot be removed without
  `git push --force`, which the contract's deny list prohibits.
  Suggested fix: Accept as immutable historical artifact; ensure future
  commit messages use role-neutral phrasing ("the operator", "the user").

## Drift between docs and code

_(none — all doc/code gaps identified in prior audits have been resolved
at HEAD `605b569`)_

## Stale items

- `agents/lib/copyright.sh:10` — comment "Still TODO: audio fingerprint
  (audioprint.sh)"; tracked in `docs/copyright-policy.md` "Still TODO"
  block.  Acceptable; roadmap-aware.
- `scripts/gen-upload-metadata.sh:105-108` — `<TODO: fill manually>`
  placeholder inside an error handler; deliberate design, not dead code.
- No files under `agents/` or `scripts/` showing zero git activity in
  90+ days that are also unreferenced by other files.  All scripts are
  referenced in architecture docs or mission `run.sh` files.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: "bypassPermissions"` in `.claude/settings.json`; allow list covers brew, git, gh, ffmpeg, yt-dlp, whisper, ollama, jq, python3, all file ops; no config-level user-action requirement |
| §2 Never pause | PASS | Behavioral rule; no config-level violation detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` (blank placeholder only); `ANTHROPIC_API_KEY` excluded — "Set in shell, NOT in this file"; `config/policies.yaml` gates `paid_external`; launchd plists use `RunAtLoad=false` for auditor/audit-poll; `disk-watch` uses `RunAtLoad=true` but invokes only local `df` and `osascript` — no paid APIs; `scripts/gen-script-claude.sh` runs against Max-plan subscription quota (pre-paid, no new billing) |
| §4 Auto-approve mode | PASS | Allow list covers all documented local-resource tools; deny list includes `git push --force origin main`, `rm -rf /`, and self-modification of `.claude/settings.json` |
| §5 Logic changes require OK | PASS | Zero commits to `.claude/agents/*.md` or root-level `agents/*.md` after the marker-convention date (`7c6ff4f`, 2026-05-17 04:00). Commits `1537ca6` and `605b569` touched `agents/missions/faceless-short/run.sh` (a shell script, not a `.md` definition file) and both carry `Requested-by: user`. All `.claude/agents/*.md` changes predate `7c6ff4f` — exempt per §5 carry-forward clause |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored (`.gitignore` line 4); `.env` gitignored (`.gitignore` line 52, confirmed via `git check-ignore -v .env` → `.gitignore:52:.env`); `.env` has never appeared in any commit (`git log --all -- '.env'` returns empty); no output artifacts (`.mp4`, `.wav`, `.jpg`, etc.) committed under `agents/` or `scripts/` |
| §7 Split commit and push | PASS (behavioral) | Deny list blocks `git push --force origin main`; no `&&`-compound `commit + push` sequence in any committed shell script |
| §8 Code/data separation | PARTIAL (low, carry-forward) | `agents/lib/env.sh:38,50` — Homebrew keg paths in fallback discovery loop; documented §8 exception (same pattern as prior audits). Four launchd plists (`auditor`, `queue`, `audit-poll`, `disk-watch`) hardcode `/Users/melons/ai/...`; each carries an inline `<!-- §8 exception -->` comment. `scripts/audit-run.sh:47` — Claude CLI fallback paths; documented §8 exception. No output data in logic directories |
| §9 Goal/roadmap source of truth | PASS | `operator-contract.md §9` subagent exclusion list and `CLAUDE.md` session-start protocol are consistent; auditor correctly listed as one of the subagents that does NOT read `docs/goal.md` or `docs/roadmap.md` |
| §10 Session resume protocol | PASS | Behavioral rule; no config-level violation detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two 2026-05-16 daily reports use non-spec `<ISO-date>-<topic>.md` naming — unchanged from prior audits. No 2026-05-17 shutdown report yet (session still active; expected at day-end) |
| §12 No PII or secrets | PASS | `git grep -nE "(api[_-]?key|secret|token|password)"` returns only NLP-tokenization matches in `scripts/correct-captions.py` (code logic, not credentials), `id-token: write` in `.github/workflows/pages.yml` (standard GitHub Actions permissions annotation), and comment/concept matches. No real names in committed file content — `scripts/disk-watch.sh:7` fix landed in `605b569`. No API keys, OAuth secrets, or passwords in any committed file. `.env` gitignored |

## Next audit hint

All medium and higher findings are now resolved. On the next audit, verify
whether the §11 daily-report naming carry-forward has been addressed (either
by renaming the 2026-05-16 files or by an explicit §11 amendment permitting
topic suffixes), and confirm the 2026-05-17 shutdown report was written at
day-end per §11.
