# Audit report — 2026-05-17 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Eleventh contract-focused audit of the session; HEAD = `00e0f3d`
(2026-05-17 ~16:11 KST).  Supersedes the tenth audit at HEAD `d9eb5f4`.
Scope: all twelve hard rules in `docs/operator-contract.md`, §5
commit-marker compliance for all commits to `.claude/agents/*.md` and
root-level `agents/*.md` after the marker-convention date (`7c6ff4f`),
model-assignment parity between `.claude/agents/*.md` frontmatter and
`docs/for-analysts.md`, `.gitignore` coverage, secrets scan, output-artifact
placement, and documentation drift.

One new commit since the tenth audit:

- `00e0f3d` — feat(music-video): integrate v6 vintage-lofi treatment into
  mission.  Only `agents/missions/music-video/run.sh` (a `.sh` file) was
  modified.  No §5-scope definition files touched (`.claude/agents/*.md` or
  root-level `agents/*.md`).  Commit carries `Requested-by: user` — this
  marker is §5-scoped and technically unnecessary for a `.sh` modification,
  but its presence is harmless and documents operator authorization.
  No new findings from this commit.

No §5-scope definition files (`.claude/agents/*.md`, root-level `agents/*.md`)
have been modified since `7c6ff4f`.  No credentials or API keys found in
committed file content.  `music-video` mission (`00e0f3d`) makes no Anthropic
API calls — confirmed by `grep`; uses only `aubio`, `ffmpeg`, and the Pexels
free-tier API.  Verdict remains DRIFT_DETECTED due to the unresolved [medium]
architecture drift (Mission types table and "All four" prose) carried forward
from the tenth audit.

## Findings

- **[medium]** Architecture drift — `music-video` absent from Mission types
  table and "All four" prose — `docs/architecture.md:128–135,143`
  Evidence: `docs/architecture.md` lines 130–135 Mission types table lists
  four rows only (highlight, summarize, shorts-batch, faceless-short);
  `music-video` is absent despite `agents/missions/music-video/run.sh`
  existing since `828070f`.  Line 143 reads "All four reuse the same shell
  library set" — inaccurate: there are now five missions; `music-video` uses
  `aubio`/ffmpeg/Pexels, not `ollama.sh`, `whisper.sh`, or `tts.sh`.  Commit
  `d9eb5f4` fixed the ASCII art diagram and `docs/for-analysts.md`; the
  Markdown table and prose sentence were not updated.  An analyst reading the
  table gets an incomplete picture of system surface area.
  Suggested fix: Add a `music-video` row (Input: operator-supplied music file
  + mood keywords; Output: one short, music-as-sole-audio, no captions;
  Time-dominant: beat-sync + render); change line 143 to "The first four
  transformation/generation missions reuse the shell library set below;
  `music-video` uses a distinct subset (`env.sh`, `log.sh`, `ffmpeg.sh`)."

- **[low, carry-forward]** §12 — LinkedIn URL with real-name path component
  committed — `site/index.html:157`
  Evidence: `site/index.html:157` contains
  `<a href="https://www.linkedin.com/in/eun-hyung-choi" ...>LinkedIn</a>`.
  The URL path `eun-hyung-choi` encodes a real-name identifier.  §12 is a
  hard rule; `Requested-by: user` (present in `75b10a8`) documents
  intentionality but is not a defined §12 override mechanism.  Risk is low
  (intentionally public professional-networking URL); unchanged since tenth
  audit.
  Suggested fix: Amend `docs/operator-contract.md §12` to add an explicit
  operator-override clause for identity information on intentionally public
  surfaces, and add a `<!-- §12 operator-authorized deviation -->` comment
  beside the anchor in `site/index.html:157`.

- **[low, carry-forward]** §8 — Four launchd plist files hardcode
  `/Users/melons/...` absolute paths —
  `scripts/com.melons.agents.auditor.plist:15,20,23,26,53`,
  `scripts/com.melons.agents.audit-poll.plist:15,19,22,25,46`,
  `scripts/com.melons.agents.disk-watch.plist:15,19,22,25`,
  `scripts/com.melons.agents.queue.plist:15,19,22,25`
  Evidence: launchd does not expand shell variables in plist XML values;
  absolute paths are structurally required by the launchd runtime.  All four
  plists carry an inline
  `<!-- §8 exception: launchd plist values do not expand shell variables -->`
  comment.  Unchanged across nine prior audits; no new risk added.
  Suggested fix: Generate all four plists from `*.plist.template` sources via
  `sed "s|@@REPO@@|$(pwd)|g; s|@@HOME@@|$HOME|g"` at install time in
  `scripts/install-scheduler.sh`; commit only the templates; add rendered
  `*.plist` to `.gitignore`.

- **[info, carry-forward]** `for-analysts.md:89` — "As of 2026-05-16"
  subagent table header is one day stale — `docs/for-analysts.md:89`
  Evidence: Line 89 reads "As of 2026-05-16:" but the file was last updated
  substantively by `d9eb5f4` on 2026-05-17.  Table model assignments are
  accurate (all six agents confirmed against frontmatter: orchestrator=opus,
  planner/resourcer/editor/qa/auditor=sonnet); only the date annotation lags.
  Suggested fix: Update to "As of 2026-05-17" on the next README maintenance
  trigger.

- **[info, carry-forward]** §11 — Two 2026-05-16 daily reports use
  `<ISO-date>-<topic>.md` naming, not `<ISO-date>.md` as specified —
  `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`
  Evidence: `docs/operator-contract.md §11` step 3 specifies
  `docs/daily/<ISO-date>.md`.  Both files carry the required content sections;
  only the filename pattern deviates.  No 2026-05-17 daily report exists yet
  (session still active; expected per §11 lifecycle).
  Suggested fix: Rename both files to `2026-05-16.md` (merging content) or
  amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants.

- **[info, carry-forward]** Operator first name in committed git history —
  immutable
  Evidence: Commit body of `1537ca6` contains "so Todd sees the alert"; git
  commit history cannot be rewritten without `git push --force`, which the
  deny list prohibits.
  Suggested fix: Accept as immutable artifact; use role-neutral phrasing
  ("the operator") in future commit messages.

## Drift between docs and code

- `docs/architecture.md` Mission types table (lines 130–135) omits
  `music-video` even though `agents/missions/music-video/run.sh` has existed
  since `828070f` (2026-05-17 ~15:18 KST).  The ASCII art diagram and
  `docs/for-analysts.md` were corrected in `d9eb5f4`; the Markdown table and
  prose ("All four") were not.  See [medium] finding above.

## Stale items

- `agents/lib/copyright.sh:10` — "Still TODO: Audio fingerprint check" —
  cross-referenced and tracked in `docs/copyright-policy.md` "Still TODO"
  block.  Acceptable; roadmap-aware.
- `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>`
  placeholder strings inside a JSON error-recovery handler; deliberate design
  (operator fills in if the ollama-draft fails), not development debt.
- No files under `agents/` or `scripts/` with 90+ days of inactivity that
  are unreferenced by other files (the repo was initialized 2026-05-14;
  oldest committed files are ~3 days old, well within the 90-day threshold).

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: "bypassPermissions"` in `.claude/settings.json`; allow list covers all local-resource tools; no config-level user-action requirement found |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` (blank placeholder); `ANTHROPIC_API_KEY` comment says "Set in shell, NOT in this file"; `config/policies.yaml` gates `paid_external`; `launchctl RunAtLoad=false` on auditor/poll plists; `music-video` mission makes zero Anthropic calls (confirmed by grep) |
| §4 Auto-approve mode | PASS | Allow list covers all documented local tools (brew, git, gh, ffmpeg, yt-dlp, whisper, ollama, python3, npm, etc.); deny list guards catastrophic ops (`rm -rf /`, force-push to main) |
| §5 Logic changes require OK | PASS | Zero commits to `.claude/agents/*.md` or root-level `agents/*.md` after marker-convention date (`7c6ff4f`, 2026-05-17 04:00 KST).  Commit `00e0f3d` touches `agents/missions/music-video/run.sh` (`.sh` file, not §5-scoped).  All `.claude/agents/*.md` changes (`af9857f`, `27a8c86`) predate `7c6ff4f` — exempt per §5 carry-forward clause |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored; `.env` gitignored; no output artifacts under `agents/` or `scripts/`; `assets/music/*` excluded (only `.gitkeep` + `README.md` tracked); `.mp4`, `.wav`, `.mp3`, `.srt` all gitignored globally |
| §7 Split commit and push | PASS (behavioral) | Deny list blocks `git push --force origin main`; no `&&`-compound `commit + push` found in committed shell scripts |
| §8 Code/data separation | PARTIAL (low, carry-forward) | Four launchd plists hardcode `/Users/melons/ai/...` (documented inline §8-exception comments).  `agents/lib/env.sh:38,50` uses `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg` as a known-keg fallback only when `$FFMPEG_BIN` is unset (documented §8 exception). |
| §9 Goal/roadmap source of truth | PASS | `operator-contract.md §9` subagent exclusion list consistent with `CLAUDE.md`; `docs/goal.md` active goal shows ACHIEVED per `d3c12f3`; roadmap Now is correctly in a "user sets next goal" hold state |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two 2026-05-16 daily reports use `<ISO-date>-<topic>.md` naming variant (see info finding); no 2026-05-17 shutdown report yet (session still active — expected) |
| §12 No PII or secrets | PARTIAL (low, carry-forward) | `site/index.html:157`: URL with real-name path committed in `75b10a8` with explicit `Requested-by: user`; no contract override clause defined.  Secrets scan (git grep across non-docs, non-example files) returns only placeholder/comment matches.  `.env` gitignored. |

## Next audit hint

The single highest-priority open item remains the [medium] architecture drift:
add the `music-video` row to the Mission types table and fix the "All four"
prose in `docs/architecture.md` (two-line change) — this clears the
DRIFT_DETECTED verdict and closes the CURRENT-ALERT.  The §12 LinkedIn
carry-forward needs an operator decision on formalizing the override clause
before it can be marked resolved.
