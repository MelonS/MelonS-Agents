# Audit report — 2026-05-17 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Thirteenth contract-focused audit of the session; HEAD = `6b5450e`
(2026-05-17 ~17:45 KST).  Supersedes the twelfth audit at HEAD `44a0acf`.
Scope: all twelve hard rules in `docs/operator-contract.md`, §5
commit-marker compliance for commits to `.claude/agents/*.md` and root-level
`agents/*.md` after the marker-convention date (`7c6ff4f`), model-assignment
parity between `.claude/agents/*.md` frontmatter and `docs/for-analysts.md`,
`.gitignore` coverage, secrets scan, and output-artifact placement.

Two new commits since the twelfth audit:

- `b46a2ba` — docs(roadmap): queue filter-repo backup branch deletion for
  tomorrow.  Adds an entry under "Next" that embeds `todd@lightweight.kr`
  (the email removed from git history) as a literal string in a committed,
  editable public document — a new §12 finding.
- `6b5450e` — fix(security): tighten `.claude/settings.json` — scopes `rm`
  to repo / /tmp / Trash, removes unused `ssh *` and `chown *` allow-list
  entries.  `Requested-by: user` marker present.  No §5-scope agent-definition
  files touched.

No `.claude/agents/*.md` or root-level `agents/*.md` files have been modified
since the marker-convention commit `7c6ff4f`.  Secrets scan returns only
placeholder/comment matches.  No output artifacts committed under `agents/` or
`scripts/`.  Two new [low] §12 findings surfaced (one missed by the twelfth
audit, one genuinely new); the four carry-forward items from prior audits
remain open.  Verdict stays DRIFT_DETECTED until §12 email references are
redacted or explicitly accepted.

## Findings

- **[low]** §12 — Real email address in committed daily report —
  `docs/daily/2026-05-17.md:51`
  Evidence: Line 51 contains the literal string
  `` `todd@lightweight.kr` 완전 제거 `` inside a table row documenting the
  decision to rewrite git history.  The email (personal identifier: real first
  name + domain) is now committed to a public, indexed document.  The file was
  committed in `44a0acf`; the twelfth audit ran with HEAD = `44a0acf` but did
  not surface this reference.  Unlike commit bodies, this file is editable
  without force-push.
  Suggested fix: Replace `` `todd@lightweight.kr` `` on line 51 with
  `` `[redacted email]` `` or simply "개인 이메일" to remove the literal
  address while preserving the audit trail context.

- **[low]** §12 — Real email address in committed roadmap "Next" entry —
  `docs/roadmap.md:55`
  Evidence: Line 55 reads "history rewrite (replacing `todd@lightweight.kr`
  with the GitHub noreply email)."  Added in `b46a2ba` (post-twelfth-audit).
  Same email as above; same editability argument.
  Suggested fix: Replace `` `todd@lightweight.kr` `` on line 55 with
  `` `[redacted email]` `` or "the personal commit email" — the meaning
  (backup-branch safety net for the history rewrite) survives the redaction.

- **[low, carry-forward]** §12 — LinkedIn URL with real-name path component —
  `site/index.html:169`
  Evidence: `site/index.html:169` contains
  `<a href="https://www.linkedin.com/in/eun-hyung-choi" ...>LinkedIn</a>`.
  The URL path component `eun-hyung-choi` encodes a real-name identifier in a
  public committed file.  `Requested-by: user` is present in commit `75b10a8`.
  §12 has no formal operator-override clause for intentionally public
  identity surfaces.  Unchanged across four prior audits.
  Suggested fix: Amend `docs/operator-contract.md §12` to add an explicit
  operator-override clause for identity information on intentionally public
  surfaces, then add a `<!-- §12 operator-authorized deviation -->` comment
  beside the anchor at `site/index.html:169`.

- **[low, carry-forward]** §8 — Four launchd plists hardcode `/Users/melons/…`
  with inaccurate migration comment —
  `scripts/com.melons.agents.auditor.plist:15,20,23,26,53`,
  `scripts/com.melons.agents.audit-poll.plist:15,19,22,25,46`,
  `scripts/com.melons.agents.disk-watch.plist:15,19,22,25`,
  `scripts/com.melons.agents.queue.plist:15,19,22,25`
  Evidence: launchd does not expand shell variables in plist XML values;
  absolute paths are structurally required — all four plists carry an inline
  `<!-- §8 exception: … When the repo is relocated, re-run
  scripts/install-scheduler.sh on the new machine to regenerate the LaunchAgents
  copy. -->` comment.  However, `scripts/install-scheduler.sh` performs a plain
  `cp "$src" "$dst"` (not a template expansion), so on a machine swap the
  hardcoded `/Users/melons/` paths remain wrong even after re-running the
  installer.  Unchanged across eleven prior audits.
  Suggested fix: Generate all four plists from `*.plist.template` sources via
  `sed "s|@@REPO@@|$(pwd)|g; s|@@HOME@@|$HOME|g"` at install time inside
  `scripts/install-scheduler.sh`; commit only the `.plist.template` files; add
  the rendered `*.plist` files to `.gitignore`.

- **[info, carry-forward]** `for-analysts.md:89` — "As of 2026-05-16" subagent
  table header is three days stale — `docs/for-analysts.md:89`
  Evidence: Line 89 reads "As of 2026-05-16:" but all six agents'
  model assignments (orchestrator=opus; planner/resourcer/editor/qa/auditor=
  sonnet) are current and confirmed against frontmatter.  Only the date
  annotation lags.
  Suggested fix: Update to "As of 2026-05-17" on the next README maintenance
  trigger.

- **[info, carry-forward]** §11 — Two 2026-05-16 daily reports use
  `<ISO-date>-<topic>.md` naming instead of `<ISO-date>.md` —
  `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`
  Evidence: §11 step 3 specifies `docs/daily/<ISO-date>.md`.  The 2026-05-17
  report correctly uses `2026-05-17.md`; the anomaly is confined to the two
  legacy 2026-05-16 files.  Both carry the required content sections.
  Suggested fix: Rename both to `2026-05-16.md` (merging content) or amend
  §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants for
  multi-session days.

- **[info, carry-forward]** Operator first name in committed git history body —
  immutable — commit `1537ca6`
  Evidence: Commit body of `1537ca6` contains "so Todd sees the alert."
  Git history cannot be rewritten without `git push --force`, which the deny
  list prohibits.
  Suggested fix: Accept as immutable artifact; use role-neutral phrasing
  ("the operator") in future commit messages.

## Drift between docs and code

None.  All five missions are present in `docs/architecture.md` Mission types
table (lines 132–136).  The "first four missions reuse the same shell library
set; music-video uses a distinct subset" prose (lines 146–149) accurately
reflects the code.  `docs/for-analysts.md` subagent model assignments match
`.claude/agents/*.md` frontmatter for all six agents.

## Stale items

- `agents/lib/copyright.sh:10` — "Still TODO: Audio fingerprint check" and
  "Logo / watermark detection" — cross-referenced and tracked as open items
  in `docs/copyright-policy.md`.  Roadmap-aware; not stale.
- `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>`
  placeholder strings in a JSON error-recovery handler; intentional by design.
- No files older than 90 days exist (repo initialized 2026-05-14).

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: bypassPermissions`; allow list covers all local-resource tools; no user-action requirement found in config |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` blank placeholder, `ANTHROPIC_API_KEY` explicitly set-in-shell-only; `config/policies.yaml` gates `paid_external`; `RunAtLoad=false` on all launchd plists; `music-video/run.sh` makes zero Anthropic calls (confirmed) |
| §4 Auto-approve mode | PASS | Allow list covers all documented local tools; `6b5450e` tightened `rm` scope and removed unused `ssh *` / `chown *` — current deny list guards catastrophic ops |
| §5 Logic changes require OK | PASS | Zero commits to `.claude/agents/*.md` or root-level `agents/*.md` after marker-convention date (`7c6ff4f`).  All pre-existing agent commits (`af9857f`, `27a8c86`) predate `7c6ff4f` — exempt per carry-forward clause. `6b5450e` modifies `.claude/settings.json` (not an agent definition file; §5 scope is `agents/*.md` and `.claude/agents/*.md` only) and carries `Requested-by: user` |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored (only `.gitkeep` tracked); `.env` gitignored; no output artifacts under `agents/` or `scripts/`; `assets/music/*` excluded; `.mp4`, `.wav`, `.mp3`, `.srt` all gitignored globally |
| §7 Split commit and push | PASS (behavioral) | Deny list blocks `git push --force origin main`; no `&&`-compound `commit + push` found in committed shell scripts |
| §8 Code/data separation | PARTIAL (low, carry-forward) | Four launchd plists hardcode `/Users/melons/ai/…` with inaccurate migration comment (install-scheduler does not regenerate paths). `agents/lib/env.sh:38,50` uses `/opt/homebrew/opt/ffmpeg-full/bin/ffmpeg` as fallback-discovery candidate only — documented §8 exception in `scripts/audit-run.sh:37–42` |
| §9 Goal/roadmap source of truth | PASS | `docs/goal.md` active goal ACHIEVED; roadmap Now correctly in "user sets next goal" hold state |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | `docs/daily/2026-05-17.md` committed (`44a0acf`) using correct `<ISO-date>.md` naming — §11 followed for today. Two legacy 2026-05-16 reports use non-standard naming (see info finding) |
| §12 No PII or secrets | PARTIAL (low) | Three §12 issues: (1) `docs/daily/2026-05-17.md:51` — `todd@lightweight.kr` literal email in daily report (new/missed finding); (2) `docs/roadmap.md:55` — same email in roadmap Next entry (added in `b46a2ba`); (3) `site/index.html:169` — LinkedIn real-name URL (carry-forward, operator-authorized). Secrets scan returns only placeholder/comment matches. `.env` gitignored. |

## Next audit hint

The two §12 email-redaction fixes in `docs/daily/2026-05-17.md:51` and
`docs/roadmap.md:55` are the shortest path to narrowing the DRIFT_DETECTED
finding; both are single-line edits in tracked files, unlike the immutable
commit-body reference in `1537ca6`.
