# Audit report — 2026-05-17 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Ninth contract-focused audit of the session; HEAD = `828070f`
(2026-05-17 ~15:18 KST).  Supersedes the eighth audit at HEAD `2af2c0b`.
Scope: all twelve hard rules in `docs/operator-contract.md`, §5 commit-marker
compliance for all commits to `.claude/agents/*.md` and root-level `agents/*.md`
after the marker-convention date (`7c6ff4f`), model-assignment parity between
`.claude/agents/*.md` frontmatter and `docs/for-analysts.md`, `.gitignore`
coverage, secrets scan, output-artifact placement, and documentation drift.

Two new commits since the eighth audit at `2af2c0b`:

- `e138108` — fix(faceless): music pool is local-only — never committed.
  Removes the `!assets/music/*.mp3` allowlist that `2af2c0b` introduced and
  replaces it with `assets/music/*` exclusion (only `.gitkeep` + `README.md`
  remain tracked).  Carries `Requested-by: user` marker.  No §5-scope `.md`
  definition files touched.  No new findings; corrects a detail noted in the
  prior [info] finding about `assets/music/`.
- `828070f` — feat(music-video): new mission + decision-log pick.  Adds
  `agents/missions/music-video/run.sh` (a `.sh` file, not a §5-scope `.md`
  definition file) and updates `docs/pilots/decision-log.md`.  Carries
  `Requested-by: user` marker.  The new mission type (`music-video`) is absent
  from `docs/architecture.md` Mission types table (lines 123–128) and from
  `docs/for-analysts.md` (line 111 claims "three of the four missions").
  **New [medium] finding.**

No §5-scope definition files (`.claude/agents/*.md`, root-level `agents/*.md`)
have been modified since `7c6ff4f`.  No credentials or API keys found in
committed file content.  Verdict is DRIFT_DETECTED due to the [low] §12
carry-forward finding and the new [medium] architecture drift from `828070f`.

## Findings

- **[medium]** Architecture drift — `music-video` mission absent from
  `docs/architecture.md` Mission types table and `docs/for-analysts.md` —
  `docs/architecture.md:121-128`, `docs/for-analysts.md:111`
  Evidence: Commit `828070f` added `agents/missions/music-video/run.sh`;
  `ls agents/missions/` now lists five types (faceless-short, highlight,
  music-video, shorts-batch, summarize).  `docs/architecture.md:121-128`
  Mission types table lists four types only (highlight, summarize, shorts-batch,
  faceless-short); the Tier 2 diagram at lines 63–73 likewise shows four
  mission boxes.  `docs/for-analysts.md:111` reads "Three of the four missions
  (`agents/missions/{highlight,summarize,shorts-batch}/run.sh`) do not call
  Anthropic. They call `agents/lib/ollama.sh`" — the mission count is wrong
  (five missions now), and `music-video` (which calls neither Anthropic nor
  Ollama — it is purely local: aubiotrack + aubioonset + Pexels API + ffmpeg)
  is absent.  An analyst relying on these docs would have an incorrect picture
  of the system surface area and the Tier 2 tool inventory.
  Suggested fix: Add a `music-video` row to the Mission types table in
  `docs/architecture.md`, add a fifth box in the Tier 2 diagram, and update
  `docs/for-analysts.md:111` to reflect five missions on the next README
  maintenance pass (cadence trigger #4 — architecture change).

- **[low, carry-forward]** §12 — LinkedIn URL with real-name path component
  committed to `site/index.html` — `site/index.html:157` (added in `75b10a8`)
  Evidence: `site/index.html:157`:
  `<a href="https://www.linkedin.com/in/eun-hyung-choi" ...>LinkedIn</a>`.
  The URL path `eun-hyung-choi` encodes a real-name identifier.  §12 is a hard
  rule ("non-negotiable; violating these is a bug").  The commit body carries
  `Requested-by: user` and documents the intentionality, but `Requested-by:
  user` is defined in §5 as an audit-trail for logic-change approvals — not a
  §12 override mechanism.  Risk is low (intentionally public information on a
  professional networking site) but the contract has no explicit override clause
  for this pattern.  Unchanged finding across three prior audits.
  Suggested fix: Amend `docs/operator-contract.md` §12 to add an explicit
  operator-override clause ("Identity information on intentionally public
  surfaces may be committed with an explicit `Requested-by: user` marker and a
  §12-deviation note in the commit body") and add a
  `<!-- §12 operator-authorized deviation -->` comment next to the LinkedIn
  anchor in `site/index.html`.

- **[low, carry-forward]** §8 — Four launchd plist files hardcode
  `/Users/melons/...` absolute paths —
  `scripts/com.melons.agents.auditor.plist:15,20,23,26,53`,
  `scripts/com.melons.agents.audit-poll.plist:15,19,22,25,46`,
  `scripts/com.melons.agents.disk-watch.plist:15,19,22,25`,
  `scripts/com.melons.agents.queue.plist:15,19,22,25`
  Evidence: launchd does not expand shell variables in plist XML values;
  absolute paths are structurally required.  All four plists carry an inline
  `<!-- §8 exception: launchd plist values do not expand shell variables -->`
  comment.  Unchanged finding across seven prior audits; no new risk.
  Suggested fix: Generate all four plists from `*.plist.template` sources in
  `scripts/install-scheduler.sh` using `sed "s|@@HOME@@|$HOME|g"` at install
  time; commit only the templates; add rendered `*.plist` to `.gitignore`.

- **[info, carry-forward]** Architecture drift — `assets/music/` directory not
  in `docs/architecture.md` layer table — `docs/architecture.md:114-119`
  Evidence: `assets/music/.gitkeep` and `assets/music/README.md` are tracked
  (audio files correctly gitignored after `e138108` fix — the prior audit's
  note that `.gitignore` "correctly allowlists `!assets/music/*.mp3`" is now
  superseded: `e138108` reverted that allowlist so only the metadata files
  travel with the repo).  The layer table at lines 114–119 still does not
  mention `assets/`.  No functional impact; documentation lags.
  Suggested fix: Add `assets/music/` (metadata only — `.gitkeep` + `README.md`
  tracked; audio files gitignored) to the architecture layer table on the next
  README maintenance pass.

- **[info, carry-forward]** §11 — Two 2026-05-16 daily reports use
  `<ISO-date>-<topic>.md` naming, not `<ISO-date>.md` as specified —
  `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`
  Evidence: `docs/operator-contract.md §11` step 3 specifies
  `docs/daily/<ISO-date>.md`.  Both files carry required content sections; only
  the filename pattern deviates.  Unchanged finding across six prior audits.
  Suggested fix: Rename both files to `2026-05-16.md` (merging content) or
  amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants.

- **[info, carry-forward]** `for-analysts.md:89` — "As of 2026-05-16" subagent
  table header is one day stale — `docs/for-analysts.md:89`
  Evidence: Line 89 reads "As of 2026-05-16:" but the file was last
  substantively updated by `ebce3a2` on 2026-05-17.  Table model assignments
  are accurate; only the date annotation lags.
  Suggested fix: Update to "As of 2026-05-17" on the next README maintenance
  pass (not worth a standalone commit).

- **[info, carry-forward]** Commit `1537ca6` body mentions operator first name
  in git history — immutable
  Evidence: Commit body of `1537ca6` contains "so Todd sees the alert"; git
  commit metadata cannot be removed without `git push --force`, which the deny
  list prohibits.
  Suggested fix: Accept as immutable artifact; use role-neutral phrasing
  ("the operator") in future commit messages.

## Drift between docs and code

- `agents/missions/music-video/run.sh` exists (added in `828070f`) but
  `music-video` is absent from `docs/architecture.md` Mission types table
  (lines 121–128) and the Tier 2 diagram (lines 63–73).
  `docs/for-analysts.md:111` mission count is now wrong ("four missions" where
  there are five).  See [medium] finding above.
- `assets/music/` directory (`.gitkeep` + `README.md` tracked; audio files
  gitignored after `e138108` fix) not listed in `docs/architecture.md` layer
  table (line 114–119).  See [info] finding above.

## Stale items

- `agents/lib/copyright.sh:10` — "Still TODO: audio fingerprint" — tracked in
  `docs/copyright-policy.md` "Still TODO" block.  Acceptable; roadmap-aware.
- `scripts/gen-upload-metadata.sh:105-108` — `<TODO: fill manually>` placeholder
  strings inside a JSON error-recovery handler; deliberate design, not
  development debt.
- No files under `agents/` or `scripts/` with 90+ days of inactivity that are
  also unreferenced by other files.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: "bypassPermissions"` in `.claude/settings.json`; allow list covers all local-resource tools; no config-level user-action requirement found |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` (blank placeholder); `ANTHROPIC_API_KEY` comment says "Set in shell, NOT in this file"; `config/policies.yaml` gates `paid_external`; GitHub Actions workflow deploys static HTML only; launchd plists use `RunAtLoad=false` for auditor/poll |
| §4 Auto-approve mode | PASS | Allow list covers all documented local tools; deny list guards catastrophic ops (`rm -rf /`, force-push to main, `curl \| sh`, `sudo`) |
| §5 Logic changes require OK | PASS | Zero commits to `.claude/agents/*.md` or root-level `agents/*.md` after marker-convention date (`7c6ff4f`).  New commits `e138108` and `828070f` both modify `.sh` / `.gitignore` / `decision-log.md` (non-definition files) and both carry `Requested-by: user`.  All `.claude/agents/*.md` changes predate `7c6ff4f` — exempt per §5 carry-forward clause |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored; `.env` gitignored (never committed — `git log --all -- '.env'` empty); no output artifacts under `agents/` or `scripts/`; `e138108` corrected `assets/music/` gitignore: audio files now properly excluded, only `.gitkeep` + `README.md` tracked |
| §7 Split commit and push | PASS (behavioral) | Deny list blocks `git push --force origin main`; no `&&`-compound `commit + push` in committed shell scripts; `audit-run.sh:99` `&&` pattern is inside a log string, not executed code |
| §8 Code/data separation | PARTIAL (low, carry-forward) | Four launchd plists hardcode `/Users/melons/ai/...` (documented §8 exceptions with inline comments).  `agents/lib/env.sh:38,50` ffmpeg-full keg fallback paths (documented §8 exception).  `assets/music/` is logic-layer metadata (README + .gitkeep only) — correct placement |
| §9 Goal/roadmap source of truth | PASS | `operator-contract.md §9` subagent exclusion list consistent with `CLAUDE.md`; auditor correctly excluded from reading `docs/goal.md` or `docs/roadmap.md` |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two 2026-05-16 reports use `<ISO-date>-<topic>.md` naming variant; no 2026-05-17 shutdown report yet (session still active) |
| §12 No PII or secrets | PARTIAL (low, carry-forward) | `site/index.html:157`: URL with real-name path committed in `75b10a8` with explicit `Requested-by: user`; no contract override clause yet defined.  No API keys, tokens, or passwords in committed files.  `.env` gitignored.  Secrets scan: only code-logic and annotation matches (NLP tokenization code, `id-token: write` GitHub Actions annotation) |

## Next audit hint

The highest-priority open item is the [medium] architecture drift: `music-video`
exists as `agents/missions/music-video/run.sh` but is absent from
`docs/architecture.md` Mission types table, the Tier 2 diagram, and
`docs/for-analysts.md` mission count.  Both files mislead analysts about system
surface area and Tier 2 tool inventory.  Closable in one README maintenance pass
(cadence trigger #4 — architecture change has landed).  The carry-forward [low]
§12 finding requires a contract amendment decision from the operator.
