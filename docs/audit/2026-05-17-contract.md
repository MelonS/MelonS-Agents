# Audit report — 2026-05-17 (contract)

**Verdict**: CLEAN

## Summary

Fourth contract-focused audit of the day; HEAD = `764f3f0` (2026-05-17 ~04:24 KST).
Supersedes the L1-triggered audit at HEAD `d498c6a` (~04:07 KST). Scope: all twelve
hard rules in `docs/operator-contract.md`, §5 commit-marker compliance across git history,
model-assignment parity between `.claude/agents/*.md` frontmatter and `docs/for-analysts.md`,
`.gitignore` coverage, secrets scan, and output-artifact placement. Commit `764f3f0`
("fix(contract): clear 3 LOW findings") landed after the previous audit and resolved two
of three [low] findings from that run: the `docs/for-analysts.md:91` / `CLAUDE.md:58`
auditor-row drift is fixed (rows now correctly describe all three trigger layers), and the
`operator-contract.md §5` pre-marker carry-forward clause is now formally documented (commits
predating `7c6ff4f` are officially exempt). The previous audit's "next hint" is also verified:
`records/audit/poll-state.json` is correctly gitignored by the `records/` rule and carries no
risk of accidental commit. The only surviving carry-forward items are the launchd plist
hardcoded paths ([low] §8, structural constraint, documented exception in each plist XML)
and the two `docs/daily/` reports using a topic-suffix naming form not in the §11 spec
([info]). No secrets, no PII in committed file content, no output artifacts committed under
`agents/` or `scripts/`, no §5-scope commits after the marker convention date missing the
`Requested-by: user` footer. All six agent model assignments match between frontmatter and
the `docs/for-analysts.md` table.

## Findings

- **[low]** §8 — Three launchd plist files hardcode `/Users/melons/...` absolute paths —
  `scripts/com.melons.agents.auditor.plist:15,20,23,26,53`,
  `scripts/com.melons.agents.queue.plist:15,19,22,25`,
  `scripts/com.melons.agents.audit-poll.plist:15,19,22,25,46`
  Evidence: `com.melons.agents.auditor.plist:15`: `<string>/Users/melons/ai/scripts/audit-run.sh</string>`;
  `com.melons.agents.audit-poll.plist:15`: `<string>/Users/melons/ai/scripts/audit-poll.sh</string>`;
  `com.melons.agents.auditor.plist:53`: PATH entry begins
  `/Users/melons/.local/bin:/Users/melons/.local/opt/ffmpeg-static:...`. §8 prohibits
  hardcoding `~/...`; `/Users/melons/...` is the literal `$HOME` expansion. All three plists
  carry the comment `<!-- §8 exception: launchd plist values do not expand shell variables
  like $HOME, so absolute paths are required. -->` — structural constraint, not negligence.
  Carry-forward [low] from prior audits; no change since last run.
  Suggested fix: Generate all three plists from a `*.plist.template` in
  `scripts/install-scheduler.sh` using `sed "s|@@HOME@@|$HOME|g"` at install time; commit
  only the template; add rendered `*.plist` to `.gitignore` with a
  `!*.plist.template` exception so templates stay tracked.

- **[info]** §11 — Two `docs/daily/` reports use `<ISO-date>-<topic>.md` naming not in
  the §11 spec — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`
  Evidence: `docs/operator-contract.md §11` step 3 specifies `docs/daily/<ISO-date>.md`.
  The 2026-05-14 and 2026-05-15 reports follow the spec; the two 2026-05-16 reports use a
  topic-suffix form. All four files contain the required sections (요약, 푸시된 커밋, etc.);
  only the naming deviates. No 2026-05-17 report yet — expected at shutdown, not overdue.
  Carry-forward [info] from prior audits.
  Suggested fix: Rename the two 2026-05-16 files to `2026-05-16.md` (append/merge content),
  or amend §11 step 3 to permit `<ISO-date>-<topic>.md` as an explicit allowed variant.

## Drift between docs and code

(empty — the docs-drift finding from the prior audit was resolved by `764f3f0`:
`docs/for-analysts.md:91` and `CLAUDE.md:58` auditor rows now read "Out-of-band read-only
audit (three trigger layers: L1 post-commit hook on drift-risk paths, L2 15-min
mission-anomaly poll, L3 daily 03:00 baseline via launchd)" — consistent with
`docs/architecture.md:48–54`.)

## Stale items

- **TODO / FIXME scan**: Two TODO occurrences outside docs/tests:
  `agents/lib/copyright.sh:10` — "Still TODO (see docs/copyright-policy.md)" — explicitly
  tracked in `docs/copyright-policy.md` "Still TODO" section; not stale.
  `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>` in an error-fallback
  stub template (triggered only when ollama returns non-JSON); this is deliberate placeholder
  text inside an error handler, not an untracked work item. Both acceptable.

- No files under `agents/` or `scripts/` with zero changes in 90+ days that are also
  unreferenced. All scripts are referenced in architecture.md, for-analysts.md, or via
  active mission run.sh files.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: "bypassPermissions"` in `.claude/settings.json`; wide allow list covers all local-resource commands; deny list guards catastrophic ops; no config-level user-action requirement |
| §2 Never pause | PASS | Behavioral rule; no config-level violation detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` empty placeholder; `ANTHROPIC_API_KEY` explicitly excluded ("Set in shell, NOT in this file"); `config/policies.yaml` gates `paid_external` in both interactive and autonomous modes; all three launchd plists use `RunAtLoad=false` to avoid surprise API spend on install |
| §4 Auto-approve mode | PASS | Allow list in `.claude/settings.json` covers brew, git, gh, ffmpeg, yt-dlp, whisper, ollama, jq, python3, file ops; deny list blocks `rm -rf` at system roots, `sudo`, force-push to main, disk erase, piped-curl-to-shell |
| §5 Logic changes need OK | PASS (carry-forward exempt) | Two §5-scope commits exist in history: `af9857f` (auditor.md) and `27a8c86` (init scaffold). Both predate the marker convention defined at `7c6ff4f` (2026-05-17). `docs/operator-contract.md §5` now includes the formal Pre-marker carry-forward clause exempting these. No §5-scope commits since `7c6ff4f` are missing the `Requested-by: user` footer (zero such commits exist post-convention). |
| §6 Auto-commit + auto-push | PASS | `records/` correctly gitignored (line 4 of `.gitignore`); `.env` gitignored; no output artifacts committed under `agents/` or `scripts/`; all code/doc changes committed at HEAD `764f3f0` |
| §7 Split commit and push | PASS (behavioral) | Deny list blocks `git push --force origin main`; no `&&`-compound for commit+push found in any committed shell script |
| §8 Code/data separation | PARTIAL (low, carry-forward) | No output artifacts in `agents/` or `scripts/` ✓; `agents/lib/env.sh:38,50` ffmpeg keg-path fallback discovery — deliberate, documented §8 exception; launchd plist hardcoded paths — documented §8 exception in each plist XML; `scripts/audit-run.sh:47` launchd Claude binary fallback — documented §8 exception at that line |
| §9 Goal/roadmap source of truth | PASS | `docs/operator-contract.md §9` and `CLAUDE.md` subagent exclusion list consistent; `auditor` listed in the "do not read goal.md/roadmap.md" subagents |
| §10 Session resume protocol | PASS | Behavioral rule; no config-level violation |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | `docs/daily/` has reports through 2026-05-16; 2026-05-17 report not yet written (active session; expected at shutdown); 2026-05-16 naming variant — carry-forward [info] |
| §12 No PII or secrets | PASS | No API keys, tokens, passwords, or OAuth secrets in committed files (scan of non-docs, non-example files returned only comment/keyword matches); `.env` gitignored; operator email not in committed file content; plist `/Users/melons/...` paths are documented §8 exception and do not meet the §12 definition of "real name, contact info, credential" |

## Next audit hint

Verify the §11 daily-report naming convention drift is resolved at the next shutdown: either
the 2026-05-16 reports are renamed to the canonical form or §11 is amended to allow
`<ISO-date>-<topic>.md` — whichever lands, future audits should find the naming consistent
with the spec.
