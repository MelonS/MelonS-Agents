# Audit report — 2026-05-23 (all)

**Verdict**: DRIFT_DETECTED

## Summary

All-focus audit against HEAD `d6dcfb8` (2026-05-23 00:21 KST).  All six
dimensions checked: (1) architecture vs documentation drift, (2) roadmap
freshness, (3) operator-contract compliance, (4) cost-model accuracy,
(5) stale TODOs / dead code, (6) security / secrets.  The primary finding
is a **persistent model-assignment drift** introduced by commit `2778316`
(2026-05-22 ~17:50 KST) and first flagged by `2026-05-22-contract.md`:
`.claude/agents/planner.md` and `.claude/agents/resourcer.md` both carry
`model: opus` but `docs/for-analysts.md`, `docs/architecture.md`, and
`docs/cost-model.md` still describe them as `sonnet`.  Commit `2778316`
updated only `README.md` and `README.ko.md`; the three other docs were
missed and remain stale.  Secondary findings: §5 `Requested-by: user`
markers absent on three recent `.claude/agents/*.md` commits (carry-forward
from the prior contract audit); roadmap Done is 24 commits behind HEAD; 10
test scripts in `scripts/` are not referenced in docs/ or README.md; two
tracked files contain hardcoded `/Users/melons/ai/` paths outside the §8
exception registry.  Security scan is clean: no secrets, no PII, `.env`
is gitignored, all §8 exception registry entries have in-code markers, and
the skill-activation drift report from `scripts/audit-skill-drift.sh`
returns 0 findings.

## Findings

- **[high]** Model-assignment drift across three doc files — `docs/for-analysts.md:100-101`, `docs/architecture.md:26,30`, `docs/cost-model.md:23,60,177`
  Evidence: `.claude/agents/planner.md:5` = `model: opus`; `.claude/agents/resourcer.md:5` = `model: opus` (set by commit `2778316`, 2026-05-22).  Three docs still say sonnet: (a) `docs/for-analysts.md:100-101` table rows `planner → sonnet`, `resourcer → sonnet`; (b) `docs/architecture.md:26` box label `planner (sonnet)`, line 30 `resourcer (sonnet)`; (c) `docs/cost-model.md:23` tier diagram `planner / resourcer / editor / qa (sonnet)`, line 60 `¢ — sonnet, small input bounded by file size`, line 177 `Sonnet for subagents, Opus only for orchestrator`.  `README.md` and `README.ko.md` are correct.  `docs/for-analysts.md:94-95` also carries a stale provenance claim: "unchanged since 2026-05-15 — last commit touching `.claude/agents/*.md` was `8570a9c`" — the actual most-recent commit to that path is `2778316` (2026-05-22).  The `docs/for-analysts.md:106-107` line "A common analyst suggestion is 'downgrade subagents from opus to sonnet.' This is already done" is now factually reversed (planner/resourcer are opus).  `docs/for-analysts.md:109-111` suggesting "a valid future move is downgrading `resourcer` to haiku" is anachronistic since resourcer is already at opus.
  Suggested fix: Update `docs/for-analysts.md` (model table, provenance line, "already done" sentence, haiku note), `docs/architecture.md` box labels, and `docs/cost-model.md` tier diagram and table row to reflect planner=opus, resourcer=opus, editor=sonnet, qa=sonnet (mirroring the 2778316 README update).

- **[low]** §5 `Requested-by: user` footer absent — commit `fcc5fee` — `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git show fcc5fee --format="%B"` returns only the subject line "abtest(planner+resourcer): flip to opus for A/B" with an effectively empty body (diff only, no prose approval).  Commit modifies both `planner.md` and `resourcer.md` (§5-scope), is dated 2026-05-22 (post-convention threshold 2026-05-17), and is reachable from `origin/main`.  No `Requested-by: user` footer; no in-body approval citation.
  Suggested fix: Future §5-scope commits — including interim A/B flips — include the canonical footer.

- **[low]** §5 `Requested-by: user` footer absent — commit `f714a5848` — `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git show f714a5848 --format="%B"` includes "Operator §5 approval granted in-session via AskUserQuestion" in prose, confirming intent, but no `Requested-by: user` trailer line.  Per §5, the footer is required to "make the approval mechanically verifiable in `git log`"; body-prose is not mechanically parseable by `scripts/pre-merge-check.sh`.
  Suggested fix: Include the literal `Requested-by: user` trailer in addition to the prose note.

- **[low]** §5 `Requested-by: user` footer absent — commit `2778316` — `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git show 2778316 --format="%B"` shows detailed operator rationale ("Operator decision 2026-05-22 ~17:50 KST: keep pair upgrade …") but no `Requested-by: user` trailer.  This commit establishes the production configuration and is the highest-stakes §5 commit of the three.
  Suggested fix: Add the `Requested-by: user` footer in any follow-up commit referencing this decision arc.

- **[low]** Roadmap Done section 24 commits behind HEAD
  Evidence: `git log 4f3e717..HEAD --oneline | wc -l` = 24.  The most recent Done entry references commit `4f3e717` ("docs(roadmap): auto-bulk-reconciliation — 4 commits backfilled (16:03 KST)", 2026-05-22 ~16:04 KST).  Since then, 24 commits have landed on `main` (range `364377019b` → `d6dcfb8`) with no corresponding Done entries.  Per §9, every commit needs a Done entry.
  Suggested fix: Run `scripts/roadmap-done-sync.sh --apply` to auto-backfill the 24-commit gap.

- **[low]** 10 test scripts in `scripts/` not referenced in `docs/` or `README.md`
  Evidence: The following scripts are in the tracked `scripts/` tree but do not appear in any file under `docs/` or in `README.md` / `README.ko.md` (excluding prior audit reports): `test-first-touch.sh`, `test-lyric-extract.sh`, `test-music-video-batch.sh`, `test-music-video-trim.sh`, `test-music-video-upload-meta.sh`, `test-music-video-validate.sh`, `test-qa-anchor.sh`, `test-shader-gates.sh`, `test-shot-plan.sh`, `test-thumbnail.sh`.  All are test harnesses for already-documented features.
  Suggested fix: Add a "Test harnesses" entry or table to `docs/for-analysts.md` listing the test scripts alongside their corresponding feature scripts.

- **[low]** Hardcoded machine path in tracked lyrics instructions file — `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7`
  Evidence: Line 7 reads `**Where to save downloaded mp3s:** \`/Users/melons/ai/assets/music/\`` — an absolute machine-specific path in a committed operational guidance file.  This file is tracked (`git ls-files assets/lyrics/` confirms), is not in the §8 exception registry, and has no `§8 exception:` comment.  On a different machine the path is wrong.
  Suggested fix: Replace with a relative path (e.g., `assets/music/` relative to repo root) or use `$REPO_ROOT/assets/music/` with a note that `$REPO_ROOT` is defined by the bootstrap.

- **[low]** Hardcoded machine path in research/test doc — `docs/skills/hermes-interop-test.md:135`
  Evidence: Line 135 contains `OUR_SKILL = Path("/Users/melons/ai/skills/music-video")` in a Python code example.  The file is tracked in git and not in the §8 exception registry.  Lower severity than the lyrics file since this is a research/interop doc rather than operational guidance.
  Suggested fix: Replace with a portable placeholder such as `Path("<repo_root>/skills/music-video")` and add a note that `<repo_root>` should be substituted at runtime.

- **[info]** Roadmap "Now" structurally stale — acknowledged
  Evidence: The roadmap "Now" section describes an operator-activation step for `job-hunt` v0.4.0 that has been the active item for 40+ commits.  The roadmap text itself explains: "this 'Now' subgoal remains the active operator-side step (no agent work queued)."  The five most recent commits (`d6dcfb8`, `2778316`, `2d5a775`, `11abbe9`, `1457b75`) reference LinkedIn docs, shot-plan wiring, and model config — none reference the "Now" item.  Not actionable for the agent; operator-only gate.
  Suggested fix: No agent action required; stale is intentional and acknowledged.

- **[info]** `agents/lib/copyright.sh:10` carry-forward TODO items
  Evidence: Lines 10-12 carry "Still TODO (see docs/copyright-policy.md): Audio fingerprint check" and "Still TODO: Logo / watermark detection".  Both items are tracked in `docs/copyright-policy.md` and have appeared in audits since 2026-05-17.  No roadmap entry, but explicitly deferred.
  Suggested fix: No immediate action; acceptable carry-forward.

## Drift between docs and code

| Doc location | Doc claim | Code / config reality |
|---|---|---|
| `docs/for-analysts.md:100` | `planner` → `sonnet` | `.claude/agents/planner.md:5` → `model: opus` |
| `docs/for-analysts.md:101` | `resourcer` → `sonnet` | `.claude/agents/resourcer.md:5` → `model: opus` |
| `docs/for-analysts.md:94-95` | last commit to agents = `8570a9c` (2026-05-15) | last commit = `2778316` (2026-05-22) |
| `docs/for-analysts.md:107` | "downgrade to sonnet already done" | planner + resourcer are at opus — statement reversed |
| `docs/architecture.md:26` | `planner (sonnet)` box label | `.claude/agents/planner.md` → opus |
| `docs/architecture.md:30` | `resourcer (sonnet)` box label | `.claude/agents/resourcer.md` → opus |
| `docs/cost-model.md:23` | `planner / resourcer / editor / qa (sonnet)` | planner + resourcer = opus; editor + qa = sonnet |
| `docs/cost-model.md:60` | `¢ — sonnet, small input` | planner/resourcer subagent calls now at opus rate |
| `docs/cost-model.md:177` | "Sonnet for subagents, Opus only for orchestrator" | planner + resourcer upgraded to opus since 2026-05-22 |

## Stale items

- `docs/roadmap.md` Done section is 24 commits behind HEAD (last entry `4f3e717`, 2026-05-22 ~16:04 KST).  Per §9, every commit deserves a Done entry.  `scripts/roadmap-done-sync.sh` exists to close this gap.
- `docs/for-analysts.md` model-assignment note has been stale since `2778316` landed ~3.5 hours after the last all-audit on 2026-05-22; the note at line 93-95 has now persisted past midnight into 2026-05-23.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✓ PASS | No user-recipe instructions in committed files |
| §2 Never pause unless told | ✓ PASS | No pause-seeking patterns in committed docs |
| §3 Money firewall | ✓ PASS | All mission `run.sh` files call `ollama.sh` (Tier 2, local); no direct Anthropic SDK calls; `.env.example` has schema-only placeholders |
| §4 Auto-approve mode | ✓ PASS | `config/policies.yaml` intact; no non-local API calls in mission code |
| §5 Logic changes need explicit OK | ⚠ FAIL [low×3] | `fcc5fee` (no marker, no prose approval), `f714a5848` (prose approval, no canonical footer), `2778316` (body cites operator decision, no footer) |
| §6 Auto-commit + auto-push / gitignore | ✓ PASS | `records/` gitignored; `.env` and `.env.*.local` gitignored; `.claude/settings.json` gitignored; `.claude/*.lock` gitignored; no MP4/WAV/JPG output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | ✓ PASS | No `git commit … && git push` compounds in committed scripts |
| §8 Code/data separation + portability | ⚠ PARTIAL | All 12 §8 exception registry entries have ≥1 `# §8 exception:` comment (grep-verified ✓); BUT `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7` and `docs/skills/hermes-interop-test.md:135` contain hardcoded `/Users/melons/ai/` paths outside the registry → [low×2] |
| §9 Goal and roadmap as source of truth | ⚠ NOTE | Roadmap Done is 24 commits behind HEAD; "Now" stale is acknowledged in-file |
| §10 Session resume protocol | ✓ PASS (behavioral) | Not verifiable from static code; no committed violations found |
| §11 Shutdown protocol | ✓ PASS (behavioral) | Daily reports present under `docs/daily/`; last one written 2026-05-22 |
| §12 No PII or secrets | ✓ PASS | Secret scan (`git grep -nE "(api[_-]?key\|secret\|token\|password)" -- ':!docs/' ':!*.example'`) returns only placeholder/comment matches; `.env.example` schema-only; no real names or emails in committed files; `docs/social/` LinkedIn snippets use only public GitHub handles |

## Next audit hint

Confirm `docs/for-analysts.md`, `docs/architecture.md`, and `docs/cost-model.md` are all updated to reflect `planner=opus, resourcer=opus` — the CURRENT-ALERT will remain until these three files match the frontmatter; clearing only `for-analysts.md` (the previous fix target) will leave `architecture.md` and `cost-model.md` still drifted.
