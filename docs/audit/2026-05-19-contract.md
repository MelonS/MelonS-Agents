# Audit report — 2026-05-19 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Contract-focused audit at HEAD `c9ecb15` on branch `feat/skill-music-video`
(2026-05-19 ~00:41 KST, "docs(ideas): park A/B test — planner + resourcer
at Opus vs Sonnet").  This is the twenty-first audit overall; the most recent
prior audit was the all-scope run that wrote `docs/audit/2026-05-18-all.md`
at HEAD `65a5045` on `feat/post-merge-detection`.  Current `origin/main` =
`a537018` (GitHub Actions main-protection); local `feat/skill-music-video`
is five commits ahead with structural work: §8 portability principles
codified, Skill #1 shipped, `config/claude-settings.template.json` +
`scripts/install-claude-local.sh` created, `scripts/bootstrap.sh` wired.
All five structural commits are correctly isolated on the feat branch and
have NOT been merged to `origin/main` — confirming §6 is being followed for
current work.

**Note on the previous contract audit file at this same path**: an earlier
run today (~00:41 KST) wrote an incorrect [medium] finding asserting that
commits `a993753`, `912d61c`, and `40aeab1` "are on `main`."  `git log
feat/skill-music-video ^main` disproves this: those three commits appear
exclusively on the feat branch.  The earlier auditor appears to have read
`git log` output from the feat branch and mistaken "visible in history"
for "on main."  This report supersedes that erroneous run.

Key positives: §8 `.claude/settings.json` hardcode finding (carried since
2026-05-17) resolved in `912d61c`; all six agent frontmatter models match
`docs/for-analysts.md` table; no secrets in any committed file; no §5
marker violations (all `.claude/agents/*.md` edits predate the convention).
Remaining concerns: one [medium] contract-process violation (pre-merge gate
bypassed for `feat/post-merge-detection` → `origin/main`), and four [low]
items.

## Findings

- **[medium]** §6 pre-merge gate not satisfied when `feat/post-merge-detection` content reached `origin/main` — `docs/audit/CURRENT-ALERT.md` + `git log origin/main`
  Evidence: `CURRENT-ALERT.md` (generated 2026-05-19 00:06:50 KST) records
  the 20th audit running at HEAD `65a5045` with verdict DRIFT_DETECTED.
  Eight minutes later `a537018` ("feat(ci): GitHub Actions main-protection")
  is the `origin/main` tip — the same subject line as `65a5045`.  Hash
  mismatch (`65a5045` ≠ `a537018`; `git merge-base` confirms `a537018` is
  not descended from `65a5045`) shows the content was re-committed rather
  than fast-forward merged per §6 step 4 (`git merge --ff-only`).  Gate 1
  of the pre-merge gate ("Audit CLEAN on feat branch HEAD") was not met at
  the time of transition: the most recent audit verdict was DRIFT_DETECTED.
  The DRIFT_DETECTED was caused by a pre-existing issue (`.claude/settings.json`
  hardcoded paths) not introduced by the feat branch, and the fix landed in the
  immediate next session (`912d61c`).  Nevertheless, the contract is explicit:
  "All four gates must pass before merge."
  Suggested fix: For the current `feat/skill-music-video`, run
  `./scripts/pre-merge-check.sh` and obtain a CLEAN audit verdict before
  merging; use `git merge --ff-only feat/skill-music-video` to preserve the
  branch-development history.

- **[low]** §6 structural-change trigger list references `.claude/skills/` but skills are tracked at `skills/<name>/` — `docs/operator-contract.md` §6 vs §8 and `scripts/pre-merge-check.sh:142`
  Evidence: `operator-contract.md` §6 structural-change list (on feat branch,
  commit `a2a3807` area) reads ".claude/skills/* create / edit — new layer —
  Skills work."  §8 asset placement table (commit `5b1aafb`) reads: Skills
  tracked at `skills/<name>/` (top-level).  `scripts/pre-merge-check.sh:142`
  checks `'^\.claude/skills/'` — this pattern never matches changes under
  `skills/` at the top level.  A feat branch with only `skills/` changes
  would show `SCOPE="unclassified"` rather than being flagged as structural,
  and gate 3 (`Requested-by:` check at line 104) would silently skip skills
  commits.
  Suggested fix: Update §6 structural-change list entry from `.claude/skills/*`
  to `skills/<name>/`; update `pre-merge-check.sh:142` to also match `'^skills/'`.

- **[low]** §5 text scope (`agents/*.md`) and §6 gate #3 scope (`agents/` any file) disagree — `docs/operator-contract.md:87-88` vs `:186` and `scripts/pre-merge-check.sh:104`
  Evidence: §5 (line 87–88): "Editing `agents/*.md` or `.claude/agents/*.md`".
  §6 pre-merge gate #3 (line 186): "every commit touching `agents/` or
  `.claude/agents/*.md` carries `Requested-by: user`."
  `pre-merge-check.sh:104`: `grep -qE '^(\.claude/agents/.+\.md|agents/)'`
  — matches any file under `agents/`, not just `*.md`.  A `run.sh`
  change lacking the marker would fail gate 3 despite being out of §5
  scope.  These are not the same gate.
  Suggested fix: Align §6 gate #3 description to match §5 scope (`agents/*.md`),
  or explicitly expand §5 to cover `agents/` broadly and synchronise both
  documents.

- **[low]** `CLAUDE.md:35` says remote is "(private)" while `operator-contract.md:121` says "(public)" — `CLAUDE.md:35`
  Evidence: `CLAUDE.md` line 35 reads
  `Remote: git@github.com:MelonS/MelonS-Agents.git (private).`
  `docs/operator-contract.md` line 121 reads
  `Remote: git@github.com:MelonS/MelonS-Agents.git (public).`
  The repo has been public since 2026-05-18 (first external clone documented
  in `a2a3807`).  The contract is the source of truth per its own opening
  paragraph.  This finding was first raised in `2026-05-18-contract.md` and
  persists through three audit runs.
  Suggested fix: Change `CLAUDE.md:35` from `(private)` to `(public)`; this
  is a micro-commit eligible for direct `main` per §6.

- **[low]** `origin/main` `.gitignore` does not cover `.claude/settings.json` — `git show main:.gitignore`
  Evidence: `git show main:.gitignore | grep settings` returns no output.
  The fix (`912d61c` on `feat/skill-music-video`) adds `.claude/settings.json`
  to `.gitignore` alongside creating the template; this fix is not yet on
  `origin/main`.  Until the feat branch merges, an accidental `git add
  .claude/settings.json` on the `main` branch would commit machine-specific
  allow-list paths (a §8 and §12 violation).
  Suggested fix: Fast-track `feat/skill-music-video` merge after passing all
  four pre-merge gates; the `.gitignore` fix is bundled there.

## Drift between docs and code

- **`docs/operator-contract.md` §6 vs `scripts/pre-merge-check.sh`**: `pre-merge-check.sh:142`
  checks `.claude/skills/` while §8 asset table (added in `5b1aafb` on feat branch) places
  skills at `skills/<name>/`.  Once feat branch merges, the script will silently mis-classify
  skills changes.  Documented as [low] finding above.

- **`docs/operator-contract.md` §5 vs §6 pre-merge gate #3**: Scope of `Requested-by:`
  marker is `agents/*.md` in §5 text but `agents/` (any file) in §6 gate #3 table and the
  actual gate script.  Documented as [low] finding above.

- **`CLAUDE.md:35` vs `operator-contract.md:121`**: repo visibility label mismatch (private
  vs public).  Documented as [low] finding above.

- **Subagent model assignments** — `docs/for-analysts.md` table vs `.claude/agents/*.md`
  frontmatter confirmed consistent across all six agents:
  orchestrator=opus, planner/resourcer/editor/qa/auditor=sonnet. ✅

- **`docs/architecture.md` component existence** — all five mission directories and all
  six lib files referenced in the doc exist on disk. ✅  The new `skills/` top-level
  directory is not yet reflected in the `docs/architecture.md` Layers table; this is
  expected until `feat/skill-music-video` merges and triggers a README/architecture
  maintenance pass.

## Stale items

- `main-backup-pre-filter-20260517-173615` remote branch: roadmap Next #2 schedules
  deletion "on or after 2026-05-18"; today is 2026-05-19.  Window is open; the branch
  is confirmed dead weight on `origin` (was the rollback safety net for the 2026-05-17
  email-history rewrite; no issues observed since).

- Roadmap "Now" expired reminders: two "revisit at home ~23:00 KST 2026-05-18"
  entries at `docs/roadmap.md:29,37` are 20+ hours past their review window.

- `CURRENT-ALERT.md` stale: references the 20th audit (2026-05-18-all.md, DRIFT_DETECTED
  on `.claude/settings.json` hardcoding).  The triggering [medium] has been resolved in
  `912d61c` (feat branch, not yet on `origin/main`).  Will auto-clear on next CLEAN run.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | ✅ | No user-terminal recipes in committed docs or scripts |
| §2 Never pause | ✅ | No structural violations found |
| §3 Money firewall | ✅ | `config/policies.yaml:budget_usd_ceiling: 5.00`; `.env.example` schema-only; no API credentials committed |
| §4 Auto-approve local resources | ✅ | `config/claude-settings.template.json` allow-list covers all local tools; `policies.yaml` autonomous `allowed:` list matches §4 |
| §5 Logic changes need OK | ✅ | No `.claude/agents/*.md` edits post-2026-05-17; all six agent files untouched since 2026-05-15 (carry-forward exempt) |
| §5 Requested-by marker scope | ⚠️ [low] | §5 text scope (`agents/*.md`) and §6/script gate scope (`agents/` any file) disagree — documented above |
| §6 Auto-commit / auto-push | ✅ | All commits pushed; `records/` gitignored; remote URL correct |
| §6 Branch strategy — current work | ✅ | Five structural commits correctly on `feat/skill-music-video` only; merge to `origin/main` has NOT occurred — §6 is being followed |
| §6 Branch strategy — previous merge | ⚠️ [medium] | `feat/post-merge-detection` content reached `origin/main` when Gate 1 (Audit CLEAN) was not satisfied; content re-committed (non-FF) rather than FF-merged |
| §6 Structural-change trigger list | ⚠️ [low] | List says `.claude/skills/*`; actual path is `skills/<name>/` per §8 — pre-merge gate script will miss skills-only commits |
| §7 Split commit+push | ✅ | No `&&`-compound git patterns in committed scripts or docs |
| §8 Code/data separation | ✅ | `912d61c` (feat branch) resolves the long-running `.claude/settings.json` hardcode finding; template uses `@@HOME@@`/`@@REPO_ROOT@@` placeholders |
| §8 `origin/main` gitignore gap | ⚠️ [low] | `main:.gitignore` lacks `.claude/settings.json`; fix pending merge of feat branch |
| §8 Hardcoded paths in committed scripts | ✅ | Rendered `.plist` files are gitignored; committed scripts use env vars; three documented §8 exception registry entries unchanged |
| §8 Output artifacts in code dirs | ✅ | No media files under `agents/` or `scripts/` |
| §9 Goal + roadmap source of truth | ✅ | Roadmap Done section updated in `401f9ff` with feat-branch deliverables; goal.md subgoal 1 ticked |
| §9 Roadmap Now freshness | ⚠️ [low] | Two expired "revisit at home ~23:00 KST 2026-05-18" reminders remain in roadmap "Now" |
| §10 Session resume | ✅ | Protocol documented; daily reports present |
| §11 Shutdown protocol | ✅ | `docs/daily/` reports present; procedure documented |
| §12 No PII / secrets | ✅ | `git grep` for credential patterns returns only placeholder comments and variable names; no personal emails or real names in committed files; `.env` gitignored |

## Next audit hint

Verify that the `feat/skill-music-video` pre-merge gate is exercised correctly
before the merge: run `./scripts/pre-merge-check.sh` from the feat branch and
confirm all four gates pass — specifically Gate 1 (Audit CLEAN) and Gate 3
(§5 marker check covers only `agents/*.md`, not the broader `agents/` pattern
currently in the script).  This will be the first full exercise of the §6
branch strategy on a non-trivial feat branch.
