# Audit report — 2026-05-19 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Twenty-fifth contract audit (this run supersedes all same-day prior reports at this path). HEAD = `fdcedbc` (`v0.3.0`, "feat(music-video): MUSIC_VIDEO_BROLL_DIR + AI-anime B-roll generator") on `origin/main`, fully synced. All twelve contract hard rules walked against committed code, configuration, and git history. Three mediums, six lows, zero critical/high.

One **new** [medium] §8 finding was missed by the ~22:06 KST prior same-day run: `scripts/fetch-ai-anime-broll.sh:25` hardcodes `/Users/melons/.local/opt/ffmpeg-static/ffmpeg` without an inline `§8 exception:` comment and without a registry entry in `docs/operator-contract.md`. That prior run's compliance table incorrectly marked §8 hardcoded paths as ✅. Two §9 mediums carry forward: roadmap "Now" says "No active goal" while `docs/goal.md` has had an active goal since 2026-05-19 ~00:55 KST; roadmap Done section has no entries for the v0.3.0 milestone work. Six lows: stale "awaiting merge" in `docs/for-analysts.md:115`; stale "Not yet merged to main" in `docs/goal.md:358`; contract trigger-list text mismatch (`.claude/skills/*` vs `skills/<name>/`); §5 text vs gate-3 scope inconsistency; v0.3.0 tag convention deviation; carry-forward `a537018` CI-to-main. All six subagent model assignments match `docs/for-analysts.md`. No secrets. §5 marker compliance clean.

## Findings

- **[medium]** Roadmap "Now" says "No active goal" while `docs/goal.md` active goal is "Multi-skill AI assistant framework" — `docs/roadmap.md:18-21`
  Evidence: `docs/roadmap.md` lines 18–21: "_Last updated 2026-05-18 ~14:10 KST. … No active goal — operator sets the next one._" `docs/goal.md:19`: "### 2026-05-19 | Multi-skill AI assistant framework — Skill #1 (music-video) refactor first" (promoted via `8b39cac` at 2026-05-19 ~00:55 KST). Two `<!-- suggest -->` HTML comments flagged this mismatch at lines 24–32 and 68–76 but the user-editable "Now" prose has not been updated. §9 requires `docs/roadmap.md` "Now" to reflect the active goal. Persists from prior audit.
  Suggested fix: Operator rewrites `docs/roadmap.md` "Now" to name the current active goal and operative subgoal; remove the two expired `<!-- suggest -->` blocks.

- **[medium]** v0.3.0 milestone (7 commits since `ae07233`) has no roadmap Done entry — `docs/roadmap.md` Done section
  Evidence: `git log ae07233..HEAD --oneline` shows 20 commits. The roadmap Done section ends at "**2026-05-19** (~12:35 KST) **v0.2.0 milestone shipped**" covering `ae07233`/`febb1f3`/`8c3e045`. None of the subsequent substantive commits have Done entries: `c496a0a` (feat/permission-bootstrap — user-level permission bootstrap), `2bd082806` (broaden allow list), `7897e96` (fix), `7ee36709` (test), `3bcfd6d` (onboarding doc), `fdcedbc` (feat/custom-broll-dir — MUSIC_VIDEO_BROLL_DIR + AI-anime B-roll), and `3bec8e9` (CRITICAL candidate goal filed). Tag v0.3.0 annotated by operator confirms these represent a shipped milestone. §9: "After work lands, append a one-line entry to roadmap 'Done' with the commit hash and date."
  Suggested fix: Append a single v0.3.0 Done entry covering the milestone (e.g. "2026-05-19 ~17:xx KST — v0.3.0: permission bootstrap + pluggable B-roll, `fdcedbc`").

- **[medium]** §8 hardcoded `/Users/melons/` path in committed script, no exception comment — `scripts/fetch-ai-anime-broll.sh:25`
  Evidence: Line 25 reads `FFMPEG="${FFMPEG_BIN:-/Users/melons/.local/opt/ffmpeg-static/ffmpeg}"`. This is a literal `/Users/melons/...` path in a tracked committed file. The §8 exception registry in `docs/operator-contract.md` (lines 289–298) lists three documented exceptions (`agents/lib/env.sh:38,50`; `scripts/audit-run.sh:37–42`; `scripts/music-video-shaders.sh:57,64`); `scripts/fetch-ai-anime-broll.sh` is absent. No inline `§8 exception:` comment is present. On any machine other than the operator's, `$FFMPEG_BIN` is empty and the fallback path does not exist, causing silent ffmpeg failure. §8 principle 4: "No hardcoded machine-specific values in committed files." Introduced in commit `fdcedbc` (feat/custom-broll-dir, 2026-05-19). **This finding was absent from the prior same-day run's compliance table.**
  Suggested fix: Replace line 25 fallback with `"${FFMPEG_BIN:-ffmpeg}"` (rely on PATH like all other scripts), or add an inline `§8 exception:` comment with justification and register the exception in `docs/operator-contract.md` §8 exception registry.

- **[low]** `docs/operator-contract.md:154` structural-change trigger list still says `.claude/skills/*`; actual tracked path is `skills/<name>/` (top-level) — `docs/operator-contract.md:154`
  Evidence: `docs/operator-contract.md` line 154: "`.claude/skills/*` create / edit (new layer — Skills work)." §8 asset-placement table (same file, line 276) correctly says `skills/<name>/` (top-level). `scripts/pre-merge-check.sh:144` was already corrected to `'^skills/'`, but the contract prose has not been updated to match. Carry-forward from prior audit; partially resolved (script fixed, prose not yet).
  Suggested fix: Replace `.claude/skills/*` with `skills/<name>/` at `docs/operator-contract.md:154`.

- **[low]** `docs/for-analysts.md:115` says skills layer is "on `feat/skill-music-video`, awaiting merge" — that branch merged and was deleted at v0.2.0 (2026-05-19 ~12:35 KST) — `docs/for-analysts.md:115`
  Evidence: `docs/for-analysts.md` line 115: "Added 2026-05-19 (on `feat/skill-music-video`, awaiting merge):". `docs/roadmap.md` Done section (line 199): "ae07233 Skill #1 12-commit train merged FF to main; … Both merged feat branches deleted from origin per §6 step 5." The branch no longer exists and the skills layer is live on main. The phrase "awaiting merge" is now false.
  Suggested fix: Reword `docs/for-analysts.md:115` to remove the feat-branch context and "awaiting merge" qualifier (e.g. "Added 2026-05-19 (merged to main in v0.2.0):").

- **[low]** `docs/goal.md:358` says "Not yet merged to main" despite v0.2.0 merge — `docs/goal.md:358`
  Evidence: Lines 357–359 read "Shipped 2026-05-19 ~00:35 KST in commit `a993753` on `feat/skill-music-video` branch. **Not yet merged to main** (still pending Step 1.4 cross-runtime Hermes drop-in test + pre-merge gate manual review)." A `<!-- suggest 2026-05-19 ~12:40 KST -->` comment at line 360 correctly flags the text as outdated and notes the branch merged at v0.2.0; body text has not been updated by operator.
  Suggested fix: Operator strikes or overwrites the "Not yet merged to main" paragraph at `docs/goal.md:358-359`.

- **[low]** §5 scope text (`agents/*.md` — direct children only) vs `pre-merge-check.sh:106` scope (`agents/.+\.md` — any depth) remain inconsistent — `docs/operator-contract.md:87-88` vs `scripts/pre-merge-check.sh:106`
  Evidence: §5 line 87–88: "Editing `agents/*.md` or `.claude/agents/*.md`" (glob: direct children only). `pre-merge-check.sh:106`: `grep -qE '^(\.claude/agents/.+\.md|agents/.+\.md)'` — matches MD files at any depth under `agents/`, including `agents/missions/highlight/select-highlight.prompt.md`. A commit touching only `agents/missions/*/run.sh` would pass gate 3 because it's not `.md`; a commit touching `agents/missions/highlight/select-highlight.prompt.md` would trigger gate 3 even though §5 text doesn't cover it. The discrepancy is small but adds ambiguity to the merge gate. Persists from prior audit (improved from prior state of matching all `agents/` files).
  Suggested fix: Either expand §5 text to explicitly include `agents/**/*.md` (all depths), or restrict `pre-merge-check.sh:106` regex to `^(\.claude/agents/[^/]+\.md|agents/[^/]+\.md)` (direct children only). Choose one and sync both.

- **[low]** `8b39cac` ("docs: promote multi-skill framework to active goal") still missing from roadmap Done section — `docs/roadmap.md` Done section
  Evidence: `git log --oneline main | grep 8b39cac` returns the commit. `grep "8b39cac" docs/roadmap.md` returns only line 28 (inside a `<!-- suggest -->` comment, not in Done). The v0.2.0 Done entry (`9f19708` "tick v0.2.0 milestone Done entries") did not include `8b39cac`. Persists from prior audit.
  Suggested fix: Append a one-line Done entry for `8b39cac` (e.g. "2026-05-19 Multi-skill framework promoted to active goal, `8b39cac`"). Can bundle with the v0.3.0 Done entry fix.

- **[low]** v0.3.0 annotated tag on "Permission bootstrap + pluggable B-roll" deviates from §6 tag convention (v0.x.0 = skill milestones) — `docs/operator-contract.md:200-201`
  Evidence: §6 tag convention line 200–201: "v0.x.0 bumps for skill milestones (Skill #1 shipped, Skill #2 shipped, etc.)." Tag `v0.3.0` annotation (by operator, 2026-05-19): "Permission bootstrap + pluggable B-roll — Two parallel feat branches merged in this milestone." Neither feat branch represents a new Skill delivery. v0.2.0 was the Skill #1 milestone; v0.3.0 is infra/UX work, which by the convention would be a patch bump (v0.2.1) or a documented exception.
  Suggested fix: Either update §6 tag convention to include "major infra/UX milestones" as valid v0.x.0 triggers, or document `v0.3.0` as an explicit exception with an inline note in §6.

- **[low, carry-forward]** `a537018` ("feat(ci): GitHub Actions main-protection") committed directly to main as a structural change after the pre-merge gate existed — `git log main`, commit `a537018`
  Evidence: `a537018` is a new `.github/workflows/` YAML file (structural per §6 "New `scripts/` tooling"). Both `a2a3807` (branch-strategy rules) and `22a45ea` (pre-merge-check script) were already on `main` when `a537018` landed, meaning the gate existed. No feat branch reference in the commit message. Bootstrap exception text in §6 covers only `a2a3807` and `22a45ea`. Persists from prior audit.
  Suggested fix: Extend the §6 bootstrap-exception note at `docs/operator-contract.md` to explicitly cover `a537018` (CI infrastructure), so future audits don't re-flag it; or accept it as an implicit extension of the bootstrap exception and note it as closed.

- **[info]** `docs/audit/CURRENT-ALERT.md` references `2026-05-19-contract.md` (generated 00:59:45 KST) as the full report, while a later all-scope audit (`2026-05-19-all.md`) also found DRIFT_DETECTED and was committed separately without updating the alert file — `docs/audit/CURRENT-ALERT.md:9`
  Evidence: `CURRENT-ALERT.md` line 9: "`docs/audit/2026-05-19-contract.md`" and "Generated: 2026-05-19 00:59:45 KST". `docs/audit/2026-05-19-all.md` exists and is committed (also DRIFT_DETECTED, HEAD `401f9ff`). The all-audit should have overwritten `CURRENT-ALERT.md` per `audit-run.sh` lifecycle, but the alert still points to the older contract report. The current report (this file) will supersede both once `audit-run.sh` is invoked.
  Suggested fix: Re-run `./scripts/audit-run.sh contract` (or `all`) to regenerate `CURRENT-ALERT.md` with the current report reference; or accept that the alert will self-correct on the next scheduled 03:00 KST run.

## Drift between docs and code

- **`scripts/fetch-ai-anime-broll.sh:25`** — hardcoded `/Users/melons/.local/opt/ffmpeg-static/ffmpeg` without §8 exception comment or exception registry entry. Documented as [medium] above.

- **`docs/for-analysts.md:115` "awaiting merge" vs merged state**: for-analysts.md describes `skills/` as pending merge on `feat/skill-music-video`; that branch was merged and deleted at v0.2.0. Documented as [low] above.

- **`docs/goal.md:358`** — "Not yet merged to main" vs actual merged state; `<!-- suggest -->` comment at line 360 flags it. Documented as [low] above.

- **`docs/operator-contract.md:154` `.claude/skills/*` vs `skills/<name>/`**: structural-change trigger list uses the old gitignored path; §8 asset table and `pre-merge-check.sh` use the correct top-level path. Documented as [low] above.

- **Subagent model assignments** — `.claude/agents/*.md` frontmatter vs `docs/for-analysts.md` table: all six agents confirmed matching (orchestrator=opus; planner/resourcer/editor/qa/auditor=sonnet). ✅

- **§5 scope vs gate-3 scope**: contract §5 text uses `agents/*.md` (direct children); `pre-merge-check.sh:106` uses `agents/.+\.md` (any depth). Documented as [low] above.

## Stale items

- **Roadmap "Now" expired reminders**: two "_Pending operator review (revisit at home ~23:00 KST 2026-05-18)_" entries at `docs/roadmap.md:52,60` are ~20+ hours past their review window. The overnight session added `<!-- suggest -->` comments flagging both; operator has not yet acted.

- **`main-backup-pre-filter-20260517-173615` branch**: roadmap Next #2 schedules deletion "on or after 2026-05-18". Today is 2026-05-19. Deletion window open; no issues observed since the 2026-05-17 email-history rewrite. Branch remains live on `origin` per `docs/roadmap.md` Next #2.

- **`8b39cac` Done entry** and **`docs/goal.md:358` "Not yet merged" body text**: both predate v0.2.0 merge and need operator housekeeping. Documented as [low] above.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | ✅ | No user-terminal recipes in committed docs or scripts |
| §2 Never pause | ✅ | No structural violations found in agent definitions |
| §3 Money firewall | ✅ | `.env.example` is schema-only; `config/policies.yaml` `budget_usd_ceiling: 5.00` present |
| §4 Auto-approve local resources | ✅ | `config/claude-settings.template.json` allow list covers all local tools; `policies.yaml` `allowed:` list matches §4 |
| §5 Logic changes need OK | ✅ | No `.claude/agents/*.md` or `agents/*.md` edits since 2026-05-15; all carry-forward exempt (pre-2026-05-17 marker convention) |
| §5 Requested-by marker (post-2026-05-17 scope) | ✅ | Zero §5-scope `.md` commits on main since marker convention; no marker required |
| §5/§6 scope mismatch | ⚠️ [low] | §5 `agents/*.md` (direct children); `pre-merge-check.sh:106` `agents/.+\.md` (any depth) — documented above |
| §6 Auto-commit + auto-push | ✅ | Working tree clean; all changes committed and pushed to `origin/main` |
| §6 Branch strategy — feat branches used | ✅ | v0.3.0 tag annotation confirms `feat/permission-bootstrap` (6 commits) + `feat/custom-broll-dir` (1 commit) were separate feat branches before merge |
| §6 Branch strategy — pre-merge gate documentation | ⚠️ [info] | Gate 4 (operator merge OK) evidenced by operator-created annotated tag; gates 1–3 undocumented in commit trail — cannot confirm or deny |
| §6 Branch strategy — `a537018` on main | ⚠️ [low, carry-forward] | Structural CI change committed directly to main after gate existed; documented above |
| §6 Tag convention | ⚠️ [low] | v0.3.0 on "Permission bootstrap + pluggable B-roll" deviates from "v0.x.0 = skill milestones" convention |
| §6 Structural-change trigger list | ⚠️ [low] | Contract doc lists `.claude/skills/*`; actual path `skills/<name>/`; `pre-merge-check.sh:144` already corrected |
| §7 Split commit+push | ✅ | No `&&`-compound git patterns in any committed script |
| §8 Code/data separation | ✅ | No media files or output artifacts under `agents/` or `scripts/` |
| §8 Hardcoded paths — `.claude/settings.json` | ✅ RESOLVED | Prior medium finding resolved: `.claude/settings.json` now gitignored; `config/claude-settings.template.json` uses `@@HOME@@`/`@@REPO_ROOT@@` placeholders |
| §8 Hardcoded path — `fetch-ai-anime-broll.sh:25` | ⚠️ [medium] | Line 25 hardcodes `/Users/melons/.local/opt/ffmpeg-static/ffmpeg`; no `§8 exception:` comment; not in exception registry — **new finding, missed by prior same-day run** |
| §8 Hardcoded paths — other committed scripts | ✅ | All other tracked scripts use `$HOME`/`$REPO_ROOT`/`$FFMPEG_BIN`; three registered exceptions carry `§8 exception:` comments |
| §8 `.gitignore` coverage | ✅ RESOLVED | `.claude/settings.json` and `.claude/skills` patterns present in current `.gitignore` |
| §9 Goal + roadmap source of truth | ⚠️ [medium] | Roadmap "Now" contradicts `docs/goal.md` active goal; documented above |
| §9 Roadmap Done entries | ⚠️ [medium] | v0.3.0 milestone undocumented in Done; `8b39cac` also missing |
| §10 Session resume | ✅ | Protocol documented; `docs/daily/` entries present through 2026-05-19 |
| §11 Shutdown protocol | ✅ | Daily reports present; procedure documented in contract |
| §12 No PII / secrets | ✅ RESOLVED | `.claude/settings.json` no longer tracked; `.env.example` schema-only; no operator username exposed in committed files |

## Next audit hint

Run a security-focused audit targeting `scripts/install-claude-permissions.sh` — it writes to `~/.claude/settings.json` and should be verified to use only env-resolved paths and not embed machine-specific values; also confirm the §8 hardcoded-path finding at `scripts/fetch-ai-anime-broll.sh:25` has been resolved and that `docs/roadmap.md` "Now" is updated to the active goal before marking all three mediums cleared.
