# Audit report — 2026-05-19 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Twenty-sixth contract audit (this run supersedes all same-day prior reports at this path).
HEAD = `81320a3` ("chore(audit): clear 3 mediums + 3 lows from 2026-05-19 contract audit")
on `origin/main`, fully synced. All twelve hard rules in `docs/operator-contract.md` were
walked against committed code, configuration, git history, and the `.claude/agents/*.md`
frontmatter table. The previous audit cleanup commit (`81320a3`) resolved five of nine
findings from the ~22:41 KST prior run (§8 hardcoded path in `fetch-ai-anime-broll.sh`,
roadmap Done gap for v0.3.0/8b39cac, stale "awaiting merge" in for-analysts.md,
stale "Not yet merged" in goal.md, and wrong `skills/` path in operator-contract §6
trigger list). Four findings carry forward: one [medium] §9 roadmap-vs-goal drift
(operator-edit-only section), and three [low] items explicitly deferred for operator
direction. No critical, no high, no new findings beyond these carry-forwards.
All six subagent model assignments match `docs/for-analysts.md`. No secrets detected.
§5 marker compliance is clean (no `.claude/agents/*.md` edits since 2026-05-17).

## Findings

- **[medium]** Roadmap "Now" contradicts goal.md active goal — `docs/roadmap.md:18–44`
  Evidence: `roadmap.md` "Now" block states "No active goal — operator sets the next one."
  (last updated 2026-05-18 ~14:10 KST). `docs/goal.md` has had an active goal ("Multi-skill
  AI assistant framework — Skill #1") since 2026-05-19 ~00:55 KST (commit `8b39cac`). Two
  `<!-- suggest -->` comments in the roadmap (at 02:30 KST and 17:40 KST on 2026-05-19)
  flag the stale "Now" but the operator has not actioned either. Any session resuming purely
  from roadmap "Now" would incorrectly believe there is no active goal, violating §9's
  "source of truth" contract.
  Suggested fix: Operator to rewrite the "Now" block to name the active goal per maintenance
  contract (user-edit-only; Claude cannot silently rewrite this section).

- **[low]** §5 contract text vs gate-3 regex scope mismatch — `scripts/pre-merge-check.sh:106`
  Evidence: §5 text says `agents/*.md` (glob = direct children of `agents/`). Gate-3 regex
  at line 106 is `^agents/.+\.md` (matches any markdown anywhere under `agents/`, including
  `agents/missions/highlight/README.md` and prompt template files). No direct `.md` children
  of `agents/` exist today so no false gates fire, but a future commit touching mission
  prompt templates would erroneously require the `Requested-by: user` marker. Pre-existing
  from prior audit; operator direction needed.
  Suggested fix: Either expand §5 text to `agents/**/*.md`, or restrict regex to
  `^agents/[^/]+\.md` (direct children only).

- **[low]** v0.3.0 tag deviates from §6 "v0.x.0 = skill milestone" convention —
  `docs/operator-contract.md:201–203`
  Evidence: §6 states "v0.x.0 bumps for skill milestones (Skill #1 shipped, Skill #2 shipped,
  etc.)". The v0.3.0 tag (pushed 2026-05-19 ~17:51 KST per roadmap Done) covers
  "Permission bootstrap + pluggable B-roll" — not a skill milestone. v0.2.0 was the skill
  milestone (Skills framework + Skill #1). The convention table in §6 is now inconsistent
  with practice. Pre-existing from prior audit; operator direction needed.
  Suggested fix: Either update §6 tag convention to allow non-skill minor bumps explicitly,
  or add a named exception entry in the §6 tag table for v0.3.0.

- **[low]** for-analysts.md states "No CI / no automated tests on push" but GitHub Actions
  CI exists — `docs/for-analysts.md:257`
  Evidence: Line 257 reads "No CI / no automated tests on push." Commit `a537018`
  (2026-05-18 ~22:30 KST) added `.github/workflows/` (GitHub Actions main-protection
  workflow) covering bash syntax, secret scan, required-files checks, `.env.example`
  sanity, README link hygiene, and gitignore coverage. The claim is now factually incorrect.
  Pre-existing from prior audit; operator direction needed.
  Suggested fix: Update `docs/for-analysts.md:257–261` to acknowledge the GitHub Actions
  workflow and describe its six static checks.

## Drift between docs and code

- `docs/for-analysts.md:257` claims "No CI / no automated tests on push" but
  `.github/workflows/` exists (commit `a537018`). — [low], see Findings above.
- `docs/roadmap.md` "Now" says "No active goal" while `docs/goal.md` Active goal is
  non-empty (set 2026-05-19 ~00:55 KST). — [medium], see Findings above.

## Stale items

- Two `<!-- suggest -->` comments in `docs/roadmap.md` (lines 24–44, 81–88) have aged
  without operator action. The 02:30 KST comment has been outstanding ~20 hours as of
  audit time. These are not violations (the suggest-comment mechanism is the correct
  channel) but their age makes them audit-relevant. Next session should either action
  them or explicitly park.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all work | ✅ PASS | No "please run X" patterns in committed code |
| §2 Never pause | ✅ PASS | Behavioral; not code-checkable |
| §3 Money firewall | ✅ PASS | `config/policies.yaml:budget_usd_ceiling: 5.00`; no paid-API auto-approval in scripts |
| §4 Auto-approve mode | ✅ PASS | `.claude/settings.json` gitignored and rendered per-machine; template at `config/claude-settings.template.json` |
| §5 Logic changes require OK | ✅ PASS | Last `.claude/agents/*.md` change: `8570a9c` (2026-05-15) — pre-marker convention, exempt. No `agents/*.md` changes since 2026-05-17. Gate-3 regex scope is [low] drift vs text. |
| §6 Git workflow — branch strategy | ⚠️ LOW | Gitignore correct. Recent structural commits (feat/permission-bootstrap, feat/custom-broll-dir) went through proper feat→main FF merge. v0.3.0 tag deviates from "skill milestone" convention — [low] finding above. |
| §6 Git workflow — gitignore | ✅ PASS | `records/` gitignored ✓; `.env` gitignored ✓; `.claude/settings.json` gitignored ✓; `.claude/*.lock` gitignored ✓; rendered `.plist` files gitignored ✓ |
| §7 Split commit and push | ✅ PASS | No `&&`-compound commit+push patterns in recent history |
| §8 Code/data separation | ✅ PASS | No output artifacts under `agents/` or `scripts/` in tracked tree. Previous §8 violation (`fetch-ai-anime-broll.sh:25` hardcoded `/Users/melons/` path) resolved in `81320a3`. Hardcoded-path exception registry in §8 still accurate. |
| §9 Goal/roadmap source of truth | ⚠️ MEDIUM | roadmap "Now" says "No active goal" while goal.md Active goal exists — [medium] finding above |
| §10 Session resume protocol | ✅ PASS | Behavioral; not code-checkable |
| §11 Shutdown protocol | ✅ PASS | Daily reports present in `docs/daily/`. Most recent: `docs/daily/2026-05-19.md` |
| §12 No PII or secrets in repo | ✅ PASS | `.env.example` contains only placeholders (empty `PEXELS_API_KEY=`, model name strings). "Friend meeting" reference in `docs/roadmap.md:95` correctly anonymized ("external security professional (3 yr exp, n=1, anonymized)"). No real names, contact info, or credentials found in tracked files. |

**Subagent model assignments** (§5 / for-analysts.md table cross-check):

| Agent | Frontmatter model | for-analysts.md claim | Match? |
|-------|------------------|-----------------------|--------|
| orchestrator | `opus` | `opus` | ✅ |
| planner | `sonnet` | `sonnet` | ✅ |
| resourcer | `sonnet` | `sonnet` | ✅ |
| editor | `sonnet` | `sonnet` | ✅ |
| qa | `sonnet` | `sonnet` | ✅ |
| auditor | `sonnet` | `sonnet` | ✅ |

## Next audit hint

The roadmap "Now" vs goal.md drift is the only medium finding and is user-edit-only; the
next contract audit should confirm whether the operator has updated "Now" and, if so,
verify the three [low] deferred items (§5 regex scope, v0.3.0 tag convention,
for-analysts.md CI claim) have been addressed before declaring CLEAN.
