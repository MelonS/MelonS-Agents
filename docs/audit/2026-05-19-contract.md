# Audit report — 2026-05-19 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Twenty-third audit overall; contract-focused.  HEAD = `4c0698b` ("docs(daily):
morning brief + audit ack + low-severity bookkeeping") on branch
`feat/skill-music-video`, 8 commits ahead of `origin/main` at `a537018`.
This report supersedes the earlier same-day contract audit (`c9ecb15` HEAD)
from `~00:41 KST`.  Since that prior run, one low finding was resolved
(`CLAUDE.md:35` "(private)" corrected to "(public)" in `4c0698b`), and four
roadmap Done entries were appended (`aa10ba0`, `a2a3807`, `22a45ea`, `a537018`).

All six `.claude/agents/*.md` model assignments still match `docs/for-analysts.md`
(orchestrator=opus, all others=sonnet).  No secrets found in committed files.
§5 marker compliance is clean — the only `.claude/agents/*.md` edits (`8570a9c`,
2026-05-15) predate the marker convention (2026-05-17) and are carry-forward exempt.

Three medium findings remain or are new: (1) `origin/main` tracks `.claude/settings.json`
with nine hardcoded `/Users/melons/` path strings — §8 portability + §12 operator-username
exposure on a public repo; (2) roadmap "Now" still says "No active goal" while
`docs/goal.md` has had an active multi-skill framework goal since 2026-05-19 ~00:55 KST —
§9 source-of-truth violation; (3) working tree contains uncommitted changes to
`docs/architecture.md` and `docs/for-analysts.md` plus the untracked
`docs/audit/2026-05-19-all.md` report — §6 auto-commit-on-completion violated.
Four low carry-forward items from the prior audit persist unchanged.

## Findings

- **[medium]** `origin/main` tracks `.claude/settings.json` with 9 hardcoded `/Users/melons/` path strings — `git show main:.claude/settings.json`
  Evidence: `git show main:.claude/settings.json` returns a file with
  `Read(/Users/melons/**)`, `Write(/Users/melons/ai/**)`,
  `Write(/Users/melons/.local/share/whisper-models/**)`,
  `Write(/Users/melons/.claude/projects/-Users-melons-ai/memory/**)`,
  and `Bash(rm /Users/melons/ai/**)` — nine lines containing
  `/Users/melons`.  §8 forbids committed files with literal `/Users/...`
  machine-specific paths.  §12 forbids committing the operator's Unix
  username on this public repo.  The fix (`912d61c` on `feat/skill-music-video`)
  removes `settings.json` from tracking and creates
  `config/claude-settings.template.json` with `@@HOME@@`/`@@REPO_ROOT@@`
  placeholders; this fix is not yet on `origin/main`.
  Suggested fix: Complete the §6 pre-merge gate for `feat/skill-music-video`
  (run `./scripts/pre-merge-check.sh`, obtain operator merge OK), then
  FF-merge to `main`; the `.gitignore` and template fixes bundle with the merge.

- **[medium]** Roadmap "Now" says "No active goal" while `docs/goal.md` has active goal since 2026-05-19 ~00:55 KST — `docs/roadmap.md:18-22`
  Evidence: `docs/roadmap.md` lines 18–22: "_Last updated 2026-05-18 ~14:10 KST.
  … No active goal — operator sets the next one._"  `docs/goal.md` line 19:
  "### 2026-05-19 | Multi-skill AI assistant framework — Skill #1
  (music-video) refactor first" (promoted via `8b39cac`).  The autonomous
  overnight session (2026-05-19 ~02:30 KST) added two `<!-- suggest -->`
  HTML comments at lines 24–32 and 68–76 flagging the stale content, but
  user-editable "Now" prose has not been updated.  Per §9, the roadmap "Now"
  is the operator's section to edit; Claude only appends `<!-- suggest -->`
  comments, never silently rewrites — so the gap persists until operator acts.
  Suggested fix: Operator rewrites roadmap "Now" to reference the current
  active goal ("Multi-skill AI assistant framework") and the operative queue
  item (zero-friction onboarding, roadmap Next #1); remove the two expired
  `<!-- suggest -->` blocks once done.

- **[medium]** Working tree has uncommitted doc changes: `docs/architecture.md` and `docs/for-analysts.md` modified (staged? no) plus `docs/audit/2026-05-19-all.md` untracked — `git status`
  Evidence: `git status` shows `modified: docs/architecture.md` (adds Skills
  row to Layers table, diff confirmed), `modified: docs/for-analysts.md` (adds
  complete "Skills" section ~30 lines), and `Untracked: docs/audit/2026-05-19-all.md`
  (the most recent full-scope audit, generated ~00:51 KST and referenced by
  the current `CURRENT-ALERT.md`).  The architecture/for-analysts edits appear
  to be an editor fix for the [medium] `skills/` undocumented finding from
  `2026-05-19-all.md`, but the commit never landed.  §6 states "every code or
  doc change… is committed and pushed to the appropriate branch on completion."
  Note: `audit-run.sh:14` says "Commit is left to the human in the loop" —
  this exempts `2026-05-19-all.md` from auto-commit, but the auditor definition
  says reports are committed ("docs/audit/ … committed — never records/
  (gitignored)").  There is a process gap between the two.
  Suggested fix: Commit the three files to `feat/skill-music-video` in a
  single docs commit (`git add docs/audit/2026-05-19-all.md docs/architecture.md
  docs/for-analysts.md`); also reconcile `audit-run.sh` comment ("left to human")
  with the auditor definition's "committed" requirement — one should match the
  other.

- **[low]** §6 structural-change trigger list references `.claude/skills/*` but actual tracked path is `skills/<name>/` — `docs/operator-contract.md` §6 structural list and `scripts/pre-merge-check.sh:142`
  Evidence: `docs/operator-contract.md` §6 structural-change list includes
  "`.claude/skills/* create / edit (new layer — Skills work)`."  §8 asset
  placement table (commit `5b1aafb`) specifies `skills/<name>/` (top-level).
  `scripts/pre-merge-check.sh:142`: `echo "$CHANGED" | grep -qE '^\.claude/skills/'`
  — a pattern that never matches commits under top-level `skills/`.  A feat
  branch with only `skills/` changes would show `SCOPE="unclassified"` instead
  of "skills", and gate 3 (`Requested-by:` check at line 104) would silently
  skip the classification step.  Carry-forward from `2026-05-19-contract.md`
  (prior run) and `2026-05-19-all.md`.
  Suggested fix: Replace `.claude/skills/*` with `skills/<name>/` in §6
  structural-change list; update `pre-merge-check.sh:142` pattern to
  `'^skills/'`.

- **[low]** §5 text scope (`agents/*.md`) and §6 gate #3 text + `pre-merge-check.sh` implementation (`agents/` any file) disagree — `docs/operator-contract.md:87-88` vs `:186` and `scripts/pre-merge-check.sh:104`
  Evidence: §5 line 87–88: "Editing `agents/*.md` or `.claude/agents/*.md`."
  §6 gate #3 table (line 186): "every commit touching `agents/` or
  `.claude/agents/*.md`."  `pre-merge-check.sh:104`: `grep -qE
  '^(\.claude/agents/.+\.md|agents/)'` — matches any file under `agents/`,
  not just `*.md`.  An `agents/missions/*/run.sh` commit without
  `Requested-by: user` would fail gate 3 even though §5 text considers it
  out-of-scope.  Carry-forward from prior audits.
  Suggested fix: Align §6 gate #3 description and `pre-merge-check.sh:104`
  to match §5 scope (`agents/*.md`), or explicitly expand §5 to cover all
  `agents/` — choose one definition and synchronise both.

- **[low]** `origin/main:.gitignore` missing `.claude/settings.json` and `.claude/skills` — `git show main:.gitignore`
  Evidence: `git diff main HEAD -- .gitignore` shows two lines added only on
  the feat branch: `.claude/settings.json` and `.claude/skills`.  On `main`,
  these patterns are absent; an accidental `git add .claude/settings.json` on
  the main branch would commit the file with its hardcoded paths (compounding
  the [medium] §8/§12 finding above).  Carry-forward; resolves when
  `feat/skill-music-video` merges.
  Suggested fix: Fast-track `feat/skill-music-video` merge after all four
  pre-merge gates pass; the `.gitignore` fix is bundled.

- **[low]** Roadmap Done section missing entry for `8b39cac` ("docs: promote multi-skill framework to active goal") — `docs/roadmap.md` Done section
  Evidence: `git log --oneline main` shows `8b39cac 2026-05-19 docs: promote
  multi-skill framework to active goal + park main-protection v2` landed on
  `main`.  `grep -n "8b39cac" docs/roadmap.md` returns only line 28 (inside a
  `<!-- suggest -->` comment), not in the Done section.  The four other missing
  Done entries from the prior contract audit (`aa10ba0`, `a2a3807`, `22a45ea`,
  `a537018`) were appended in `4c0698b`; `8b39cac` was not included.  Per §9,
  every work-bearing commit that lands on `main` should have a one-line Done
  entry with hash + date.
  Suggested fix: Append a one-line Done entry for `8b39cac` to the roadmap
  Done section (e.g. "- **2026-05-19** Multi-skill framework promoted to active
  goal + main-protection v2 parked (commit `8b39cac`)").

- **[low, carry-forward]** §6 pre-merge gate not satisfied when `a537018` (GitHub Actions) committed directly to `main` — `git log main`
  Evidence: `a537018` ("feat(ci): GitHub Actions main-protection") is a
  structural change (new `.github/workflows/main-protection.yml` tooling) that
  arrived directly on `main` as a single-parent commit.  The §6 bootstrap
  exception covers only `a2a3807` (branch strategy) and `22a45ea` (the
  pre-merge-check script itself) since the gate didn't exist yet at those
  commits.  By the time `a537018` landed, both `a2a3807` and `22a45ea` were
  already on `main`, meaning the gate existed and should have been run.
  Gate 1 (Audit CLEAN) was not satisfied at the time (`CURRENT-ALERT.md` at
  `00:06:50 KST` showed DRIFT_DETECTED on `.claude/settings.json` hardcoding).
  Carry-forward from `2026-05-19-contract.md` (prior run); the daily brief
  `4c0698b` noted this may be a false positive but the structural evidence
  (direct main commit without FF merge) remains.
  Suggested fix: Accept as a bootstrap-extension exception for CI infrastructure
  and document it with a one-line inline comment in `operator-contract.md` §6's
  bootstrap-exception note; or apply the finding to all future structural
  `main`-direct commits as a policy reminder.

## Drift between docs and code

- **§8 `origin/main:.claude/settings.json` vs §8 contract rule**: `main` tracks
  a file with 9 hardcoded `/Users/melons/` paths; the contract forbids this.
  Fix is on `feat/skill-music-video`; pending merge.  Documented as [medium] above.

- **`docs/roadmap.md` "Now" vs `docs/goal.md` active goal**: roadmap says "No
  active goal"; goal.md shows active multi-skill framework goal since 2026-05-19
  ~00:55 KST.  Documented as [medium] above.

- **Subagent model assignments** — `.claude/agents/*.md` frontmatter vs
  `docs/for-analysts.md` table: all six agents confirmed matching
  (orchestrator=opus; planner/resourcer/editor/qa/auditor=sonnet). ✅

- **§6 structural-change trigger list vs §8 asset placement table**: contract
  lists `.claude/skills/*` but the actual path is `skills/<name>/` (top-level).
  `pre-merge-check.sh:142` pattern will miss `skills/` changes.  Documented
  as [low] above.

- **§5 scope vs §6 gate #3 scope**: `agents/*.md` in §5 vs `agents/` (any file)
  in §6 text and `pre-merge-check.sh:104`.  Documented as [low] above.

## Stale items

- **Roadmap "Now" expired reminders**: Two "_Pending operator review (revisit at
  home ~23:00 KST 2026-05-18)_" entries at `docs/roadmap.md:40,48` are
  ~20+ hours past their review window.  Two `<!-- suggest -->` comments were
  appended autonomously flagging both items; operator has not yet acted.

- **`main-backup-pre-filter-20260517-173615` branch**: roadmap Next #2 schedules
  deletion "on or after 2026-05-18"; today is 2026-05-19.  Deletion window is
  open; verified no issues since the 2026-05-17 email-history rewrite.  Branch
  is confirmed dead weight on `origin`.

- **`docs/audit/2026-05-19-all.md` untracked**: the full-scope audit generated
  at ~00:51 KST is the file referenced by `CURRENT-ALERT.md` but is not yet
  committed to `feat/skill-music-video`.  The audit trail durability goal
  ("history must survive a machine swap") is not met for this report until it
  is committed.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | ✅ | No user-terminal recipes in committed docs or scripts |
| §2 Never pause | ✅ | No structural violations found in agent definitions |
| §3 Money firewall | ✅ | `.env.example` schema-only; `ANTHROPIC_API_KEY` comment explicitly says "Set in your shell, NOT in this file"; `config/policies.yaml` budget cap `5.00` present |
| §4 Auto-approve local resources | ✅ | `config/claude-settings.template.json` allow-list covers all local tools (ffmpeg/ollama/whisper/brew/git/gh); `policies.yaml` autonomous `allowed:` list matches §4 |
| §5 Logic changes need OK | ✅ | No `.claude/agents/*.md` or `agents/*.md` edits since 2026-05-15 (all six agent files and all mission prompt files last touched pre-2026-05-17); carry-forward exempt per §5 pre-marker clause |
| §5 Requested-by marker (post-2026-05-17 scope) | ✅ | Zero §5-scope `.md` commits on feat branch or recent main; no marker required |
| §5/§6 scope mismatch | ⚠️ [low] | §5 text `agents/*.md`; §6 gate #3 text and `pre-merge-check.sh:104` match `agents/` (broader) — documented above |
| §6 Auto-commit + auto-push | ⚠️ [medium] | Three uncommitted working-tree changes (`docs/architecture.md`, `docs/for-analysts.md`, `docs/audit/2026-05-19-all.md`) violate completion-commit rule |
| §6 Branch strategy — current structural work | ✅ | All 8 structural feat-branch commits (`5b1aafb`, `a993753`, `912d61c`, `40aeab1`, `c9ecb15`, `4c0698b` + 2 more) are on `feat/skill-music-video` only; NOT on `main`; no unauthorized `main` commits observed in this session |
| §6 Branch strategy — `a537018` on main | ⚠️ [low, carry-forward] | Structural CI change committed directly to main after gate existed; documented above |
| §6 Structural-change trigger list | ⚠️ [low] | Lists `.claude/skills/*`; actual path `skills/<name>/`; `pre-merge-check.sh:142` mis-pattern |
| §7 Split commit+push | ✅ | No `&&`-compound git patterns in any committed script |
| §8 Code/data separation — agent/scripts dirs | ✅ | No media files or output artifacts under `agents/` or `scripts/` |
| §8 Hardcoded paths — `origin/main:.claude/settings.json` | ⚠️ [medium] | Nine lines with `/Users/melons/`; fix on feat branch (`912d61c`), pending merge |
| §8 Hardcoded paths — committed scripts | ✅ | Tracked scripts use `$HOME`/`$REPO_ROOT`; three documented §8-exception-registry entries unchanged |
| §8 `origin/main:.gitignore` gap | ⚠️ [low] | Missing `.claude/settings.json` and `.claude/skills` patterns on `main`; pending feat merge |
| §9 Goal + roadmap source of truth | ⚠️ [medium] | Roadmap "Now" contradicts `docs/goal.md` active goal; documented above |
| §9 Roadmap Done entries | ⚠️ [low] | `8b39cac` missing Done entry; `2026-05-19-all.md` audit-trail entry also missing |
| §10 Session resume | ✅ | Protocol documented; `docs/daily/2026-05-19.md` present |
| §11 Shutdown protocol | ✅ | Daily reports present through 2026-05-19 morning; procedure documented |
| §12 No PII / secrets | ⚠️ [medium, overlaps §8] | `origin/main:.claude/settings.json` exposes operator Unix username (`/Users/melons/`) in 9 lines on a public repo; fix same as §8 finding above |

## Next audit hint

Before the next audit run, verify that `feat/skill-music-video` has committed the three
uncommitted working-tree files (`docs/architecture.md`, `docs/for-analysts.md`,
`docs/audit/2026-05-19-all.md`) and that the §6 pre-merge gate (`scripts/pre-merge-check.sh`)
returns CLEAN + Gate-3 PASS before the FF-merge to `main` — this will resolve the two
mediums and one low in a single merge event.
