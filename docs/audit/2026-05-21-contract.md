# Audit report — 2026-05-21 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Thirty-third contract audit (second run this date; supersedes the earlier instance).
Current HEAD: `6225afa` (`feat(skill-job-hunt): digest renders role_fit + hire_prob
breakdown`) on `main`, synced with `origin/main`.  Checked all twelve hard rules and
the Conventions block in `docs/operator-contract.md` against code, configuration, and
recent git history.  Five findings: one medium (output artifacts committed while
architecture.md claims none, unresolved from the prior 2026-05-20 audit), three lows
(§8 exception comments absent in four scripts; §8 exception-registry line numbers
stale in two entries; `docs/for-analysts.md` date label 4 days stale), and one low
(roadmap "Now" states "no further scaffolding needed" while three subsequent Done
entries show continued scaffolding).  §5 is CLEAN — both commits to
`.claude/agents/*.md` predate the 2026-05-17 marker convention and are exempt.  Secret
scan is CLEAN.  No §7 violations.  §12 is CLEAN.

## Findings

- **[medium]** `outputs/publish/upload-meta-v2/*.json` tracked — `docs/architecture.md` Layers table claim stale — `docs/architecture.md:126`
  Evidence: `git ls-files outputs/publish/` returns `outputs/publish/.gitkeep` **and**
  five `outputs/publish/upload-meta-v2/*.json` files (committed `a182380`,
  2026-05-21 02:08 KST).  `docs/architecture.md` Layers table line 126 asserts the
  `outputs/publish/` row has "only `.gitkeep` tracked; mp4/wav/jpg gitignored."
  `.gitkeep` itself reads "No output artifact has ever been committed under outputs/"
  — now false.  The `.gitignore` comment on line 167 acknowledges v2 was "committed
  intentionally" but the architecture table was not updated to match.
  Suggested fix: Either `git rm --cached outputs/publish/upload-meta-v2/*.json` and
  extend gitignore to `outputs/publish/upload-meta-v*/`; or update
  `docs/architecture.md:126` to list v2 JSON as a committed exception with an inline
  `<!-- §8 operator-authorized deviation -->` marker and update the `.gitkeep`
  comment to remove the "never committed" claim.

- **[low, carry-forward — 31st + 32nd audits]** §8 exception comment absent — `scripts/ffmpeg-throttled.sh:33–35`
  Evidence: Lines 33–35 probe `[/opt/homebrew/bin/ffmpeg, /usr/local/bin/ffmpeg,
  /usr/bin/ffmpeg]` in a bare loop with no `# §8 exception:` inline comment and no
  entry in the `docs/operator-contract.md §8` exception registry.  The identical
  probe-loop pattern in `scripts/music-video-shaders.sh:103–110` and
  `scripts/audit-run.sh:37–42` carries the required comment; `ffmpeg-throttled.sh`
  does not.  First flagged in the 31st audit; unresolved across two cycles.
  Suggested fix: Add `# §8 exception: probe-loop fallback — same documented pattern
  as agents/lib/env.sh` above line 33 and append an entry for
  `scripts/ffmpeg-throttled.sh:33–35` to the registry table in
  `docs/operator-contract.md §8`.

- **[low]** §8 exception comments absent in three scripts added 2026-05-21 — `scripts/music-video-lyrics.sh:63–64`, `scripts/music-video-stillzoom.sh:37`, `scripts/music-video-canvas.sh:42`
  Evidence: All three scripts use bare `${FFMPEG_BIN:-/opt/homebrew/bin/ffmpeg}` (and
  `${FFPROBE_BIN:-/opt/homebrew/bin/ffprobe}`) parameter-expansion defaults with no
  `# §8 exception:` comment above the assignment and no entry in the
  `docs/operator-contract.md §8` exception registry.
  `scripts/music-video-shaders.sh:103–110` establishes the required comment template;
  the three new scripts skip it.  `music-video-lyrics.sh:64` also has a bare
  `FFPROBE=` assignment using the same pattern.
  Suggested fix: Add a one-line `# §8 exception:` comment before each occurrence
  (following `shaders.sh:103` as template) and add all three files to the exception
  registry table.

- **[low]** §8 exception registry — stale line numbers for two entries — `docs/operator-contract.md:329,336`
  Evidence: The exception registry table (§8 "Hardcoded path exception registry")
  lists `agents/lib/env.sh:38,50` and `scripts/music-video-shaders.sh:57,64`.
  Actual positions verified today:
  — `agents/lib/env.sh`: hardcoded keg paths appear at lines 44–45 and 58–59
    (§8 comment block begins at line 36); not 38,50.
  — `scripts/music-video-shaders.sh`: §8 exception comment is at lines 103–110
    (file grew from ~190 to 367 lines since the registry was written); not 57,64.
  `scripts/audit-run.sh:37–42` remains accurate.
  Suggested fix: Update the registry entries to
  `agents/lib/env.sh:44–45,58–59` and `scripts/music-video-shaders.sh:103–110`.

- **[low]** Roadmap "Now" — claim "no further scaffolding needed" contradicted by Done entries — `docs/roadmap.md:27`
  Evidence: The "Now" block (last updated 2026-05-20 ~19:55 KST) states "Active
  subgoal — operator-activation of `job-hunt` v0.4.0 (operator-only, no further
  scaffolding needed)."  The Done section's `a05c12b` batch entry (23 commits) and
  the three subsequent commits `6b61f84` (fit-score hire_prob dimension),
  `6eaf9be` (company-tier table), `6225afa` (digest schema) all extend the
  `job-hunt` skill scaffold after the "Now" claim was written.  Per the maintenance
  contract, "Now" is user-edited and Claude only appends to Done — so no agent
  overstepped — but the stated expectation ("no further scaffolding needed") is
  now inconsistent with the Done history.
  Suggested fix: Operator should update the "Now" section to reflect the current
  state (v0.4.x enhancements landed; state whether further Claude-side work is
  queued or the active focus has shifted).

- **[info]** `docs/for-analysts.md:93` date label stale — "As of 2026-05-17"
  Evidence: Line 93 reads "`.claude/agents/*.md` frontmatter sets the model.
  As of 2026-05-17:" — four days stale.  All six model assignments verified
  accurate against current frontmatter (orchestrator: opus; planner/resourcer/
  editor/qa/auditor: sonnet); only the date is outdated.
  Suggested fix: Replace "As of 2026-05-17" with "As of 2026-05-21" (or "current;
  verified by each contract audit run").

## Drift between docs and code

- **`docs/architecture.md` Layers table (line 126) vs `git ls-files`**: Table asserts
  `outputs/publish/` has "only `.gitkeep` tracked"; five `upload-meta-v2/*.json`
  files are also tracked as of `a182380`.  The `.gitkeep` marker note "No output
  artifact has ever been committed" is now false.

- **`docs/operator-contract.md §8` exception registry vs committed scripts**: Registry
  lists three entries (`agents/lib/env.sh`, `scripts/audit-run.sh`,
  `scripts/music-video-shaders.sh`).  At least four qualifying scripts
  (`ffmpeg-throttled.sh`, `music-video-lyrics.sh`, `music-video-stillzoom.sh`,
  `music-video-canvas.sh`) use the same probe-loop / parameter-expansion pattern
  without registration.  Documentation is incomplete relative to the codebase.

- **`docs/operator-contract.md §8` exception registry line numbers**: Two of the three
  entries reference lines that have drifted due to file growth.  See [low] finding
  above.

## Stale items

- `docs/for-analysts.md:93` — "As of 2026-05-17" date label; content accurate but
  cosmetically stale (4 days).

- `docs/roadmap.md:27` — "no further scaffolding needed" claim, set 2026-05-20, now
  contradicted by three Done entries on 2026-05-21.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all work | ✅ PASS | Behavioral; not code-checkable |
| §2 Never pause | ✅ PASS | Behavioral; not code-checkable |
| §3 Money firewall | ✅ PASS | `config/policies.yaml` `budget_usd_ceiling: 5.00`; `ANTHROPIC_API_KEY` instruction in `.env.example` says "Set in your shell, NOT in this file"; schema-only |
| §4 Auto-approve mode | ✅ PASS | `.claude/settings.json` gitignored (line 144); rendered per-machine from `config/claude-settings.template.json` |
| §5 Logic changes require OK | ✅ PASS | Only two commits touch `.claude/agents/*.md`: `8570a9c` (2026-05-15) and `d857391` (2026-05-14). Both predate the 2026-05-17 marker convention; carry-forward exemption applies. Zero post-convention agent-definition edits found. |
| §6 Gitignore coverage | ✅ PASS | `records/` ✓ (line 4); `.env` ✓ (line 69); `.claude/settings.json` ✓ (line 144); rendered plists (`scripts/com.melons.agents.*.plist`) ✓ (line 142); `.claude/*.lock` ✓ (line 133); `assets/music/*` ✓ (line 17); `outputs/publish/*.md` ✓ (line 165); future `upload-meta-v[3-9]*` ✓ (line 172).  Note: pattern `upload-meta-v[3-9]*/` will not catch v10+ if ever reached — low future risk. |
| §6 Branch strategy | ✅ PASS | Flexible guideline in effect since `6ddba86`. Only `main` present locally; all `feat/` branches cleaned up per Done entries. |
| §7 Split commit and push | ✅ PASS | No `git commit … && git push` compounds in any tracked script. |
| §8 Code/data separation — artifacts | ⚠️ MEDIUM | `outputs/publish/upload-meta-v2/*.json` (5 files, `a182380`) are committed outside `$RECORDS_DIR`; `docs/architecture.md:126` still claims only `.gitkeep` tracked. |
| §8 Code/data separation — hardcoded paths | ⚠️ LOW | Four scripts (`ffmpeg-throttled.sh:33–35` carry-forward; `music-video-lyrics.sh:63–64`, `music-video-stillzoom.sh:37`, `music-video-canvas.sh:42` new 2026-05-21) use probe-loop / parameter-expansion fallbacks to `/opt/homebrew/bin/ffmpeg` without `§8 exception:` comments. Three registered exceptions (`agents/lib/env.sh`, `scripts/audit-run.sh`, `scripts/music-video-shaders.sh`) carry inline comments; registry line numbers for `env.sh` and `shaders.sh` are stale. |
| §9 Goal/roadmap source of truth | ⚠️ LOW | `docs/goal.md` active goal present ✓; `docs/roadmap.md` Done section backfilled for 23 commits in `a05c12b` ✓; but "Now" claim "no further scaffolding needed" is contradicted by three subsequent Done entries. |
| §10 Session resume protocol | ✅ PASS | Behavioral; not code-checkable |
| §11 Shutdown protocol | ✅ PASS | `docs/daily/2026-05-21-morning-brief.md` present; `docs/daily/` trail intact |
| §12 No PII or secrets | ✅ PASS | `git grep` across non-docs, non-example files for credential patterns returns only env-var names in comments, Python tokenizer variable names, and `.gitignore` pattern text — no credential strings. `PEXELS_API_KEY=` is blank in `.env.example`. All author footers use GitHub no-reply format. No real names or contact info in committed files. All `/Users/melons` occurrences in scripts are in code comments only. |

## Next audit hint

Resolve the medium finding by either untracking `upload-meta-v2/*.json` or updating
`docs/architecture.md:126` — those two paths account for the only medium-severity
drift; clearing them alongside the four §8 exception-comment lows would return the
next contract run to CLEAN.
