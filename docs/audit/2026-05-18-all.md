# Audit report — 2026-05-18 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Twentieth audit; HEAD = `65a5045` on branch `feat/post-merge-detection`
(one commit ahead of `main` at `22a45ea`).  Full-scope audit covering all
six dimensions: architecture vs documentation drift, roadmap freshness,
operator-contract compliance, cost-model accuracy, stale TODOs / dead
code, and security / secrets.  Since the nineteenth audit (HEAD `e1fda78`),
twelve commits landed on `main` (primarily docs micro-commits) plus one
structural commit on the new `feat/post-merge-detection` branch.  The most
significant new work: branch strategy codified in `operator-contract.md` §6
(`a2a3807`), pre-merge gate script added (`22a45ea`), and GitHub Actions
CI workflow created (`65a5045`, still on feat branch awaiting merge).  A
same-day contract-focused audit (`docs/audit/2026-05-18-contract.md`) ran
at HEAD `22a45ea` and surfaced a [medium] finding: `.claude/settings.json`
hardcodes `/Users/melons` at nine locations, violating §8 env-driven-paths;
this remains unresolved at `65a5045`.  Additional [low] findings: `CLAUDE.md`
and `operator-contract.md` disagree on repo visibility (private vs public);
four work-bearing commits lack roadmap Done entries; roadmap "Now" contains
two expired "revisit at home ~23:00 KST 2026-05-18" reminders; and
`CURRENT-ALERT.md` is stale.  Architecture, model assignments, cost model,
secrets scan, §5 marker compliance, and `.gitignore` coverage all pass clean.

## Findings

- **[medium]** `.claude/settings.json` hardcodes `/Users/melons` at nine locations — `.claude/settings.json:5–9,28,72–73,82`
  Evidence: Lines 5–9 contain `Read(/Users/melons/**)`, `Write(/Users/melons/ai/**)`,
  `Edit(/Users/melons/ai/**)`, `Write(/Users/melons/.local/share/whisper-models/**)`,
  `Write(/Users/melons/.claude/projects/-Users-melons-ai/memory/**)`.  Lines 28, 72–73,
  82 contain `Bash(rm /Users/melons/ai/**)`, `Bash(cd /Users/melons/ai/* && *)`, and
  `Bash(rm -rf /Users/melons)` (deny rule).  §8 requires env-driven paths; the plist
  hardcode problem was solved via `@@REPO_ROOT@@`/`@@HOME@@` substitution in `ab6555e`
  but `.claude/settings.json` received no analogous treatment.  On a machine swap the
  allow-list silently blocks all agent operations.  First raised as [info, carry-forward]
  in prior all-audits; elevated to [medium] by the contract audit at `22a45ea` because
  the plist fix proves the pattern is feasible and the gap is now intentional omission.
  Suggested fix: Create `.claude/settings.json.template` with `@@HOME@@`/`@@REPO_ROOT@@`
  tokens; generate `.claude/settings.json` via `scripts/install-scheduler.sh` (or a new
  `scripts/install-settings.sh`); gitignore the rendered file and track only the template.

- **[low]** `CLAUDE.md:35` says remote is "(private)" — `CLAUDE.md:35` vs `docs/operator-contract.md:121`
  Evidence: `CLAUDE.md` line 35 reads `Remote: git@github.com:MelonS/MelonS-Agents.git (private).`
  `docs/operator-contract.md` line 121 reads `Remote: git@github.com:MelonS/MelonS-Agents.git (public).`
  Commit `a2a3807` updated operator-contract.md §6 to "(public)" when the branch strategy
  was codified (first external clone verified 2026-05-18); `CLAUDE.md` was not updated in
  that commit.  The contract is the source of truth per its own opening paragraph.
  Suggested fix: Update `CLAUDE.md:35` from "(private)" to "(public)" in a micro-commit.

- **[low]** Roadmap Done entries missing for four work-bearing commits — `docs/roadmap.md` Done section
  Evidence: The Done section's most recent entry is `e1fda78` (~14:55 KST 2026-05-18).
  Since then, four commits qualify under §9 ("after work lands, append a one-line entry
  with the commit hash and date"): `aa10ba0` ("feat(onboarding): music-video-first quick
  start + bootstrap covers aubio/jq/Pexels" — rewrote `scripts/bootstrap.sh` and READMEs,
  375 changed lines); `a2a3807` ("docs(contract): codify branch strategy" — rewrote §6 of
  operator-contract, a governance-level change); `22a45ea` ("feat(branch-strategy):
  pre-merge gate + automated check script" — added `scripts/pre-merge-check.sh`);
  `65a5045` ("feat(ci): GitHub Actions main-protection" — added
  `.github/workflows/main-protection.yml`, on feat branch).  Pure tracking commits
  (`docs/goal.*`, `docs/roadmap.*`, `docs/daily.*`, `docs/pilots.*`) are self-describing
  and exempt.
  Suggested fix: Append four Done lines to the Done section of `docs/roadmap.md`.

- **[low]** `CURRENT-ALERT.md` stale — `docs/audit/CURRENT-ALERT.md`
  Evidence: `CURRENT-ALERT.md` (generated 2026-05-18 14:56:20 KST) references the
  nineteenth audit (`e1fda78`).  That audit's three [low] findings were all resolved:
  finding #1 (stale alert loop) is inherently self-referential; finding #2 (missing
  `e1fda78` Done entry) was resolved by `03fdda9`; finding #3 (`d40abd3` misattribution)
  was also resolved by `03fdda9`.  The alert now reports a DRIFT_DETECTED that no longer
  reflects the current state.  Will auto-clear on the next CLEAN `audit-run.sh` run.
  Suggested fix: Run `./scripts/audit-run.sh all`; a CLEAN verdict auto-removes the file.

- **[low]** Roadmap "Now" section contains two expired "revisit at home" reminders — `docs/roadmap.md:27–55`
  Evidence: Lines 27–35 read "_Pending operator review (revisit at home ~23:00 KST
  2026-05-18)_: format-variation brainstorm parked in `docs/ideas.md`".  Lines 36–55
  read "_Pending operator review (revisit at home ~23:00 KST 2026-05-18, second
  pointer)_: problem-solver framing".  Today is 2026-05-19; the "home session" they
  reference has passed.  The "Now" section should reflect the current focus, not
  yesterday's pending reminders.
  Suggested fix: Operator clears or acts on both pending items; update Now to reflect
  the current state (active goal candidate pending confirmation, next action is operator
  pick from `docs/goal.md` candidate).

- **[low]** `feat/post-merge-detection` merge to `main` pending with pre-merge gate unverified — branch HEAD `65a5045`
  Evidence: `git branch -a` shows `feat/post-merge-detection` at `65a5045`; `main` at
  `22a45ea` — one commit behind.  The commit body of `65a5045` states "Branch:
  feat/post-merge-detection (will run pre-merge-check.sh before fast-forward merging to
  main)" — future-tense language, gate not yet reported as run.  Per §6, all four gates
  must pass before merge.  Gate 1 (audit CLEAN) requires running `scripts/pre-merge-check.sh`
  on the feat branch.  Current state: gate unrun or result unrecorded.
  Suggested fix: Run `scripts/pre-merge-check.sh feat/post-merge-detection`; if all gates
  pass plus operator gives gate-4 OK, fast-forward merge to `main`.

- **[low]** `main-backup-pre-filter-20260517-173615` eligible for deletion — `remotes/origin/main-backup-pre-filter-20260517-173615`
  Evidence: Branch exists at `origin` and locally per `git branch -a`.  Roadmap Next #2
  reads: "Delete filter-repo backup branch — on or after 2026-05-18, if no issues
  observed with the rewritten history."  No issues reported in any commit, daily report,
  or audit since the history rewrite on 2026-05-17.  Deletion window opened 2026-05-18;
  today is 2026-05-19.
  Suggested fix: `git branch -D main-backup-pre-filter-20260517-173615 && git push origin --delete main-backup-pre-filter-20260517-173615` (roadmap Next #2 command exactly).

- **[info]** `feat/skill-music-video` placeholder branch — no commits ahead of `main`
  Evidence: `git log main..feat/skill-music-video` returns nothing; branch points to
  `a2a3807`, one commit behind `main` at `22a45ea`.  Created as a placeholder for the
  upcoming Skills work at the time branch strategy was codified.  No development work
  has ever landed on it.  Stale empty branches add noise to `git branch -a`.
  Suggested fix: Delete with `git branch -d feat/skill-music-video && git push origin --delete feat/skill-music-video`; recreate from `main` HEAD when Skills work actually starts.

- **[info]** Two auto-created `claude/` local branches — `claude/cool-swartz-2dceac`, `claude/xenodochial-mclean-6ee111`
  Evidence: `git branch -a` lists both without a `remotes/origin/` counterpart (local only).
  Both point to `f968629` (2026-05-15 commit "docs: apply documentation style guide to
  README EN/KO").  These are Claude Code session auto-branches; §6 branch strategy
  documents only `main`, `feat/<name>`, and `v<x>.<y>.<z>` tags — `claude/` is outside
  the defined strategy.  No operational risk; local-only, not pushed.
  Suggested fix: Prune with `git branch -d claude/cool-swartz-2dceac claude/xenodochial-mclean-6ee111`.

- **[info, carry-forward]** Copyright TODO items in `agents/lib/copyright.sh:10–12`
  Evidence: "Still TODO (see docs/copyright-policy.md): Audio fingerprint check" and
  "Logo / watermark detection" — tracked in `docs/copyright-policy.md` "Still TODO" block.
  Unchanged across all prior audits.  Not stale — actively tracked.
  Suggested fix: No action; promote from copyright-policy.md when load-bearing.

- **[info, carry-forward]** `scripts/gen-upload-metadata.sh:105–108` `<TODO: fill manually>` placeholder strings
  Evidence: Four JSON fallback fields with `<TODO: fill manually>` sentinel values — emitted
  when ollama fails to generate platform copy.  Intentional design: signals to operator
  that manual fill is required rather than silently emitting empty strings.
  Suggested fix: No action needed; intentional fallback sentinel.

- **[info, carry-forward]** Two 2026-05-16 daily reports use non-standard naming — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`
  Evidence: §11 step 3 specifies `docs/daily/<ISO-date>.md`; these files use
  `<ISO-date>-<topic>.md`.  Unchanged across nine prior audits.
  Suggested fix: Rename to `2026-05-16.md` (merging content), or amend §11 to
  explicitly allow `<ISO-date>-<topic>.md` variants for multi-session days.

## Drift between docs and code

**Resolved since nineteenth audit:**
- `docs/roadmap.md` `d40abd3` Done entry misattribution — corrected by `03fdda9`. ✓
- `docs/roadmap.md` missing `e1fda78` Done entry — added by `03fdda9`. ✓

**Active drift at HEAD `65a5045`:**
- `CLAUDE.md:35` "(private)" vs `docs/operator-contract.md:121` "(public)":
  `operator-contract.md` was updated to "(public)" in `a2a3807`; `CLAUDE.md` was not.
  The contract is the source of truth per its own opening paragraph; the CLAUDE.md
  cache entry is now incorrect.  (Detailed above as [low] finding.)

**All structural components verified clean:**
All six `.claude/agents/*.md` frontmatter model assignments match the
`docs/for-analysts.md` table (orchestrator=opus; planner/resourcer/editor/qa/auditor=sonnet).
All components named in `docs/architecture.md` have corresponding files:
`agents/lib/{env,log,ollama,whisper,ffmpeg,tts}.sh`, `agents/lib/clamp-window.jq`,
all five `agents/missions/*/run.sh`, `.github/workflows/{main-protection,pages}.yml`
(new; present on feat branch).  All scripts under `scripts/` referenced in `docs/`
or README.  `docs/cost-model.md` claims are accurate: `music-video/run.sh` has no
Anthropic calls; `faceless-short/run.sh:94–98` defaults to `ollama_generate` with
`FACELESS_SCRIPT_OVERRIDE` as opt-in Tier-1 path.  `docs/architecture.md` §6 branch
strategy section correctly describes the new `pre-merge-check.sh` gate.

## Stale items

- `docs/audit/CURRENT-ALERT.md` — stale (references 19th audit findings, all resolved
  by `03fdda9`); will auto-clear on next CLEAN `audit-run.sh` run.
- `docs/roadmap.md` Done — four work-bearing commits (`aa10ba0`, `a2a3807`, `22a45ea`,
  `65a5045`) have no Done entries.
- `docs/roadmap.md` "Now" — two "revisit at home ~23:00 KST 2026-05-18" reminders
  are now expired (today is 2026-05-19).
- `main-backup-pre-filter-20260517-173615` branch — eligible for deletion per roadmap
  Next #2; window opened 2026-05-18.
- `feat/skill-music-video` — stale empty placeholder branch (no commits).
- No files older than 90 days (repo initialized 2026-05-14); no dead code or dead scripts.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: bypassPermissions`; allow list covers local-resource tools; no user-action recipes in committed config |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example` schema-only; no API key values committed; `ANTHROPIC_API_KEY` comment says "set in your shell"; `config/policies.yaml` gates `paid_external`; all plist templates have `RunAtLoad=false` |
| §4 Auto-approve mode | PASS | `.claude/settings.json` allow list covers brew, git, gh, ffmpeg, yt-dlp, whisper, ollama, jq, python, file ops |
| §5 Logic changes require OK | PASS | No `.claude/agents/*.md` or root `agents/*.md` edits since marker convention date (`7c6ff4f`, 2026-05-17); pre-convention commits are carry-forward |
| §6 Auto-commit + branch strategy | PARTIAL (low) | `records/` gitignored; `.env` gitignored; all media/caption types gitignored. Branch strategy: `a2a3807` + `22a45ea` land on main as documented bootstrap exceptions. `65a5045` correctly on `feat/post-merge-detection` awaiting pre-merge gate. `feat/skill-music-video` stale placeholder; `claude/` local branches undocumented. Gate for `65a5045` → `main` merge not yet run |
| §7 Split commit + push | PASS | No `&&`-compound `git commit ... && git push` found in committed scripts; deny list blocks force-push |
| §8 Code/data separation | PARTIAL (medium) | `outputs/publish/.gitkeep` has `<!-- §8 operator-directed deviation -->` marker. `scripts/music-video-shaders.sh` and `scripts/audit-run.sh` have inline §8 exception comments. **Gap**: `.claude/settings.json` hardcodes `/Users/melons` at nine locations (see [medium] finding above) |
| §9 Goal + roadmap source of truth | PARTIAL (low) | `docs/goal.md` active goal correctly empty with candidate pending operator confirmation. `docs/roadmap.md` "Now" has expired reminders. Four work-bearing commits missing Done entries |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two 2026-05-16 daily reports use non-standard `<ISO-date>-<topic>.md` naming; unchanged across nine prior audits |
| §12 No PII or secrets | PASS | `site/index.html:169` LinkedIn anchor carries `<!-- §12 operator-authorized deviation -->` marker. Full `git grep -nE "(api[_-]?key\|secret\|token\|password)"` (excluding docs/, examples) returns only placeholder text, Python tokenizer variable names, workflow comment strings, and policy text — zero credential values. `.env` not in git index |

## Next audit hint

The pre-merge gate for `feat/post-merge-detection` → `main` is the first real exercise
of the newly codified §6 branch strategy; verify gate 1 (audit CLEAN) passes on the
feat branch HEAD before the merge, then confirm the `CURRENT-ALERT.md` auto-clears
after a CLEAN run and that `CLAUDE.md:35` is updated to "(public)".
