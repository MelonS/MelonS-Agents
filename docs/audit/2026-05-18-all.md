# Audit report — 2026-05-18 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Nineteenth audit of the session; HEAD = `e1fda78` (2026-05-18).
Supersedes the eighteenth audit at HEAD `d40abd3` (2026-05-18, ~14:34 KST).
Full-scope audit covering all six dimensions: architecture vs documentation drift,
roadmap freshness, operator-contract compliance, cost-model accuracy, stale TODOs /
dead code, and security / secrets.  One commit landed since the eighteenth audit:
`e1fda78` ("docs(audit): close last 2 lows — gitignore .claude/*.lock + roadmap Done
entry for d40abd3") resolved two of the three 18th-audit [low] findings — `d40abd3`
Done entry is now present in `docs/roadmap.md` (line 63) and `.claude/*.lock` is now
covered by `.gitignore` (line 130).  Three new [low] findings: (1) `CURRENT-ALERT.md`
still shows DRIFT_DETECTED — two of its three listed findings are resolved but the
alert has not auto-cleared (requires a CLEAN `audit-run.sh` run); (2) `e1fda78` itself
has no Done entry in `docs/roadmap.md` per §9; (3) `docs/roadmap.md:72–75` attributes
the `.gitignore` fix to `d40abd3` when `git show d40abd3 --name-only` confirms `.gitignore`
was not in `d40abd3`'s changed-files list — it was changed by `e1fda78`.  Architecture,
model assignments, cost model, secrets scan, and §5 marker compliance all pass clean.

## Findings

- **[low]** `CURRENT-ALERT.md` stale — reports 3 [low] findings but 2 are resolved — `docs/audit/CURRENT-ALERT.md`
  Evidence: `CURRENT-ALERT.md` (generated 2026-05-18 14:42:36 KST, subsequently touched by `e1fda78`) lists three [low] findings from the 18th audit.  `e1fda78` resolved finding #2 (roadmap Done missing `d40abd3` entry — now present at line 63) and finding #3 (`.claude/scheduled_tasks.lock` not gitignored — `.claude/*.lock` now at `.gitignore:130`).  The alert's summary paragraph still describes all three as active; only finding #1 (stale-alert meta-loop) is structurally unresolved and requires a CLEAN run to auto-clear.
  Suggested fix: Re-run `./scripts/audit-run.sh all`; a CLEAN verdict auto-removes `CURRENT-ALERT.md`.

- **[low]** `e1fda78` missing Done entry in `docs/roadmap.md` — `docs/roadmap.md` Done section
  Evidence: Commit `e1fda78` ("docs(audit): close last 2 lows — gitignore .claude/*.lock + roadmap Done entry for d40abd3") fixed `.gitignore` and wrote the `d40abd3` Done entry, but has no standalone Done entry for itself.  Operator-contract §9: "after work lands, append a one-line entry to roadmap's Done section with the commit hash and date."  `git log --oneline -1` returns `e1fda78 docs(audit): close last 2 lows...`; this hash does not appear in the Done section.
  Suggested fix: Append `- **2026-05-18** **Close 2 eighteenth-audit lows — .claude/*.lock gitignore + d40abd3 Done entry** (commit \`e1fda78\`).` to `docs/roadmap.md` Done.

- **[low]** `d40abd3` Done entry incorrectly attributes `.gitignore` fix — `docs/roadmap.md:72–75`
  Evidence: Lines 72–75 inside the `d40abd3` Done entry read: "Plus \`.gitignore\` now covers \`.claude/*.lock\` after the eighteenth audit flagged \`.claude/scheduled_tasks.lock\`..."  `git show d40abd3 --name-only` lists changed files as `docs/architecture.md`, `docs/audit/2026-05-18-all.md`, `docs/audit/CURRENT-ALERT.md`, `docs/for-analysts.md`, `docs/roadmap.md`, `outputs/publish/.gitkeep` — `.gitignore` absent.  `git show e1fda78 --name-only` confirms `.gitignore` was changed in `e1fda78`.  The Done entry authored by `e1fda78` conflated its own `.gitignore` work into the `d40abd3` heading.
  Suggested fix: Remove the misattributed lines 72–75 from the `d40abd3` Done entry and fold that attribution into a new `e1fda78` Done entry (see finding above).

- **[info, carry-forward]** Copyright TODO items in `agents/lib/copyright.sh:10–12` — tracked in `docs/copyright-policy.md`
  Evidence: "Still TODO (see docs/copyright-policy.md): Audio fingerprint check" and "Logo / watermark detection" comments.  Cross-referenced in `docs/copyright-policy.md` "Still TODO" block.  Unchanged across all prior audits.  Not stale — tracked.
  Suggested fix: No action; carry-forward until promoted from `docs/copyright-policy.md`.

- **[info, carry-forward]** `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>` placeholder strings in JSON error-recovery handler.  Intentional fallback for when ollama fails.
  Suggested fix: No action needed; intentional sentinel.

- **[info, carry-forward]** Two 2026-05-16 daily reports use non-standard `<ISO-date>-<topic>.md` naming — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`.  Unchanged since prior audits.
  Suggested fix: Rename both to `2026-05-16.md` (merging content) or amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants for multi-session days.

- **[info, carry-forward]** Operator first name in commit body `1537ca6` ("so Todd sees the alert") — immutable git history.
  Suggested fix: Accept as immutable; use role-neutral phrasing in future commit messages.

- **[info, carry-forward]** `.claude/settings.json` hardcodes machine-specific `/Users/melons/` paths — `.claude/settings.json:5–9,28,72–73,82`
  Evidence: Committed `settings.json` contains absolute `/Users/melons/` in Claude Code allow-list permission patterns.  On a different machine these paths won't match the user's home directory.  Known necessary limitation of Claude Code's permission model.  Prior `fbf3d70` normalized double-slash variants; fundamental machine-specificity remains.
  Suggested fix: Add `scripts/install-settings.sh` generating the file from `settings.template.json` via `@@HOME@@` substitution (same pattern as plist templating in `ab6555e`); or document in `docs/architecture.md` that `.claude/settings.json` must be updated after a machine swap.

## Drift between docs and code

**18th-audit items: FULLY RESOLVED**

- `docs/for-analysts.md:78` — stale goal reference corrected by `d40abd3`. ✓
- `outputs/publish/.gitkeep` — §8 deviation marker added by `d40abd3`. ✓
- `.gitignore` — `.claude/*.lock` coverage added by `e1fda78`. ✓

**New drift at HEAD `e1fda78`:**

- `docs/roadmap.md:72–75` — `d40abd3` Done entry states "Plus `.gitignore` now covers `.claude/*.lock`" but this change was made by `e1fda78`, not `d40abd3` (verified via `git show d40abd3 --name-only`).  Minor audit-trail confusion: the commit referenced in the Done heading is not the commit that performed the described work.

**All structural components verified clean.**  All six agent definitions in `.claude/agents/*.md` frontmatter match the `docs/for-analysts.md` model table (orchestrator=opus; planner/resourcer/editor/qa/auditor=sonnet).  All components named in `docs/architecture.md` have corresponding files: `agents/lib/{env,log,ollama,whisper,ffmpeg,tts}.sh`, `agents/lib/clamp-window.jq`, all five `agents/missions/*/run.sh`, all six `.claude/agents/*.md`.  All scripts under `scripts/` referenced in `docs/` or `README.md`.  `docs/cost-model.md` correctly describes music-video as fully Tier-2 and faceless-short as opt-in Tier-1 defaulting to local ollama.

## Stale items

- **`docs/audit/CURRENT-ALERT.md`** — stale (2 of 3 reported findings resolved); will auto-clear on next CLEAN `audit-run.sh` run.
- **`docs/roadmap.md` Done** — missing `e1fda78` entry; `d40abd3` Done entry contains misattributed `.gitignore` text (details above).
- **`docs/pilots/first-upload-metrics.md`** — table rows `_to be filled_`; 24h post-publish target is ~23:30 KST 2026-05-18.  Not stale — expected pending.
- No files older than 90 days (repo initialized 2026-05-14).  No dead code or dead scripts.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: bypassPermissions`; allow list covers local-resource tools; no user-action recipes in committed config |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example` `PEXELS_API_KEY=` blank; `ANTHROPIC_API_KEY` set-in-shell only (comment confirms, not in file); `config/policies.yaml` gates `paid_external`; all plist templates have `RunAtLoad=false` |
| §4 Auto-approve mode | PASS | Allow list covers brew, git, gh, ffmpeg, yt-dlp, whisper, ollama, jq, python, file ops; deny list guards catastrophic ops |
| §5 Logic changes require OK | PASS | No `.claude/agents/*.md` or root `agents/*.md` edits since marker-convention date (`7c6ff4f`, 2026-05-17).  Two post-convention commits touching `agents/missions/*/run.sh` carry `Requested-by: user` voluntarily (`c5fd261`, `2dbf15e`); not strictly §5-scope (`.sh` not `.md`) but trail is clean |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored; `.env` not in git index; `*.mp4/wav/srt/ass` globally gitignored; `.claude/*.lock` now gitignored (`e1fda78`); no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | PASS (behavioral) | No `&&`-compound `commit + push` found in committed scripts; deny list blocks `--force` push to remotes |
| §8 Code/data separation | PASS | `outputs/publish/.gitkeep` carries `<!-- §8 operator-directed deviation -->` marker with Layers-table row in `docs/architecture.md`.  `scripts/music-video-shaders.sh:57–64` and `scripts/audit-run.sh:37–44` fallback paths carry inline `§8 exception` comments.  No output artifacts committed under `agents/` or `scripts/` |
| §9 Goal/roadmap source of truth | PARTIAL (low) | `docs/goal.md` active goal correctly empty.  `docs/roadmap.md` "Now" correctly empty.  **Gaps**: (a) `e1fda78` has no Done entry; (b) `d40abd3` Done entry attributes `.gitignore` fix to wrong commit — see Findings |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two 2026-05-16 reports use `<ISO-date>-<topic>.md` naming; unchanged across nine prior audits |
| §12 No PII or secrets | PASS | `site/index.html:169` LinkedIn anchor has `<!-- §12 operator-authorized deviation -->` marker (`e793662`).  Full `git grep -nE "(api[_-]?key\|secret\|token\|password)"` (excluding docs/examples) returns only permission-pattern text in `settings.json`, policy text in `config/policies.yaml`, Python tokenization code — zero credential values.  `.env` confirmed not in git index |

## Next audit hint

Three one-line fixes clear DRIFT_DETECTED — (1) append `e1fda78` Done entry to roadmap, (2) remove the misattributed `.gitignore` line from the `d40abd3` Done entry, (3) re-run `audit-run.sh` to auto-clear `CURRENT-ALERT.md`; also verify whether `docs/pilots/first-upload-metrics.md` has been filled with 24h post-publish data (~23:30 KST 2026-05-18 target).
