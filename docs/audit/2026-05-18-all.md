# Audit report — 2026-05-18 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Seventeenth audit of the session; HEAD = `39c5db3` (2026-05-18, ~14:23 KST).
Supersedes the sixteenth audit at HEAD `f86c2f0` (also 2026-05-18, 03:08 KST).
Full-scope audit covering all six dimensions: architecture vs documentation drift,
roadmap freshness, operator-contract compliance, cost-model accuracy, stale TODOs /
dead code, and security / secrets.  Six commits landed since the prior audit:
`a1846f6` (daily-queue fix), `aa10ba0` (onboarding bootstrap), `5e831fe` (fresh-clone
PASS log), `303949a` (readme polish), `9d7f954` (readme pricing/path update),
`39c5db3` (audit fix — resolved all three medium findings from the prior run).
All prior medium findings are now resolved.  Two low findings remain open:
`outputs/publish/.gitkeep` (§8 carry-forward) and a stale "current active goal"
reference in `docs/for-analysts.md:78` (new).  Security, secrets, model assignments,
roadmap commit-hash validity, and §5 marker compliance all pass clean.

## Findings

- **[low, carry-forward]** `outputs/publish/.gitkeep` commits a repo-root output directory — `outputs/publish/.gitkeep`
  Evidence: `git ls-files outputs/` returns `outputs/publish/.gitkeep`.  §8 states "All outputs go to `$RECORDS_DIR` (default `./records/`, gitignored)."  The `outputs/` directory at the repo root conflicts with the §8 code/data separation convention.  `scripts/daily-music-video.sh:43` writes to `$PWD/outputs/publish`; `docs/goal.md` directed this specific path as the publish-ready delivery surface.  No mp4 files are committed (globally gitignored), so no output artifact has leaked; only the `.gitkeep` scaffolding is tracked.  Unchanged from prior audit.
  Suggested fix: Move the publish-ready path to `records/publish/` (gitignored for mp4, add a `!records/publish/.gitkeep` exception to `.gitignore`), or add an explicit `<!-- §8 operator-directed deviation -->` comment to `outputs/publish/.gitkeep` and document the deviation in `docs/architecture.md` Layers table.

- **[low]** Stale "current active goal" reference in `docs/for-analysts.md:78` — `docs/for-analysts.md:78`
  Evidence: Line 78 reads "The faceless-short pilots (the current active goal's deliverables) live under `pilots/`".  The active goal was cleared by commit `39c5db3` (2026-05-18 14:23 KST) — `docs/goal.md` active goal section now reads "no active goal — operator sets the next one."  The faceless-short pilots belong to the 2026-05-16 niche-selection goal, which is in Past goals.  Commit `39c5db3` updated architecture.md and multiple for-analysts.md sections but did not update this sentence.
  Suggested fix: Update `docs/for-analysts.md:78` to remove "current active goal's" — e.g., "The faceless-short pilots (2026-05-16 niche-selection deliverables) live under `pilots/`".

- **[info, carry-forward]** Copyright TODO items in `agents/lib/copyright.sh:10` — tracked in `docs/copyright-policy.md`
  Evidence: "Still TODO: Audio fingerprint check" and "Logo / watermark detection" comments.  Cross-referenced and tracked in `docs/copyright-policy.md` "Still TODO" block.  Unchanged across all prior audits.  Not stale — roadmap-referenced.
  Suggested fix: No action; carry-forward until promoted from `docs/copyright-policy.md`.

- **[info, carry-forward]** `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>` placeholder strings in JSON error-recovery handler.  Intentional by design (fallback strings for when ollama fails).
  Suggested fix: No action needed; intentional sentinel.

- **[info, carry-forward]** Two 2026-05-16 daily reports use non-standard `<ISO-date>-<topic>.md` naming — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`.  Unchanged since prior audits.
  Suggested fix: Rename both to `2026-05-16.md` (merging content) or amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants for multi-session days.

- **[info, carry-forward]** Operator first name in commit body `1537ca6` ("so Todd sees the alert") — immutable git history.
  Suggested fix: Accept as immutable; use role-neutral phrasing in future commit messages.

- **[info]** `.claude/settings.json` hardcodes machine-specific `/Users/melons/` paths — `.claude/settings.json:5–9,28,72–73,82`
  Evidence: `.claude/settings.json` is committed to git and contains absolute paths `/Users/melons/` in the Claude Code allow-list permission patterns (`Read(/Users/melons/**)`, `Write(/Users/melons/ai/**)`, etc.).  On a different machine these paths won't match the user's home directory, rendering the allow list non-functional.  This is a known necessary limitation of Claude Code's permission model (the allow list requires absolute paths to express scope); the prior `fbf3d70` audit-cleanup commit normalized double-slash variants but did not change the fundamental machine-specificity.  The `operator-contract.md` §8 rule targets env-driven *tool binary paths*; `settings.json` permission scopes are a different category.
  Suggested fix: Document in `docs/architecture.md` or a `README` note that `.claude/settings.json` must be updated to reflect the new user home path after a machine swap; alternatively, add a `scripts/install-settings.sh` that generates the file from a `settings.template.json` using `@@HOME@@` substitution (same pattern as the plist templating in `ab6555e`).

## Drift between docs and code

**Faceless-short Tier routing: RESOLVED** — Prior audit [medium] finding resolved by `39c5db3`.  `docs/architecture.md` faceless-short pipeline now correctly shows ollama as the default script-generation path, with Sonnet as opt-in via `FACELESS_SCRIPT_OVERRIDE`.  `docs/for-analysts.md` TL;DR and mission-execution sections now correctly describe the same opt-in pattern.  `docs/cost-model.md` was already correct and was the canonical reference.

**for-analysts.md stale goal reference (low):** `docs/for-analysts.md:78` describes faceless-short pilots as "the current active goal's deliverables" — stale since `39c5db3` cleared the active goal.  All other for-analysts.md sections updated correctly by the same commit.

## Stale items

- **`docs/for-analysts.md:78`** — "current active goal" reference is stale; goal cleared 2026-05-18 (detailed in Findings above).
- **`docs/audit/CURRENT-ALERT.md`** — Generated at 03:08 KST by the L3 daily scheduler, pointing at the prior version of this same report file.  The fix commits landed at 14:23 KST, which is post-L3; L2 (15-min poll) only fires on new blockers/QA-FAIL bursts, so the alert has not auto-cleared.  This report replaces the file content; CURRENT-ALERT.md remains accurate (verdict is still DRIFT_DETECTED for the two remaining low findings) but its summary paragraph refers to the prior findings rather than the current ones.  Will self-correct on the next `scripts/audit-run.sh` CLEAN run.
- **`docs/pilots/first-upload-metrics.md`** — snapshot table rows are `_to be filled_`; expected pending 24h post-publish data (target ~23:30 KST 2026-05-18), not stale.
- No files older than 90 days (repo initialized 2026-05-14).
- No dead scripts; all 28 scripts under `scripts/` are referenced in docs or README.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: bypassPermissions`; allow list covers local-resource tools; no user-action recipes in committed config |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` blank placeholder; `ANTHROPIC_API_KEY` set-in-shell only (comment confirms, not in file); `config/policies.yaml` gates `paid_external`; all plist templates have `RunAtLoad=false` |
| §4 Auto-approve mode | PASS | Allow list covers all documented local tools (brew, git, ffmpeg, yt-dlp, whisper, ollama, python, jq); deny list guards catastrophic ops |
| §5 Logic changes require OK | PASS | All post-convention §5-scope commits carry `Requested-by: user` marker: `c5fd261` (2026-05-17 15:18 — music-video/run.sh created), `2dbf15e` (2026-05-17 16:11 — music-video/run.sh updated).  No `.claude/agents/*.md` edits since convention date (`7c6ff4f`, 2026-05-17 04:00 KST).  No §5-scope commits in the 6 commits since prior audit (all touched `docs/`, `scripts/`, or `onboarding/` only) |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored via `records/` rule; `.env` not in git index (confirmed via `git show HEAD:.env`); `*.mp4`/`*.wav`/`*.srt` globally gitignored; no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | PASS (behavioral) | No `&&`-compound `commit + push` found in committed scripts; deny list blocks `--force` push to remotes |
| §8 Code/data separation | PARTIAL (low, carry-forward) | `outputs/publish/.gitkeep` commits repo-root output directory — §8 says outputs go to `$RECORDS_DIR`.  `scripts/music-video-shaders.sh:57,64` `/opt/homebrew/bin/ffmpeg[probe]` fallbacks: now have inline `§8 exception` comment (added in `39c5db3`); same documented-exception pattern as `agents/lib/env.sh` and `scripts/audit-run.sh`.  Prior §8 plist finding FULLY RESOLVED by `ab6555e` |
| §9 Goal/roadmap source of truth | PASS | `docs/goal.md` active goal section correctly cleared ("no active goal — operator sets the next one") by `39c5db3`.  `docs/roadmap.md` "Now" is cleared and updated 2026-05-18 ~14:10 KST.  No stale Now items |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two legacy 2026-05-16 reports use non-standard `<ISO-date>-<topic>.md` naming; unchanged across six prior audits |
| §12 No PII or secrets | PASS | `site/index.html:169` LinkedIn anchor has `<!-- §12 operator-authorized deviation -->` marker (`e793662`).  `assets/music/SOURCES.md` contains Suno free-tier license URLs — no secrets, no PII.  Full secrets scan (`git grep -nE "(api[_-]?key\|secret\|token\|password)"` excluding docs/examples) returns only permission-pattern text in `settings.json`, policy-text in `config/policies.yaml`, and text-tokenization code in Python scripts — zero actual credential values.  `.env` confirmed not in git index |

## Next audit hint

Both remaining actionable findings are mechanical one-line fixes: update `docs/for-analysts.md:78` to drop the "current active goal's" phrasing, and either move `outputs/publish/` to `records/publish/` or add an explicit §8-deviation marker; resolving those two would return the verdict to CLEAN on the next run.
