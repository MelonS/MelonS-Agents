# Audit report — 2026-05-18 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Sixteenth audit of the session; HEAD = `f86c2f0` (2026-05-18, latest commit as of audit time).
Supersedes the fifteenth audit at HEAD `e793662` (2026-05-17-contract.md).
Full-scope audit covering all six dimensions: architecture vs documentation drift,
roadmap freshness, operator-contract compliance, cost-model accuracy, stale TODOs /
dead code, and security / secrets.  Seven commits landed since the prior audit:
`ab6555e` (plist templating — closes prior §8 plist finding), `a8291f5`, `6a460b0`,
`23832fa`, `36323eb`, `65c35a6`, `f86c2f0`.  Key new surfaces: `scripts/music-video-shaders.sh`,
`scripts/daily-music-video.sh`, `assets/music/SOURCES.md`, and `outputs/publish/`.
Three medium findings identified: architecture docs describe the faceless-short script
stage as Tier-1-Claude-primary, but the code defaults to Tier-2 ollama (opt-in override
required); roadmap "Now" is stale after goal completion; CURRENT-ALERT.md was not
auto-cleared after the §8 plist fix landed.  The prior §8 plist finding is fully
resolved by `ab6555e`; all other prior high/critical findings remain clean.

## Findings

- **[medium]** Faceless-short Tier-routing docs conflict with code default — `agents/missions/faceless-short/run.sh:98` and `docs/architecture.md` (faceless-short pipeline diagram), `docs/for-analysts.md:119`
  Evidence: `docs/architecture.md` (faceless-short pipeline section, lines ~172–183) shows `claude --print --model claude-sonnet-4-6` as the primary script-generation step with no mention of an opt-in flag.  `docs/for-analysts.md:119` states "narration-script generation routes to Sonnet via `scripts/gen-script-claude.sh`" — unqualified.  `agents/missions/faceless-short/run.sh:98` defaults to `ollama_generate "$OLLAMA_MODEL_HIGHLIGHT"` unless `$FACELESS_SCRIPT_OVERRIDE` is set; the in-code comment at line 5 says "1. ollama writes the 60s narration script."  `docs/cost-model.md` correctly describes the Claude route as opt-in ("routes through Sonnet when `FACELESS_SCRIPT_OVERRIDE` points at a pre-generated script file"), but architecture.md and for-analysts.md give the opposite impression.
  Suggested fix: Amend the faceless-short pipeline diagram in `docs/architecture.md` and the routing sentence in `docs/for-analysts.md` to reflect that the Claude script route is opt-in via `FACELESS_SCRIPT_OVERRIDE` / `scripts/gen-script-claude.sh`, with ollama as the default; update the run.sh comment at line 5 to match.

- **[medium]** Roadmap "Now" is stale — `docs/roadmap.md:17–48`
  Evidence: The "Now" section header reads "Last updated 2026-05-17 ~20:10 KST" and lists four items (music source resolution, render pass, operator review, stage for upload) for the "Production-ready music-video short" goal.  `docs/goal.md` active goal heading now reads "**ACHIEVED 2026-05-17 ~23:30 KST**" with all three mandatory subgoals checked.  Per operator-contract §9 ("Do not leave a stale 'Now' that refers to something already shipped") and the shutdown protocol §11 step 5, the "Now" should have been cleared on goal completion.
  Suggested fix: Replace the four Now items with either the next queued item (roadmap "Next" item #1 — delete the filter-repo backup branch) or an explicit "active goal just closed; user sets next goal" placeholder.

- **[medium]** CURRENT-ALERT.md persists after its triggering finding was resolved — `docs/audit/CURRENT-ALERT.md`
  Evidence: `docs/audit/CURRENT-ALERT.md` shows `Verdict: DRIFT_DETECTED`, `Generated: 2026-05-17 18:09:21 KST`, referencing `2026-05-17-contract.md`.  The sole remaining [low] finding in that report was the §8 plist hardcoding issue.  Commit `ab6555e` (landed after the alert was written) fully resolves that finding: `scripts/install-scheduler.sh` now uses `render_plist()` (sed substitution of `@@REPO_ROOT@@` / `@@HOME@@`), four `*.plist.template` files are committed, and `.gitignore` excludes `scripts/com.melons.agents.*.plist`.  Neither L2 (15-min poll) nor L3 (03:00 daily) has fired a clearing audit since `ab6555e`; the alert is stale as of this audit.
  Suggested fix: Re-run `scripts/audit-run.sh all` (which this report satisfies); confirm this report returns CLEAN or DRIFT_DETECTED only on new findings; `audit-run.sh` will auto-remove `CURRENT-ALERT.md` on a CLEAN verdict.

- **[low]** `docs/goal.md` active goal achieved but not migrated to Past goals — `docs/goal.md:19`
  Evidence: The active goal "Production-ready music-video short" heading contains "**ACHIEVED 2026-05-17 ~23:30 KST**" inline, meaning the goal is done, but the entire section remains under "## Active goal" rather than being moved to "## Past goals."  The goal.md maintenance contract states "when an active goal is achieved, the agent moves it to 'Past goals' intact."  The optional subgoal 4 (24h watch-time capture in `docs/pilots/first-upload-metrics.md`) is also incomplete and awaits operator action; that template exists but rows are unfilled.
  Suggested fix: User sets the next active goal, at which point the agent migrates the achieved goal to "## Past goals" in the same commit that writes the new goal — or the user initiates the migration manually to unlock the session-start protocol behavior.

- **[low]** `scripts/music-video-shaders.sh:57,131` hardcode `/opt/homebrew/bin/ffmpeg[probe]` as fallback — new surface, not in §8 exception registry
  Evidence: Line 57: `[[ -x "${FFMPEG_BIN:-/opt/homebrew/bin/ffmpeg}" ]]`; line 131: `"${FFPROBE_BIN:-/opt/homebrew/bin/ffprobe}"`.  The script sources `agents/lib/env.sh` at line 46 with `|| true`, so if sourcing fails silently (e.g., path issues), `$FFMPEG_BIN` may be unset and the hardcoded Homebrew path is used.  Prior audits documented `agents/lib/env.sh:38,50` as a "§8 exception" (discovery-fallback, not primary path) with a note in `scripts/audit-run.sh:37–42`.  The `music-video-shaders.sh` fallbacks lack this documentation and were added in `23832fa` after the exception registry was established.
  Suggested fix: Either add `source "$REPO_ROOT/agents/lib/env.sh"` with a hard fail (no `|| true`) so `$FFMPEG_BIN` is always resolved before the fallback line is reached, or add the music-video-shaders.sh lines to the §8 exception comment block in `scripts/audit-run.sh`.

- **[low]** `outputs/publish/.gitkeep` commits a repo-root output directory — `6a460b0`
  Evidence: `git ls-files outputs/` returns `outputs/publish/.gitkeep`.  §8 states "All outputs go to `$RECORDS_DIR` (default `./records/`, gitignored)."  The `outputs/` tree is at the repo root, not under `records/`.  Actual mp4 files are gitignored via the global `*.mp4` rule, so no output artifact is committed.  `scripts/daily-music-video.sh:43` writes to `$PWD/outputs/publish`, and `docs/goal.md` directed this path ("not buried under `records/missions/<date>/<id>/outputs/`").  The `.gitkeep` is infrastructure scaffolding, but the directory name ("outputs") conflicts with the §8 convention terminology.
  Suggested fix: Consider moving the publish-ready path to `records/publish/` (still gitignored for mp4, add `records/publish/.gitkeep` exception to `.gitignore`), or add an explicit §8-exception comment in `6a460b0`'s follow-up and the architecture doc to document that `outputs/publish/` is an operator-directed deviation.

- **[info, carry-forward]** Copyright TODO items in `agents/lib/copyright.sh:10` — tracked in `docs/copyright-policy.md`
  Evidence: "Still TODO: Audio fingerprint check" and "Logo / watermark detection" comments. Cross-referenced and tracked in `docs/copyright-policy.md` "Still TODO" block. Unchanged across all prior audits. Not stale — roadmap-referenced.
  Suggested fix: No action; carry-forward until promoted from copyright-policy.md.

- **[info, carry-forward]** `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>` placeholder strings in JSON error-recovery handler. Intentional by design (fallback strings for when ollama fails).
  Suggested fix: No action needed; intentional sentinel.

- **[info, carry-forward]** Two 2026-05-16 daily reports use non-standard `<ISO-date>-<topic>.md` naming — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`. Unchanged across six prior audits.
  Suggested fix: Rename both to `2026-05-16.md` (merging content) or amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants for multi-session days.

- **[info, carry-forward]** Operator first name in commit body `1537ca6` ("so Todd sees the alert") — immutable git history.
  Suggested fix: Accept as immutable; use role-neutral phrasing in future commit messages.

## Drift between docs and code

**Faceless-short Tier routing (medium):** `docs/architecture.md` faceless-short pipeline diagram and `docs/for-analysts.md:119` both present Claude Sonnet script generation as the primary / default path.  `agents/missions/faceless-short/run.sh:98` and its header comment (line 5) show ollama as the default, with Claude as opt-in via `FACELESS_SCRIPT_OVERRIDE`.  `docs/cost-model.md` (the most technically accurate of the three docs) correctly describes this as opt-in.

**Resolved since prior audit:** The §8 plist drift (four plists hardcoding `/Users/melons/ai/…`) is fully resolved by `ab6555e`.  All five mission types remain in the `docs/architecture.md` Mission types table.  `docs/for-analysts.md` subagent model-assignment table (heading "As of 2026-05-17") matches `.claude/agents/*.md` frontmatter for all six agents: `orchestrator=opus`; `planner/resourcer/editor/qa/auditor=sonnet`.

## Stale items

- **Roadmap "Now"** — four items tied to the completed "Production-ready music-video short" goal (detailed in Findings above).
- **`docs/goal.md` active goal** — marked ACHIEVED but not migrated to Past goals.
- **`docs/pilots/first-upload-metrics.md`** — per-day snapshot table row is `_to be filled_`; this is expected pending 24h post-publish data, not stale.
- No files older than 90 days (repo initialized 2026-05-14).
- No dead code; all scripts under `scripts/` are referenced in docs or README (verified via grep).

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: bypassPermissions`; allow list covers local-resource tools; no user-action recipes in config |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` blank; `ANTHROPIC_API_KEY` set-in-shell only; `config/policies.yaml` gates `paid_external`; all plist templates have `RunAtLoad=false` |
| §4 Auto-approve mode | PASS | Allow list covers all documented local tools; deny list guards catastrophic ops |
| §5 Logic changes require OK | PASS | All four commits touching `agents/` after convention date (`7c6ff4f`, 2026-05-17 04:00 KST) carry `Requested-by: user`: `3015f14` (12:47), `6d3fa51` (13:20), `c5fd261` (15:18), `2dbf15e` (16:11).  `e3f11ca` (03:23, touched `faceless-short/run.sh`) predates the convention — carry-forward.  No `.claude/agents/*.md` edits since the marker convention date |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored; `.env` gitignored; `.mp4`/`.wav`/`.srt` globally gitignored; no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | PASS (behavioral) | No `&&`-compound `commit + push` found in committed scripts; deny list blocks `--force` push |
| §8 Code/data separation | PARTIAL (low) | `scripts/music-video-shaders.sh:57,131` hardcode `/opt/homebrew/bin/ffmpeg[probe]` as fallback — not in the §8 exception registry.  `agents/lib/env.sh:38,50` and `scripts/audit-run.sh:37–42` remain in the documented exception registry.  Prior §8 plist finding RESOLVED by `ab6555e` (template generation via sed, plists gitignored) |
| §9 Goal/roadmap source of truth | PARTIAL (medium) | `docs/goal.md` active goal marked ACHIEVED (subgoals 1–3 checked); not yet migrated to Past goals.  `docs/roadmap.md` "Now" still references the completed goal's work items — stale per §9 "Do not leave a stale Now" |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two legacy 2026-05-16 reports use non-standard `<ISO-date>-<topic>.md` naming; unchanged since prior audits |
| §12 No PII or secrets | PASS | `site/index.html:169` LinkedIn anchor has `<!-- §12 operator-authorized deviation -->` marker (`e793662`).  `assets/music/SOURCES.md` contains Suno free-tier license URLs — no secrets, no PII.  Secrets scan returns only placeholder/comment matches.  `.env` gitignored.  No email addresses in committed non-doc files beyond the SSH remote |

## Next audit hint

The remaining actionable items are (a) correcting the faceless-short Tier routing description in `docs/architecture.md` and `docs/for-analysts.md` to match the actual opt-in behavior, and (b) clearing the stale roadmap "Now" after setting the next goal — both are mechanical doc updates that would return this audit to CLEAN; verify those two surfaces first on the next run.
