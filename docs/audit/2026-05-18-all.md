# Audit report — 2026-05-18 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Eighteenth audit of the session; HEAD = `d40abd3` (2026-05-18, ~14:34 KST).
Supersedes the seventeenth audit at HEAD `39c5db3` (2026-05-18, ~14:23 KST).
Full-scope audit covering all six dimensions: architecture vs documentation drift,
roadmap freshness, operator-contract compliance, cost-model accuracy, stale TODOs /
dead code, and security / secrets.  Two commits landed since the prior audit run
that generated `CURRENT-ALERT.md` (14:32:34 KST): `39c5db3` (already captured) and
`d40abd3` (audit-fix — resolved both [low] findings from the seventeenth audit).
Both previously-flagged [low] findings are now fully resolved: `docs/for-analysts.md:78`
rephrased correctly and `outputs/publish/.gitkeep` carries the `<!-- §8 operator-directed
deviation -->` marker with a matching row in the `docs/architecture.md` Layers table.
Three new [low] findings: (1) `docs/audit/CURRENT-ALERT.md` still shows DRIFT_DETECTED
for findings resolved ~40 seconds after the alert was written; (2) `docs/roadmap.md`
Done section missing the `d40abd3` entry per §9 contract; (3) `.claude/scheduled_tasks.lock`
is an untracked Claude Code runtime file not covered by `.gitignore`, creating persistent
noise in `git status`.  Security, secrets, model assignments, roadmap commit-hash
validity, and §5 marker compliance all pass clean.

## Findings

- **[low]** `CURRENT-ALERT.md` stale — shows DRIFT_DETECTED for findings resolved by `d40abd3` — `docs/audit/CURRENT-ALERT.md`
  Evidence: `CURRENT-ALERT.md` was generated at 14:32:34 KST by the L3 daily auditor firing `audit-run.sh all` at 03:00; the summary paragraph and verdict text refer to two [low] findings (`outputs/publish/.gitkeep` §8 carry-forward, `docs/for-analysts.md:78` stale goal reference) that were both resolved by commit `d40abd3` at ~14:34 KST.  L2 poll fires only on new blockers / QA-FAIL bursts; L3 fires again at 03:00 — neither clears the alert automatically on an audit-fix commit.  CURRENT-ALERT.md summary text is now factually incorrect.
  Suggested fix: Re-run `./scripts/audit-run.sh all` after this report is committed — a CLEAN verdict auto-removes CURRENT-ALERT.md.

- **[low]** Roadmap Done missing `d40abd3` entry — `docs/roadmap.md` Done section
  Evidence: Commit `d40abd3` (2026-05-18 ~14:34 KST, "resolve remaining 2 low findings — stale goal ref + §8 outputs deviation") is not listed in `docs/roadmap.md` Done.  Operator-contract §9 states "after work lands, append a one-line entry to roadmap's Done section with the commit hash and date."  The most recent Done entry describes `39c5db3` and ends with "Re-running audit-run.sh all to confirm CLEAN"; the follow-on fix commit is absent.
  Suggested fix: Append a one-line Done entry for `d40abd3` — e.g., `- **2026-05-18** (~14:34 KST) **Resolve two remaining audit lows — §8 marker + stale goal ref** (commit \`d40abd3\`).`

- **[low]** `.claude/scheduled_tasks.lock` not gitignored — `/.gitignore` + `.claude/`
  Evidence: `git status` shows `.claude/scheduled_tasks.lock` as an untracked file (confirmed by `git check-ignore -v .claude/scheduled_tasks.lock` returning "NOT GITIGNORED").  The lock file (130 bytes, mtime 2026-05-18 14:29) is a Claude Code runtime artifact — it does not belong in the repo but will persistently appear in `git status` until gitignored.  `.gitignore` covers `.claude/sessions/`, `.claude/cache/`, `.claude/logs/`, `.claude/settings.local.json`, `.claude/.last-session`, `.claude/statusline.sh` — but not `scheduled_tasks.lock`.
  Suggested fix: Add `.claude/scheduled_tasks.lock` (or `.claude/*.lock`) to `.gitignore`.

- **[info, carry-forward]** Copyright TODO items in `agents/lib/copyright.sh:10` — tracked in `docs/copyright-policy.md`
  Evidence: "Still TODO (see docs/copyright-policy.md): Audio fingerprint check" and "Logo / watermark detection" comments.  Cross-referenced and tracked in `docs/copyright-policy.md` "Still TODO" block.  Unchanged across all prior audits.  Not stale — roadmap-referenced.
  Suggested fix: No action; carry-forward until promoted from `docs/copyright-policy.md`.

- **[info, carry-forward]** `scripts/gen-upload-metadata.sh:105–108` — `<TODO: fill manually>` placeholder strings in JSON error-recovery handler.  Intentional by design (fallback strings for when ollama fails).
  Suggested fix: No action needed; intentional sentinel.

- **[info, carry-forward]** Two 2026-05-16 daily reports use non-standard `<ISO-date>-<topic>.md` naming — `docs/daily/2026-05-16-faceless.md`, `docs/daily/2026-05-16-overnight.md`.  Unchanged since prior audits.
  Suggested fix: Rename both to `2026-05-16.md` (merging content) or amend §11 step 3 to explicitly allow `<ISO-date>-<topic>.md` variants for multi-session days.

- **[info, carry-forward]** Operator first name in commit body `1537ca6` ("so Todd sees the alert") — immutable git history.
  Suggested fix: Accept as immutable; use role-neutral phrasing in future commit messages.

- **[info, carry-forward]** `.claude/settings.json` hardcodes machine-specific `/Users/melons/` paths — `.claude/settings.json:5–9,28,72–73,82`
  Evidence: Committed `settings.json` contains absolute `/Users/melons/` in the Claude Code allow-list permission patterns.  On a different machine these paths won't match the user's home directory.  This is a known necessary limitation of Claude Code's permission model.  Prior `fbf3d70` normalized double-slash variants; fundamental machine-specificity remains.
  Suggested fix: Add a `scripts/install-settings.sh` that generates the file from a `settings.template.json` using `@@HOME@@` substitution (same pattern as the plist templating in `ab6555e`); or document in `docs/architecture.md` that `.claude/settings.json` must be updated after a machine swap.

## Drift between docs and code

**Previously-flagged items: FULLY RESOLVED**

- `docs/for-analysts.md:78` — prior text "current active goal's deliverables" rephrased to "2026-05-16 niche-selection deliverables, now in Past goals" by `d40abd3`.  No longer stale.
- `outputs/publish/.gitkeep` — `<!-- §8 operator-directed deviation -->` marker added by `d40abd3`; matching Layers table row added to `docs/architecture.md`.  §8 finding closed.

**No new doc/code drift detected.**  All six agent models in `.claude/agents/*.md` frontmatter match the table in `docs/for-analysts.md` (auditor=sonnet, orchestrator=opus, planner/resourcer/editor/qa=sonnet).  All components named in `docs/architecture.md` have corresponding files (`agents/lib/*.sh`, `agents/missions/*/run.sh`, `scripts/*.sh`, `.claude/agents/*.md`).  All 36 scripts under `scripts/` are referenced in `docs/` or `README.md` (including newer additions: `daily-music-video.sh` → `README.md`; `tts-kokoro.py` → `agents/lib/tts.sh`).  `docs/cost-model.md` correctly describes music-video as fully Tier-2 and faceless-short as opt-in Tier-1.

## Stale items

- **`docs/audit/CURRENT-ALERT.md`** — stale since `d40abd3` resolved the two findings it reports (detailed in Findings above); will auto-clear on next CLEAN `audit-run.sh` run.
- **`docs/roadmap.md` Done** — missing `d40abd3` entry (detailed in Findings above).
- **`docs/pilots/first-upload-metrics.md`** — snapshot table rows are `_to be filled_`; operator-actioned pending 24h post-publish data (~23:30 KST 2026-05-18 target).  Not stale — expected pending.
- No files older than 90 days (repo initialized 2026-05-14).  No dead scripts.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | PASS | `defaultMode: bypassPermissions`; allow list covers local-resource tools; no user-action recipes in committed config |
| §2 Never pause | PASS | Behavioral rule; not config-detectable |
| §3 Money firewall | PASS | `.env.example`: `PEXELS_API_KEY=` blank placeholder; `ANTHROPIC_API_KEY` set-in-shell only (comment confirms, not in file); `config/policies.yaml` gates `paid_external`; all plist templates have `RunAtLoad=false` |
| §4 Auto-approve mode | PASS | Allow list covers all documented local tools (brew, git, ffmpeg, yt-dlp, whisper, ollama, python, jq); deny list guards catastrophic ops |
| §5 Logic changes require OK | PASS | All post-convention §5-scope commits carry `Requested-by: user` marker: `c5fd261` (2026-05-17 — music-video/run.sh created), `2dbf15e` (2026-05-17 — music-video/run.sh v6 update).  No `.claude/agents/*.md` edits since convention date.  No §5-scope commits since the prior audit |
| §6 Auto-commit + auto-push | PASS | `records/` gitignored; `.env` not in git index; `*.mp4`/`*.wav`/`*.srt` globally gitignored; no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | PASS (behavioral) | No `&&`-compound `commit + push` found in committed scripts; deny list blocks `--force` push to remotes |
| §8 Code/data separation | PASS | `outputs/publish/.gitkeep` now carries `<!-- §8 operator-directed deviation -->` marker (added by `d40abd3`) with Layers table row in `docs/architecture.md`.  `scripts/music-video-shaders.sh:57–64` and `scripts/audit-run.sh:37–44` fallback paths carry inline `§8 exception` comments.  No output artifacts committed under `agents/` or `scripts/` |
| §9 Goal/roadmap source of truth | PARTIAL (low) | `docs/goal.md` active goal correctly cleared ("no active goal — operator sets the next one").  `docs/roadmap.md` "Now" correctly cleared (2026-05-18 ~14:10 KST).  **Gap**: `d40abd3` Done entry missing — see Findings |
| §10 Session resume protocol | PASS | Behavioral rule; not config-detectable |
| §11 Shutdown protocol | PARTIAL (info, carry-forward) | Two legacy 2026-05-16 reports use non-standard `<ISO-date>-<topic>.md` naming; unchanged across seven prior audits |
| §12 No PII or secrets | PASS | `site/index.html:169` LinkedIn anchor has `<!-- §12 operator-authorized deviation -->` marker (`e793662`).  Full secrets scan (`git grep -nE "(api[_-]?key\|secret\|token\|password)"` excluding docs/examples) returns only permission-pattern text in `settings.json`, policy-text in `config/policies.yaml`, text-tokenization code in Python scripts — zero actual credential values.  `.env` confirmed not in git index |

## Next audit hint

Three mechanical one-line fixes (append `d40abd3` to roadmap Done, add `.claude/scheduled_tasks.lock` to `.gitignore`, re-run `audit-run.sh` to clear the stale alert) would return the verdict to CLEAN; focus the next audit on verifying those landed cleanly and checking whether `docs/pilots/first-upload-metrics.md` has been filled with the 24h post-publish data (~23:30 KST 2026-05-18 is the fill target).
