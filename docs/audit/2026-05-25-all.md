# Audit report — 2026-05-25 (all)

**Verdict**: DRIFT_DETECTED

## Summary

All-focus audit against HEAD `f78e283` (2026-05-25).  All six dimensions
checked: (1) architecture vs documentation drift, (2) roadmap freshness,
(3) operator-contract compliance, (4) cost-model accuracy, (5) stale TODOs
/ dead code, (6) security / secrets.  The primary finding is a **persistent
model-assignment drift now entering its 5th consecutive unresolved audit
cycle**: `.claude/agents/planner.md:5` and `.claude/agents/resourcer.md:5`
both carry `model: opus` (set by commit `2778316`, 2026-05-22 ~17:50 KST),
but `docs/for-analysts.md`, `docs/architecture.md`, and `docs/cost-model.md`
still describe them as `sonnet`.  Four commits have landed since the
previous all-audit (`2026-05-24-all.md`, HEAD `7edba96`) — all are
README/site refresh and gitignore work; none resolve the open findings
and none introduce new contract violations.  Security scan is clean: no
secrets, no PII, `.env` not tracked in git, all §8 exception-registry
files retain their `# §8 exception:` markers.  Skill-activation drift
report returns 0 findings.  Roadmap Done gap has grown from 31 to 35
commits since the last Done entry.

## Findings

- **[high]** Model-assignment drift (5th consecutive unresolved cycle) —
  `docs/for-analysts.md:100-101`, `docs/architecture.md:26,30`,
  `docs/cost-model.md:23,60`
  Evidence: `.claude/agents/planner.md:5` = `model: opus`;
  `.claude/agents/resourcer.md:5` = `model: opus` (commit `2778316`,
  2026-05-22 ~17:50 KST).  Three docs still claim sonnet: (a)
  `docs/for-analysts.md:100-101` — table rows `planner → sonnet`,
  `resourcer → sonnet`; (b) `docs/architecture.md:26` — diagram label
  `planner (sonnet)`, line 30 `resourcer (sonnet)`; (c)
  `docs/cost-model.md:23` — tier block `planner / resourcer / editor /
  qa (sonnet)`, line 60 — `¢ — sonnet, small input bounded by file size`.
  Additionally: `docs/for-analysts.md:94-95` claims "unchanged since
  2026-05-15 — last commit touching `.claude/agents/*.md` was `8570a9c`"
  (actual last commit: `2778316`, 2026-05-22); `docs/for-analysts.md:106-107`
  reads "downgrade subagents from opus to sonnet — this is already done"
  (statement is now reversed); `docs/for-analysts.md:109-111` implies
  `resourcer` is at sonnet by suggesting haiku as "a valid future move"
  downgrade.  `README.md` and `README.ko.md` are correct (updated by
  commit `2778316`); the three listed docs were missed by that commit
  and have not been updated in any subsequent commit.
  Suggested fix: Update `docs/for-analysts.md` (table rows 100-101,
  provenance line 94-95, "already done" sentence 106-107, haiku-demotion
  note 109-111), `docs/architecture.md` box labels (lines 26, 30), and
  `docs/cost-model.md` tier diagram (line 23) and subagent-cost row
  (line 60) to reflect planner=opus, resourcer=opus since 2026-05-22.

- **[low]** §5 `Requested-by: user` footer absent — commit `2778316` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" 2778316` shows detailed operator
  rationale ("Operator decision 2026-05-22 ~17:50 KST: keep pair upgrade…")
  but no `Requested-by: user` footer trailer.  Commit is §5-scope, dated
  2026-05-22 (post-convention threshold 2026-05-17).  Carry-forward from
  `2026-05-23-contract.md` through `2026-05-24-all.md`.
  Suggested fix: Add `Requested-by: user` footer in any follow-up §5-scope
  commit that references this decision arc.

- **[low]** §5 `Requested-by: user` footer absent — commit `f714a58` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" f714a58` includes the prose sentence
  "Operator §5 approval granted in-session via AskUserQuestion" — intent
  is clear but no canonical `Requested-by: user` trailer line is present.
  Per §5, the footer is required to "make the approval mechanically
  verifiable in `git log`"; body prose is not parseable by
  `scripts/pre-merge-check.sh`.  Carry-forward from `2026-05-23-contract.md`.
  Suggested fix: Include the literal `Requested-by: user` trailer in
  addition to prose approval citation on future §5-scope commits.

- **[low]** §5 `Requested-by: user` footer absent — commit `fcc5fee` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" fcc5fee` returns only subject line
  "abtest(planner+resourcer): flip to opus for A/B" with no body and no
  `Requested-by: user` trailer.  Commit is §5-scope, dated 2026-05-22.
  Carry-forward from `2026-05-23-contract.md`.
  Suggested fix: Include the canonical `Requested-by: user` footer on
  all §5-scope commits, including interim A/B test flips.

- **[low]** §8 hardcoded machine path in operator guidance —
  `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7`
  Evidence: Line 7 reads `**Where to save downloaded mp3s:**
  \`/Users/melons/ai/assets/music/\`` — a machine-specific absolute path
  in a committed operator-facing file.  Not in the §8 exception registry;
  no `# §8 exception:` comment present.  On a different clone path the
  instruction is wrong.  Carry-forward from `2026-05-23-contract.md`.
  Suggested fix: Replace with `$REPO_ROOT/assets/music/` and note that
  `$REPO_ROOT` is set by `agents/lib/env.sh`.

- **[low]** §8 hardcoded machine path in test-script comment —
  `scripts/test-lyric-extract.sh:10`
  Evidence: Line 10 is `# Uses /Users/melons/ai/assets/music/vocal-dreampop-blue-hours.mp3`
  — a machine-specific absolute path in a comment.  Functional code on
  line 18 correctly uses `$REPO_ROOT`; the comment-only violation has no
  runtime impact but §8 prohibits literal `/Users/...` in committed files
  regardless of comment vs executable context.  Carry-forward from
  `2026-05-23-contract.md`.
  Suggested fix: Replace the comment path with `$REPO_ROOT/assets/music/...`
  to match the functional code on line 18.

- **[low]** Roadmap Done section 35 commits behind HEAD — `docs/roadmap.md`
  Evidence: `git log 4f3e717..HEAD --oneline | wc -l` = 35.  The most
  recent Done entry references commit `4f3e717` (2026-05-22 ~16:04 KST).
  35 commits have landed since then with no corresponding Done entries
  (up from 31 at the 2026-05-24 audit — 4 new commits this cycle added
  to the gap).  Per §9, every commit needs a Done entry.
  Suggested fix: Run `scripts/roadmap-done-sync.sh --apply` to auto-backfill.

- **[low]** 10 test scripts in `scripts/` not documented in user-facing
  docs — carry-forward from `2026-05-23-all.md` and `2026-05-24-all.md`
  Evidence: The following scripts appear in the tracked `scripts/` tree
  but are not mentioned in `docs/for-analysts.md`, `docs/architecture.md`,
  `docs/music-video-pipeline-reference.md`, `README.md`, or `README.ko.md`
  (their only mentions are in prior audit reports themselves, which is
  circular): `test-first-touch.sh`, `test-lyric-extract.sh`,
  `test-music-video-batch.sh`, `test-music-video-trim.sh`,
  `test-music-video-upload-meta.sh`, `test-music-video-validate.sh`,
  `test-qa-anchor.sh`, `test-shader-gates.sh`, `test-shot-plan.sh`,
  `test-thumbnail.sh`.  All are test harnesses for already-documented
  features; functional risk is low.
  Suggested fix: Add a "Test harnesses" subsection in `docs/for-analysts.md`
  that names each test script as a companion regression suite for its
  feature counterpart.

## Drift between docs and code

| Doc location | Doc claim | Code / config reality |
|---|---|---|
| `docs/for-analysts.md:100` | `planner` → `sonnet` | `.claude/agents/planner.md:5` → `model: opus` |
| `docs/for-analysts.md:101` | `resourcer` → `sonnet` | `.claude/agents/resourcer.md:5` → `model: opus` |
| `docs/for-analysts.md:94-95` | "last commit touching `.claude/agents/*.md` was `8570a9c`" | actual last commit = `2778316` (2026-05-22) |
| `docs/for-analysts.md:106-107` | "downgrade to sonnet already done" | planner + resourcer are at opus; statement is reversed |
| `docs/for-analysts.md:109-111` | "valid future move: downgrade `resourcer` to haiku" | resourcer is at opus; framing incorrectly implies a sonnet baseline |
| `docs/architecture.md:26` | `planner (sonnet)` box label | `.claude/agents/planner.md` → `model: opus` |
| `docs/architecture.md:30` | `resourcer (sonnet)` box label | `.claude/agents/resourcer.md` → `model: opus` |
| `docs/cost-model.md:23` | `planner / resourcer / editor / qa (sonnet)` | planner + resourcer = opus since 2026-05-22 |
| `docs/cost-model.md:60` | `¢ — sonnet, small input bounded by file size` | planner + resourcer are opus (higher per-call token cost) |

## Stale items

- **Roadmap Done section** is 35 commits behind HEAD `f78e283`.  The
  auto-reconciliation script (`scripts/roadmap-done-sync.sh`) exists and
  can close the gap idempotently; the gap grew from 24 → 31 → 35 across
  the last three audit cycles without any reconciliation run.

- **Roadmap "Now"** continues to describe an operator-activation step for
  `job-hunt` v0.4.0 (explicitly noted as "operator-only, no agent work
  queued" in the roadmap itself).  All 5 most-recent commits are
  README/site/gitignore work unrelated to this item.  Structurally stale
  for 5+ consecutive audit cycles; not actionable for the agent.

- **`docs/for-analysts.md:94-95` stale provenance claim** still reads
  "unchanged since 2026-05-15 — last commit touching `.claude/agents/*.md`
  was `8570a9c`".  Actual last commit is `2778316` (2026-05-22).  This
  stale claim directly contradicts the [high] model-drift finding and
  will mislead analysts reading the file at face value.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✓ PASS | No user-recipe instructions found in committed files |
| §2 Never pause unless told | ✓ PASS | No pause-seeking patterns in committed docs or scripts |
| §3 Money firewall | ✓ PASS | All five missions route through local tools; highlight/summarize/shorts-batch use `agents/lib/ollama.sh`; music-video uses local aubio; faceless-short defaults to ollama with no direct Anthropic SDK calls; `config/policies.yaml` `budget_usd_ceiling: 5.00` intact |
| §4 Auto-approve mode | ✓ PASS | `config/policies.yaml` intact; local-resource allow-list in `config/claude-settings.template.json` covers declared tools |
| §5 Logic changes require explicit user OK | ⚠ FAIL [low×3] | `fcc5fee` (no body, no marker), `f714a58` (prose approval, no canonical `Requested-by: user` trailer), `2778316` (detailed rationale, no trailer); all three carry forward from `2026-05-23-contract.md` |
| §6 Auto-commit + auto-push / branch strategy | ✓ PASS | `records/` gitignored; rendered plists gitignored; `.env` not tracked; `.claude/settings.json` gitignored; no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | ✓ PASS | No `git commit ... && git push` compound found in any committed script |
| §8 Code/data separation + portability | ⚠ FAIL [low×2] | `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7` — `/Users/melons/ai/assets/music/` hardcoded; `scripts/test-lyric-extract.sh:10` — same in comment.  All §8-registry-listed files retain their `# §8 exception:` comments (12/12 verified via grep). |
| §9 Goal and roadmap as source of truth | ✓ PASS | Structural stale in "Now" explicitly acknowledged in-file; Done lag is a maintenance gap addressable by `roadmap-done-sync.sh` |
| §10 Session resume protocol | ✓ PASS (behavioral) | Not fully verifiable from static code; no committed violation patterns found |
| §11 Shutdown protocol | ✓ PASS (behavioral) | Most recent daily report present in `docs/daily/` |
| §12 No PII or secrets | ✓ PASS | `git grep` secret scan returns only placeholder/comment/variable-name matches; `.env.example` is schema-only with no actual credentials; no real names, emails, or OAuth secrets in committed files |

## Next audit hint

Resolve the model-assignment drift in `docs/for-analysts.md`,
`docs/architecture.md`, and `docs/cost-model.md` — this [high] finding
has now persisted across **five consecutive audits** (22-contract,
23-contract, 23-all, 24-all, 25-all); the doc update is a 5-minute fix
that will return the verdict to CLEAN.
