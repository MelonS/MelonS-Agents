# Audit report — 2026-05-19 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Twenty-second audit (first full-scope "all" run on 2026-05-19); HEAD = `401f9ff` on
branch `feat/skill-music-video` (6 commits ahead of `main` at `a537018`).  All six
dimensions audited: architecture vs documentation drift, roadmap freshness,
operator-contract compliance, cost-model accuracy, stale TODOs / dead code, and
security / secrets.  A contract-focused audit also ran today at HEAD `c9ecb15`
(`docs/audit/2026-05-19-contract.md`); findings from that report are incorporated
and updated for the current HEAD.

Since the prior "all" audit (`2026-05-18-all.md`, HEAD `65a5045`), substantial work
landed: branch strategy codified in `operator-contract.md` §6 (`a2a3807`), pre-merge
gate added (`22a45ea`), GitHub Actions CI added on `main` (`a537018`), and the
`feat/skill-music-video` branch started with 6 structural commits — most notably
Skill #1 music-video (`a993753`), `.claude/settings.json` portability fix
(`912d61c`), `scripts/install-claude-local.sh` (`912d61c`/`40aeab1`), and five
portability principles codified in `operator-contract.md` §8 (`5b1aafb`).  The
[medium] §8 finding from the prior audit (hardcoded `/Users/melons` in tracked
`.claude/settings.json`) is **resolved** by `912d61c` — `.claude/settings.json`
is now rendered from `config/claude-settings.template.json` using `@@HOME@@` /
`@@REPO_ROOT@@` placeholders and is properly gitignored.

Two new mediums: (1) the new `skills/` top-level directory is absent from all three
architecture reference documents; (2) `docs/roadmap.md` "Now" still reads "No active
goal" while `docs/goal.md` has had an active goal since 2026-05-19 ~00:55 KST.
No critical or high findings.  Security, cost model, model assignments, §5 marker
compliance, and `.gitignore` coverage all pass clean.

## Findings

- **[medium]** `skills/` directory undocumented in all three architecture reference docs — `docs/architecture.md`, `docs/for-analysts.md`, `README.md`
  Evidence: `skills/README.md`, `skills/music-video/SKILL.md`, and
  `skills/music-video/scripts/run.sh` are tracked (committed `a993753`, 2026-05-19).
  `docs/architecture.md` Layers table lists `.claude/agents/`, `agents/`, `scripts/`,
  `config/` but has no `skills/` row.  `docs/for-analysts.md` subagent section does
  not mention the new layer.  `README.md` Architecture block and Code/Data separation
  table also omit `skills/`.  Any new reader or analyst following these docs will miss
  the new first-class top-level tracked directory.
  Suggested fix: Add a `skills/` row to the Layers table in `docs/architecture.md`
  (tracked, not mutable in autonomous mode); add a brief Skills section to
  `docs/for-analysts.md`; add `skills/` to the README Code/Data separation table.

- **[medium]** `docs/roadmap.md` "Now" says "No active goal" while `docs/goal.md` has active goal — `docs/roadmap.md:17-18` vs `docs/goal.md:19`
  Evidence: Roadmap "Now" reads "_Last updated 2026-05-18 ~14:10 KST. … No active
  goal — operator sets the next one._"  `docs/goal.md` section "Active goal" shows
  "2026-05-19 | Multi-skill AI assistant framework — Skill #1 (music-video) refactor
  first" promoted on 2026-05-19 ~00:55 KST (`8b39cac`).  A session picking up the
  repo now reads a stale "Now" that contradicts the active goal.
  Suggested fix: Replace the "No active goal" text in roadmap "Now" with a one-line
  pointer to the current active goal and the operative queue item
  (zero-friction onboarding, roadmap Next #1).

- **[low]** `README.md:384` links to `.claude/settings.json` (gitignored, not on GitHub) — `README.md:384`
  Evidence: `README.md` line 384 contains `[.claude/settings.json](.claude/settings.json)`.
  `.gitignore:139` gitignores `.claude/settings.json` since commit `912d61c`; on the
  public GitHub repo the file does not exist and the link returns 404.  The canonical
  tracked equivalent is `config/claude-settings.template.json`.
  Suggested fix: Replace the `README.md:384` link target from `.claude/settings.json`
  to `config/claude-settings.template.json`.

- **[low]** `CLAUDE.md:35` says remote is "(private)" while `docs/operator-contract.md:121` says "(public)" — `CLAUDE.md:35`
  Evidence: `CLAUDE.md` line 35: "Remote: `git@github.com:MelonS/MelonS-Agents.git` (private)."
  `docs/operator-contract.md` line 121: "(public)."  Repo has been public since
  2026-05-18 (first external clone verified that day).  Operator-contract is canonical.
  This finding was also raised in `2026-05-19-contract.md` and remains unresolved.
  Suggested fix: Change `CLAUDE.md:35` from `(private)` to `(public)`.

- **[low]** Three expired "revisit at home ~23:00 KST 2026-05-18" reminders in roadmap "Now" — `docs/roadmap.md:27,29,37`
  Evidence: Line 27 contains "~23:30 KST 2026-05-18"; lines 29 and 37 contain
  "~23:00 KST 2026-05-18" twice.  Today is 2026-05-19; all three reminders are
  20+ hours past their intended review window.
  Suggested fix: Remove or migrate the three expired inline reminders; if the
  underlying topics (format brainstorm at `docs/ideas.md`, problem-solver framing)
  remain actionable, move them to a dated Next entry.

- **[low]** Roadmap "Done" missing entries for five work-bearing commits that landed on `main` since `e1fda78` — `docs/roadmap.md` Done section
  Evidence: Last Done entry referencing `main` commits is `e1fda78` (2026-05-18
  ~14:55 KST).  Since then, five work-bearing commits reached `main` without a Done
  entry: `aa10ba0` (music-video-first onboarding + bootstrap rewrite, 2026-05-18
  ~13:23), `a2a3807` (branch strategy in operator-contract.md §6, 2026-05-18
  ~23:49), `22a45ea` (pre-merge gate script, 2026-05-18 ~23:54), `a537018`
  (GitHub Actions CI, 2026-05-18 ~23:58), `8b39cac` (multi-skill framework active
  goal, 2026-05-19 ~00:16).  Per §9, each work-landing commit should have a
  one-line Done entry.
  Suggested fix: Append five one-line entries to the roadmap Done section, one per
  commit (hash + date + one-line description).

- **[low]** `CURRENT-ALERT.md` references a stale finding that was resolved by `912d61c` — `docs/audit/CURRENT-ALERT.md`
  Evidence: Alert was generated at 2026-05-19 00:06:50 KST pointing at
  `2026-05-18-all.md` whose primary [medium] finding was `.claude/settings.json`
  hardcoded paths.  Commit `912d61c` (2026-05-19 ~00:39 KST) resolved that finding
  by removing `settings.json` from tracking and creating the template.  The alert now
  points at a resolved finding and is stale.
  Suggested fix: Re-run `./scripts/audit-run.sh all` after fixing the two medium
  findings in this report; a CLEAN verdict will auto-remove `CURRENT-ALERT.md`.

- **[low]** §6 structural-change trigger list references `.claude/skills/*` but the actual tracked path is `skills/<name>/` — `docs/operator-contract.md` §6 structural list and `scripts/pre-merge-check.sh:142`
  Evidence: `docs/operator-contract.md` §6 lists ".claude/skills/* create / edit
  (new layer — Skills work)" as a structural-change trigger.  But §8 asset placement
  table specifies `skills/<name>/` (top-level) as the tracked path; this was borne out
  by commit `a993753` which added `skills/` at the top level.  `scripts/pre-merge-check.sh:142`
  checks `'^\.claude/skills/'` — a pattern that will never match a `skills/` change.
  A feat branch containing only `skills/` edits would show `SCOPE="unclassified"`.
  Suggested fix: Replace `.claude/skills/*` with `skills/<name>/` in the §6
  structural list and update `scripts/pre-merge-check.sh:142` to check `'^skills/'`.

- **[low]** §5 scope ("agents/*.md") conflicts with §6 gate #3 text ("agents/") and `pre-merge-check.sh:104` implementation — `docs/operator-contract.md:87-88` vs `docs/operator-contract.md:186` vs `scripts/pre-merge-check.sh:104`
  Evidence: §5 text (line 87): "Editing `agents/*.md` or `.claude/agents/*.md`."
  §6 gate #3 table (line 186): "every commit touching `agents/` or `.claude/agents/*.md`."
  `pre-merge-check.sh:104`: `grep -qE '^(\.claude/agents/.+\.md|agents/)'` — matches
  any file under `agents/`, not just `*.md` files.  An `agents/missions/*/run.sh`
  commit without `Requested-by: user` would fail gate 3 even though §5 text considers
  it out-of-scope.
  Suggested fix: Align §6 gate #3 description and `pre-merge-check.sh:104` to match
  §5 scope (`agents/*.md`, not all of `agents/`), or explicitly expand §5 text to
  cover all `agents/` files — choose one definition and synchronise both.

- **[info, carry-forward]** `agents/lib/copyright.sh:10-12` "Still TODO" items — audio fingerprint + logo detection
  Evidence: Two inline comments mark `probe_license()` extensions as deferred.
  Tracked in `docs/copyright-policy.md` "Still TODO" block with rationale.
  No roadmap link needed until one becomes load-bearing.

- **[info, carry-forward]** `scripts/gen-upload-metadata.sh:105-108` `<TODO: fill manually>` strings
  Evidence: Four JSON fallback fields emit sentinel placeholder text in generated
  output when ollama fails to produce valid JSON.  These are runtime output sentinels,
  not code-maintenance debt.  Operator reviews the generated metadata before
  publishing.  No change needed.

## Drift between docs and code

- `skills/` top-level directory (tracked since `a993753`) not reflected in
  `docs/architecture.md` Layers table, `docs/for-analysts.md`, or `README.md`.
  Documented as [medium] finding above.

- `README.md:384` links to `.claude/settings.json` (now gitignored since `912d61c`);
  the tracked source is `config/claude-settings.template.json`.
  Documented as [low] finding above.

- `docs/roadmap.md` "Now" contradicts `docs/goal.md` active goal (set 2026-05-19
  ~00:55 KST).  Documented as [medium] finding above.

- `CLAUDE.md:35` "(private)" vs `docs/operator-contract.md:121` "(public)".
  Documented as [low] finding above.

- `docs/operator-contract.md` §6 structural list uses `.claude/skills/*`; actual
  tracked path is `skills/<name>/` per §8 asset placement table.
  Documented as [low] finding above.

- Subagent model assignments in `.claude/agents/*.md` frontmatter vs
  `docs/for-analysts.md` table: all six match (orchestrator=opus,
  planner/resourcer/editor/qa/auditor=sonnet). ✅

## Stale items

- `main-backup-pre-filter-20260517-173615` backup branch: roadmap Next #2 schedules
  deletion "on or after 2026-05-18"; today is 2026-05-19.  Deletion window is open;
  branch is dead weight on `origin`.

- Five missing roadmap Done entries for `main` commits since `e1fda78` (documented
  as [low] finding above).

- Three expired "revisit at home" reminders in roadmap "Now" (documented as [low]
  finding above).

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | ✅ | No user-terminal recipes in committed files |
| §2 Never pause | ✅ | No structural violations in docs or agent definitions |
| §3 Money firewall | ✅ | `.env.example` schema-only; Anthropic vars are model names, not keys |
| §4 Auto-approve local resources | ✅ | `config/claude-settings.template.json` allow-list covers all local tools; `.claude/settings.json` gitignored |
| §5 Logic changes need OK | ✅ | No `.claude/agents/*.md` edits since 2026-05-15 (pre-marker; carry-forward exempt) |
| §5 Requested-by marker | ✅ | No §5-scope `.md` changes at all on `feat/skill-music-video` or recent `main` |
| §5/§6 scope mismatch | ⚠️ [low] | §5 text says `agents/*.md`; §6 table and `pre-merge-check.sh:104` say `agents/` (broader) |
| §6 Auto-commit / auto-push | ✅ | All commits pushed to `origin`; `records/` gitignored |
| §6 Branch strategy — structural work on feat branch | ✅ | `a993753` (skills), `912d61c` (settings template), `40aeab1` (bootstrap) are all on `feat/skill-music-video` only; NOT on `main` |
| §6 Branch strategy — structural list outdated | ⚠️ [low] | Lists `.claude/skills/*` but path is `skills/<name>/` per §8 |
| §7 Split commit+push | ✅ | No `&&`-compound git patterns in committed scripts |
| §8 Code/data separation | ✅ | **RESOLVED** by `912d61c`; no output artifacts in `agents/` or `scripts/`; `.env` gitignored; rendered plists gitignored |
| §8 Hardcoded paths | ✅ | Tracked scripts use `$HOME`/`$REPO_ROOT`; documented §8 exception registry entries in `env.sh`, `audit-run.sh`, `music-video-shaders.sh` unchanged |
| §9 Goal+roadmap source of truth | ⚠️ [medium/low] | Roadmap "Now" stale vs active goal; five Done entries missing |
| §10 Session resume | ✅ | Protocol documented in contract and CLAUDE.md |
| §11 Shutdown protocol | ✅ | Daily reports present through 2026-05-18; docs/daily/2026-05-19.md untracked (new) |
| §12 No PII / secrets | ✅ | `git grep` for secrets returns only placeholder comments and variable names; `.env` gitignored; no credentials in tracked files |

## Next audit hint

On the next run, verify that the `feat/skill-music-video` branch has run the full
four-gate pre-merge check (`scripts/pre-merge-check.sh`) and received operator
merge OK before `FF-merging` to `main` — this will be the first complete exercise
of the §6 branch-strategy gate since it was introduced on 2026-05-18.
