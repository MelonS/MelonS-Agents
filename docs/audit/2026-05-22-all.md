# Audit report — 2026-05-22 (all)

**Verdict**: DRIFT_DETECTED

## Summary

41st audit cycle, all-focus.  HEAD `9ac5d22` (2026-05-22, KST morning).  All six dimensions
checked: architecture vs documentation drift, roadmap freshness, operator-contract compliance,
cost-model accuracy, stale TODOs / dead code, and security / secrets.  Seven of the nine
findings carry forward from the 40th contract audit (HEAD `4cd3aaa`); none were resolved
between the two runs.  Two new findings emerge: the roadmap `<!-- suggest -->` block still
lists B.1 Stage-2/3 shaders as future work with time estimates despite commit `7d8e9b1` and
the new Done entry in `9ac5d22` confirming they shipped, and `docs/review-digest.md` sits
in the committed `docs/` tree as an untracked generated output with no `.gitignore` entry,
creating an accidental-commit risk.  The §9 roadmap Done finding from the 40th audit is
partially resolved — `9ac5d22` added Done entries for six commits (`7d8e9b1`, `ea6d5d0`,
`9057d77`, `77535c3`, `7e52ab8`, `a2e2d3f`) — but seven commits (`1a513a6`, `ff7fe5d`,
`5c7c9a0`, `1747d70`, `a701e41`, `4cd3aaa`, `bde9453`) remain without Done entries.
Architecture is CLEAN: all six `.claude/agents/*.md` model assignments match the
`docs/for-analysts.md` table; all components named in `docs/architecture.md` have
corresponding files; `agents/lib/clamp-window.jq` exists.  Cost model is CLEAN: all five
mission `run.sh` scripts route model calls exclusively through `agents/lib/ollama.sh` with
zero direct Anthropic SDK calls; `music-video/run.sh` makes no model calls at all.  §5
CLEAN — zero commits to `.claude/agents/*.md` or `agents/*.md` since `8570a9c`
(2026-05-15); all pre-2026-05-17 are carry-forward exempt.  §7 CLEAN — no `&&`-compound
git calls.  §12 CLEAN — secret scan returns only tokenization code and comment lines;
`.env.example` and `config/copyright-allowlist.yaml` are schema/config-only.

## Findings

- **[medium]** §8 portability violation — hardcoded Claude project dir key (carry-forward, 2nd cycle) — `scripts/generate-intervention-chart.py:72`
  Evidence: Line 71 reads `# "-Users-melons-ai" for cwd /Users/melons/ai` and line 72 reads `CC_PROJECT_DIR = pathlib.Path.home() / ".claude" / "projects" / "-Users-melons-ai"`.  `grep -c "§8 exception" scripts/generate-intervention-chart.py` returns 0; the file is absent from the §8 hardcoded-path exception registry in `docs/operator-contract.md`.  On any clone outside `/Users/melons/ai`, Panel B of the intervention chart silently returns zero session data.  §8 principle 4: "no hardcoded machine-specific values in committed files."  Unfixed since the 40th audit.
  Suggested fix: Replace the literal string with a dynamically derived key, e.g. `("-" + str(pathlib.Path(__file__).resolve().parents[1]).replace("/", "-")).lstrip("-")`, add `# §8 exception:` if any residual literal must remain, and add the file to the §8 registry in `docs/operator-contract.md`.

- **[medium]** §9 roadmap "Now" stale (carry-forward, 3rd cycle) — `docs/roadmap.md` Now section
  Evidence: The Now section header still reads "Last updated 2026-05-20 ~19:55 KST" and the active subgoal is "operator-activation of `job-hunt` v0.4.0 (operator-only, no further scaffolding needed)".  The five most recent commits (`9ac5d22` roadmap Done, `7e52ab8` A.3 QA gate, `bde9453` daily note, `77535c3` C.1 Phase 2 shader gate, `9057d77` broll tune) all relate to music-video quality-bar work — none reference job-hunt activation.  Per auditor dimension 2: "If the most recent 5 commits are about a different topic, the roadmap is stale."  Stale for 3 consecutive audit cycles.
  Suggested fix: Operator updates "Now" to reflect the active music-video quality-bar stream, or adds an explicit note that "Now" is operator-blocked and autonomous work is driven by the `<!-- suggest -->` queue.

- **[medium]** §9 roadmap Done — 7 commits without entries — `docs/roadmap.md` Done section
  Evidence: Commit `9ac5d22` added Done entries for `7d8e9b1`, `ea6d5d0`, `9057d77`, `77535c3`, `7e52ab8`, `a2e2d3f` (partial resolution from 40th audit).  Seven commits still have no Done entries: `1a513a6` (docs/daily: morning brief — quality-bar 5 phases + YT stats Phase 1), `ff7fe5d` (docs/readme: expand operator tooling), `5c7c9a0` (docs/case-studies: add #9), `1747d70` (feat/metrics: intervention chart trend_7d annotations), `a701e41` (docs/daily: note A.2 marker-leak fix), `4cd3aaa` (feat/operator: morning-brief.sh), `bde9453` (docs/daily: note C.1 Phase 2 + A.3 tune in morning brief).  Contract §9: "After work lands, append a one-line entry to roadmap Done with the commit hash and date."
  Suggested fix: Batch-append Done entries for these seven commits; `1a513a6`, `a701e41`, `bde9453` can be grouped as one doc/daily entry; `ff7fe5d`, `5c7c9a0` as one docs entry; `1747d70` and `4cd3aaa` each deserve individual entries.

- **[medium]** Roadmap `<!-- suggest -->` block stale after Stage-2/3 shipped (new) — `docs/roadmap.md:132-133`
  Evidence: Lines 132–133 of the suggest block read "B.1 Stage-2  paper_grain / dust_speck / posterize.  ~2h." and "B.1 Stage-3  trail_echo / soft_bloom.  ~2-3h." — framed as future work with time estimates.  The Done section line 162 (added by `9ac5d22`) confirms these shipped: "`7d8e9b1` shader Stage-2 + Stage-3 = catalog now 23".  The earlier Done entry at line 199 also contains the stale phrase "Stage-2/3 queued in suggest block above."  The suggest block is operator-read for autonomous work selection; leaving shipped items as "to-do with time estimates" may cause redundant re-work on a fresh autonomous session.
  Suggested fix: Remove the Stage-2 and Stage-3 lines from the suggest block and update the line 199 parenthetical to "Stage-2/3 shipped in `7d8e9b1`".

- **[low]** `scripts/install-claude-local.sh` renders global template without `@@REPO_ROOT@@` substitution (carry-forward, 2nd cycle) — `scripts/install-claude-local.sh:159,167,179`
  Evidence: `config/claude-global.template.md:19` contains the placeholder `@@REPO_ROOT@@/config/claude-global.template.md`.  All three write-paths in the global-template rendering block (fresh install `cat "$GLOBAL_TEMPLATE"` at line 159, awk `getline` at line 167, append `cat "$GLOBAL_TEMPLATE"` at line 179) pass the template verbatim without substitution.  The sed substitution at lines 81–86 applies only to `$TEMPLATE` (the JSON settings template).  A fresh `install-claude-local.sh` run will embed the literal `@@REPO_ROOT@@` in `~/.claude/CLAUDE.md`.  The current operator machine is unaffected (install not re-run since the template was changed in `fef5752`).
  Suggested fix: In all three write-paths, pipe through `sed -e "s|@@REPO_ROOT@@|$REPO_ROOT|g"` before writing, mirroring the existing substitution at lines 81–86.

- **[low]** `.playwright-mcp/` runtime directory not in `.gitignore` (carry-forward, 2nd cycle) — `.gitignore`
  Evidence: `ls .playwright-mcp/` shows 7 Playwright MCP runtime artifacts (3 console log files and 4 page-dump YAML files dated 2026-05-21T17:29–17:32).  `grep "playwright" .gitignore` returns no output.  `git status --short` shows `?? .playwright-mcp/` — untracked and ungitignored.  Risk of accidental inclusion in a future `git add -A` or reviewer confusion.
  Suggested fix: Add `.playwright-mcp/` to `.gitignore`.

- **[low]** Hardcoded machine path in committed evidence log (carry-forward, 2nd cycle) — `docs/onboarding/demo-mode-log.txt:1`
  Evidence: Line 1 reads `2026-05-19 01:25:59 +0900  PASS  remote=/Users/melons/ai  short=repo/records/…` — the local checkout path `/Users/melons/ai` is embedded.  Lines 2–4 correctly use the public `https://github.com/MelonS/MelonS-Agents.git` URL.  §8 principle 4 prohibits machine-specific values in committed files.  Severity is low: username `melons` matches the public GitHub handle, disclosing no additional identity surface.
  Suggested fix: In a cleanup commit, redact line 1's `remote=` value to `remote=<local-clone-path>`.

- **[low]** `scripts/morning-brief.sh` not documented in `docs/for-analysts.md` or `README.md` (carry-forward, 2nd cycle) — `scripts/morning-brief.sh`
  Evidence: `grep -c "morning-brief" docs/for-analysts.md README.md README.ko.md` returns 0 for all three files.  Hits in other `docs/` files (audit reports, `docs/autonomous-decisions.md`, `docs/metrics/intervention.json`) record its existence but are not inventory documentation.  Per auditor dimension 1: "For every file under `scripts/`, confirm it's mentioned somewhere in `docs/` or `README.md`."  Companion scripts `scripts/log-decision.sh` and `scripts/review-queue-digest.sh` are already listed in `docs/for-analysts.md`.
  Suggested fix: Add a one-line bullet for `scripts/morning-brief.sh` to `docs/for-analysts.md` after the `log-decision.sh` entry (line 180), e.g. "wraps doctor + audit + intervention + decisions + review queue into a single operator-facing overnight digest."

- **[low]** `docs/review-digest.md` untracked generated output not in `.gitignore` (new) — `.gitignore`
  Evidence: `scripts/review-queue-digest.sh:21` writes to `$REPO_ROOT/docs/review-digest.md` on every run.  `git status --short` shows `?? docs/review-digest.md`.  `git check-ignore -v docs/review-digest.md` returns nothing — the file is not gitignored.  Current content is a generated markdown contact-sheet ("Queue is empty. No pending artifacts awaiting review.").  The architecture table in `docs/architecture.md` documents only `outputs/review-queue/README.md` and `.gitkeep` as tracked; the digest file is an output artifact.  A future `git add docs/` or `git add -A` would accidentally commit it.
  Suggested fix: Add `docs/review-digest.md` to `.gitignore`.

## Drift between docs and code

- `docs/roadmap.md:132-133` `<!-- suggest -->` block lists B.1 Stage-2/3 shaders as future work (~2h, ~2-3h) while the Done section (line 162, added by `9ac5d22`) and commit `7d8e9b1` confirm they shipped.
- `docs/roadmap.md` Now section (last updated 2026-05-20) describes job-hunt activation as the active subgoal; all five most recent commits are music-video quality-bar work.
- `scripts/generate-intervention-chart.py:72` hardcodes `-Users-melons-ai` without a `§8 exception` comment; the §8 exception registry in `docs/operator-contract.md` does not list this file.
- `scripts/install-claude-local.sh` rendering of `config/claude-global.template.md` omits `@@REPO_ROOT@@` substitution, creating a divergence between the committed template and what a fresh `install-claude-local.sh` run produces in `~/.claude/CLAUDE.md`.

## Stale items

- `docs/roadmap.md` Now: last updated 2026-05-20; 12+ commits of autonomous work have landed on music-video quality-bar and infrastructure topics with no Now update.
- `docs/roadmap.md` Done: 7 commits (`1a513a6`, `ff7fe5d`, `5c7c9a0`, `1747d70`, `a701e41`, `4cd3aaa`, `bde9453`) unrecorded; all are post-`87920d6` (2026-05-22 ~02:51 KST).
- `docs/roadmap.md:132-133` suggest block: Stage-2 and Stage-3 shader entries should be struck through or removed — they shipped in `7d8e9b1`.
- `docs/roadmap.md:199` parenthetical "Stage-2/3 queued in suggest block above" — stale; should read "shipped in `7d8e9b1`".

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✅ CLEAN | No user-directed terminal recipe in committed scripts |
| §2 Never pause | ✅ CLEAN (behavioral) | Not code-checkable |
| §3 Money firewall | ✅ CLEAN | `config/policies.yaml` present; `.env.example` schema-only; Pexels/YT keys env-var gated |
| §4 Auto-approve | ✅ CLEAN | `.claude/settings.json` gitignored; template at `config/claude-settings.template.json` |
| §5 Logic changes need OK | ✅ CLEAN | Zero commits to `.claude/agents/*.md` or `agents/*.md` since `8570a9c` (2026-05-15); all pre-2026-05-17 carry-forward exempt |
| §6 Git workflow | ✅ CLEAN | `records/` gitignored at `.gitignore:4`; `.env` gitignored at `.gitignore:69`; plist templates use `@@REPO_ROOT@@`/`@@HOME@@`; no output artifacts under `agents/` or `scripts/` |
| §7 Split commit + push | ✅ CLEAN | No `&&`-compound `git commit … && git push` in tracked scripts |
| §8 Code/data separation | ⚠ DRIFT | `scripts/generate-intervention-chart.py:72` hardcodes `-Users-melons-ai` — §8 principle 4 violation, missing from exception registry (medium, 2nd cycle); `scripts/install-claude-local.sh` does not substitute `@@REPO_ROOT@@` in global template rendering (low, 2nd cycle); `docs/onboarding/demo-mode-log.txt:1` hardcoded machine path (low, 2nd cycle).  All 11 §8 registry files confirmed ≥ 1 `# §8 exception:` comment via `grep -n "§8 exception" <file>`. |
| §9 Goal + roadmap | ⚠ DRIFT | "Now" stale since 2026-05-20 (medium, 3rd cycle); 7 commits without Done entries (medium, partially resolved from 40th audit); `CURRENT-ALERT.md` persists from 40th audit |
| §10 Session resume | ✅ CLEAN (behavioral) | `docs/daily/` trail intact; `docs/daily/2026-05-22-morning-brief.md` present |
| §11 Shutdown protocol | ✅ CLEAN (behavioral) | Not code-checkable from audit |
| §12 No PII / secrets | ✅ CLEAN | Secret scan returns only tokenization code, CI annotation strings, comment lines; `.env.example` schema-only; no Anthropic SDK direct calls |
| Conventions — doc style | ✅ CLEAN | README EN/KO split; center-aligned headers; for-the-badge badges; role-emoji architecture table intact |
| Conventions — README cadence | ✅ CLEAN | Recent README touches (`ff7fe5d`) are legitimate cadence triggers (contract/architecture change); EN+KO updated in same commit |

## Next audit hint

On the next cycle verify whether `scripts/generate-intervention-chart.py:72` has been patched with a dynamic project-dir key derivation (the medium §8 carry-forward now in its 2nd consecutive cycle), and check whether `.playwright-mcp/` has been added to `.gitignore` — those two fixes together would reduce the finding count from 9 to 7 and likely allow downgrade of the overall verdict if the roadmap Now is also updated by the operator.
