# Audit report — 2026-07-05 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Contract-focus audit of `docs/operator-contract.md` compliance at HEAD
`8194d51` (2026-07-05 17:37 KST), run about five hours after the
previous contract-focus report (`2026-07-05-contract.md`, generated
12:39:20, itself the second consecutive DRIFT_DETECTED verdict after
`2026-07-03-contract.md`). This is now the third consecutive
contract-focus audit cycle in roughly 2.5 days, and the pattern is
unambiguous: not one suggested fix from either prior report has
landed. Every persistent medium/low finding below reproduces
unchanged, several have grown worse (the roadmap Done-citation gap
widened from 5 to 14 to 16 uncited commits across the three cycles;
the subagent roster count grew from 24 to 26 live files while both
docs describing it stayed frozen at 22/23), and the most severe
finding is a live recurrence of a race condition first raised as a
hypothetical (info, 07-03), then observed once as fact (high, 07-05
12:39), and now directly re-observed a second time in real time while
this exact report was being compiled: the L1 post-commit hook's
audit-run.sh contract process for this session's own HEAD commit was
still running (bash PID 50731/50733, claude CLI child PID 50842, all
launched 17:37:48 to 17:37:51) and targeting this same output path
for the entire multi-minute duration of this audit's data-gathering
pass, with no lock-check on either side. Section 12 (no PII/secrets)
remains clean: .env stays untracked, .env.example and
config/copyright-allowlist.yaml contain only placeholders, and a
direct git grep for key/secret/token/password patterns outside docs/
and *.example returned only variable names and comments. Section 3
(money firewall) and Section 8's core no-committed-artifacts check
both remain clean.

## Findings

- **[high]** Live write-race on this exact report's output path, directly re-observed a second time - `records/audit/hook-trigger.log`, `records/audit/.hook.inflight`, `records/audit/hook-run-20260705-173748-8194d51.log`
  Evidence: `records/audit/hook-trigger.log` shows `[2026-07-05
  17:37:48] 8194d51 -> audit-run.sh contract (drift-risk paths
  changed)` with no corresponding "done" line yet at the time this
  report was compiled. The log at
  `records/audit/hook-run-20260705-173748-8194d51.log` contains only
  `writing to: /g/ai/MelonS-Agents/docs/audit/2026-07-05-contract.md`
  - the identical path this report occupies.
  `records/audit/.hook.inflight` held PID `50731` throughout this
  audit's compilation window (checked repeatedly between 17:39:06 and
  17:41:39), and `ps -ef` confirmed `50731`, its child `50733`, and the
  `claude` CLI grandchild `50842` were all still live processes for the
  full duration. This is the exact scenario flagged as an info
  hypothetical on 07-03 and observed as fact (high) on 07-05 12:39 -
  the suggested fix (have audit-run.sh check .hook.inflight before
  writing) still has not landed, and the race has now recurred a
  second confirmed time on the same day. Whichever of the two writers
  (this session vs. the concurrent hook-triggered one) finishes last
  silently wins; there is no reconciliation and no detectable conflict
  on either side.
  Suggested fix: unchanged from the last two reports - have
  `scripts/audit-run.sh` check `records/audit/.hook.inflight` (or a
  per-focus/per-date lock file) before writing and defer/queue instead
  of racing. Given this is now the second directly-observed occurrence
  with zero mitigation after two prior write-ups, treat this as the
  top priority fix - a third recurrence without the lock in place
  should be escalated to critical, since it threatens the integrity of
  the audit trail itself (`docs/audit/` is committed specifically so
  it survives a machine swap; a silently-clobbered report undermines
  that guarantee).

- **[high]** `skills/job-hunt/scripts/status.sh` still crashes on this machine - unresolved for a third consecutive audit cycle - `skills/job-hunt/scripts/status.sh:55-61`, `skills/job-hunt/config/activation.tsv`, `.gitattributes`
  Evidence: `bash skills/job-hunt/scripts/status.sh` (run directly,
  exit code captured explicitly rather than through a pipe) exits `3`
  with `[status] malformed manifest row: ''`, identical to both prior
  reports (07-03, 07-05 12:39). `grep -n tsv .gitattributes` still
  returns nothing - no `*.tsv text eol=lf` rule has been added.
  `git config core.autocrlf` is still `true` on this machine, and
  `git show HEAD:skills/job-hunt/config/activation.tsv | grep -cU $'\r'`
  confirms the committed blob is pure LF (`0`) while the checked-out
  working-tree file is CRLF (`cat -A` shows `^M$` on every line). This
  is the same live Section 8 portability violation reported twice
  before, still unresolved on the machine `docs/roadmap.md` names as
  primary production.
  Suggested fix: unchanged - add `skills/*/config/*.tsv text eol=lf`
  to `.gitattributes` and harden `status.sh` / `audit-skill-drift.sh`'s
  manifest read loops to strip a trailing carriage-return and treat
  whitespace-only `$flag` as blank.

- **[high]** L3 daily scheduled audit remains silently broken - same failure snapshot as the 12:39 report, no new run to re-test - `records/audit-scheduler.log`, Windows Task Scheduler
  Evidence: `docs/audit/` still has no `2026-07-04-all.md` or
  `2026-07-05-all.md` despite `records/audit-scheduler.log` showing the
  task fired and attempted to write both (`[03:00:03] writing to:
  .../2026-07-04-all.md` and `.../2026-07-05-all.md`).
  `Get-ScheduledTaskInfo -TaskName MelonS-auditor-daily` (via
  PowerShell, since `schtasks /Query` mis-parses under this Git Bash's
  path-conversion) reports `LastRunTime = 2026-07-05 03:00:01`,
  `LastTaskResult = 3221225786` (0xC000013A, STATUS_CONTROL_C_EXIT),
  `NextRunTime = 2026-07-06 03:00:00`, `NumberOfMissedRuns = 0` -
  identical to the 12:39 report's snapshot, because no new scheduled
  run has occurred between then and now (next fire isn't until
  tomorrow 03:00). This is not a new occurrence but confirms the
  failure has not self-healed and remains the only documented "catch
  anything L1+L2 missed" mechanism producing zero output.
  Suggested fix: unchanged - manually run `schtasks /run /tn
  MelonS-auditor-daily` (or the PowerShell equivalent
  `Start-ScheduledTask`) and watch the child `claude` process in real
  time to find what sends it STATUS_CONTROL_C_EXIT; add a dead-man's-
  switch doctor.sh check for `docs/audit/*-all.md` mtime staleness in
  the meantime.

- **[medium]** Precomputed skill-drift finding is a reproducible false positive, same CRLF root cause, unfixed for a third cycle - `scripts/audit-skill-drift.sh:78-91`
  Evidence: the supplied precomputed report line (`medium job-hunt
  manifest references missing script`) reproduces identically
  (`bash scripts/audit-skill-drift.sh` returns 1 finding, empty
  subject/description columns). All 16 flag-to-script pairs declared in
  `skills/job-hunt/config/activation.tsv` resolve to real files on
  disk. Root cause (unchanged from the last two reports): two blank
  CRLF separator lines in the manifest feed a lone carriage-return into
  `$flag` under the script's `read -r` loop, which then fails the
  blank/comment checks and produces a spurious "missing script" finding
  against an empty path. job-hunt's actual activation manifest has zero
  real drift.
  Suggested fix: same as the `status.sh` finding above - the
  `.gitattributes` fix resolves both simultaneously; re-run
  `scripts/audit-skill-drift.sh` afterward to confirm 0 findings.

- **[medium]** `docs/roadmap.md` Done-section citation gap keeps growing every audit cycle - now 16 commits, up from 14 five hours ago and 5 two days ago - `docs/roadmap.md:220-230`
  Evidence: the last Done entry citing a real hash references
  `2a94adf` (2026-07-03 05:20 KST). `git log --oneline 2a94adf..HEAD`
  now lists 16 commits, none of which appear anywhere in
  `docs/roadmap.md` (checked individually via `grep <hash>
  docs/roadmap.md` for each): `06d4497`, `9559d1c`, `592bfc7`,
  `c64efd2`, `f6e38d6`, `85332e7`, `8c9a763`, `7b83d8b`, `a06061c`,
  `48f40cb`, `d9f2810`, `2c9708a`, `253da2d`, `a6e8c0c`, `444ee5f`,
  `8194d51` - spanning 2026-07-03 05:24 KST through 2026-07-05 17:37
  KST. This includes the entire Wan 2.2 video-generation tooling line
  (TI2V/I2V/FLF2V CLIs, FLUX still generator, ESRGAN upscaler), the
  YouTube Analytics stats sampler, and the newest note-judge/still-judge
  pipeline stages - none logged in the work queue's own record of
  itself. Three consecutive audit cycles have flagged this with an
  identical suggested fix and the gap has widened every single time
  (5, then 14, then 16), meaning `scripts/roadmap-done-sync.sh` itself
  is not being re-run despite existing for exactly this purpose.
  Suggested fix: run `scripts/roadmap-done-sync.sh --apply` covering
  `2a94adf..HEAD`; given three flagged-and-ignored cycles, consider
  wiring it to fire automatically post-commit alongside the audit hook
  rather than relying on a manual re-run that keeps not happening.

- **[medium]** Subagent roster count three-way mismatch has widened further - `docs/for-analysts.md:93-96`, `README.md:84`
  Evidence: `docs/for-analysts.md:94` still reads "22 definitions
  total" (an arithmetic error independent of staleness: its own text
  says 6+12+5, which is 23, not 22). `README.md:84` still says
  ".claude/agents/ holds 23 definitions (6 core + 12 game roster + 5
  content-pipeline teams)". `ls .claude/agents/*.md | wc -l` now
  returns 26 at current HEAD - up from 24 at the 07-05 12:39 check and
  unchanged-24 at 07-03 - because `8194d51` (this session's HEAD) added
  `note-judge.md` and `still-judge.md` on top of the already
  undocumented `cut-judge.md` from 07-03. All three video-judge agents
  (cut-judge, note-judge, still-judge - 3 files) remain unaccounted for
  in either doc's category breakdown, and the three-way count
  disagreement (22 / 23 / 26) has now persisted and widened across
  three straight audit cycles with zero edits to either document.
  Suggested fix: recompute from `ls .claude/agents/*.md` and update
  both docs in one commit, adding a "video judge" category (cut-judge +
  note-judge + still-judge = 3); consider a doctor.sh assertion that
  fails when the live count diverges from the documented one so this
  stops re-drifting on every new subagent file.

- **[low]** Section 5 audit-trail marker (`Requested-by: user`) usage remains at zero, now 10 qualifying commits - `.claude/agents/*.md` commit history
  Evidence: `git log --format=%H %ad %s --date=short -- .claude/agents/`
  lists 10 commits dated 2026-05-17 or later without the marker (the 9
  previously identified plus this session's `8194d51`): `8194d51`,
  `9559d1c`, `9425a3e`, `9ef5d1f`, `ff7fa19`, `2b44b47`, `32ec30f`,
  `2778316`, `f714a58`, `fcc5fee`. `8194d51`'s commit body ("Operator-
  commissioned verification stages: ... Fail cheap at 9s/still, not at
  7min/video - the one-pass imugi run showed exactly which cuts slip
  through without these gates.") narrates operator-sourced reasoning
  informally but carries no formal `Requested-by: user` trailer, the
  same pattern the 07-03 report noted for several siblings. Contract
  pre-classifies a missing marker as low, not a hard failure, and the
  mechanism is proven to work elsewhere (job-hunt commits use it
  correctly) - it simply has never been applied to an agent-definition
  commit across the project's entire history.
  Suggested fix: add the trailer starting with the next Section-5-scope
  commit; backfilling history remains explicitly optional per the
  contract's own carve-out.

- **[low]** Section 8 hardcoded-path exception registry incomplete for the same 8 scripts, unchanged for a third cycle - `operator-contract.md` Section 8 registry
  Evidence: `scripts/audio-analyze.sh`, `scripts/music-video-grade.sh`,
  `scripts/music-video-thumbnail.sh`, `scripts/music-video-trim.sh`,
  `scripts/music-video-upload-meta.sh`, `scripts/music-video-validate.sh`,
  `scripts/pollinations-gen.sh`, and `scripts/video-audio-pad.sh` all
  still return 0 hits for `grep -ci "section 8 exception" <file>` and
  none are named in the registry. Same env-var-gated
  `${FFMPEG_BIN:-/opt/homebrew/bin/ffmpeg}` fallback pattern as the 12
  already-registered files, so severity stays low (correct behavior
  whenever `$FFMPEG_BIN` is set).
  Suggested fix: add a "section 8 exception" comment to each of the 8
  files and append them to the registry list in
  `operator-contract.md` Section 8.

- **[info]** `docs/architecture.md` "Autonomous flow" section still describes launchd-only scheduling, unchanged for a third cycle - `docs/architecture.md:54,308,314,320`
  Evidence: lines 54 ("L3 (scheduled) - daily 03:00 baseline via
  launchd"), 308, 314, and 320 all still describe the mission-queue job
  and the auditor L3 baseline purely via launchd, while
  `docs/for-analysts.md`'s subagent table already documents the actual
  macOS/Windows split correctly. The live Windows scheduler on this
  machine registers only `MelonS-auditor-daily` - no Windows port of
  the 30-minute mission-queue job exists. Kept at info rather than
  medium this cycle since it is now well-established, unchanged,
  low-harm doc drift rather than new information; still worth fixing.
  Suggested fix: mirror `for-analysts.md`'s macOS/Windows split into
  `architecture.md`'s autonomous-flow section; note the mission-queue
  job has no Windows port yet, or add one.

## Drift between docs and code

- `docs/architecture.md` "Autonomous flow" (launchd-only) vs. the actual
  Windows Task Scheduler split already documented correctly in
  `for-analysts.md` - unchanged, 3rd cycle stale.
- `docs/for-analysts.md` (22) vs. `README.md` (23) vs. live
  `.claude/agents/*.md` count (26) - widened this cycle (24 to 26); doc
  side has not moved across 3 cycles.
- `skills/job-hunt/config/activation.tsv` (CRLF at checkout) vs. its two
  consumers (`scripts/audit-skill-drift.sh`, `status.sh`) which both
  assume LF - unchanged, 3rd cycle stale; one still crashes, the other
  still produces a bogus finding.
- `docs/audit/README.md`'s description of L3 as "the always-on
  baseline" vs. its actual state: silently non-functional, no new
  evidence this cycle (next scheduled fire is tomorrow 03:00).
- `docs/audit/CURRENT-ALERT.md` still points at the 07-05 12:39:20
  report and has not been regenerated to reflect this cycle's findings
  - expected, since regeneration depends on a fresh full
  `scripts/audit-run.sh` pass completing, and the concurrent
  hook-triggered contract-focus run was still in flight throughout this
  audit's compilation window.

## Stale items

- `docs/roadmap.md` Done section - 16 uncited commits, up from 14 five
  hours ago and 5 two days ago; the gap has widened at every single
  check.
- Section 8 exception registry - 8 scripts, unchanged for at least 3
  audit cycles.
- Section 5 marker - 10 qualifying commits now (was 9), zero marker
  usage across the entire project history for this commit class.
- Subagent roster count docs (`for-analysts.md`, `README.md`) - 3
  cycles stale against a live count that keeps growing out from under
  them (24 to 26 this cycle alone).
- `docs/architecture.md` autonomous-flow section - 3 cycles stale
  against the documented (in a sibling file) Windows scheduler split.
- The audit-write race-condition fix - flagged 3 cycles running (info,
  then high, then high again), zero implementation.

## Compliance with operator-contract.md

- Section 1 Agent does all the work - no findings from static repo
  state.
- Section 2 Never pause unless told - not assessable from static repo
  state (behavioral).
- Section 3 Money firewall - no violations; `config/policies.yaml`'s
  `budget_usd_ceiling: 5.00` matches `.env.example`'s
  `AUTONOMY_BUDGET_USD=5.00`; no Anthropic SDK/API references found in
  any `agents/missions/*/run.sh`.
- Section 4 Auto-approve mode - no findings.
- Section 5 Logic changes require explicit user OK - persistent,
  slightly worsened violation pattern: 10 commits since 2026-05-17
  (was 9) touch `.claude/agents/*.md` without the `Requested-by: user`
  trailer; contract pre-classifies this as low.
- Section 6 Git workflow - `git ls-files records/` returns only
  `.gitkeep`; no compound `commit && push` patterns found in recent
  history; `.gitignore` still excludes `records/`, media extensions,
  secrets, and per-operator personal files as documented, with no
  evidence of over- or under-exclusion.
- Section 7 Split commit and push - not assessable from static repo
  state.
- Section 8 Code/data separation + portability - one confirmed live
  violation (`skills/job-hunt/scripts/status.sh` crash, high finding,
  unresolved 3 cycles), one confirmed-clean check (no output artifacts
  committed under `agents/`/`scripts/`), one persistent low (exception
  registry gap, unchanged).
- Section 9 Goal and roadmap as source of truth - Done-section
  citation gap has grown to 16 commits (medium finding, worsening every
  cycle); "Now" section staleness is operator-owned per the contract's
  own maintenance note and is roadmap-focus territory, not re-litigated
  here beyond confirming Claude's own Done-appending obligation is the
  part currently lapsing.
- Section 10 / 11 Session-resume and shutdown protocol - not
  assessable from static repo state.
- Section 12 No PII or secrets - clean; `.env` gitignored and
  untracked; `.env.example` and `config/copyright-allowlist.yaml`
  contain only placeholders; `git grep` for key/secret/token/password
  patterns outside `docs/` and `*.example` returned only variable
  names, comments, and config-key references - no literal secret
  values.

## Next audit hint

Top priority for the next cycle: check whether the write-race fix has
landed in `scripts/audit-run.sh` - this is now the second directly
observed live occurrence in one day with an unimplemented,
twice-suggested fix, and a third recurrence should be treated as
critical given it threatens the audit trail's own integrity guarantee.
Second priority: confirm whether `scripts/roadmap-done-sync.sh --apply`
has been re-run - the Done-citation gap has widened at every one of
three consecutive checks despite an existing, purpose-built tool for
exactly this reconciliation.
