# Audit report — 2026-07-10 (all)

**Verdict**: CRITICAL

## Summary

Full-scope audit at HEAD `8b5268d` (2026-07-09 12:23:31 +0900). All six
dimensions were walked (architecture/doc drift, roadmap freshness,
operator-contract compliance, cost-model accuracy, stale TODOs/dead code,
secrets/security), cross-checked against the prior five cycles
(2026-07-05-contract through 2026-07-09-all) to distinguish reproduced
findings from new ones. The precomputed skill-drift report (1 medium
finding, job-hunt) was re-derived to the same root cause identified in
every prior cycle: CRLF contamination in `skills/job-hunt/config/activation.tsv`
(confirmed directly this cycle -- `file` reports "CRLF line terminators",
`.gitattributes` has no `.tsv` rule), not a genuinely missing script.

Headline result: the audit-trail-durability finding carried critical for
five-plus consecutive cycles reproduces live for a sixth. At session start
`git status --porcelain` shows `M docs/audit/CURRENT-ALERT.md` plus three
untracked reports -- `docs/audit/2026-07-08-all.md`,
`docs/audit/2026-07-08-contract.md`, and, new to this cycle,
`docs/audit/2026-07-09-all.md` itself (confirmed via `git log -- <path>`
returning zero commits for all three, and `CURRENT-ALERT.md`'s last commit
still `52fda03`, 2026-07-06). `scripts/audit-run.sh:120` is still exactly
the advisory string `log_info "review then git-add-and-commit to preserve"`
-- never executed. Every finding from the 2026-07-09 report was
independently re-verified this cycle and reproduces unchanged (subagent-
roster count, `refactor_check.py` and three YT-analytics scripts' hardcoded
paths, the roadmap Done-gap -- now 31 commits and still widening -- the
`cost-model.md` self-contradiction, the job-hunt CRLF gap, Section-5
marker non-adoption, the `goal.md` orphaned-goal silence, `for-analysts.md`'s
stale line citation, `.env.example`'s missing ElevenLabs/Typecast/
YT_CONFIG_DIR entries, the untracked-`.claude/wb/` gap, and the "0 mission
cost" claim omitting ElevenLabs/Typecast). One commit landed since the
07-09 report (`8b5268d`, docs-only, `docs/generative-shorts-pipeline.md`
+10 lines) and introduces no new findings and no Section-5-scope change.
No `docs/daily/` report has landed since 2026-05-27 (44 days). Given the
finding set is now stable and fully reproduced for a sixth cycle with zero
remediation despite four-plus identical prior suggestions, this report
keeps the findings compact and focuses evidence on what changed (or
conspicuously did not) since 07-09.

## Findings

- **[critical]** Audit-trail durability gap reproduces live for a sixth
  consecutive cycle -- `scripts/audit-run.sh:120`,
  `docs/audit/CURRENT-ALERT.md`, `docs/audit/2026-07-08-all.md`,
  `docs/audit/2026-07-08-contract.md`, `docs/audit/2026-07-09-all.md`.
  Evidence: `git status --porcelain` at session start: `M
  docs/audit/CURRENT-ALERT.md`, `?? docs/audit/2026-07-08-all.md`,
  `?? docs/audit/2026-07-08-contract.md`, `?? docs/audit/2026-07-09-all.md`.
  `git log -- docs/audit/2026-07-09-all.md` returns no commits at all --
  the 07-09 audit's own deliverable was never committed, so the cycle that
  diagnosed the durability gap became another instance of it.
  `CURRENT-ALERT.md`'s last commit remains `52fda03` (2026-07-06); its
  current on-disk content (pointing at 2026-07-09-all.md, verdict CRITICAL)
  has never been committed either. `grep -n "git commit"
  scripts/audit-run.sh` still returns only line 120, an advisory
  `log_info` string inside a message, never an executed command. This is
  the identical root cause rated critical in 2026-07-05, 2026-07-06, both
  2026-07-08 cycles, and 2026-07-09 -- five prior cycles, same suggested
  fix, zero remediation.
  Suggested fix: add an unconditional `git add docs/audit/ && git commit
  && git push` step at the end of `scripts/audit-run.sh` (docs/audit/ is
  always safe to auto-commit per the Section 6 scope list) so the
  auditor's own output stops being the thing that goes uncommitted.

- **[high]** `docs/roadmap.md` Done-section citation gap continues to
  widen, now 31 commits behind HEAD -- `docs/roadmap.md:229`.
  Evidence: the last Done entry citing a real hash still references
  `2a94adf` (dated 2026-07-03). `git log --oneline 2a94adf..HEAD` returns
  31 commits with zero Done entries -- up from 30 at the 07-09 cycle, 27
  before that, 25, 21 -- the gap has grown on every measurement across six
  consecutive audit cycles. `scripts/roadmap-done-sync.sh` exists
  specifically for this and has not been run since the 2026-05-22
  bulk-backfill batches.
  Suggested fix: run `scripts/roadmap-done-sync.sh --apply` covering
  `2a94adf..HEAD`; given six-plus consecutive cycles of zero remediation,
  wire it to run automatically post-commit rather than relying on manual
  invocation.

- **[high]** `docs/goal.md` active goal remains silently orphaned, now 28
  days, still with no self-correcting suggest-comment in the file itself
  -- `docs/goal.md:19`.
  Evidence: Active goal (line 19, dated 2026-06-12, PawnSim colony-sim
  prototype) has had its last related commit at `03056e9` (2026-06-15
  animal-animation parity fix) -- nothing since. `grep -n suggest
  docs/goal.md` returns zero hits, unchanged from the last three cycles
  (then 23, then 24 days). Meanwhile real production activity since has
  moved to the generative-shorts / content-shorts line (ElevenLabs
  adoption, Wan2.2 video pipeline, YouTube analytics, shorts-production
  handoff doc) -- none of which `goal.md` or its absent suggest-comment
  acknowledges. The only acknowledgment anywhere is a `docs/roadmap.md`
  Next-section suggest-comment dated 2026-07-03, now a full week stale
  relative to everything shipped since.
  Suggested fix: append a suggest-comment to `docs/goal.md` itself (not
  just roadmap.md) proposing either migrating PawnSim to Past goals with
  an honest paused-not-achieved status, or promoting generative-shorts
  production to Active goal; let the operator confirm at next session
  start.

- **[high]** Subagent roster count is wrong in two independent docs,
  unchanged across at least five cycles -- `docs/for-analysts.md:93-98`,
  `README.md:29`.
  Evidence: `.claude/agents/*.md` currently contains 26 files (counted
  directly this cycle). `docs/for-analysts.md:94` still claims "22
  definitions total" -- its own stated breakdown (6 core + 12 game-* + 5
  content-shorts = 23) doesn't even match its own headline number, and
  neither matches the live 26 (the breakdown omits the three judge agents
  `cut-judge.md` / `note-judge.md` / `still-judge.md` entirely).
  `README.md:29`'s hero-stats image alt text independently claims "23
  subagents". Three different numbers (22, 23, 26) for one countable fact.
  Suggested fix: update `docs/for-analysts.md`'s table and prose to 26
  with an explicit judge-agents row, and regenerate
  `docs/visuals/01-hero-stats.png` + its README alt text to match.

- **[high]** `skills/game-dev-agent/scripts/refactor_check.py` still
  hardcodes machine-specific absolute paths, including the operator's
  Windows account name, with zero env-var fallback -- unchanged since
  first flagged 2026-07-06 -- lines 27-31, 182, 247, 308.
  Evidence: `BASELINE`, `PLAYER_LOG`, `SCENE_LOG`, `BUILD_LOG` are literal
  `Path(...)` assignments with no `os.environ.get` anywhere in the file
  (confirmed by direct read this cycle). `PLAYER_LOG` embeds
  `C:/Users/comdo/AppData/LocalLow/...` -- a personal account name in a
  tracked public-repo file. No section-8-exception comment present
  (confirmed via grep on the file), and it is absent from the
  `docs/operator-contract.md` Section 8 registry.
  Suggested fix: introduce env-var overrides (e.g. `REFACTOR_BASELINE_DIR`,
  `GAME_PLAYER_LOG`) with current values as fallback defaults, replace the
  literal account name with a `$USERNAME`/`%USERNAME%` resolution, and
  either add a section-8-exception comment plus registry entry or remove
  the hardcoding.

- **[high]** Three YouTube-analytics scripts hardcode Windows-drive-letter
  config paths with zero env-var fallback, unchanged since first flagged
  2026-07-09 -- `scripts/yt-analytics-query.py:7`,
  `scripts/yt-scoreboard.py:6`, `scripts/yt-stats-sample.sh:6-7`.
  Evidence: `yt-analytics-query.py:7` and `yt-scoreboard.py:6` both read
  `CONF = Path("G:/config/youtubeuploader")` unconditionally.
  `yt-stats-sample.sh:6-7` hardcodes two paths under
  `/g/config/youtubeuploader/` plus a hardcoded `jq` path
  (`/g/tools/jq/jq`), none with an environment-variable check. None
  carries a section-8-exception comment or registry entry. Sibling script
  `yt-analytics-auth.py` (same ship commit `6da5a18`) does read
  `YT_CONFIG_DIR` first -- these three never got that treatment.
  Suggested fix: introduce a shared `YT_CONFIG_DIR` env-var read
  (matching `yt-analytics-auth.py`'s pattern) in all three, falling back
  to the current hardcoded values, then register all four
  YouTube-analytics scripts as one Section-8 registry entry.

- **[medium]** Seven files carry hardcoded machine-specific fallback
  defaults in the acceptable env-first pattern but remain unregistered in
  the Section-8 exception registry, unchanged since 07-09 --
  `skills/product-cf/scripts/product-hero.sh:48`,
  `scripts/yt-analytics-auth.py:24`, `scripts/elevenlabs-tts.py:40-45`,
  `scripts/elevenlabs-music.py:11-13`, `scripts/elevenlabs-sfx.py:8-10`,
  `scripts/typecast-tts.py:47-52`, `agents/lib/tts.sh:80-97`.
  Evidence: each resolves an env var first (`PRODUCT_CF_DEPTH_VENV`,
  `YT_CONFIG_DIR`, `ELEVENLABS_API_KEY`/`ELEVENLABS_KEY_FILE`,
  `TYPECAST_API_KEY`/`TYPECAST_KEY_FILE`) before falling back to a literal
  absolute path -- the pattern Section 8 treats as acceptable -- but a
  grep for "section-8 exception" across all seven returns nothing, and
  none appears in `docs/operator-contract.md`'s registry.
  Suggested fix: add inline section-8-exception comments to all seven and
  a single batched registry addition in `docs/operator-contract.md`
  Section 8, grouped by feature area (YouTube analytics; ElevenLabs/
  Typecast TTS).

- **[medium]** `docs/cost-model.md` self-contradicts on subagent model
  tiering, unchanged since 07-08 -- `docs/cost-model.md:178` versus
  `docs/cost-model.md:20-24`.
  Evidence: lines 22-24 correctly list orchestrator on opus,
  planner/resourcer on opus, editor/qa on sonnet, matching current
  `.claude/agents/*.md` frontmatter. Line 178 still asserts "Sonnet for
  subagents, Opus only for orchestrator." -- directly contradicting the
  table 150+ lines above it in the same file.
  Suggested fix: rewrite line 178 to match lines 20-24 (planner/resourcer
  are opus, only editor/qa/auditor are sonnet).

- **[medium]** Precomputed skill-drift finding (job-hunt, "manifest
  references missing script") remains a CRLF parsing artifact, not a real
  missing file -- unchanged since 07-08 --
  `skills/job-hunt/config/activation.tsv`, `.gitattributes`.
  Evidence: `file skills/job-hunt/config/activation.tsv` reports "with
  CRLF line terminators" (confirmed directly this cycle); `.gitattributes`
  has explicit `eol=lf` rules for `.sh`/`.py`/`.yaml`/`.yml`/`.json`/`.md`
  and others but no rule for `.tsv`, so it falls through to the generic
  `text=auto` default which does not force LF. `scripts/audit-skill-drift.sh`
  parses the CRLF-terminated manifest and mis-reads a lone-CR line as
  non-blank, producing the blank-subject "manifest references missing
  script" finding fed into this report.
  Suggested fix: add `*.tsv text eol=lf` to `.gitattributes`, renormalize
  the tracked file, and harden `scripts/audit-skill-drift.sh` /
  `skills/job-hunt/scripts/status.sh` to strip a trailing carriage return
  before the blank-line test.

- **[medium]** `.env.example` has zero entries for the five environment
  variables that gate shipped ElevenLabs/Typecast/YouTube-analytics
  scripts, unchanged since 07-09 -- `.env.example` (no matching lines).
  Evidence: a search for ELEVENLABS, TYPECAST, and YT_CONFIG_DIR across
  `.env.example` returns nothing (confirmed directly this cycle), while
  `scripts/elevenlabs-tts.py`, `-music.py`, `-sfx.py`, and
  `scripts/typecast-tts.py` all read `ELEVENLABS_API_KEY`/`_KEY_FILE` or
  `TYPECAST_API_KEY`/`_KEY_FILE`, and `scripts/yt-analytics-auth.py` reads
  `YT_CONFIG_DIR`. None of these five secret locations are documented in
  the one file operator-contract Section 8 names as canonical for the
  schema.
  Suggested fix: add a commented-out placeholder block to `.env.example`
  for the five vars, pointing at the per-tool notes docs
  (`docs/elevenlabs-tts-notes.md`, `docs/typecast-tts-notes.md`).

- **[medium]** The two canonical "where the money is" documents omit the
  ElevenLabs/Typecast paid-API narration path entirely, unchanged since
  07-09 -- `docs/for-analysts.md:271`, `docs/cost-model.md` (no mention).
  Evidence: a search for "ElevenLabs" or "Typecast" across
  `docs/cost-model.md`, `docs/for-analysts.md`, `docs/architecture.md`,
  and `docs/operator-contract.md` returns nothing. Both are credit-metered
  paid APIs (documented as the primary narration backend in
  `docs/generative-shorts-pipeline.md` and `docs/elevenlabs-tts-notes.md`),
  gated behind the opt-in `FACELESS_TTS_PLAN` env var (confirmed via a
  repo-wide search -- referenced in `agents/lib/tts.sh` and the two
  per-tool notes docs), so the money-firewall's opt-in-confirmation
  mechanism is not violated, but `for-analysts.md:271`'s "Total
  mission-execution API cost: 0" reads as a blanket claim that is stale
  for this production line.
  Suggested fix: add a "Tier 1 (opt-in) within Tier 2" row for
  ElevenLabs/Typecast to both docs, parallel to the existing
  `FACELESS_SCRIPT_OVERRIDE` row, and soften the "cost: 0" claim to name
  the documented opt-in exceptions.

- **[medium]** 94 files under `.claude/wb/*.json` plus
  `.claude/whiteboard.json` (95 total) are tracked in git as per-lane
  multi-agent session/task working-state, contradicting operator-contract
  Section 8's stated end-state that `.claude/` becomes fully gitignored,
  and are undocumented anywhere -- unchanged since 07-09.
  Evidence: `git ls-files .claude/wb/ | wc -l` returns 94; `.gitignore`
  has no rule for `.claude/wb` or `whiteboard`. Last commit touching this
  path is `9ef5d1f` (2026-07-01) -- no new files added or removed since
  the 07-09 measurement. `docs/operator-contract.md` (around lines
  313-317) states the target end-state: "`.claude/` becomes truly local
  over time... Final state: `.claude/` is fully gitignored." No
  `docs/*.md` or `skills/game-dev-agent/ARCHITECTURE.md` mentions this
  convention.
  Suggested fix: either move this working-state to `$RECORDS_DIR`
  (matches code/data separation, since it reads as session output rather
  than reusable logic) or, if the operator intends `.claude/wb/` to stay
  tracked, document it explicitly and carve out the exception in
  Section 8.

- **[low]** Operator-contract Section 5 marker adoption remains 0 of 10
  qualifying commits; no new qualifying commit landed this cycle to
  change the count -- `.claude/agents/*.md` and `agents/*.md` commit
  history since 2026-05-17.
  Evidence: a commit-history search scoped to `agents/*.md` and
  `.claude/agents/*.md` since the last audit cycle returns nothing -- the
  one commit since the 07-09 cycle (`8b5268d`) touches only
  `docs/generative-shorts-pipeline.md`, outside this rule's scope. Per the
  contract's own calibration this is explicitly low, not a build failure.
  Suggested fix: none urgent; start using the marker on the next
  qualifying commit if the convention is meant to be load-bearing.

- **[low]** `docs/for-analysts.md:152` cites a stale line number for the
  faceless-short ollama call, unchanged across at least three cycles --
  versus `agents/missions/faceless-short/run.sh:100`.
  Evidence: the doc still says line 98; the actual narration-script call
  site is confirmed at line 100 this cycle (`ollama_generate
  "$OLLAMA_MODEL_HIGHLIGHT" "$SCRIPT_PROMPT" false`).
  Suggested fix: drop the specific line number and cite the function/
  variable name instead, consistent with the Section-8 registry's own
  no-line-numbers convention (adopted for exactly this reason).

- **[info]** Eight recently-shipped scripts remain undocumented in any
  `docs/*.md` or `README.md`, unchanged since 07-08/07-09 --
  `scripts/elevenlabs-sfx.py`, `scripts/esrgan-upscale.py`,
  `scripts/generate-quality-trend-chart.py`, `scripts/mix3-rhythm.py`,
  `scripts/wan-flf2v.py`, `scripts/yt-analytics-auth.py`,
  `scripts/yt-analytics-query.py`, `scripts/yt-stats-sample.sh`.
  Evidence: a basename sweep against every tracked `docs/*.md` (recursive)
  and `README*.md` found no reference for these eight. Per the
  README-maintenance-cadence convention, shipping new capability scripts
  is one of the four docs-pass triggers; none has landed for this batch.
  Suggested fix: fold into the next docs pass alongside the subagent-
  roster and cost-model fixes above.

- **[info]** No `docs/daily/<date>.md` shutdown report has been written in
  44 days -- `docs/daily/2026-05-27-colony-sim-vanilla-day14-26.md` is
  still the most recent entry.
  Evidence: directory listing shows nothing past 2026-05-27, despite six
  weeks of subsequent commits spanning two production pivots
  (music-video into a Windows/RTX hardware pivot into content-shorts/
  generative-shorts). Operator-contract Section 11 only triggers on an
  explicit end-of-day phrase from the user, so this is not a rule
  violation -- noted for visibility, unchanged since 07-09.
  Suggested fix: none required by contract; the session-resume briefing
  (Section 10) has no fresh daily-report anchor in this window if one is
  needed.

## Drift between docs and code

- `docs/for-analysts.md` (claims 22, internally inconsistent with its own
  6+12+5=23 breakdown) versus `README.md` (claims 23) versus the live
  `.claude/agents/*.md` count (26) -- three-way mismatch, unchanged
  across at least six cycles.
- `docs/cost-model.md:178` contradicts `docs/cost-model.md:20-24` on which
  subagents run opus versus sonnet.
- `docs/roadmap.md` Done section is 31 commits behind HEAD, widening
  every cycle measured: 20 to 21 to 25 to 27 to 30 to 31 across six
  cycles in under a week.
- `docs/goal.md` active goal (PawnSim) has had zero related commits in 28
  days while `docs/roadmap.md` "Now" (job-hunt operator-activation) has
  had zero related commits in roughly nine weeks; the roadmap's own
  2026-07-03 suggest-comment proposing a content-shorts pivot is now a
  full week stale relative to the ElevenLabs/Typecast/Wan2.2/handoff-doc
  work that has landed since.
- `skills/job-hunt/config/activation.tsv` (CRLF at checkout) versus its
  two consumers (`scripts/audit-skill-drift.sh`, `status.sh`), both of
  which assume LF.
- `docs/cost-model.md` and `docs/for-analysts.md` "where the money is"
  claims omit the ElevenLabs/Typecast opt-in paid path entirely.
- `.env.example` schema is missing five env vars that five shipped
  scripts actually read.
- `docs/operator-contract.md` Section 8's stated "final state: `.claude/`
  is fully gitignored" versus 95 tracked files under `.claude/wb/` plus
  `.claude/whiteboard.json`.
- `docs/audit/CURRENT-ALERT.md` (last committed 2026-07-06, `52fda03`) is
  now several cycles stale on disk relative to its own uncommitted
  current content (which points at an also-uncommitted
  2026-07-09-all.md).

## Stale items

- `docs/audit/CURRENT-ALERT.md` (modified, uncommitted) and
  `docs/audit/2026-07-08-all.md`, `docs/audit/2026-07-08-contract.md`,
  `docs/audit/2026-07-09-all.md` (all untracked) -- sitting in the
  working tree at session start; this report will join them once this
  session ends. Sixth consecutive cycle this is the norm rather than the
  exception.
- `docs/roadmap.md` Done section -- 31 uncited commits, growing every
  cycle without exception since first measured.
- `docs/goal.md` Active goal (PawnSim, dated 2026-06-12) -- unworked for
  28 days, no suggest-comment in the file itself.
- `docs/roadmap.md` "Now -- active focus" block -- still dated
  2026-05-20/2026-05-22 (job-hunt operator-activation); its own
  2026-07-03 suggest-comment proposing a content-shorts pivot is itself
  now a week stale.
- Section 8 exception registry -- at least 8 known-qualifying files
  unregistered (3 zero-fallback high-severity, 7 medium
  env-first-with-fallback), none fixed since first flagged.
- Section 5 marker -- 10 qualifying commits, zero adoption, unchanged.
- Subagent roster count docs (`for-analysts.md`, `README.md`) -- stale
  against a live count that has held at 26 since 2026-07-05.
- `docs/daily/` -- no entry since 2026-05-27 (44 days).

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| Section 1 Agent does all the work | PASS | Behavioral; not code-checkable from static state |
| Section 2 Never pause unless told | PASS | Behavioral; not code-checkable |
| Section 3 Money firewall | PASS | `config/policies.yaml` `budget_usd_ceiling: 5.00`; ElevenLabs/Typecast paid calls remain opt-in via `FACELESS_TTS_PLAN`; no unconditional paid-API call found in any mission `run.sh` |
| Section 4 Auto-approve mode | PASS | `.claude/settings.json` confirmed gitignored, untracked |
| Section 5 Logic changes require explicit user OK (marker) | LOW, stable | 10 of 10 qualifying commits since 2026-05-17 lack the audit-trail marker; no new qualifying commit this cycle |
| Section 6 Git workflow, gitignore coverage | PASS, one caveat | `records/`, secrets, and per-operator files correctly excluded; caveat -- `.claude/wb/*` tracked despite Section 8's stated final-state intent, see Section 8 row below |
| Section 6 Git workflow, auto-commit + push of every doc change | CRITICAL FAIL | Reproducing for a sixth consecutive cycle; root cause (no commit step in `audit-run.sh`) still unfixed |
| Section 7 Split commit and push | PASS | No compound commit-and-push found in tracked shell scripts |
| Section 8 Code/data separation, committed artifacts | PASS | No stray media/output files found under `agents/` or `scripts/` |
| Section 8 Code/data separation, hardcoded paths with no fallback | HIGH FAIL | `refactor_check.py` + 3 YT-analytics scripts, unchanged |
| Section 8 Code/data separation, hardcoded paths with env fallback but unregistered | MEDIUM | 7 files known, unchanged |
| Section 8 `.claude/` final-state-gitignored intent | FAIL | 95 files under `.claude/wb/` + `.claude/whiteboard.json` tracked, undocumented, unchanged |
| Section 9 Goal and roadmap source of truth | HIGH FAIL | Done-gap 31 commits and growing; `goal.md` active goal unworked 28 days with no self-correcting proposal; roadmap Now stale on multiple layers |
| Section 10/11 Session-resume and shutdown protocol | INFO | Behavioral; no `docs/daily/` entry since 2026-05-27; not scored (Section 11 only triggers on explicit user phrase) |
| Section 12 No PII or secrets | PASS, one caveat | Repo-wide search for key/secret/token/password patterns with real-looking values returned only placeholders/prose; `.env.example` and `config/copyright-allowlist.yaml` schema/documentation-only. Caveat: `refactor_check.py`'s literal operator account name, scored under Section 8 above |

## Next audit hint

Highest priority for the next cycle: confirm this report and the three
files it found sitting uncommitted (`CURRENT-ALERT.md`,
`2026-07-08-all.md`, `2026-07-08-contract.md`, `2026-07-09-all.md`)
actually landed in a commit; if a seventh consecutive cycle finds the
audit trail itself uncommitted, treat the recurrence pattern -- not just
the underlying bug -- as the literal headline finding, since the
identical fix has now been suggested six times without landing. Second
priority: check whether the Section-8 zero-fallback cluster
(`refactor_check.py` + YT-analytics trio), the `.claude/wb/` tracking
question, or the `docs/goal.md` orphan have moved at all in either
direction.
