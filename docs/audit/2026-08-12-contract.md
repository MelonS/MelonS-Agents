# Audit report — 2026-08-12 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Focused pass over `docs/operator-contract.md` compliance at HEAD `d9aeca1`
(2026-08-12, ~09:35 KST), cross-checked against `docs/for-analysts.md`,
`docs/architecture.md`, `docs/roadmap.md`, `docs/goal.md`, every
`.claude/agents/*.md` frontmatter, `.gitignore`, `.github/workflows/`, and
`git log` (1634 commits reachable from `main`). The pre-computed
skill-drift report (`scripts/audit-skill-drift.sh`) is clean (0 findings)
and is not repeated below. `main` is green - `gh run list` shows 5/5 recent
`main-protection` runs `success`. Secret scan outside `docs/` / `*.example`
returned nothing; `.env` remains gitignored and untracked. The section-8
hardcoded-path exception registry's 12 listed files all still carry the
required `section-8 exception` grep-anchor. No output artifacts are committed
under `agents/` or `scripts/`. Subagent roster: 27 files under
`.claude/agents/`, matching `for-analysts.md`'s claimed count and group
breakdown exactly (6 core + 13 game-line + 5 content-shorts + 3 judges);
the 6 core agents' `model:` frontmatter matches the routing table exactly
(opus/opus/opus/sonnet/sonnet/sonnet).

Two items flagged high in the prior report (`2026-07-31-contract.md`)
are now resolved: the README hero-stats alt text now correctly reads "27
subagents" (was "23"), and `docs/roadmap.md` "Now" was refreshed
2026-08-10 to reflect the NAN2026-submission-complete state instead of the
72-day-stale multi-skill-framework text. Three prior medium findings
remain open unresolved for a third consecutive contract-focused cycle:
`.claude/wb/` (94 tracked files, undocumented, gitignore-absent), `ta.md`
missing a `model:` frontmatter field, and `for-analysts.md` undercounting
the CI gate ("six" vs the actual seven checks, unchanged since `06dd751`
on 2026-07-01).

Two new findings emerged this cycle that were not visible to the prior
audit. First: cross-referencing every commit hash cited in
`docs/roadmap.md`'s Done section against `git cat-file -e` shows 50
entries (dated 2026-05-14 through 2026-05-19, i.e. the project's first six
days) reference hashes that do not exist anywhere in the current repo -
not on `main`, not on any branch, not as a loose object. Root cause is
documented in the repo's own history: an email-history rewrite via
`git filter-repo` landed 2026-05-17 (`b46a2ba`), and its rollback safety
branch (`main-backup-pre-filter-20260517-173615`) was deleted three days
later (`a90bc9d`, 2026-05-20) after no issues were observed - a reasonable
operational call at the time, but nobody went back to annotate the
now-orphaned Done entries or the operator-contract's own citation of the
same pre-rewrite hash space. That citation is the second half of this
finding: `docs/operator-contract.md` section 5's pre-marker carry-forward
clause names `7c6ff4f` as the reference commit, and that hash is also
unresolvable in the current repo - the contract's own audit-trail
exemption is unverifiable by the exact mechanism (`git cat-file -e`) this
audit dimension prescribes. Second: while investigating the L1 post-commit
hook (`scripts/hooks/post-commit.sh`), a live instance was caught
mid-failure - the hook correctly fired for today's `d9aeca1` (it touched
`.claude/agents/ta.md`, a drift-risk path) at 09:27:02, and the spawned
`audit-run.sh contract` background process (pid 5788) is still alive and
producing no further log output more than 8 minutes later, well past the
short startup banner where every other logged hook-run in
`records/audit/hook-trigger.log` had already produced substantive content
by this point. That process is targeting the exact same output path this
report is writing to (`docs/audit/2026-08-12-contract.md`), which is a
live overwrite/commit-race risk, not a hypothetical one.

## Findings

- **[high]** L1 post-commit-hook audit process appears hung, targeting the same output path as this report - `scripts/hooks/post-commit.sh`, `records/audit/hook-run-20260812-092702-d9aeca1.log`, `records/audit/.hook.inflight`
  Evidence: `d9aeca1` (today, touches `.claude/agents/ta.md`) correctly triggered the hook at 09:27:02 per `records/audit/hook-trigger.log`. Its background process, pid 5788 (confirmed alive via `ps -W`), spawned `audit-run.sh contract` writing to `docs/audit/2026-08-12-contract.md` - the identical path this report is writing to. Its log file (`hook-run-20260812-092702-d9aeca1.log`) has been frozen at 5 lines (a startup banner plus two informational settings.json permission-syntax warnings) since 09:27:03, unchanged as of 09:35:36 (8.5+ minutes with zero new output). Every prior hook-run log in the same directory (e.g. `hook-run-20260731-033047-45a23d2.log`, 2852 bytes, completed in 8m21s; `hook-run-20260706-023259-4e56b21.log`, 1989 bytes, completed in about 10m) shows substantially more content well before this point. `.hook.inflight` sentinel still holds pid 5788, meaning `audit-run.sh`'s own coalescing logic still considers this run in-flight.
  Suggested fix: kill the stuck pid (5788 and child 5789) once this report lands to avoid a delayed write/commit clobbering it; investigate whether the two informational Write()-vs-Edit() permission warnings block a downstream tool call in headless/non-interactive mode (the previous 8 successful hook-runs did not print these lines, suggesting `.claude/settings.json`'s permission block changed recently - diff it against `config/claude-settings.template.json`'s rendering).

- **[high]** `docs/roadmap.md` Done section (50 entries) and `docs/operator-contract.md` section 5's carry-forward citation reference commit hashes invalidated by the 2026-05-17 filter-repo history rewrite - `docs/roadmap.md` Done entries dated 2026-05-14 to 2026-05-19; `docs/operator-contract.md:117` (`7c6ff4f`)
  Evidence: `git cat-file -e <hash>` fails ("Not a valid object name") for all 50 hashes cited in Done entries dated 2026-05-14 through 2026-05-19 (e.g. `8ae9449`, `dae3d58`, `d171d29`, `ee833a0`, `a2d0949`, `e947dc0`, `b485f29`, `e91f29b`, `d25b462`). The equivalent commits exist under different hashes in current history (e.g. the commit whose message contains "real CC fixtures + standard layout + source-attribution" is now `836b6f5`, not the cited `8ae9449`). `docs/roadmap.md:1413` documents the root cause directly: `a90bc9d` (2026-05-20) deleted the rollback branch `main-backup-pre-filter-20260517-173615` that would have preserved the old-to-new hash mapping, three days after `b46a2ba` (2026-05-17, carries a `Requested-by: user` marker) queued the deletion as a time-boxed rollback window. `docs/operator-contract.md:117` separately cites `7c6ff4f` as "prior to 2026-05-17" - that hash is also unresolvable (`fatal: Not a valid object name 7c6ff4f`).
  Suggested fix: add a one-line annotation at the top of the Done section noting that entries dated on or before 2026-05-19 predate the 2026-05-17 filter-repo rewrite and their cited hashes are non-verifiable by design (the mapping was intentionally not preserved past the rollback window); separately, either replace `7c6ff4f` in `operator-contract.md` section 5 with a currently-valid post-rewrite commit that established the same convention, or add the same non-verifiable caveat inline so a future audit doesn't need to re-derive this root cause.

- **[medium]** Section-5 audit-trail marker: 100 percent non-compliance since the convention was introduced - 12 of 12 qualifying commits (2026-05-17 to 2026-08-12) carry no `Requested-by: user` footer
  Evidence: `git log --since=2026-05-17 -- .claude/agents/*.md | sort -u` returns 12 unique commits (`fcc5fee`, `f714a58`, `2778316`, `32ec30f`, `2b44b47`, `ff7fa19`, `9425a3e`, `9559d1c`, `8194d51`, `7be3142`, `9ef5d1f`, `d9aeca1`). `git log -1 --format=%B <hash> | grep "Requested-by:"` returns no match for any of the 12. None qualifies for the pre-2026-05-17 carry-forward exemption (all postdate it). Several commit bodies self-report an operator instruction in prose (`8194d51`: "Operator-commissioned verification stages"; `9425a3e`: "operator directive"; `7be3142`: operator instruction noted inline; `d9aeca1`: "Operator instruction: PawnSim도 옮겨") without the structured footer the contract asks for. The prior audit (`2026-07-31-contract.md`) flagged 6 of these (restricted to its `--since=2026-06-01` window); this cycle's full-history scan against the actual 2026-05-17 convention date finds 6 more predating that window, plus today's `d9aeca1`.
  Suggested fix: the contract itself treats a missing marker as non-blocking; closing this cheaply means a one-time retroactive note in `docs/autonomous-decisions.md` or a decision log listing which of the 12 were in fact operator-approved in-session (most commit bodies already carry enough prose evidence to reconstruct this) rather than rewriting 12 commits' history.

- **[low]** `d9aeca1` (today) edits `.claude/agents/ta.md` without a `Requested-by: user` marker despite its own commit body flagging awareness of the rule - `.claude/agents/ta.md`, commit `d9aeca1`
  Evidence: the commit body states that the ta.md change is "not a behavior change, but flagging since editing .claude/agents/*.md is normally a confirm-first action per this repo's operator contract". The operator instruction quoted at the top of the same commit ("PawnSim도 옮겨") is about the broader file move, not a specific confirmation of the ta.md edit. No `Requested-by:` footer follows. This is the freshest instance of the systemic pattern above, distinguished by explicitly naming the gap without closing it.
  Suggested fix: same as the systemic finding - a retroactive decision-log line is enough given the edit is a path-string change with no behavioral effect on the ta agent.

- **[medium]** `.claude/wb/` - 94 tracked files, undocumented and un-gitignored, unresolved 3rd consecutive contract-focused audit cycle - `docs/architecture.md` Layers table, `.claude/wb/*.json`
  Evidence: `git ls-files .claude/wb | wc -l` returns 94. `git check-ignore -v .claude/wb` returns no match (not ignored). `docs/architecture.md`'s Layers table enumerates 8 rows (Logic / Skills / Outputs / Publish-ready / Review queue / Secrets / Committed evidence) and `.claude/wb/` fits none of them. Last file touch 2026-07-01 (`9ef5d1f`), 42 days stale as of this audit. Same gap flagged in `2026-07-26-contract.md` and `2026-07-31-contract.md`; `docs/roadmap.md:286` still lists it as an unresolved item.
  Suggested fix: unchanged from prior audits - operator picks (a) add a Layers-table row plus a section-8-style deviation marker (treat as committed evidence, mirroring `outputs/publish/`), or (b) migrate under `$RECORDS_DIR` and gitignore going forward, leaving the 94 existing files as history.

- **[medium]** `ta.md` frontmatter still omits the `model:` field present in all 26 other subagent definitions, unresolved 3rd consecutive cycle - `.claude/agents/ta.md:1-5`
  Evidence: `.claude/agents/ta.md`'s frontmatter block (`name`, `description`, `tools`) has no `model:` key, confirmed again this cycle. All 26 other files under `.claude/agents/` declare `model: opus` or `model: sonnet`. `docs/for-analysts.md`'s roster table documents models only for the core 6, so there is still no stated intended value for `ta` to compare against - same gap named in `2026-07-26-contract.md` and `2026-07-31-contract.md`.
  Suggested fix: unchanged - add an explicit `model:` line to `ta.md` (the design-QA/critique role parallels `note-judge`/`still-judge`/`cut-judge`, all opus) and add `ta` to a documented per-agent model reference in `docs/for-analysts.md`.

- **[medium]** `docs/for-analysts.md` still undercounts the CI gate by one check, unresolved 2nd consecutive cycle - `docs/for-analysts.md:294` vs `.github/workflows/main-protection.yml`
  Evidence: `for-analysts.md:294` reads "six static checks - bash syntax on tracked scripts, secret scan, required-files presence, .env.example schema sanity, README link hygiene, gitignore coverage." The workflow currently has seven named steps beyond Checkout: Bash syntax check, Secret pattern scan, Required files present, .env.example sanity, README internal links resolve, README EN-KO parity, gitignore covers sensitive paths. The README EN-KO parity step was added by `06dd751` (2026-07-01) - six weeks before this audit - and the prose was never updated. Same gap flagged in `2026-07-31-contract.md`.
  Suggested fix: unchanged - update the sentence to list all seven checks and say "seven static checks."

- **[medium]** `docs/goal.md` Active goal reads "no active goal" while roughly 10 commits of operator-directed naval-sail-prototype work have landed since 2026-08-11 - `docs/goal.md:19` (Active goal section) vs `git log --oneline -10`
  Evidence: `docs/goal.md`'s Active-goal section states it is paused with no active goal, as of 2026-08-06, per an operator instruction to turn off auto-progression. The 10 most recent commits (`d9aeca1` through `550679b`) are entirely naval-sail-prototype v0 work (ship floats and moves on 3D ocean, Kenney model swap, project split, PawnSim relocation) plus their doc trail - none reference the paused state. This is captured correctly via a `<!-- suggest 2026-08-12 -->` block inside `goal.md` itself (mechanism-compliant per rule 9 - Claude did not silently rewrite the Active-goal section), and the block explicitly defers the promotion decision to the operator. The drift is real even though the mechanism was followed: a fresh reader of `goal.md` gets "nothing is active" while the repo's actual last 24 hours of work says otherwise - the same failure shape (empty-looking outcome layer, real work happening) that the goal/roadmap split in `CLAUDE.md`'s session-start protocol was written to prevent.
  Suggested fix: no agent action required (the file's own maintenance contract reserves promotion for the operator); flagging so the operator sees the suggest-block is still pending and the staleness window is growing.

## Drift between docs and code

- `docs/roadmap.md` Done section (2026-05-14 to 2026-05-19, 50 entries) and `docs/operator-contract.md:117`'s `7c6ff4f` citation vs. the current commit graph - see high finding above.
- `.claude/wb/` present in the repo but absent from `docs/architecture.md`'s Layers table - see medium finding above.
- `ta.md` missing from any documented model-assignment source - see medium finding above.
- `docs/for-analysts.md` CI check count (six) vs `.github/workflows/main-protection.yml` (seven checks since 2026-07-01) - see medium finding above.
- `docs/goal.md` Active-goal state (no active goal) vs. the last 10 commits (naval-sail-prototype v0) - see medium finding above.

## Stale items

- `.claude/wb/` - 42 days since last file touch (2026-07-01), undocumented the whole time.
- `docs/roadmap.md` Done-section hash citations for the 2026-05-14 to 2026-05-19 window - permanently stale (the rollback branch that could re-map them was deleted 2026-05-20, about 84 days ago).
- `docs/for-analysts.md`'s CI-check-count sentence - 42 days stale relative to the workflow it describes (`06dd751`, 2026-07-01).

## Compliance with operator-contract.md

- Rule 1 Agent does all the work - no violations observed.
- Rule 2 Never pause unless told - not directly checkable from static state; no evidence against.
- Rule 3 Money firewall - no paid-API or cloud-resource code changes found in the audited window.
- Rule 4 Auto-approve mode - no violations observed; `.claude/settings.json`'s allow list stays local-resource-scoped.
- Rule 5 Logic changes require explicit user OK - failing in practice: 12 of 12 qualifying commits since the 2026-05-17 marker convention lack the `Requested-by: user` footer (see medium and low findings). The contract treats this as non-blocking by design, but a 100 percent miss rate across 3 months is worth the operator's attention.
- Rule 6 Git workflow (auto-commit/push, branch strategy) - `main` green (5/5 recent CI runs pass); `records/` correctly gitignored; local and remote were briefly diverged by one commit at audit start, resolved by the time of writing (`git log origin/main..HEAD` now empty).
- Rule 7 Split commit and push - no violations observed in recent commit pairs.
- Rule 8 Code/data separation - registry's 12 files all carry the required grep-anchor; no output artifacts under `agents/` or `scripts/`; `.claude/wb/` remains the one open gap against this rule's spirit (medium finding, 3rd consecutive cycle).
- Rule 9 Goal and roadmap as source of truth - the append/suggest-block mechanism is being followed correctly (no silent rewrites observed in either file); content freshness is failing in two places: the Done-section hash citations (high finding) and `goal.md`'s Active-goal staleness against real work (medium finding). Roadmap "Now" itself, flagged high in the prior audit, is now current (refreshed 2026-08-10).
- Rules 10 and 11 Session resume / shutdown protocols - not directly auditable from static repo state this cycle.
- Rule 12 No PII or secrets - targeted secret-pattern grep outside `docs/`, `*.example` returned nothing; `.env` gitignored and untracked.
- Rule 13 Inbound-message routing check - not directly auditable from static repo state this cycle.

## Next audit hint

Two things to check first next time: (1) whether the hung `audit-run.sh` process from this cycle (pid 5788, triggered by `d9aeca1`) ever completed, and if it did, whether it clobbered or duplicate-committed over this report - diff `docs/audit/2026-08-12-contract.md`'s git history for a second commit touching the same path; (2) whether `docs/goal.md`'s 2026-08-12 suggest-block (naval-sail-prototype promotion) has been acted on - if a second cycle passes with "no active goal" still standing against continued naval-sail commits, escalate that finding from medium to high, matching how the roadmap "Now" staleness was escalated in the 2026-07-31 report before being resolved.
