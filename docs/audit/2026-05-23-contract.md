# Audit report — 2026-05-23 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Contract-focused audit against HEAD `e9af4b7` (2026-05-23).  All twelve
hard rules (§1–§12) and the Conventions section of `docs/operator-contract.md`
were checked against committed code, configuration, and documentation.  The
primary finding — model-assignment drift between `.claude/agents/*.md`
frontmatter and three documentation files — carries forward unresolved from
the prior contract audit (`2026-05-22-contract.md`).  Both `planner` and
`resourcer` carry `model: opus` after commit `2778316` (2026-05-22 ~17:50 KST),
but `docs/for-analysts.md`, `docs/architecture.md`, and `docs/cost-model.md`
still describe them as `sonnet`; only `README.md` and `README.ko.md` were
updated.  Three §5-scope commits (`fcc5fee`, `f714a58`, `2778316`) remain
without the canonical `Requested-by: user` footer.  Two §8 hardcoded-path
issues persist: one in operational guidance (`assets/lyrics/2026-05-21-suno-vocal-prompts.md:7`)
and one in a test-script comment (`scripts/test-lyric-extract.sh:10`).  All
other contract dimensions are clean: §3 money firewall intact (mission code
routes through `agents/lib/ollama.sh`, no Anthropic SDK calls), §6 gitignore
covers `records/`, `.env`, rendered plists, and `.claude/settings.json`,
§12 secret/PII scan is clean, and the skill-activation drift report returns
0 findings.  The four commits that landed since the prior `2026-05-23-all.md`
baseline (`e1e3838`, `9015d10`, `c242e4e`, `e9af4b7`) are music/lyric feature
work and introduce no new contract violations.

## Findings

- **[high]** Model-assignment drift across three documentation files —
  `docs/for-analysts.md:100-101`, `docs/architecture.md:26,30`,
  `docs/cost-model.md:23`
  Evidence: `.claude/agents/planner.md:5` = `model: opus`;
  `.claude/agents/resourcer.md:5` = `model: opus` (set by commit `2778316`,
  2026-05-22 ~17:50 KST).  Three docs still claim sonnet: (a)
  `docs/for-analysts.md:100-101` table rows list `planner → sonnet`,
  `resourcer → sonnet`; (b) `docs/architecture.md:26` shows box label
  `planner (sonnet)`, line 30 `resourcer (sonnet)`; (c)
  `docs/cost-model.md:23` tier diagram reads `planner / resourcer /
  editor / qa (sonnet)`.  Additionally: `docs/for-analysts.md:94-95`
  still claims "last commit touching `.claude/agents/*.md` was `8570a9c`"
  — the actual most-recent commit is `2778316` (2026-05-22); and
  `docs/for-analysts.md:106-107` asserts "A common analyst suggestion is
  'downgrade subagents from opus to sonnet.' This is already done" which is
  now factually reversed (planner + resourcer are at opus).  Commit `2778316`
  updated `README.md` and `README.ko.md` but missed these three files.
  Carrying forward from `2026-05-22-contract.md` (1 unresolved cycle).
  Suggested fix: Update `docs/for-analysts.md` table rows, provenance line,
  and "already done" note; update `docs/architecture.md` box labels; update
  `docs/cost-model.md` tier diagram to reflect planner=opus, resourcer=opus.

- **[low]** §5 `Requested-by: user` footer absent — commit `fcc5fee` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" fcc5fee` returns only the subject line
  "abtest(planner+resourcer): flip to opus for A/B" — empty body, no prose
  approval citation, no `Requested-by: user` trailer.  Commit modifies both
  §5-scope files, is dated 2026-05-22 (post-convention threshold 2026-05-17),
  and is reachable from `origin/main`.  The companion commit `f714a58`
  mentions this flip was "kept local" but `git cat-file -e fcc5fee` exits 0
  and `git branch -r --contains fcc5fee` returns `origin/main`.
  Suggested fix: Future §5-scope commits, including experimental A/B flips,
  include the canonical `Requested-by: user` footer.

- **[low]** §5 `Requested-by: user` footer absent — commit `f714a58` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" f714a58` includes "Operator §5 approval
  granted in-session via AskUserQuestion" in body prose — intent is clear,
  but no `Requested-by: user` trailer line is present.  Per §5, the footer
  is required to "make the approval mechanically verifiable in `git log`";
  body prose is not parseable by `scripts/pre-merge-check.sh`'s marker
  check.
  Suggested fix: Include the literal `Requested-by: user` trailer (above
  `Co-Authored-By`) in addition to any prose approval citation.

- **[low]** §5 `Requested-by: user` footer absent — commit `2778316` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" 2778316` shows detailed operator rationale
  ("Operator decision 2026-05-22 ~17:50 KST: keep pair upgrade …") but no
  `Requested-by: user` trailer.  This is the highest-stakes §5-scope commit
  of the three — it establishes the current production model assignment for
  both planner and resourcer.
  Suggested fix: Add `Requested-by: user` footer in any follow-up commit
  referencing this decision arc.

- **[low]** Stale provenance claim in commit body — `f714a58`
  Evidence: `f714a58` body states "The intermediate fcc5fee commit (opus
  flip) was kept local — only the revert + verdict reaches origin."
  `git branch -r --contains fcc5fee` returns `origin/main`, confirming
  `fcc5fee` is publicly visible on the remote.  This factual error in the
  commit body could mislead a future audit or code-review pass.
  Suggested fix: Acknowledge the error in a daily report or next commit
  message that references the A/B arc; no code change required.

- **[low]** §8 hardcoded machine path in operational guidance —
  `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7`
  Evidence: Line 7 reads `**Where to save downloaded mp3s:**
  \`/Users/melons/ai/assets/music/\`` — a machine-specific absolute path
  in a committed operator-facing guidance file.  The file is tracked (`git
  ls-files assets/lyrics/2026-05-21-suno-vocal-prompts.md` exits 0) and is
  not in the §8 exception registry; no `§8 exception:` comment is present.
  On a different clone path the instruction is wrong.
  Suggested fix: Replace with `$REPO_ROOT/assets/music/` and add a note
  that `$REPO_ROOT` is set by `agents/lib/env.sh`.

- **[low]** §8 hardcoded machine path in test script comment —
  `scripts/test-lyric-extract.sh:10`
  Evidence: Line 10 is `# Uses /Users/melons/ai/assets/music/vocal-dreampop-blue-hours.mp3`
  — machine-specific absolute path in a comment of a tracked test script.
  Functional code on line 18 correctly uses `$REPO_ROOT` (`FIXTURE="$REPO_ROOT/assets/music/..."`),
  and the test gracefully skips when the fixture is absent (lines 22-25).
  Lower severity than the lyrics file since the comment-only path has no
  runtime impact, but the §8 rule prohibits literal `/Users/...` in committed
  files regardless of executable vs comment context.
  Suggested fix: Replace the comment path with `$REPO_ROOT/assets/music/...`
  to match the functional code on line 18.

## Drift between docs and code

| Doc location | Doc claim | Code / config reality |
|---|---|---|
| `docs/for-analysts.md:100` | `planner` → `sonnet` | `.claude/agents/planner.md:5` → `model: opus` |
| `docs/for-analysts.md:101` | `resourcer` → `sonnet` | `.claude/agents/resourcer.md:5` → `model: opus` |
| `docs/for-analysts.md:94-95` | last commit to `.claude/agents/*.md` = `8570a9c` (2026-05-15) | last commit = `2778316` (2026-05-22) |
| `docs/for-analysts.md:106-107` | "downgrade to sonnet already done" | planner + resourcer are at opus; statement is reversed |
| `docs/for-analysts.md:109-111` | "valid future move: downgrade `resourcer` to haiku" | resourcer is at opus; sonnet→haiku demotion is now two tiers, not one |
| `docs/architecture.md:26` | `planner (sonnet)` box label | `.claude/agents/planner.md` → `model: opus` |
| `docs/architecture.md:30` | `resourcer (sonnet)` box label | `.claude/agents/resourcer.md` → `model: opus` |
| `docs/cost-model.md:23` | `planner / resourcer / editor / qa (sonnet)` tier diagram | planner + resourcer = opus since 2026-05-22 |

## Stale items

- **Roadmap Done section** is 28 commits behind HEAD.  The most recent Done
  entry (`4f3e717`, 2026-05-22 ~16:04 KST) is 28 commits behind current HEAD
  `e9af4b7`.  `scripts/roadmap-done-sync.sh --apply` would auto-backfill.
  Not a contract violation per se, but §9 states "after work lands, append a
  one-line entry to roadmap's Done section with the commit hash and date".

- **Roadmap "Now"** describes an operator-activation step for `job-hunt`
  v0.4.0 that has been the current item for 40+ commits.  The roadmap text
  itself acknowledges this is structural ("this 'Now' subgoal remains the
  active operator-side step; no agent work queued").  Not actionable for
  the agent; carried forward as informational.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✓ PASS | No user-recipe instructions in committed files |
| §2 Never pause unless told | ✓ PASS | No pause-seeking patterns in committed docs or scripts |
| §3 Money firewall | ✓ PASS | All three Tier-2 missions route through `agents/lib/ollama.sh`; no direct Anthropic SDK calls in mission code; `config/policies.yaml` `budget_usd_ceiling: 5.00` intact |
| §4 Auto-approve mode | ✓ PASS | `config/policies.yaml` intact; local-resource allow-list in `.claude/settings.json` template covers declared tools |
| §5 Logic changes require explicit user OK | ⚠ FAIL [low×3] | `fcc5fee` (no marker, no body), `f714a58` (prose approval present, no canonical `Requested-by: user` trailer), `2778316` (detailed rationale, no trailer) |
| §6 Auto-commit + auto-push / branch strategy | ✓ PASS | `records/` gitignored; `rendered plists gitignored; `.env` gitignored; `.claude/settings.json` gitignored; no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | ✓ PASS | No `git commit ... && git push` compound found in any committed script |
| §8 Code/data separation + portability | ⚠ FAIL [low×2] | `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7` — hardcoded `/Users/melons/ai/assets/music/` in operator guidance; `scripts/test-lyric-extract.sh:10` — hardcoded path in comment (functional code correct).  All 9 §8-registry-listed files retain their `# §8 exception:` comments (verified). |
| §9 Goal and roadmap as source of truth | ✓ PASS | Structural stale in "Now" is explicitly acknowledged in-file; roadmap Done lag is a maintenance gap, not a routing failure |
| §10 Session resume protocol | ✓ PASS (behavioral) | Not fully verifiable from code; no committed violation patterns found |
| §11 Shutdown protocol | ✓ PASS (behavioral) | Most recent daily report exists at `docs/daily/` |
| §12 No PII or secrets | ✓ PASS | `git grep` secret scan returns only placeholder/comment/variable-name matches; `.env.example` is schema-only (no actual keys); `PEXELS_API_KEY=` is empty; no real names, emails, or credentials in committed files |

## Next audit hint

Verify the three stale documentation files (`docs/for-analysts.md`,
`docs/architecture.md`, `docs/cost-model.md`) have been updated to reflect
`planner=opus`, `resourcer=opus` — this high finding has now persisted
across two consecutive contract audits and one all-audit without being
resolved.
