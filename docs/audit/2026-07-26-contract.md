# Audit report — 2026-07-26 (contract)

**Verdict**: CRITICAL

## Summary

Focused pass over `docs/operator-contract.md` compliance, cross-checked against
`docs/for-analysts.md`, `docs/architecture.md`, `docs/roadmap.md`, every
`.claude/agents/*.md` frontmatter, `.gitignore`, and recent `git log` at HEAD
`b260b76` (2026-07-26, section 13 inbound-message-routing rule just added). The
precomputed skill-drift report is clean (0 findings) and is not repeated here.
Hard rule 6 (git workflow / gitignore) and hard rule 8 (the section-8
hardcoded-path exception registry) both check out: `records/`, `.env`, and all
eight listed section-8 exception files carry the required grep-anchor comment,
and no output artifacts (mp4/wav/jpg/etc.) are committed under `agents/` or
`scripts/`. `.env.example` contains only placeholder values, and a targeted
secret-pattern grep outside `docs/`, `*.example`, and `*.md` turned up only
code identifiers (token, access_token) and no literal credentials. The core
6-agent model table in `for-analysts.md` matches `.claude/agents/orchestrator.md`,
`planner.md`, `resourcer.md`, `editor.md`, `qa.md`, and `auditor.md` frontmatter
exactly, and the 27-definition roster count (6 core plus 13 game-line plus 5
content-shorts plus 3 judges) is verified by direct file-count, matching the
prose.

Two significant problems remain live, and both are continuations of
previously-flagged findings rather than new discoveries: first,
`docs/audit/CURRENT-ALERT.md` is still the 2026-07-10 CRITICAL alert with no
audit report landing in `docs/audit/` in the 16 days since, despite the
architecture doc describing a three-trigger-layer automation design (L1
post-commit, L2 15-min poll, L3 daily 03:00 baseline) that should have
produced one; and second, `docs/roadmap.md` "Now" section (last touched
2026-05-20) still describes job-hunt v0.4.0 activation under a
multi-skill-framework active goal, while `docs/goal.md` actual Active goal has
been PawnSim since 2026-06-12 (44 days), and the most recent five commits are
about README/CI parity, LangGraph graph generation, and inbound-message
routing, none of which touch either goal. Three accumulated suggest-comment
blocks (2026-05-25, 2026-06-11, 2026-07-03) propose a Now rewrite that was
never applied. Additionally, one frontmatter inconsistency was found
(`ta.md` has no `model:` field, unlike all 26 other subagent definitions), six
section-5-scope commits since 2026-06-01 lack the required audit-trail marker
(none qualify for the pre-2026-05-17 carry-forward exemption), and
`.claude/wb/` (94 tracked JSON files) remains an undocumented, unignored
tracked directory outside the Layers table in `docs/architecture.md`.

## Findings

- **[critical]** Audit-trail durability gap reproduces — `docs/audit/CURRENT-ALERT.md:9-10`
  Evidence: `CURRENT-ALERT.md` still reports Verdict: CRITICAL, Generated: 2026-07-10 03:06:50, linking `2026-07-10-all.md`. A directory listing of `docs/audit/` shows no report dated between 2026-07-10 and 2026-07-26 (this file). `docs/architecture.md` describes L1 (post-commit hook, roughly 30s latency), L2 (15-min poll), and L3 (daily 03:00 launchd/Task-Scheduler baseline) as active trigger layers — 16 days with zero output from any of them (or output that was never committed) means either the schedulers are not running or `scripts/audit-run.sh` line 120 (`log_info "review then git-add-and-commit to preserve"`) is still advisory-only, exactly the root cause the roadmap 2026-07-26 Done entry names as still pending operator approval (item marked as audit-trail auto-commit awaiting operator sign-off, since it changes unattended git behavior).
  Suggested fix: confirm the L1/L2/L3 schedulers are actually installed and firing on this machine via `./scripts/install-scheduler.sh status`; if firing but not committing, get explicit operator sign-off to make the audit-run.sh trail-commit non-advisory.

- **[high]** `docs/roadmap.md` Now section stale against `docs/goal.md` Active goal and the last 5 commits — `docs/roadmap.md:16-79`, `docs/goal.md:19`
  Evidence: roadmap Now (block dated "Last updated 2026-05-20") states the active goal is the multi-skill AI assistant framework, with the open subgoal being operator-side job-hunt v0.4.0 activation. `docs/goal.md` line 19 shows the actual Active goal is dated 2026-06-12, titled PawnSim colony-sim prototype (set by direct operator instruction, 44 days old as of this audit). The last five commits (b260b76, aa4825a, 23480b7, 2fcd526, f145aeb) cover inbound-message-routing contract text, a roadmap Done entry, the LangGraph graph generator, another Done entry, and a gitattributes fix — none reference job-hunt, the multi-skill framework, or PawnSim. Three suggest-comment blocks dated 2026-05-25, 2026-06-11, and 2026-07-03 each propose replacing Now and none was applied — the maintenance contract mechanism (Now is user-edited, Claude only appends suggest comments) has accumulated three unactioned proposals instead of resolving. Per operator-contract section 9, this is the exact failure mode the goal/roadmap split exists to prevent. This reproduces a finding already present in `docs/audit/CURRENT-ALERT.md` (goal.md active goal remains silently orphaned) and is explicitly left unresolved in the 2026-07-26 roadmap Done entry.
  Suggested fix: since section 9 reserves Now/Next/Blocked edits for the user, surface this directly to the operator for a one-line Now rewrite pointing at the PawnSim goal subgoals, rather than the agent silently rewriting it.

- **[low]** Six section-5-scope commits since 2026-06-01 lack the required audit-trail marker — `.claude/agents/*.md`, `agents/*.md`
  Evidence: filtering git log since 2026-06-01 against paths `.claude/agents` and `agents` markdown files returns six commits: 7be3142, 8194d51, 9559d1c, 9425a3e, 9ef5d1f, and ff7fa19. None of their commit bodies contain a Requested-by marker line. All six postdate the 2026-05-17 marker-convention cutoff commit 7c6ff4f, so none qualify for the documented carry-forward exemption in operator-contract.md section 5.
  Suggested fix: none of these need retroactive edits since the contract explicitly says a missing marker is not a build failure; going forward add the marker footer on the next section-5-scope commit, and if these six were in fact user-approved in-session, a one-time note in `docs/intervention-log.md` closes the gap cheaply.

- **[medium]** `ta.md` frontmatter omits the model field present in all 26 other subagent definitions — `.claude/agents/ta.md:1-5`
  Evidence: the frontmatter block of `ta.md` shows only name, description, and tools keys — no model key, confirmed with a raw byte check (no hidden or malformed line). Every other file under `.claude/agents/` (all 26) declares model: opus or model: sonnet. `docs/architecture.md` states the routing mechanism is that `.claude/agents/*.md` frontmatter sets the model, and `docs/for-analysts.md` roster section counts ta as one of the 27 definitions but never states its model anywhere, so there is no documented intended model for ta to compare against — it simply falls outside the mechanism the docs describe as universal.
  Suggested fix: add an explicit model line to `ta.md` (the design-QA/critique role suggests opus, matching the other judge-tier agents note-judge, still-judge, and cut-judge), and add ta to the per-agent model table in `docs/for-analysts.md` so the intended value is documented rather than inferred.

- **[medium]** `.claude/wb/` — 94 tracked files outside the documented Layers model and any gitignore rule — `docs/architecture.md:121-131` (Layers table), `.claude/wb/*.json`
  Evidence: `git ls-files .claude/wb` counts 94 tracked files. `git check-ignore -v .claude/wb` returns no match — not covered by any gitignore rule (the `.claude/` block in `.gitignore` around lines 136-148 only excludes sessions/, cache/, logs/, settings.local.json, .last-session, star.lock, statusline.sh variants, settings.json, and skills). `docs/architecture.md` Layers table (lines 121-131) enumerates `.claude/agents/`, `skills/<name>/`, the records directory, `.env`, `outputs/publish/`, `outputs/review-queue/`, and the caption-verify/metrics-dashboard evidence paths — `.claude/wb/` appears in none of these rows. Sample content (`.claude/wb/first-raid-survivable.json`) is per-task completion evidence (status, changes, result, tests_preserved, bug_firewall_check, date fields) from the PawnSim multi-agent workflow — generated output, the category hard rule 8 says belongs under the records directory (gitignored) unless given an explicit documented section-8 deviation marker, as `outputs/publish/` and `docs/caption-verify/` both have. `.claude/wb/` has neither. This reproduces the untracked-wb-directory gap already named in `docs/audit/CURRENT-ALERT.md` and left explicitly unresolved in the 2026-07-26 roadmap Done entry.
  Suggested fix: operator decides one of two paths — treat `.claude/wb/` as intentional committed evidence like `docs/caption-verify/` and add a Layers-table row plus a deviation-marker comment convention for it, or move it under the records tree and gitignore it going forward, leaving the 94 existing files as history. Either path resolves the drift; the current tracked-but-undocumented state is what is flagged.

- **[info]** Batch/wave commit messages literally titled "WIP ... (pre-verify)" — commits touching `.claude/wb/`
  Evidence: commit history for `.claude/wb/*` surfaces multiple commits with subjects such as "WIP cx wave 1 (pre-verify)" and "WIP C-001 wave 2 (pre-verify)". Operator-contract section 11 (shutdown protocol) bars WIP commits at end-of-day, but these read as an internal per-wave checkpoint convention for autonomous multi-agent batches rather than an end-of-day dirty-tree state, so this is not scored as a rule violation — noting it only because the convention is not documented anywhere and a future contract read could misclassify it as a section-11 violation.
  Suggested fix: if this wave-checkpoint pattern is intentional, a one-line note in `docs/worktree-workflow.md` or the PawnSim skill docs would pre-empt future audit cycles re-raising it.

## Drift between docs and code

- `docs/roadmap.md` Now section vs. `docs/goal.md` Active goal — see the [high] finding above.
- `.claude/wb/` present in the repo but absent from `docs/architecture.md` Layers table — see the [medium] finding above.
- `ta.md` missing from any documented model-assignment source (`for-analysts.md` table only covers the core 6) — see the [medium] finding above.

## Stale items

- `docs/audit/CURRENT-ALERT.md` — 16 days old (2026-07-10 to 2026-07-26), see the [critical] finding.
- `docs/roadmap.md` Now block — 67 days old (2026-05-20 to 2026-07-26) relative to its own Last-updated stamp, see the [high] finding.
- Three unactioned suggest-comment blocks in `docs/roadmap.md` (2026-05-25, 2026-06-11, 2026-07-03) proposing a Now rewrite that was never applied.

## Compliance with operator-contract.md

- Section 1, agent does all the work — no violations observed in this pass; not evaluable from a static repo read.
- Section 2, never pause unless told — not evaluable from a static repo read.
- Section 3, money firewall — `config/policies.yaml` line 42 sets budget_usd_ceiling to 5.00, matching the ceiling documented in `docs/for-analysts.md`; no drift.
- Section 4, auto-approve local resources — not evaluable from a static repo read.
- Section 5, logic changes require explicit OK — fails for 6 commits; see the [low] finding.
- Section 6, git workflow and gitignore — passes. The records directory and `.env` are correctly gitignored; the working tree is clean at HEAD; no accidental artifacts (mp4, wav, jpg, png, etc.) are tracked under `agents/` or `scripts/`.
- Section 7, split commit and push — not evaluable from a static repo read.
- Section 8, code and data separation — partial fail. The documented hardcoded-path exception registry is fully compliant — all eight listed files carry the required exception comment. `.claude/wb/` is an undocumented deviation from the separation principle; see the [medium] finding.
- Section 9, goal and roadmap as source of truth — fails; see the [high] finding.
- Section 10, session-resume protocol — not evaluable from a static repo read.
- Section 11, shutdown protocol — not evaluable from a static repo read; see the [info] note on WIP commit naming, which is not scored as a violation.
- Section 12, no PII or secrets — passes. A targeted grep for credential-shaped literals outside docs, example files, and markdown returned only code identifiers (token, access_token), no literal secret values. `.env` is gitignored and `.env.example` is placeholder-only.
- Section 13, inbound-message routing check — newly added this session (commit b260b76); no commits since to evaluate compliance against.

## Next audit hint

Both open high-severity items (audit-trail durability, roadmap-versus-goal drift) have now reproduced across many consecutive cycles with an identical remediation blocked on an operator decision — the next audit should check whether either was actually acted on (schedulers re-verified running and a Now rewrite landed) rather than re-deriving the same evidence again; if still unresolved, consider escalating the roadmap-versus-goal drift to critical given its 67-day age.
