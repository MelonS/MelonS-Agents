# Audit report — 2026-07-09 (all)

**Verdict**: CRITICAL

## Summary

Full-scope audit at HEAD `5b04cd4` (2026-07-08 05:54:28 +0900), run via the
L3-equivalent `focus=all` invocation whose own log line (`records/audit-scheduler.log`,
"writing to: .../docs/audit/2026-07-09-all.md" at 03:00:02) matches this
session's output path — this report *is* today's scheduled cycle completing,
not a separate manual run. All six audit dimensions were walked: architecture-
code drift, roadmap freshness, operator-contract compliance, cost-model
accuracy, stale TODOs/dead code, and secrets/security. The precomputed
skill-drift report (1 medium finding, job-hunt) was re-derived to the same
root cause identified in the prior two cycles (CRLF contamination, not a
real missing file).

Headline result: the critical audit-trail-durability finding carried in the
last four-plus cycles (2026-07-05, 07-06, 07-08 contract, 07-08 all)
reproduces live for at least a fifth distinct cycle. At session start,
`git status --porcelain` showed a modified `docs/audit/CURRENT-ALERT.md`
and two untracked reports (`docs/audit/2026-07-08-all.md`,
`docs/audit/2026-07-08-contract.md`) sitting in the working tree - three
files that were themselves the deliverable of the immediately-prior audit
cycle and have sat uncommitted for at least a full day. `scripts/audit-run.sh`
still contains exactly one reference to `git commit` at line 120, an
advisory `log_info` string, never an executed command. Separately, this
cycle's own smooth completion (a normal ~03:00 to report-ready run, same
window as the successful 07-08 cycle) stands in contrast to the four
consecutive complete failures immediately before it: no `docs/audit/2026-07-04-all.md`
through `2026-07-07-all.md` exist on disk despite four scheduled attempts,
confirming the L3-hang failure mode from the 07-08 cycle was real for that
window and has not been fixed in code (no timeout wrapper exists in
`scripts/audit-run.sh` today either) - it simply did not recur today.

Independent re-verification of every carried-over finding from the 07-08
cycles: all reproduce unchanged (subagent-roster count, `refactor_check.py`
hardcoded paths, roadmap Done-gap growth, `docs/cost-model.md`
self-contradiction, the job-hunt CRLF/`.gitattributes` gap, Section-5 marker
non-adoption, `goal.md` orphaned-goal silence, `for-analysts.md` stale line
citation, undocumented-scripts list). Five findings are new to this cycle,
none raised in either 07-08 report: (1) three more scripts shipped in the
same 2026-07-06 commit as the YouTube-analytics tooling ship
(`yt-analytics-query.py`, `yt-scoreboard.py`, `yt-stats-sample.sh`) hardcode
Windows-drive-letter config paths with zero env-var fallback - the same
severity class as `refactor_check.py`, shipped on the very day that file was
first flagged; (2) six more files (the ElevenLabs/Typecast TTS trio plus
`agents/lib/tts.sh`, `yt-analytics-auth.py`) hardcode fallback defaults
in the acceptable env-first pattern but are unregistered in the Section-8
exception registry, the same gap already open for `product-hero.sh`; (3)
`.env.example`, the tracked schema file operator-contract Section 8 names
as the canonical place secrets are documented, has zero entries for any of
`ELEVENLABS_API_KEY`, `ELEVENLABS_KEY_FILE`, `TYPECAST_API_KEY`,
`TYPECAST_KEY_FILE`, or `YT_CONFIG_DIR`, all required by shipped scripts;
(4) `docs/cost-model.md` and `docs/for-analysts.md` - the two canonical
"where the money is" documents - contain zero mention of ElevenLabs or
Typecast anywhere, even though both are paid, credit-metered APIs
documented elsewhere (`docs/elevenlabs-tts-notes.md`,
`docs/generative-shorts-pipeline.md`) as the primary narration/BGM
backend for the generative-shorts production line, making the
`for-analysts.md` claim "Total mission-execution API cost: 0" stale for
that pipeline; (5) 94 files under `.claude/wb/*.json` plus
`.claude/whiteboard.json` (95 total) are tracked in git, read as per-lane
multi-agent task/session working-state (status, output paths, QA flags),
and are mentioned nowhere in any doc - directly contradicting
operator-contract Section 8's stated "Final state: `.claude/` is fully
gitignored" end-state.

## Findings

- **[critical]** Audit-trail durability gap reproduces live for at least a
  fifth distinct cycle, root cause still unfixed in code -- `scripts/audit-run.sh:120`,
  `docs/audit/CURRENT-ALERT.md`, `docs/audit/2026-07-08-all.md`,
  `docs/audit/2026-07-08-contract.md`.
  Evidence: `git status --porcelain` at session start showed:
  `M docs/audit/CURRENT-ALERT.md`, `?? docs/audit/2026-07-08-all.md`,
  `?? docs/audit/2026-07-08-contract.md`.
  These three files are the full deliverable of the prior (07-08) audit
  cycle and have been sitting uncommitted since that cycle ended. A search
  for `git add`, `git commit`, `git push` across `scripts/audit-run.sh`,
  `scripts/hooks/post-commit.sh`, `scripts/audit-poll.sh` still returns
  exactly one hit -- `scripts/audit-run.sh:120`,
  the line `log_info "review then git-add-and-commit to preserve"`
  -- advisory text, never executed. This is the identical root cause the
  2026-07-05, 2026-07-06, and both 2026-07-08 cycles rated critical; commit
  `52fda03` (2026-07-06) manually cleared that cycle's backlog without
  adding the missing commit step, so the same state has now recurred at
  least three more times. Separately: `records/audit-scheduler.log` shows
  four complete failures immediately before this cycle
  (2026-07-04 through 2026-07-07, each only a "writing to" line with no
  file ever landing) -- this cycle itself completed normally (comparable
  timing to the successful 07-08 run), so the L3-hang class of failure did
  not recur today, but nothing in `scripts/audit-run.sh` explains why it
  didn't, meaning the underlying hang-without-timeout defect (no timeout
  wrapper around the claude-agent-auditor invocation) is unaddressed
  and could resume on any future run.
  Suggested fix: add an unconditional commit-and-push step at the end of
  `scripts/audit-run.sh` after `CURRENT-ALERT.md` is finalized (docs/audit/
  is always safe to auto-commit per the Section 6 scope list, no
  human-in-the-loop gate needed), and wrap the auditor invocation in a
  timeout so a future hang fails loudly instead of silently, per the
  standing suggestion from the last three cycles.

- **[high]** `docs/roadmap.md` Done-section citation gap continues to widen
  every cycle it is checked, now 30 commits -- `docs/roadmap.md:229`.
  Evidence: the last Done entry citing a real hash still references
  `2a94adf` (line 229, dated 2026-07-03). `git log --oneline 2a94adf..HEAD`
  returns 30 commits with zero Done entries, up from 27 at the 07-08
  contract cycle six hours prior, 25 the cycle before that, 21 before that
  -- the gap has grown on every single measurement across at least five
  consecutive audit cycles. `scripts/roadmap-done-sync.sh` exists
  specifically for this and has not been run since the 2026-05-22
  bulk-backfill batches.
  Suggested fix: run `scripts/roadmap-done-sync.sh --apply` covering
  `2a94adf..HEAD`; given five-plus consecutive cycles of zero remediation,
  strongly consider wiring it to run automatically post-commit rather than
  relying on manual invocation.

- **[high]** `docs/goal.md` active goal remains silently orphaned, now 24
  days, with no self-correcting suggest-comment in the file itself --
  `docs/goal.md:19`.
  Evidence: the Active goal section (line 19, dated 2026-06-12, PawnSim
  prototype) has had zero corresponding gameplay commits since `03056e9`
  (2026-06-15 18:49:46, animal animation parity fix). A history search
  scoped to `skills/game-prototype/` and `docs/goal.md` since 2026-06-14
  shows only two later commits (`4e7dd7b`, `9ef5d1f`, both 2026-07-01) and
  both are doc-genericization/IP-cleanup only, no gameplay work.
  `grep -n suggest docs/goal.md` returns zero hits -- the file still
  contains no proposal acknowledging the gap, unchanged from the 07-08
  cycle's finding (then 23 days; now 24 and counting). The only
  acknowledgment anywhere remains a `docs/roadmap.md` Next-section
  suggest-comment (not Now, not in goal.md), dated 2026-07-03, now six days
  stale relative to work that has landed since (ElevenLabs primary-backend
  adoption, Typecast backup, news-shorts fact-check hardening, a new
  shorts-production handoff doc, a new multi-machine-portability Core rule
  in CLAUDE.md) -- none of which the stale suggest-comment mentions either.
  Suggested fix: append a suggest-comment to `docs/goal.md` itself (not just
  `roadmap.md`), proposing either migrating PawnSim to Past goals with an
  honest paused-not-achieved status, or promoting content-shorts/generative-
  shorts production to the active goal, and let the operator confirm at the
  next session start.

- **[high]** Subagent roster count is wrong in two independent docs,
  unchanged across at least four cycles -- `docs/for-analysts.md:93-98`,
  `README.md:29`.
  Evidence: `.claude/agents/*.md` currently contains 26 files (verified by
  directory listing). `docs/for-analysts.md:94` still claims "22
  definitions total" (its own stated breakdown -- 6 core plus 12 game-star
  plus 5 content-shorts -- sums to 23, not 22, a pre-existing internal
  arithmetic error). `README.md:29`'s hero-stats alt text independently
  claims "23 subagents". Three judge agents (`cut-judge.md`,
  `note-judge.md`, `still-judge.md`) remain entirely unmentioned in the
  subagent-layout table.
  Suggested fix: update `docs/for-analysts.md`'s table to 26 with a
  judge-agents row, and regenerate `docs/visuals/01-hero-stats.png` plus
  its README alt text.

- **[high]** `skills/game-dev-agent/scripts/refactor_check.py` still
  hardcodes machine-specific absolute paths, including the operator account
  name, with zero env-var fallback -- unchanged since first flagged
  2026-07-06 -- around lines 27-31, 182, 247, 308.
  Evidence: `BASELINE`, `PLAYER_LOG`, `SCENE_LOG`, `BUILD_LOG` (lines
  27-31), `REAL_QA_SHOT` (182), `QA_LOG` (247), and `report_path` (308) are
  all literal Path assignments with no `os.environ.get` anywhere in
  the file. `PLAYER_LOG` embeds `C:/Users/comdo/AppData/LocalLow/...` --
  a personal Windows account name in a tracked public-repo file. No
  section-8-exception comment exists in the file, and it is absent from
  the `docs/operator-contract.md` Section 8 registry.
  Suggested fix: introduce env-var overrides (e.g. `REFACTOR_BASELINE_DIR`,
  `GAME_PLAYER_LOG`) with current values as fallback defaults, replace the
  literal account name with a `$USERNAME` resolution, and either register
  the file with a section-8-exception comment or remove the hardcoding.

- **[high]** Three scripts shipped in the same commit as the YouTube-
  analytics tooling ship hardcode Windows-drive-letter config paths with
  zero env-var fallback -- the same severity class as `refactor_check.py`,
  and shipped on the exact day that file was first flagged --
  `scripts/yt-analytics-query.py:7`, `scripts/yt-scoreboard.py:6`,
  `scripts/yt-stats-sample.sh:6-7`.
  Evidence: `git log -1 --format='%h %ci %s'` for all three points to
  commit `6da5a18`, 2026-07-06 13:43:09 +0900, "Add YouTube analytics
  tooling and ElevenLabs SFX generator". `yt-analytics-query.py:7` and
  `yt-scoreboard.py:6` both read `CONF = Path("G:/config/youtubeuploader")`
  with no environment-variable check anywhere in either file.
  `yt-stats-sample.sh:6` reads two hardcoded paths for CFG and TOK under
  `/g/config/youtubeuploader/`, and line 7 falls back to a second
  hardcoded path for jq, `/g/tools/jq/jq`, also with no env override.
  None of the three carries a section-8-exception comment or a registry
  entry. This is a strictly worse pattern than the sibling script shipped
  the same day, `yt-analytics-auth.py`, which does read `YT_CONFIG_DIR`
  first (see medium finding below) -- these three simply never got that
  treatment.
  Suggested fix: introduce a shared `YT_CONFIG_DIR` env-var read (matching
  `yt-analytics-auth.py`'s existing pattern) in all three files, falling
  back to the current hardcoded values, then register all four
  YouTube-analytics scripts together as one Section-8 registry entry.

- **[medium]** Seven files carry hardcoded machine-specific fallback
  defaults using the acceptable env-first pattern but remain unregistered
  in the Section-8 exception registry -- `skills/product-cf/scripts/product-hero.sh:48`
  (carried from 07-08), plus six new to this cycle:
  `scripts/yt-analytics-auth.py:24`, `scripts/elevenlabs-tts.py:40-45`,
  `scripts/elevenlabs-music.py:11-13`, `scripts/elevenlabs-sfx.py:8-10`,
  `scripts/typecast-tts.py:47-52`, `agents/lib/tts.sh:80-97`.
  Evidence: each resolves an env var first (`PRODUCT_CF_DEPTH_VENV`,
  `YT_CONFIG_DIR`, `ELEVENLABS_API_KEY`/`ELEVENLABS_KEY_FILE`,
  `TYPECAST_API_KEY`/`TYPECAST_KEY_FILE` respectively) before falling back
  to a literal absolute path (`/Users/melons/ai/.venv`,
  `G:/config/youtubeuploader`, `/g/config/elevenlabs/api.key` plus
  `G:/config/elevenlabs/api.key`, `/g/config/typecast/api.key` plus
  `G:/config/typecast/api.key`) -- the pattern operator-contract Section 8
  treats as acceptable -- but none carries a section-8-exception comment
  and none appears in the registry in `docs/operator-contract.md`. The
  registry now covers roughly 10 files while at least 7 known qualifying
  files sit outside it.
  Suggested fix: add inline section-8-exception comments to all seven and
  a single batch of registry entries in `docs/operator-contract.md`
  Section 8, grouped by feature area (YouTube analytics; ElevenLabs and
  Typecast TTS) to keep the registry from growing one line at a time every
  cycle.

- **[medium]** `docs/cost-model.md` self-contradicts on subagent model
  tiering, a leftover the 2026-06-05 model-drift fix missed -- unchanged
  since 07-08 -- `docs/cost-model.md:178` versus `docs/cost-model.md:20-24`.
  Evidence: lines 20-24 correctly list orchestrator/planner/resourcer on
  opus and editor/qa on sonnet, matching current `.claude/agents/*.md`
  frontmatter (independently re-verified this cycle: `planner.md` and
  `resourcer.md` both declare `model: opus`). Line 178 still asserts
  "Sonnet for subagents, Opus only for orchestrator."
  Suggested fix: rewrite `docs/cost-model.md:178` to match lines 20-24.

- **[medium]** Precomputed skill-drift finding (job-hunt, "manifest
  references missing script") remains a CRLF parsing artifact, not a real
  missing file -- unchanged since 07-08 -- `skills/job-hunt/config/activation.tsv`,
  `.gitattributes`.
  Evidence: `.gitattributes` (read in full this cycle) has explicit
  `eol=lf` rules for `.sh`, `.py`, `.yaml`, `.yml`, `.json`, `.md`, and
  several other extensions, but no rule at all for `.tsv` -- it falls
  through to the generic `text=auto` default, which does not force LF.
  The tracked `skills/job-hunt/config/activation.tsv` still carries CRLF
  endings including a lone-CR blank separator line that both
  `scripts/audit-skill-drift.sh` and `skills/job-hunt/scripts/status.sh`
  mis-parse as non-blank, producing this cycle's precomputed "manifest
  references missing script" finding with a blank subject field.
  Suggested fix: add a `*.tsv text eol=lf` rule to `.gitattributes`
  (scoped to `skills/*/config/` or repo-wide), renormalize the tracked
  file, and harden both consumers to strip a trailing carriage return
  before the blank-line test.

- **[medium]** `.env.example` -- the tracked schema file operator-contract
  Section 8 names as canonical -- has zero entries for any of the five
  environment variables that gate the shipped ElevenLabs/Typecast/YouTube-
  analytics scripts -- `.env.example` (no matching lines).
  Evidence: a search for ELEVENLABS, TYPECAST, YT_CONFIG_DIR, and
  youtubeuploader in `.env.example` returns nothing, while
  `scripts/elevenlabs-tts.py`, `scripts/elevenlabs-music.py`,
  `scripts/elevenlabs-sfx.py`, and `scripts/typecast-tts.py` all read
  `ELEVENLABS_API_KEY` / `ELEVENLABS_KEY_FILE` / `TYPECAST_API_KEY` /
  `TYPECAST_KEY_FILE`, and `scripts/yt-analytics-auth.py` reads
  `YT_CONFIG_DIR`. `.env.example`'s own header states its purpose is to be
  the copy-to-.env schema template; none of these five newer secret
  locations are documented there, only in scattered per-tool doc files
  (`docs/elevenlabs-tts-notes.md`, `docs/typecast-tts-notes.md`,
  `docs/shorts-production-handoff.md`).
  Suggested fix: add a commented-out block to `.env.example` for the five
  vars (placeholder-only, matching the existing style), pointing at the
  per-tool notes docs for full setup instructions.

- **[medium]** The two canonical "where the money is" documents omit the
  ElevenLabs/Typecast paid-API narration path entirely, making the
  headline "zero mission-execution API cost" claim stale for the
  generative-shorts production line -- `docs/for-analysts.md:271`,
  `docs/cost-model.md` (no mention anywhere).
  Evidence: a search for ElevenLabs or Typecast across `docs/cost-model.md`,
  `docs/for-analysts.md`, `docs/architecture.md`, and
  `docs/operator-contract.md` returns nothing. Yet
  `docs/generative-shorts-pipeline.md:20-21` documents ElevenLabs v3 as the
  pipeline's TTS stage and `elevenlabs-music.py` as the BGM stage, and
  `docs/elevenlabs-tts-notes.md:4` describes ElevenLabs as the default
  engine within the opt-in `FACELESS_TTS_PLAN` path -- commit messages
  `1146eb6` ("make ElevenLabs v3 the primary emotion-directed narration
  backend") and `4e56b21` ("adopt Typecast as emotion-directed narration
  backend") confirm this is real, recurring production usage, not a
  one-off experiment. Both ElevenLabs and Typecast are credit/character-
  metered paid APIs (`generative-shorts-pipeline.md:58`: "900cr per
  minute"). This is activated only via the opt-in `FACELESS_TTS_PLAN` env
  var (so it does not violate the money-firewall's opt-in-confirmation
  mechanism -- the operational gate is separately documented in
  `docs/shorts-production-handoff.md`), but `docs/for-analysts.md:271`'s
  "Total mission-execution API cost: 0" and `docs/cost-model.md`'s entire
  "where the money is" section still read as if no such path exists, which
  is the exact analyst-facing failure mode dimension 4 of this audit checks
  for.
  Suggested fix: add a "Tier 1 (opt-in) within Tier 2" row for ElevenLabs
  and Typecast to `docs/cost-model.md` and `docs/for-analysts.md`, parallel
  to the existing `FACELESS_SCRIPT_OVERRIDE` opt-in-Sonnet row, and soften
  the "cost: 0" claim to name the two documented opt-in exceptions.

- **[medium]** 95 files under `.claude/` are tracked in git as apparent
  per-lane multi-agent session/task working-state, contradicting
  operator-contract Section 8's stated end-state that `.claude/` becomes
  fully gitignored, and are undocumented anywhere -- `.claude/wb/*.json`
  (94 files), `.claude/whiteboard.json`.
  Evidence: `git ls-files .claude/wb/ | wc -l` returns 94; `.gitignore` has
  no rule for `.claude/wb` or `whiteboard`. A sample file
  (`.claude/wb/gizmo-bar.json`) contains per-lane task bookkeeping -- role,
  lane ID, task description, status "Done", output file paths, a
  lane-compliance self-report, and QA flags -- for a specific multi-agent
  game-dev work session, i.e. session/mission working-state rather than
  reusable agent logic. `git log --oneline -- .claude/wb/ | wc -l` shows 38
  commits touching this path, most recently `9ef5d1f` (2026-07-01).
  `docs/operator-contract.md` around lines 313-317 states as the target
  end-state: "`.claude/` becomes truly local over time... Final state:
  `.claude/` is fully gitignored." A search of every `docs/*.md` and
  `skills/game-dev-agent/ARCHITECTURE.md` for "wb/" or "whiteboard" finds
  no mention of this convention anywhere.
  Suggested fix: either move this working-state to $RECORDS_DIR (matching
  the code/data separation principle, since it reads as session output
  rather than logic) or, if the operator intends `.claude/wb/` to remain a
  tracked coordination convention, document it explicitly in
  `skills/game-dev-agent/ARCHITECTURE.md` and reconcile the "final state:
  fully gitignored" line in `docs/operator-contract.md` Section 8 to carve
  out this exception.

- **[low]** Operator-contract Section 5 marker adoption remains 0 of 10
  qualifying commits, count stable since no new qualifying commit landed
  this cycle -- `.claude/agents/*.md` and `agents/*.md` commit history
  since 2026-05-17.
  Evidence: commits touching `.claude/agents/*.md` or `agents/*.md` between
  the last audit's HEAD (`7f089ce`) and this cycle's HEAD return nothing --
  the three commits at HEAD since the last audit cycle (`75f5183`,
  `ae4ec3f`, `5b04cd4`) touch only docs files, outside this rule's scope.
  The count of 10 qualifying commits with zero markers is unchanged from
  the 07-08 cycle. Per the contract's own calibration this is explicitly
  low, not a build failure.
  Suggested fix: none urgent; start using the marker on the next qualifying
  commit if the convention is meant to be load-bearing.

- **[low]** `docs/for-analysts.md:152` cites a stale line number for the
  faceless-short ollama call -- versus
  `agents/missions/faceless-short/run.sh:100`.
  Evidence: unchanged from the last two cycles -- the doc still says line
  98; the actual call site is line 100.
  Suggested fix: drop the specific line number and cite the function or
  variable name instead, consistent with the Section-8 registry's own
  no-line-numbers convention.

- **[info]** Eight recently-shipped scripts (plus several test-*.sh
  companions) remain undocumented in any `docs/*.md` or `README.md`,
  unchanged from the 07-08 cycle -- `scripts/elevenlabs-sfx.py`,
  `scripts/esrgan-upscale.py`, `scripts/generate-quality-trend-chart.py`,
  `scripts/mix3-rhythm.py`, `scripts/wan-flf2v.py`,
  `scripts/yt-analytics-auth.py`, `scripts/yt-analytics-query.py`,
  `scripts/yt-stats-sample.sh`.
  Evidence: a basename sweep of every scripts/*.py and scripts/*.sh file
  against every tracked docs/*.md (recursive) and README*.md file found no
  reference for these eight (`scripts/yt-scoreboard.py`,
  `scripts/elevenlabs-tts.py`, `scripts/elevenlabs-music.py`, and
  `scripts/typecast-tts.py` were flagged undocumented in an earlier, buggy
  glob pass this cycle but are in fact documented in
  `docs/new-channel-setup.md`, `docs/shorts-production-handoff.md`,
  `docs/elevenlabs-tts-notes.md`, and `docs/typecast-tts-notes.md` once the
  pathspec bug was fixed, so they are correctly excluded here). Per the
  README-maintenance-cadence Convention, shipping new capability scripts is
  one of the four triggers for a README/docs pass; none has landed yet for
  this batch.
  Suggested fix: fold into the next docs pass alongside the subagent-roster
  and cost-model fixes above.

- **[info]** No `docs/daily/<date>.md` shutdown report has been written in
  six weeks -- `docs/daily/2026-05-27-colony-sim-vanilla-day14-26.md` is
  still the most recent entry.
  Evidence: a listing of `docs/daily/` shows nothing past 2026-05-27,
  despite five-plus weeks of subsequent commits including two full
  production pivots (music-video quality-bar to Windows/RTX hardware pivot
  to content-shorts/generative-shorts). Operator-contract Section 11 only
  triggers on an explicit end-of-day phrase from the user, so this is not a
  rule violation, but it does mean the session-resume briefing (Section 10)
  has had no fresh daily-report anchor to read from in that entire window.
  Suggested fix: none required by contract; noted for visibility only, as
  in the 07-08 cycle.

## Drift between docs and code

- `docs/for-analysts.md` (22, internally inconsistent with its own 6+12+5
  breakdown) versus `README.md` (23) versus the live `.claude/agents/*.md`
  count (26) -- three-way mismatch, unchanged across at least five cycles.
- `docs/cost-model.md:178` contradicts `docs/cost-model.md:20-24` on which
  subagents run opus versus sonnet.
- `docs/roadmap.md` Done section is 30 commits behind HEAD, widening every
  cycle it has been measured: 20 to 21 to 25 to 27 to 30 across five
  cycles in under a week.
- `docs/goal.md` active goal (PawnSim) has had zero related commits in 24
  days while `docs/roadmap.md` Now (job-hunt) has had zero related commits
  in roughly seven-plus weeks; the roadmap's own 2026-07-03 stopgap
  suggest-comment proposing a content-shorts pivot is itself six days stale
  relative to the ElevenLabs-primary-backend, Typecast, and
  shorts-production-handoff work that has landed since -- three successive
  layers of staleness on the same section.
- `skills/job-hunt/config/activation.tsv` (CRLF at checkout) versus its two
  consumers, `scripts/audit-skill-drift.sh` and `status.sh`, both of which
  assume LF.
- `docs/cost-model.md` and `docs/for-analysts.md` "where the money is"
  claims omit the ElevenLabs/Typecast opt-in paid path entirely, despite it
  being documented as the primary backend elsewhere in the repo.
- `.env.example` schema is missing five env vars that five shipped scripts
  actually read.
- `docs/operator-contract.md` Section 8's stated "final state: `.claude/`
  is fully gitignored" versus 95 tracked files under `.claude/wb/` plus
  `.claude/whiteboard.json`.
- `docs/audit/CURRENT-ALERT.md`, generated 2026-07-08 04:30:55 pointing at
  `2026-07-08-contract.md`, is now itself one cycle stale relative to this
  report (same root critical finding, but the file's own content predates
  this cycle's re-verification).

## Stale items

- `docs/audit/CURRENT-ALERT.md` (modified, uncommitted) and
  `docs/audit/2026-07-08-all.md` plus `docs/audit/2026-07-08-contract.md`
  (both untracked) -- all three sitting in the working tree at session
  start; this report will join them once this session ends, per the
  auditor's own read-only mandate. This is now the norm rather than the
  exception across at least five consecutive cycles.
- `docs/roadmap.md` Done section -- 30 uncited commits, growing every cycle
  without exception since first measured.
- `docs/goal.md` Active goal (PawnSim, dated 2026-06-12) -- unworked for 24
  days, no suggest-comment in the file itself.
- `docs/roadmap.md` "Now -- active focus" block -- still dated
  2026-05-20/2026-05-22 (job-hunt); its own 2026-07-03 suggest-comment
  proposing a content-shorts pivot is itself six days stale.
- Section 8 exception registry -- at least 8 files now known unregistered
  (1 zero-fallback high-severity carried over plus 3 new zero-fallback,
  plus 1 carried-over medium plus 6 new medium), none fixed since first
  flagged.
- Section 5 marker -- 10 qualifying commits, zero adoption, unchanged.
- Subagent roster count docs (`for-analysts.md`, `README.md`) -- stale
  against a live count that has held at 26 since 2026-07-05.
- `docs/daily/` -- no entry since 2026-05-27.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| Section 1 Agent does all the work | PASS | Behavioral; not code-checkable from static state |
| Section 2 Never pause unless told | PASS | Behavioral; not code-checkable |
| Section 3 Money firewall | PASS | config/policies.yaml budget_usd_ceiling 5.00; ElevenLabs/Typecast paid calls are opt-in via FACELESS_TTS_PLAN, with the confirmation gate documented in docs/shorts-production-handoff.md; no unconditional paid-API call found in any mission run.sh |
| Section 4 Auto-approve mode | PASS | .claude/settings.json confirmed gitignored and untracked |
| Section 5 Logic changes require explicit user OK (marker) | LOW, stable | 10 of 10 qualifying commits since 2026-05-17 lack the audit-trail marker |
| Section 6 Git workflow, gitignore coverage | PASS, one caveat | records/, secrets, and per-operator personal-context files all correctly excluded; caveat -- .claude/wb/* tracked despite Section 8's stated final-state intent, see Section 8 row below |
| Section 6 Git workflow, auto-commit and push of every doc change | CRITICAL FAIL | Reproducing for a fifth-plus cycle; root cause (no commit step in audit-run.sh) still unfixed |
| Section 7 Split commit and push | PASS | No compound commit-and-push found in any tracked shell script |
| Section 8 Code/data separation, committed artifacts | PASS | No stray media or output files found under agents/ or scripts/ |
| Section 8 Code/data separation, hardcoded paths with no fallback | HIGH FAIL | refactor_check.py (carried) plus yt-analytics-query.py, yt-scoreboard.py, yt-stats-sample.sh (new this cycle) |
| Section 8 Code/data separation, hardcoded paths with env fallback but unregistered | MEDIUM | 7 files now known (1 carried plus 6 new) |
| Section 8 .claude/ final-state-gitignored intent | FAIL, new this cycle | 95 files under .claude/wb/ plus .claude/whiteboard.json tracked, undocumented |
| Section 9 Goal and roadmap source of truth | HIGH FAIL | Done-gap 30 commits and growing; goal.md active goal unworked 24 days with no self-correcting proposal; roadmap Now stale on three successive layers |
| Section 10 and 11 Session-resume and shutdown protocol | INFO | Behavioral; no docs/daily/ entry since 2026-05-27; not scored since Section 11 only triggers on an explicit user phrase |
| Section 12 No PII or secrets | PASS, one caveat | Repo-wide search for key/secret/token/password patterns with real-looking values returned only placeholders and prose; .env.example and config/copyright-allowlist.yaml are schema/documentation-only. Caveat: refactor_check.py's literal operator account name, scored under Section 8 above, cross-referenced here for visibility |

## Next audit hint

Highest priority for the next cycle: confirm the three files this cycle
found sitting uncommitted (CURRENT-ALERT.md, 2026-07-08-all.md,
2026-07-08-contract.md) plus this report actually landed in a commit; if a
sixth consecutive cycle finds the audit trail itself uncommitted, the
recurrence pattern (not just the bug) should become the literal headline
finding, since the fix has now been suggested identically at least four
times without landing. Second priority: check whether either the
refactor_check.py / yt-analytics-* zero-fallback cluster or the newly
tracked .claude/wb/ files moved at all -- both are now large enough
(4 zero-fallback files; 95 tracked whiteboard files) that continued
non-response is itself worth escalating in framing.
