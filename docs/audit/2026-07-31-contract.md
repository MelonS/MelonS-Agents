# Audit report — 2026-07-31 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Focused pass over `docs/operator-contract.md` compliance at HEAD `45a23d2`
(2026-07-31 03:30 KST), cross-checked against `docs/for-analysts.md`,
`docs/architecture.md`, `docs/roadmap.md`, `docs/goal.md`, every
`.claude/agents/*.md` frontmatter, `.gitignore`, `.github/workflows/`, and
`git log`. The pre-computed skill-drift report is clean (0 findings) and is
not repeated below. `main` is green (`gh run list` shows 5/5 recent
`main-protection` runs `success`). Secret scan outside `docs/`, `*.example`,
`*.md` returned nothing. `.env` remains gitignored and untracked. No output
artifacts (mp4/wav/jpg/etc.) are committed under `agents/` or `scripts/`.
The section-8 hardcoded-path exception registry's 11 listed files all still
carry the required grep-anchor; several additional Users-shaped path strings
found under `scripts/` are all inert comments (illustrative paths, an
already-fixed-and-annotated historical reference, a stale test-fixture
comment shadowed by a REPO_ROOT-relative path in the actual code) and are
not new registry gaps.

One genuinely good-news item: the CRITICAL "audit reports generated but
never committed" finding that has now reproduced across six consecutive
audit cycles was fixed at the code level today, in-session, immediately
before this run — commit `45a23d2` adds `commit_audit_trail()` to
`scripts/audit-run.sh`, which now runs `git commit --only` scoped exactly
to `docs/audit/` (report + `CURRENT-ALERT.md`) and best-effort pushes, with
merge/rebase/lock guards and an `AUDIT_NO_COMMIT=1` escape hatch. The commit
message documents a temp-clone self-test (no-op path, new-report path,
unrelated-staged-changes preserved, idempotent re-run). This is a
`scripts/` change, not an `agents/*.md` or `.claude/agents/*.md` change, so
it is outside hard rule 5's scope and needs no Requested-by marker; it is
downgraded from the standing CRITICAL to informational below pending one
live, unattended trigger cycle (L1/L2/L3) actually exercising the new
commit path, since every test cited in the commit message was run
interactively in a temp clone rather than through the real
hook/launchd/Task-Scheduler path.

Two problems remain live from the 2026-07-26 report, both still unresolved
five days later: `docs/roadmap.md` "Now" (own last-updated stamp:
2026-05-20) still frames the active work as multi-skill-framework /
job-hunt-v0.4.0-activation, while `docs/goal.md`'s actual Active goal has
been PawnSim since 2026-06-12 (49 days) and the most recent five commits are
about a duplicate-upload incident fix, a shorts scene-layout mode, and the
audit-trail self-commit fix — none of which touch either the stated Now or
the actual goal; and `.claude/wb/` (94 tracked JSON files, last touched
2026-07-01) remains outside `docs/architecture.md`'s Layers table with no
gitignore rule and no section-8 deviation marker. `ta.md` also still has no
`model:` field, unlike the other 26 subagent definitions, and the same six
section-5-scope commits from 2026-06-01 through 2026-07-24 still lack the
Requested-by marker (no new section-5-scope commits landed since the last
audit, so the set is unchanged, not growing). One new finding this cycle:
`README.md`/`README.ko.md` are now self-inconsistent — the hero-stats image
plus alt text (line 31, both files) still claims 23 subagents while the
prose two paragraphs down (line 177 EN) correctly says 27 — and
`for-analysts.md` undercounts the CI gate by one (it says "six static
checks"; the workflow has had seven since `06dd751` on 2026-07-01, when the
README EN-KO parity check was added).

## Findings

- **[high]** `docs/roadmap.md` "Now" section stale against `docs/goal.md` Active goal and the last 5 commits, unresolved 2nd consecutive audit cycle — `docs/roadmap.md:16-50`, `docs/goal.md:19`
  Evidence: roadmap "Now" (own timestamp "Last updated 2026-05-20 ~19:55 KST", line 18) states the Active goal is the Multi-skill AI assistant framework with an Active subgoal of operator-activation of job-hunt v0.4.0. `docs/goal.md` line 19 shows the real Active goal has been "2026-06-12 | PawnSim" for 49 days. The last 5 commits (`45a23d2`, `b8f7d26`, `511e0ec`, `3035962`, `884ccbd`) are audit-trail tooling and a content-shorts publish-hardrule/incident fix — none reference PawnSim or job-hunt. Three unactioned suggest blocks (2026-05-25, 2026-06-11, 2026-07-03) already propose a Now rewrite; none has been applied by the operator. This exact drift was flagged high in `2026-07-26-contract.md` and has not moved.
  Suggested fix: per contract section 9, Claude may only append a suggest block, not silently rewrite Now — the fix here is an operator decision to accept one of the three pending suggest blocks (or write a fresh Now) reflecting the PawnSim/content-shorts reality; until that lands, treat this as a standing, operator-owned item rather than something an agent session can clear unilaterally.

- **[medium]** README hero-stats image and alt text still claim 23 subagents while the body prose (same file) correctly says 27 — `README.md:31` vs `README.md:177`; `README.ko.md:31` mirrors the same drift
  Evidence: `README.md:31` alt text reads "...23 shaders...23 subagents...", pointing at `docs/visuals/01-hero-stats.png`. `README.md:177` states ".claude/agents/ holds 27 definitions". `docs/visuals/01-hero-stats.png` was last regenerated 2026-07-01 (`ad9825b`, subject "regenerate hero + game-roster for 23-agent roster") — before `7be3142` (2026-07-24) added `ta.md`, which is what brought the roster from 26 to 27 (matches `for-analysts.md`'s own count: 6 core + 13 game-line + 5 content-shorts + 3 judges = 27). The 2026-07-26 roadmap Done entry for the README overhaul already names this exact gap as remaining work — it is unresolved five days later. `README.ko.md:31` carries the identical Korean-language drift (23 subagents) against a body that should also read 27.
  Suggested fix: regenerate `docs/visuals/01-hero-stats.png` / `-ko.png` and their alt text with the current count (27), sourced by re-running whatever generator produced them against the live `.claude/agents/*.md` file count rather than a remembered number, so the next roster change doesn't require a manual find-and-fix.

- **[medium]** `for-analysts.md` undercounts the CI gate by one check — `docs/for-analysts.md:291-298` vs `.github/workflows/main-protection.yml`
  Evidence: `for-analysts.md:294` says "six static checks — bash syntax on tracked scripts, secret scan, required-files presence, .env.example schema sanity, README link hygiene, gitignore coverage." `.github/workflows/main-protection.yml` currently has seven named steps beyond Checkout: Bash syntax check, Secret pattern scan, Required files present, .env.example sanity, README internal links resolve, README EN-KO parity, gitignore covers sensitive paths. The README EN-KO parity step was added by `06dd751` (2026-07-01) — a full month before this audit — and the prose was never updated to mention it or bump the count to seven.
  Suggested fix: update the sentence in `for-analysts.md` to list all seven checks (add "README EN-KO parity") and say "seven static checks."

- **[medium]** `.claude/wb/` — 94 tracked files still outside the documented Layers model and any gitignore rule, unresolved 2nd consecutive audit cycle — `docs/architecture.md:121-131` (Layers table), `.claude/wb/*.json`
  Evidence: `git ls-files .claude/wb | wc -l` returns 94. `git check-ignore -v .claude/wb` returns no match. `docs/architecture.md`'s Layers table (lines 121-131) enumerates `.claude/agents/`, `skills/<name>/`, `$RECORDS_DIR/`, `.env`, `outputs/publish/`, `outputs/review-queue/`, and the caption-verify/metrics-dashboard evidence paths — `.claude/wb/` is in none of them. Sample content (`.claude/wb/action-sfx-3.json`) is per-task multi-agent coordination state (role/task/milestone/status/date/wave fields) from the PawnSim game-dev workflow — the same category hard rule 8 says belongs under `$RECORDS_DIR` (gitignored) absent an explicit documented deviation marker like `outputs/publish/`'s. Last file touch was 2026-07-01 (`9ef5d1f`), 30 days stale relative to today. `docs/roadmap.md:286` itself still lists ".claude/wb/ tracking status" as an explicitly unresolved item from the 2026-07-26 pass.
  Suggested fix: unchanged from the prior audit — operator picks one of two paths: (a) treat it as intentional committed evidence and add a Layers-table row plus a deviation-marker convention (mirroring outputs/publish/), or (b) migrate it under $RECORDS_DIR and gitignore going forward, leaving the 94 existing files as history. Either resolves the drift; the current tracked-but-undocumented state is what's flagged.

- **[medium]** `ta.md` frontmatter still omits the `model:` field present in all 26 other subagent definitions, unresolved 2nd consecutive audit cycle — `.claude/agents/ta.md:1-5`
  Evidence: `.claude/agents/ta.md`'s frontmatter block shows only name, description, tools — no model key. All other 26 files under `.claude/agents/` declare model: opus or model: sonnet (verified directly across all 27 files this cycle). `docs/architecture.md` states the routing mechanism is that `.claude/agents/*.md` frontmatter sets the model; `docs/for-analysts.md`'s roster table only documents models for the core 6, leaving no documented intended value for ta to compare against.
  Suggested fix: unchanged from the prior audit — add an explicit model line to ta.md (the design-QA/critique role parallels note-judge/still-judge/cut-judge, all opus), and add ta to a per-agent model reference in docs/for-analysts.md so the intended value is documented rather than inferred.

- **[low]** Six section-5-scope commits (2026-06-01 to 2026-07-24) still lack the Requested-by:user audit-trail marker — `.claude/agents/*.md` edits at `7be3142`, `8194d51`, `9559d1c`, `9425a3e`, `9ef5d1f`, `ff7fa19`
  Evidence: `git log --since=2026-06-01 -- .claude/agents/*.md agents/*.md` returns exactly these six commits; none of the six commit bodies contain a Requested-by:user line (checked via git show -s --format=%B). None qualifies for the pre-2026-05-17 carry-forward exemption. This is the same set flagged in `2026-07-26-contract.md` — no new section-5-scope commits landed in the intervening five days, so the set is static, not growing.
  Suggested fix: unchanged — the contract explicitly treats a missing marker as low/non-blocking rather than a build failure; if these six were in fact user-approved in-session (several read as operator-commissioned per their own commit bodies — e.g. `8194d51` "Operator-commissioned verification stages", `9425a3e` "Operator directive 2026-07-03"), a one-time retroactive note in an intervention/decision log closes the gap cheaply without rewriting six commits' history.

- **[info]** Audit-trail self-commit fix landed but unverified via a live unattended trigger — `scripts/audit-run.sh` (added by `45a23d2`, 2026-07-31 03:30 KST)
  Evidence: `commit_audit_trail()` now runs at the end of every audit-run.sh invocation, scoping git commit --only to docs/audit/ and best-effort pushing to origin/main. The commit message documents thorough self-testing, but all of it was exercised in a temp clone via direct script invocation, not through the actual L1 post-commit hook, L2 15-minute poll, or L3 daily launchd/Task-Scheduler path that the standing CRITICAL finding was about. `docs/audit/CURRENT-ALERT.md` still shows the stale 2026-07-26 CRITICAL verdict as of this run's start — it has not yet been regenerated to reflect either the fix or this report.
  Suggested fix: no action needed from this report — whichever process commits this report's output (directly, or via a subsequent audit-run.sh run) will naturally produce the first live-path exercise of commit_audit_trail(); if docs/audit/ still shows uncommitted output after that, the fix did not actually close the gap and should be re-opened as CRITICAL.

## Drift between docs and code

- `README.md`/`README.ko.md` hero-stats image plus alt text (23) vs body prose (27) — see medium finding above.
- `docs/for-analysts.md` CI check count (six) vs `.github/workflows/main-protection.yml` (seven checks since 2026-07-01) — see medium finding above.
- `docs/roadmap.md` "Now" vs `docs/goal.md` Active goal and recent commit history — see high finding above.
- `.claude/wb/` present in the repo but absent from `docs/architecture.md`'s Layers table — see medium finding above.
- `ta.md` missing from any documented model-assignment source — see medium finding above.

## Stale items

- `docs/roadmap.md` "Now" block — 72 days old (2026-05-20 to 2026-07-31) relative to its own last-updated stamp.
- Three unactioned suggest blocks in `docs/roadmap.md` (2026-05-25, 2026-06-11, 2026-07-03) proposing a Now rewrite that was never applied.
- `.claude/wb/` — 30 days since last file touch (2026-07-01), undocumented the whole time.
- `docs/visuals/01-hero-stats.png` / `-ko.png` — 30 days since last regeneration (2026-07-01), now under-counting the subagent roster by 4.
- `docs/audit/CURRENT-ALERT.md` — still displays the 2026-07-26 CRITICAL verdict as of this run's start (5 days, much improved from the prior 16-day gap, but not yet CLEAN).

## Compliance with operator-contract.md

- Rule 1 Agent does all the work — no violations observed.
- Rule 2 Never pause unless told — not directly checkable from static state; no evidence against.
- Rule 3 Money firewall — no paid-API or cloud-resource code changes found in the audited window.
- Rule 4 Auto-approve mode — no violations observed.
- Rule 5 Logic changes require explicit user OK — six historical commits lack the marker (see low finding); no new violations since 2026-07-24.
- Rule 6 Git workflow (auto-commit/push, branch strategy) — `main` green (5/5 recent CI runs pass); `records/` correctly gitignored; no evidence of compound git commit-and-push in recent history.
- Rule 7 Split commit and push — no violations observed in recent commit pairs.
- Rule 8 Code/data separation — registry's 11 files all carry the required grep-anchor; no output artifacts under agents/ or scripts/; `.claude/wb/` is an open question against this rule's spirit (see medium finding).
- Rule 9 Goal and roadmap as source of truth — Done-section append discipline is working well (spot-checked 18 recent hashes, all exist and are ancestors of main); "Now" freshness is failing (see high finding).
- Rules 10/11 Session resume / shutdown protocols — not directly auditable from static repo state this cycle.
- Rule 12 No PII or secrets — targeted secret-pattern grep outside docs/, *.example, *.md returned nothing; .env gitignored and untracked.
- Rule 13 Inbound-message routing check — not directly auditable from static repo state this cycle.

## Next audit hint

Two things to check first next time: (1) whether `docs/audit/CURRENT-ALERT.md` actually got refreshed by this report landing (confirms `commit_audit_trail()` works end-to-end, not just in the commit-message self-test) — if it's still showing 2026-07-26 after this report is committed, re-open the audit-trail finding as CRITICAL; (2) whether the operator has acted on any of the three `docs/roadmap.md` "Now" suggest-blocks — if a fourth cycle passes with the same 2026-05-20 stamp, escalate the staleness finding itself, not just repeat it.
