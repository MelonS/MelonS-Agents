# Audit report — 2026-05-20 (contract)

**Verdict**: CLEAN

## Summary

Twenty-ninth contract audit (third same-day run; HEAD = `82b102d`
"docs: resolve 2026-05-20-contract.md audit findings (3 items)" on
`main`, fully synced with `origin/main`).  All twelve hard rules in
`docs/operator-contract.md` were walked against committed code,
configuration, git history, and `.claude/agents/*.md` frontmatter.
This run immediately follows the `82b102d` fix commit that resolved
all three findings from the twenty-eighth audit (HEAD `b3fc7f3`,
~19:53 KST): the stale roadmap "Now" subgoal, the gate-3 description
drift in `operator-contract.md`, and the missing `outputs/publish/*.md`
gitignore guard.  All three are confirmed resolved.  No new findings.
Working tree is clean.  Subagent model assignments all match the
`docs/for-analysts.md` table.  `CURRENT-ALERT.md` still present
(committed as a trail record in `82b102d`) — self-clears when this
CLEAN run is consumed by `audit-run.sh`.

## Findings

_(none)_

## Drift between docs and code

_(none)_

## Stale items

_(none)_

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all work | ✅ PASS | Behavioral; not code-checkable |
| §2 Never pause | ✅ PASS | Behavioral; not code-checkable |
| §3 Money firewall | ✅ PASS | `config/policies.yaml budget_usd_ceiling: 5.00`; `.env.example` schema-only (blank `PEXELS_API_KEY=`; no literal credentials) |
| §4 Auto-approve mode | ✅ PASS | `.claude/settings.json` gitignored (`.gitignore:142`); rendered per-machine from `config/claude-settings.template.json` |
| §5 Logic changes require OK | ✅ PASS | All `.claude/agents/*.md` edits are `8570a9c` + `d857391` (pre-2026-05-17, exempt as carry-forward per convention defined at `7c6ff4f`). No `agents/*.md` direct-child changes post-convention. `scripts/pre-merge-check.sh:108` regex `agents/[^/]+\.md` correctly restricts to direct-child markdown files |
| §6 Git workflow — gitignore | ✅ PASS | `records/` (`.gitignore:4`), `.env` (`:69`), `.claude/settings.json` (`:142`), rendered plists (`:141`), `.claude/*.lock` (`:130`), `outputs/publish/*.md` (`:166`, added `82b102d`) all gitignored |
| §6 Branch strategy | ✅ PASS | All recent commits are micro-commit eligible per §6 allowlist: `82b102d` is an audit-fix commit; `b3fc7f3` is a gitignore chore; `587ad01` is a test log; `a8dac78` + `4f43e63` are docs. No structural changes on `main` without a feat branch |
| §6 Pre-merge gate 3 description | ✅ PASS | `operator-contract.md:186` now reads "direct-child `.md` files in `agents/` or `.claude/agents/`" with the regex `agents/[^/]+\.md` spelled out — matches `scripts/pre-merge-check.sh:108`. Fixed in `82b102d` |
| §7 Split commit and push | ✅ PASS | No `&&`-compound commit+push observed in recent git history |
| §8 Code/data separation | ✅ PASS | Working tree clean; no output artifacts committed under `agents/` or `scripts/`; `outputs/publish/` has only `.gitkeep` tracked; date-named subdirectories contain `.mp4` files (gitignored by global `*.mp4` pattern) |
| §9 Goal/roadmap source of truth | ✅ PASS | Roadmap "Now" correctly reflects current state (v0.4.0 SHIPPED; active subgoal is operator-activation, no remaining Claude work). v0.4.0 Done entry appended in `82b102d` with tag `96d1270`, test counts, and fresh-clone PASS row |
| §10 Session resume protocol | ✅ PASS | Behavioral; not code-checkable |
| §11 Shutdown protocol | ✅ PASS | Daily reports present through 2026-05-20 (`docs/daily/2026-05-20-overnight.md` present) |
| §12 No PII or secrets | ✅ PASS | `git grep` for `api[_-]?key\|secret\|token\|password` outside docs/examples returns only code-level processing references (SRT tokens, caption token alignment, GitHub Actions `id-token: write` permissions declaration, Anthropic-quota comments) — no credential strings. `.env.example` schema-only. No real names, contact info, or credentials detected in tracked files |

**Subagent model assignments** (`.claude/agents/*.md` frontmatter vs `docs/for-analysts.md` table):

| Agent | Frontmatter model | for-analysts.md | Match? |
|-------|------------------|-----------------|--------|
| orchestrator | `opus` | `opus` | ✅ |
| planner | `sonnet` | `sonnet` | ✅ |
| resourcer | `sonnet` | `sonnet` | ✅ |
| editor | `sonnet` | `sonnet` | ✅ |
| qa | `sonnet` | `sonnet` | ✅ |
| auditor | `sonnet` | `sonnet` | ✅ |

**INFO — CURRENT-ALERT.md lifecycle**: The alert file is still present
on disk (committed as a trail record in `82b102d`).  It describes findings
that are resolved.  Per `audit-run.sh` convention, the wrapper
auto-removes `CURRENT-ALERT.md` on a CLEAN verdict.  This audit
produces CLEAN; the alert will self-clear when `audit-run.sh` processes
this report.  No action required — this is the expected lifecycle.

**Resolved since twenty-eighth audit** (HEAD `b3fc7f3`, ~19:53 KST):

| Prior finding | Resolution commit |
|--------------|-------------------|
| [medium] Roadmap "Now" still listed "Land v0.4.0" as active subgoal after v0.4.0 shipped | `82b102d` — "Now" rewritten; v0.4.0 Done entry added with tag hash and test summary |
| [low] `operator-contract.md:186` gate-3 Source cell said "every commit touching `agents/`" while `pre-merge-check.sh:108` uses `agents/[^/]+\.md` | `82b102d` — doc now matches script: "direct-child `.md` files" + regex spelled out |
| [low] `outputs/publish/*.md` not covered by `.gitignore`; future `git add -A` could commit operator upload-pack markdown | `82b102d` — `outputs/publish/*.md` added to `.gitignore:166` |

## Next audit hint

The CURRENT-ALERT.md auto-clear lifecycle depends on `audit-run.sh`
correctly consuming this report's CLEAN verdict; verify in the next
`all`-focus audit that `CURRENT-ALERT.md` was removed and the
end-of-milestone roadmap hygiene (Now ↔ Done parity) has held across
the next shipped milestone.
