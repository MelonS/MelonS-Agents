# Audit report — 2026-05-20 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Twenty-seventh contract audit. HEAD = `a90bc9d`
("chore: filter-repo backup branch deleted (Roadmap Next #2)") on `origin/main`,
fully synced. All twelve hard rules in `docs/operator-contract.md` were walked
against committed code, configuration, git history, and the `.claude/agents/*.md`
frontmatter table. Since the prior contract audit (`2026-05-19-contract.md`,
HEAD `81320a3`), four commits landed: `f752d72` (README/site framing), `95308f6`
(PII-scrub per §12), `aa56f5f` (audit-fix: closes L4 §5-regex, L5 v0.3.0-tag,
and L6 bootstrap-exception extension), and `a90bc9d` (branch deletion). The
`aa56f5f` fix commit resolved three of the four prior carry-forward findings.
Two findings remain: one [medium] roadmap-vs-goal drift (operator-edit-only)
and one [low] stale docs claim about CI (missed by `aa56f5f`). No new findings
introduced. All six subagent model assignments match `docs/for-analysts.md`.
No secrets detected. No §5-scope agent-file changes since 2026-05-15 (all
pre-marker-convention, exempt).

## Findings

- **[medium]** Roadmap "Now" contradicts goal.md active goal — `docs/roadmap.md:18`
  Evidence: The "Now" block is stamped "Last updated 2026-05-18 ~14:10 KST" and
  reads "No active goal — operator sets the next one." `docs/goal.md` carries an
  active goal ("Multi-skill AI assistant framework — Skill #1") promoted at
  `8b39cac` on 2026-05-19 ~00:55 KST. Two `<!-- suggest -->` comments in the
  roadmap (lines 24–44 and 81–88, both from 2026-05-19) have gone unactioned
  for a second full day. Per §9, roadmap "Now" is the session-start source of
  truth; a session resuming from "Now" alone would incorrectly conclude there is
  no active goal.
  Suggested fix: Operator to rewrite the "Now" block to reflect the active
  multi-skill framework goal (user-edit-only section per §9 maintenance contract;
  Claude cannot silently overwrite it).

- **[low]** for-analysts.md claims "No CI" but GitHub Actions CI exists —
  `docs/for-analysts.md:257`
  Evidence: Line 257 reads "No CI / no automated tests on push." Commit `a537018`
  (2026-05-18 ~22:30 KST) added `.github/workflows/main-protection.yml` covering
  bash syntax, secret scan, required-files check, `.env.example` sanity, README
  link hygiene, and gitignore coverage. The claim has been incorrect since
  2026-05-18. The `aa56f5f` fix commit resolved three other prior [low] findings
  but did not touch `docs/for-analysts.md` (its changed-files list is limited to
  `docs/operator-contract.md` and `scripts/pre-merge-check.sh`). Carry-forward
  from 2026-05-19 audit.
  Suggested fix: Update `docs/for-analysts.md:257–261` to replace the "No CI"
  claim with an accurate description of the GitHub Actions workflow and its six
  static checks.

## Drift between docs and code

- `docs/for-analysts.md:257` claims "No CI / no automated tests on push" but
  `.github/workflows/main-protection.yml` exists (commit `a537018`). — [low],
  see Findings above.
- `docs/roadmap.md` "Now" states "No active goal" while `docs/goal.md` Active
  goal is non-empty (set 2026-05-19 ~00:55 KST). — [medium], see Findings above.

## Stale items

- Two `<!-- suggest -->` comments in `docs/roadmap.md` (lines 24–44 and 81–88,
  both dated 2026-05-19) have been outstanding for approximately 48 hours without
  operator action. These are not contract violations (the `<!-- suggest -->`
  mechanism is the correct channel per §9); their age elevates them to audit-
  relevant observations. The 02:30 KST comment already prompted a [medium] on
  the 26th audit; the same concern persists unchanged on the 27th.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all work | ✅ PASS | No "please run X" patterns in committed code |
| §2 Never pause | ✅ PASS | Behavioral; not code-checkable |
| §3 Money firewall | ✅ PASS | `config/policies.yaml budget_usd_ceiling: 5.00`; `ANTHROPIC_API_KEY` instructed to stay in shell, not in `.env.example`; `PEXELS_API_KEY=` is a blank placeholder |
| §4 Auto-approve mode | ✅ PASS | `.claude/settings.json` gitignored; rendered per-machine from `config/claude-settings.template.json` |
| §5 Logic changes require OK | ✅ PASS | Last `.claude/agents/*.md` change: `8570a9c` (2026-05-15) — pre-marker convention (prior to `7c6ff4f` 2026-05-17), all exempt as carry-forward. No `agents/*.md` direct children changed. Gate-3 regex in `scripts/pre-merge-check.sh:110` now correctly enforces `[^/]+\.md` per §5 text (fixed in `aa56f5f`). |
| §6 Git workflow — branch strategy | ✅ PASS | Structural commits use feat branches. v0.3.0 tag exception now explicitly named in §6 tag convention (fixed in `aa56f5f`). Bootstrap-exception list now covers `a2a3807`, `22a45ea`, `a537018` (fixed in `aa56f5f`). `aa56f5f` itself is an audit-fix commit → micro-commit direct to `main` per §6 allowlist. |
| §6 Git workflow — gitignore | ✅ PASS | `records/` gitignored ✓; `.env` gitignored ✓; `.claude/settings.json` gitignored ✓; `.claude/*.lock` gitignored ✓; rendered `.plist` files (`scripts/com.melons.agents.*.plist`) gitignored ✓ — local hardcoded paths in rendered plists not committed |
| §7 Split commit and push | ✅ PASS | No `&&`-compound commit+push in recent commits |
| §8 Code/data separation | ✅ PASS | No output artifacts under `agents/` or `scripts/`. The two `grep` hits for `/Users/melons` in scripts are both comments: `install-claude-local.sh:49` (example in comment) and `install-scheduler.sh:50` (historical note). Rendered plists with hardcoded paths are gitignored. §8 hardcoded-path exception registry unchanged. |
| §9 Goal/roadmap source of truth | ⚠️ MEDIUM | roadmap "Now" says "No active goal" while `docs/goal.md` Active goal exists — [medium] finding above; operator-edit-only section |
| §10 Session resume protocol | ✅ PASS | Behavioral; not code-checkable |
| §11 Shutdown protocol | ✅ PASS | Daily report present at `docs/daily/2026-05-19.md`. Audit trail in `docs/audit/`. |
| §12 No PII or secrets in repo | ✅ PASS | `.env.example` schema-only (blank `PEXELS_API_KEY=`, no actual key). `95308f6` scrubbed employment-angle phrasing per §12. No real names, contact info, or credentials detected in tracked files. |

**Subagent model assignments** (§5 / for-analysts.md table cross-check):

| Agent | Frontmatter model | for-analysts.md claim | Match? |
|-------|------------------|-----------------------|--------|
| orchestrator | `opus` | `opus` | ✅ |
| planner | `sonnet` | `sonnet` | ✅ |
| resourcer | `sonnet` | `sonnet` | ✅ |
| editor | `sonnet` | `sonnet` | ✅ |
| qa | `sonnet` | `sonnet` | ✅ |
| auditor | `sonnet` | `sonnet` | ✅ |

**Resolved since prior audit (2026-05-19-contract.md)**:

| Prior finding | Resolution |
|--------------|------------|
| [low] §5 gate-3 regex matched nested agent paths | Fixed in `aa56f5f`: regex changed from `.+\.md` to `[^/]+\.md`, restricting to direct children only |
| [low] v0.3.0 tag deviates from "skill milestone" convention | Fixed in `aa56f5f`: §6 tag convention amended to allow "named major infra/UX milestones"; v0.3.0 explicitly documented as a named exception |
| [low] `a537018` not in §6 bootstrap exception list | Fixed in `aa56f5f`: §6 bootstrap-exception paragraph explicitly lists `a2a3807`, `22a45ea`, `a537018` |

## Next audit hint

The single remaining actionable item is the stale `docs/for-analysts.md:257` "No CI"
claim — a one-line fix; confirm whether the operator has updated it and, if both open
findings are resolved, verify the verdict can return to CLEAN.
