# Audit report — 2026-05-18 (contract)

**Verdict**: DRIFT_DETECTED

## Summary

Contract-focused audit at HEAD `22a45ea` (2026-05-18, "feat(branch-strategy): pre-merge gate + automated check script").
This run supersedes the earlier same-day audit at `a2a3807` that `CURRENT-ALERT.md` currently references.
All twelve hard rules were walked against committed code, configuration, and git history.
The principal finding is unchanged from the prior run: `.claude/settings.json` still hardcodes
`/Users/melons` at nine locations, violating the §8 env-driven-paths rule and undermining the
machine-swap durability the contract was designed to protect.  Two new [low] bookkeeping items
emerged since `a2a3807`: `CLAUDE.md` now disagrees with `operator-contract.md` on whether the
remote is public or private (the contract updated to "(public)" in `a2a3807` but `CLAUDE.md` was
not synchronised), and three work-bearing commits (`aa10ba0`, `a2a3807`, `22a45ea`) still have no
Done entries in `docs/roadmap.md` per §9.  Two [info] branch-hygiene observations complete the
picture.  No critical findings, no secret leakage, no §5 marker violations, no output artifacts
in code directories.

## Findings

- **[medium]** `.claude/settings.json` hardcodes `/Users/melons` at nine locations — `.claude/settings.json:5-9,28,72,73,82`
  Evidence: Lines 5–9 contain `Read(/Users/melons/**)`, `Write(/Users/melons/ai/**)`,
  `Edit(/Users/melons/ai/**)`, `Write(/Users/melons/.local/share/whisper-models/**)`,
  `Write(/Users/melons/.claude/projects/-Users-melons-ai/memory/**)`.  Lines 28, 72–73,
  82 contain `Bash(rm /Users/melons/ai/**)`, `Bash(*)`,
  `Bash(cd /Users/melons/ai/* && *)`, and `Bash(rm -rf /Users/melons)` (deny rule).
  Contract §8 requires env-driven paths; the plist hardcode problem was solved with
  `@@REPO_ROOT@@`/`@@HOME@@` substitution in `ab6555e`, but `.claude/settings.json`
  received no analogous treatment.  On a machine swap the allow-list silently
  blocks all agent operations.  This finding was first raised in the previous audit
  at `a2a3807` and remains unresolved at `22a45ea`.
  Suggested fix: Generate `.claude/settings.json` from a `.claude/settings.json.template`
  at `scripts/install-scheduler.sh` time (substituting `@@HOME@@` and `@@REPO_ROOT@@`),
  mirroring the plist approach; gitignore the rendered file and track only the template.

- **[low]** `CLAUDE.md:35` says remote is "(private)" while `operator-contract.md:121` says "(public)" — `CLAUDE.md:35` vs `docs/operator-contract.md:121`
  Evidence: `CLAUDE.md` line 35 reads `Remote: git@github.com:MelonS/MelonS-Agents.git (private).`
  `docs/operator-contract.md` line 121 reads `Remote: git@github.com:MelonS/MelonS-Agents.git (public).`
  Commit `a2a3807` updated `operator-contract.md` §6 to "(public)" when the branch strategy was
  codified (KakaoTalk + LinkedIn sharing, first external clone verified 2026-05-18).
  `CLAUDE.md` was last committed at `5769394` (2026-05-17) and was not updated in `a2a3807`.
  The contract is the source of truth; the CLAUDE.md cache entry is now wrong.
  Suggested fix: Update `CLAUDE.md:35` to read `(public)` to match `operator-contract.md`.

- **[low]** Roadmap Done entries absent for three work-bearing commits — `docs/roadmap.md` Done section
  Evidence: The Done section's most recent entry is `e1fda78` (~14:55 KST).  Since then,
  three commits qualify under §9 ("after work lands, append a one-line entry with commit hash
  and date"): `aa10ba0` ("feat(onboarding): music-video-first quick start + bootstrap covers
  aubio/jq/Pexels" — rewrote `scripts/bootstrap.sh` + `README.md`/`README.ko.md`, 375
  changed lines); `a2a3807` ("docs(contract): codify branch strategy" — rewrote §6 of
  operator-contract, a governance-level change); `22a45ea` ("feat(branch-strategy): pre-merge
  gate + automated check script" — added `scripts/pre-merge-check.sh`, a new automation
  component).  Pure tracking commits (`docs/goal.*`, `docs/roadmap.*`, `docs/daily.*`,
  `docs/pilots.*`) are self-describing and exempt.
  Suggested fix: Append three Done lines to the Done section of `docs/roadmap.md` for
  `aa10ba0`, `a2a3807`, and `22a45ea`.

- **[info]** `feat/skill-music-video` branch has no unique commits and is one commit behind `main` — local + `remotes/origin/feat/skill-music-video`
  Evidence: `git log main ^feat/skill-music-video` returns `22a45ea` only; `git log feat/skill-music-video ^main` returns nothing.  The branch points to `a2a3807` while `main` is at `22a45ea`.  No development work has ever landed on it; it was created as a placeholder for future Skills work at the time the branch strategy was codified.  Per §6 step 5 the branch should be deleted after a merge, but it was never used for actual feature development.  Keeping a stale empty branch on `origin` adds noise to `git branch -a` and clutters the remote.
  Suggested fix: Delete with `git branch -d feat/skill-music-video && git push origin --delete feat/skill-music-video` once the next active goal is set (or immediately, since no work depends on it).

- **[info]** Two auto-created `claude/` branches exist locally with no remote tracking — `claude/cool-swartz-2dceac`, `claude/xenodochial-mclean-6ee111`
  Evidence: `git branch -a` lists both branches without a `remotes/origin/` counterpart; they are local-only.  These are Claude Code session auto-branches.  §6 branch strategy documents only `main`, `feat/<name>`, and `v<x>.<y>.<z>` tags; `claude/` branches are outside the defined strategy.  No operational risk as they are local-only.
  Suggested fix: Prune with `git branch -d claude/cool-swartz-2dceac claude/xenodochial-mclean-6ee111` when convenient; add `claude/` to `.git/info/excludes` or CI clean-up if the pattern recurs.

## Drift between docs and code

- `CLAUDE.md:35` "(private)" vs `operator-contract.md:121` "(public)": documented above as [low] finding.

- Subagent model assignments in `.claude/agents/*.md` frontmatter vs `docs/for-analysts.md` table (lines 96–102): orchestrator=opus, planner/resourcer/editor/qa/auditor=sonnet. Confirmed match across all six agent files. ✅

- `docs/for-analysts.md:120` cites `agents/missions/faceless-short/run.sh:98` for the `ollama_generate` call.  File exists; call is at lines 97–98. ✅

- `docs/architecture.md` Layers table includes `outputs/publish/` §8 deviation row with a matching `<!-- §8 operator-directed deviation -->` comment in `outputs/publish/.gitkeep`. ✅

## Stale items

- `main-backup-pre-filter-20260517-173615` branch: roadmap "Next" item #2 schedules deletion "on or after 2026-05-18" — today is 2026-05-18, making this eligible.  Not a contract violation (the roadmap correctly tracks it), but the window for safe deletion is now open.

## Compliance with operator-contract.md

| Rule | Status | Note |
|------|--------|------|
| §1 Agent does all work | ✅ | No user-terminal recipes found in committed docs or scripts |
| §2 Never pause | ✅ | No structural violations found in docs or agent definitions |
| §3 Money firewall | ✅ | `.env.example` schema-only; no API key values; Anthropic vars are model names, not credentials |
| §4 Auto-approve local resources | ✅ | `.claude/settings.json` allow-list covers all local tools (brew, git, ffmpeg, ollama, yt-dlp, whisper, python3, etc.) |
| §5 Logic changes need OK | ✅ | No `.claude/agents/*.md` edits since `8570a9c` (2026-05-15); all are pre-marker-convention carry-forward |
| §5 Requested-by marker | ✅ | Post-2026-05-17 `agents/missions/*/run.sh` commits that carry the marker: `2dbf15e`, `c5fd261`, `6d3fa51`, `3015f14` all have `Requested-by: user`; commits without marker (`e3f11ca`, `2fbb6d9`, `e25f325`, `422c3f5`) are `run.sh` files not `*.md` agent defs and therefore outside §5 scope |
| §6 Auto-commit / auto-push | ✅ | Confirmed: all commits pushed to origin; `records/` not tracked (only `.gitkeep`); remote URL correct |
| §6 Branch strategy | ⚠️ (info) | `feat/skill-music-video` + `claude/*` branches are stale / undocumented; `22a45ea` commits `pre-merge-check.sh` directly to `main` per the explicit bootstrap exception in §6 |
| §7 Split commit+push | ✅ | No `&&`-compound git patterns in committed scripts |
| §8 Code/data separation | ⚠️ (medium) | `.claude/settings.json` hardcodes `/Users/melons` — portability gap described above |
| §8 Output artifacts | ✅ | No `.mp4`/`.jpg`/`.srt`/`.wav` under `agents/` or `scripts/`; `records/` gitignored |
| §9 Goal+roadmap source of truth | ⚠️ (low) | Three substantive commits (`aa10ba0`, `a2a3807`, `22a45ea`) missing Done entries per §9 |
| §10 Session resume | ✅ | No structural violations; protocol documented in contract and CLAUDE.md |
| §11 Shutdown protocol | ✅ | Protocol documented; daily reports present under `docs/daily/` |
| §12 No PII / secrets | ✅ | `git grep` for `api[_-]?key\|secret\|token\|password` returns only placeholder comments and script variable names; `.env` gitignored; `MelonS`/`Todd` are public GitHub-visible pseudonyms; no email addresses in committed non-doc files |

## Next audit hint

Once the Skills work begins on `feat/skill-music-video` (or a fresh branch), verify that every §5-scope commit on that branch carries `Requested-by: user` before the pre-merge gate runs — that's the first real exercise of the newly codified §6 branch strategy.
