# Audit report — 2026-05-16 (roadmap)

**Verdict**: DRIFT_DETECTED

## Summary

Roadmap freshness audit run on 2026-05-16 against `docs/roadmap.md`,
`docs/goal.md`, `docs/for-analysts.md`, `docs/architecture.md`,
`.claude/agents/*.md`, `docs/operator-contract.md`, and `docs/copyright-policy.md`.
All commit hashes explicitly cited in the Done section were verified reachable from
`main` via `git cat-file -e` (100% pass, 25 hashes checked).  The "Now" section is
intentionally empty and the five most recent commits are consistent with the Done
section's most recent entry (Clone-and-go reproducibility, 2026-05-16 afternoon).
Two medium-severity drifts were found: the `docs/for-analysts.md` subagent table
omits the `auditor` agent that exists in `.claude/agents/auditor.md` and
`docs/architecture.md`; and eleven Done entries from 2026-05-15 lack commit hash
citations, violating the roadmap's own maintenance contract.  Two low-severity
issues were also found: the "Now" resume instructions have no coverage of the
2026-05-16 afternoon session (no `docs/daily/2026-05-16.md`), and the resume
instruction item 3 ("promote from copyright-policy Still TODO") points to two
items that are both explicitly parked behind external tooling.  No critical
issues or secrets.

## Findings

- **[medium]** `for-analysts.md` subagent table omits `auditor` — `docs/for-analysts.md:74`
  Evidence: Table (headed "As of 2026-05-15") lists five agents:
  orchestrator/planner/resourcer/editor/qa.  `.claude/agents/auditor.md`
  (model: sonnet) exists; `docs/architecture.md` "Out-of-band track" block
  documents the same agent.  The auditor was added on 2026-05-15 (roadmap
  Done line 167) but the for-analysts table was never updated to include it.
  Suggested fix: Add row `auditor | sonnet | Repository-wide read-only audit → docs/audit/<date>-<focus>.md` to the table in `docs/for-analysts.md` and refresh the "As of" date.

- **[medium]** 11 Done entries from 2026-05-15 lack commit hash citations — `docs/roadmap.md:154-278`
  Evidence: Roadmap maintenance contract at `docs/roadmap.md:10` states
  "Done is appended to by Claude when work lands (commit hash + date)."
  The following eleven entries have a date but no hash:
  "Auditor goes autonomous + statusline live" (line 154),
  "Repository auditor agent" (line 167),
  "Minimal Claude Code statusline" (line 180),
  "Analyst-facing docs" (line 188),
  "Pexels Videos integration" (line 199),
  "License-string probe" (line 223),
  "Strike-aware source rejection" (line 232),
  "Automated copyright filter v1" (line 240),
  "QA feedback retry loop" (line 253),
  "Source-attribution wiring" (line 263),
  "Visual layout verification" (line 271).
  Suggested fix: Append the relevant commit hash to each entry (retrieve via `git log --oneline | grep -i <keyword>`).

- **[low]** Roadmap "Now" resume instructions have no coverage of 2026-05-16 afternoon session — `docs/roadmap.md:22`
  Evidence: The resume instructions (written at 2026-05-15 shutdown) name
  `docs/daily/2026-05-16-overnight.md` as the sole context source for the
  next session.  Seven additional commits landed in the 2026-05-16 afternoon
  session (`692c755`–`ae8eba9`) covering the clone-and-go goal.  No
  `docs/daily/2026-05-16.md` exists (`git log` confirms the file has no
  history); a cold-start resume following the Now instructions would miss
  this context.
  Suggested fix: Write `docs/daily/2026-05-16.md` at next session end per
  operator-contract §11; update the Now resume pointer to reference it.

- **[low]** Resume instruction item 3 names copyright-policy "Still TODO" candidates that are both externally blocked — `docs/roadmap.md:25-27`
  Evidence: Resume instruction item 3 says "promote a candidate from
  `docs/copyright-policy.md` 'Still TODO' block — those are scoped, real,
  and load-bearing."  The current "Still TODO" list (`docs/copyright-policy.md:29-30`)
  contains only two items: "Audio-fingerprint check" (blocked: "needs a
  fingerprint database to compare against; without one the check is just CPU
  burn") and "Logo / watermark detection" (deferred: "needs OCR or a trained
  model; deferred until we hit the failure mode it would catch").  Neither is
  promotable without external tooling the user must supply first.
  Suggested fix: Update resume instruction item 3 to acknowledge both
  items are gated on external preconditions and name those preconditions
  rather than implying the items are pick-up-ready.

- **[info]** No dated audit report in `docs/audit/` — scheduled 03:00 fire may not have succeeded — `docs/audit/` (directory)
  Evidence: `docs/audit/` contains only `README.md`.  The auditor launchd
  plist was installed 2026-05-15 with `RunAtLoad=false`; the first scheduled
  fire was at 03:00 today (2026-05-16).  Commit `4c4fb8d` in `git log`
  ("fix(auditor): launchd PATH — find claude CLI in nvm-managed location")
  suggests the PATH issue was discovered and fixed; if the fix landed after
  03:00, the first scheduled run would have failed silently.
  Suggested fix: Run `./scripts/audit-run.sh all` manually to produce a
  baseline; then confirm launchd is operational via
  `launchctl list | grep melons`.

- **[info]** Two pending logic-layer edits await user OK in `<!-- suggest -->` block — `docs/roadmap.md:35-49`
  Evidence: Block proposes adding a paragraph to `.claude/agents/auditor.md`
  and a line to `CLAUDE.md`; both are gated on explicit user confirmation per
  operator-contract §5.  No contract violation — working as designed.  Full
  proposal at `docs/proposals/2026-05-15-auditor-active.md`.
  Suggested fix: None needed; user reviews and accepts or rejects when ready.

## Drift between docs and code

`docs/for-analysts.md:74-80` — the subagent table lists five agents
(orchestrator/planner/resourcer/editor/qa) but the actual agent inventory
in `.claude/agents/` has six: those five plus `auditor` (model: sonnet,
read-only, daily 03:00 via launchd).  The same agent is correctly documented
in `docs/architecture.md:43-46` ("Out-of-band track") but absent from the
table that analysts consult for model assignments.

## Stale items

`docs/roadmap.md:154-278` — eleven consecutive Done entries from 2026-05-15
lack commit hash citations per the roadmap's maintenance contract ("commit
hash + date").  The entries cover a large batch of feature work shipped that
day (copyright filter, retry loop, attribution wiring, layout verification,
Pexels integration, and infrastructure).  The hashes exist in `git log` but
are not recorded in the Done text, making the Done section less auditable
than the contract requires.

## Compliance with operator-contract.md

**§9 (goal + roadmap as source of truth):** PASS.  `docs/goal.md` active goal
is correctly empty with a clear user-signal note; all past goals properly
migrated with ticked subgoals; roadmap Done section covers all work through
2026-05-16 afternoon.  No "Now is empty therefore done" confusion present.

**§11 (shutdown protocol):** PARTIAL.  The 2026-05-15 shutdown report exists
at `docs/daily/2026-05-15.md` (commit `7003a53`).  The 2026-05-16 overnight
session has `docs/daily/2026-05-16-overnight.md`.  The 2026-05-16 afternoon
interactive session (clone-and-go goal, commits `692c755`–`ae8eba9`) has no
corresponding `docs/daily/2026-05-16.md`; the session appears to be still
open at audit time.  This is not a confirmed violation — the protocol fires
at session close, which has not yet occurred.

All other operator-contract rules are out of scope for this roadmap-focus run.

## Next audit hint

Run a `contract` focus after the two pending `<!-- suggest -->` edits
(auditor.md Principles paragraph + CLAUDE.md session-protocol line) are
accepted or rejected, to verify the auditor's self-awareness and the
session-start protocol remain in sync with each other.
