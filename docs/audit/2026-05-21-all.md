# Audit report — 2026-05-21 (all)

**Verdict**: DRIFT_DETECTED

## Summary

Thirty-second audit; first "all" focus since 2026-05-19.  Current HEAD:
`f190929` (`feat(env): CPU-throttled ffmpeg wrapper — 80% cap to prevent
thermal throttle`) on `main`, fully synced with `origin/main`.  All six
audit dimensions checked: (1) architecture vs documentation drift,
(2) roadmap freshness, (3) operator-contract compliance, (4) cost-model
accuracy, (5) stale TODOs / dead code, (6) security / secrets.  The
most significant finding is that the roadmap Done section has gone
unupdated since `0c01fcc` (2026-05-21 01:15 KST) while 20 subsequent
commits landed — a systemic contract §9 gap.  A second medium finding:
five upload-metadata JSON files in `outputs/publish/upload-meta-v2/` are
tracked by git, slipping through a gitignore hole (`.gitignore` excludes
`upload-meta/` but not `upload-meta-v2/`).  No critical or high
findings.  Cost model, security, subagent model assignments, and
contract hard rules are otherwise clean.

## Findings

- **[medium]** Roadmap Done section missing 20 commits — `docs/roadmap.md`
  Evidence: Last Done-section update was commit `0c01fcc` (2026-05-21
  01:15 KST).  Since then, 20 commits landed on `main` (from
  `be93c48` through `f190929`) with no corresponding Done entries:
  `be93c48` (Canvas 8s loop + kinetic typography), `520e6f3` (genre
  auto-detect), `0b48a25` (all-in-one auto wrapper), `2192498` (fix
  yq v4), `de08a1b` (fix vignette), `f796aad` (bulk-regenerate),
  `8c87d96` (phrase_pool per genre), `c721f41` (morning brief),
  `55cb901` (fix bash 3.2 guard), `7425689` (fix yq v4 alias),
  `57f1e2e` (morning brief v2 regen), `53d5053` (auto-fetch Pexels
  still), `a182380` (v2 upload metadata), `735306d` (genre smoke
  test), `460ae29` (audio-reactive saturation), `c505289` (morning
  brief final tally), `e5d4a2e` (wire audio-reactive flag),
  `2c3289a` (engineering case study #7), `6ddba86` (§6 branch
  strategy revision), `f190929` (CPU-throttled ffmpeg).  Contract §9
  states: "After work lands, append a one-line entry to roadmap's
  Done section with the commit hash and date."
  Suggested fix: In the next interactive session, batch-append a Done
  block for these 20 commits (grouped by theme); do not invent dates,
  use `git log --format="%h %s %ai"` for actuals.

- **[medium]** `outputs/publish/upload-meta-v2/*.json` tracked by git —
  gitignore gap — `outputs/publish/upload-meta-v2/` (5 files, commit
  `a182380`)
  Evidence: `git ls-files outputs/publish/` returns `.gitkeep` plus
  `upload-meta-v2/01-01-rain-lofi-v2.json` through
  `upload-meta-v2/05-05-noir-detective-v2.json`.  `.gitignore` lines
  165–167 exclude `outputs/publish/*.md` and `outputs/publish/upload-meta/`
  but not `outputs/publish/upload-meta-v2/`.  The `.gitignore` inline
  comment reads: "Routinely contain channel-handle, unposted
  titles/descriptions, and operator-personal context that fails the
  [[no-pii-in-repo]] bar."  The files' content (titles, tags,
  `privacyStatus`, `publishAt` timestamps) do not contain obvious PII
  today, but the policy intent is clear: they should not be committed.
  Contract §6 gitignore coverage and §8 output-artifact separation
  both apply.
  Suggested fix: Add `outputs/publish/upload-meta*/` to `.gitignore`;
  run `git rm --cached outputs/publish/upload-meta-v2/*.json` and commit.

- **[low]** `docs/architecture.md` missing 7 new music-video utility
  scripts — `scripts/music-video-auto.sh`, `music-video-canvas.sh`,
  `music-video-audio-reactive.sh`, `music-video-typography.sh`,
  `music-video-bulk-regenerate.sh`, `music-video-fetch-still.sh`,
  `music-video-genre-detect.sh`
  Evidence: `docs/architecture.md` "Mission types" and "music-video"
  subsections reference `music-video-shaders.sh`, `music-video-stillzoom.sh`,
  and `music-video-genre.sh` but not the seven scripts above.  All
  seven exist in `scripts/` and are referenced in
  `docs/engineering-case-studies.md` and/or the skill's README but
  are absent from the canonical architecture map.  `grep -rl
  "music-video-auto" docs/ README.md` returns 2 hits (neither in
  architecture.md).
  Suggested fix: Add a "Music-video utility scripts" row or subsection
  to `docs/architecture.md` Mission types section naming these scripts
  and their roles (auto-wrapper, canvas-loop, audio-reactive,
  typography, bulk-regen, still-fetch, genre-detect).

- **[low, carry-forward from 30th/31st audit]** Hardcoded
  machine-specific path in committed log file —
  `docs/onboarding/demo-mode-log.txt:1`
  Evidence: Line 1 still reads `remote=/Users/melons/ai` — a
  machine-specific absolute path.  Lines 2–4 correctly use the
  `https://github.com/...` remote URL.  §8 principle 4: "No
  hardcoded machine-specific values in committed files."  Flagged in
  30th audit (HEAD `c721f41`); 31st audit (HEAD `f190929`); still
  unresolved.
  Suggested fix: Append a corrected replacement row, or add an inline
  comment on line 1 noting it is a historical local-path artifact
  from the first test run, pre-dating the portability requirement.

- **[low, carry-forward from 31st audit]** §8 exception documentation
  missing — `scripts/ffmpeg-throttled.sh:33–35`
  Evidence: Lines 33–35 iterate a hardcoded discovery list
  (`/opt/homebrew/bin/ffmpeg`, `/usr/local/bin/ffmpeg`,
  `/usr/bin/ffmpeg`) with no `§8 exception:` inline comment and no
  entry in the exception registry in `docs/operator-contract.md §8`.
  The identical pattern at `agents/lib/env.sh:38,50` has both an
  inline comment and a registry entry.  Flagged in 31st audit; not yet
  resolved.
  Suggested fix: Add `# §8 exception: standard system ffmpeg discovery
  fallback — same pattern as agents/lib/env.sh:38` above the for-loop
  and add `scripts/ffmpeg-throttled.sh:33-35` to the exception registry
  in `docs/operator-contract.md §8`.

- **[info, carry-forward]** Copyright TODO items — `agents/lib/copyright.sh:10–12`
  Evidence: Lines 10–12 carry "Still TODO: Audio fingerprint check" and
  "Still TODO: Logo / watermark detection."  Both are tracked in
  `docs/copyright-policy.md` "Still TODO" block with rationale for
  deferral.  No action required; included for completeness.

## Drift between docs and code

- **Architecture map incomplete for music-video layer**: `docs/architecture.md`
  describes the music-video pipeline (correct as of 2026-05-17 genre.sh
  landing) but the seven utility scripts shipped between `be93c48` and
  `e5d4a2e` are not reflected.  See finding [low] above.

- **Subagent model table date label stale**: `docs/for-analysts.md:93`
  reads "As of 2026-05-17."  All six model assignments are still
  accurate (verified against `.claude/agents/*.md` frontmatter today);
  only the date label is stale.  No functional drift.

  | Agent | Frontmatter | for-analysts.md | Match? |
  |-------|-------------|-----------------|--------|
  | orchestrator | opus | opus | ✅ |
  | planner | sonnet | sonnet | ✅ |
  | resourcer | sonnet | sonnet | ✅ |
  | editor | sonnet | sonnet | ✅ |
  | qa | sonnet | sonnet | ✅ |
  | auditor | sonnet | sonnet | ✅ |

- **Prior contract audit §8 check had a gap**: The 31st contract audit
  (`2026-05-21-contract.md`) stated §8 "PASS — `outputs/publish/` has
  only `.gitkeep` tracked."  The check ran `git ls-files agents/
  scripts/` only and missed `outputs/publish/upload-meta-v2/*.json`.
  Corrected in this "all" audit (see medium finding above).

## Stale items

- **Roadmap Done section**: 20 commits unrecorded (see medium finding).
  Oldest unrecorded commit is `be93c48` (Canvas 8s loop, 2026-05-21
  after the morning brief).  No commits are older than 60 days; these
  are all from 2026-05-21.  Stale by contract rather than by age.

- **`for-analysts.md:93` date label**: "As of 2026-05-17" — 4 days
  stale.  Content accurate; cosmetic.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all work | ✅ PASS | Behavioral; not code-checkable |
| §2 Never pause | ✅ PASS | Behavioral; not code-checkable |
| §3 Money firewall | ✅ PASS | `config/policies.yaml` `budget_usd_ceiling: 5.00`; `.env.example` schema-only; `ANTHROPIC_API_KEY` instructed to stay in shell, not file |
| §4 Auto-approve mode | ✅ PASS | `.claude/settings.json` gitignored (`line 142`); rendered per-machine from `config/claude-settings.template.json` |
| §5 Logic changes require OK | ✅ PASS | `git log -- '.claude/agents/*.md'` returns only `8570a9c` (2026-05-15) and `d857391` (2026-05-14); both pre-2026-05-17 marker convention — carry-forward exempt.  No post-convention agent-definition edits |
| §6 Gitignore coverage | ⚠️ MEDIUM | `records/` ✓; `.env` ✓; `.claude/settings.json` ✓; rendered plists ✓; `.claude/*.lock` ✓; `outputs/publish/*.md` ✓; `outputs/publish/upload-meta/` ✓ — but `outputs/publish/upload-meta-v2/` NOT gitignored; 5 JSON artifact files tracked (see medium finding) |
| §6 Branch strategy | ✅ PASS | Flexible guideline codified in `6ddba86`.  Only `main` and `remotes/origin/main` branches present |
| §7 Split commit and push | ✅ PASS | No `&&`-compound `git commit … && git push` in scripts or commit bodies |
| §8 Code/data separation | ⚠️ MEDIUM | `agents/` and `scripts/` contain no media artifacts ✓; `records/` gitignored ✓; `outputs/publish/upload-meta-v2/*.json` are output artifacts (not code) tracked in git — policy violation (see medium finding) |
| §8 Hardcoded paths | ⚠️ LOW | `docs/onboarding/demo-mode-log.txt:1` (carry-forward); `scripts/ffmpeg-throttled.sh:33–35` (carry-forward from 31st audit); registered exceptions at `agents/lib/env.sh:38,50`, `scripts/audit-run.sh:37-42`, `scripts/music-video-shaders.sh:57,64` each carry inline `§8 exception:` comments and registry entries |
| §9 Goal/roadmap source of truth | ⚠️ MEDIUM | `docs/goal.md` active goal present ✓; `docs/roadmap.md` "Now" reflects current state ✓; "Done" section 20 commits behind (see medium finding) |
| §10 Session resume protocol | ✅ PASS | Behavioral; not code-checkable |
| §11 Shutdown protocol | ✅ PASS | `docs/daily/` has reports through 2026-05-21 |
| §12 No PII or secrets | ✅ PASS | `git grep` for credential patterns outside `docs/` and `*.example` returns only env-var references and token-alignment comments; no credential strings; `.env.example` schema-only; no operator personal identifiers in committed content |

## Next audit hint

Verify that `outputs/publish/upload-meta-v2/*.json` have been removed
from tracking (gitignore gap fixed), the roadmap Done section backfilled
for the 20 missing commits, and `scripts/ffmpeg-throttled.sh:33–35` has
its `§8 exception:` comment — those three items clear all medium/low
open findings.
