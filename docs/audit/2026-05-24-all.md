# Audit report — 2026-05-24 (all)

**Verdict**: DRIFT_DETECTED

## Summary

All-focus audit against HEAD `7edba96` (2026-05-23 05:20 KST).  All six
dimensions checked: (1) architecture vs documentation drift, (2) roadmap
freshness, (3) operator-contract compliance, (4) cost-model accuracy,
(5) stale TODOs / dead code, (6) security / secrets.  The primary finding
is a **persistent model-assignment drift now entering its 4th consecutive
unresolved audit cycle**: `.claude/agents/planner.md:5` and
`.claude/agents/resourcer.md:5` both carry `model: opus` (set by commit
`2778316`, 2026-05-22 ~17:50 KST), but `docs/for-analysts.md`,
`docs/architecture.md`, and `docs/cost-model.md` still describe them as
`sonnet`.  Seven commits have landed since the last all-audit
(`2026-05-23-all.md`, HEAD `d6dcfb8`) — all are music/lyric/reference work;
none resolve the open findings and none introduce new contract violations.
Two new scripts (`scripts/audio-analyze.sh`, `scripts/romanize-hangul.sh`)
added in this batch are properly documented in `docs/music-video-pipeline-reference.md`.
Security scan is clean: no secrets, no PII, `.env` gitignored, all §8
exception-registry entries retain their in-code `# §8 exception:` markers,
and the pre-computed skill-activation drift report returns 0 findings.

## Findings

- **[high]** Model-assignment drift — `docs/for-analysts.md:100-101`,
  `docs/architecture.md:26,30`, `docs/cost-model.md:23,60`
  Evidence: `.claude/agents/planner.md:5` = `model: opus`;
  `.claude/agents/resourcer.md:5` = `model: opus` (commit `2778316`,
  2026-05-22 ~17:50 KST).  Three docs still claim sonnet: (a)
  `docs/for-analysts.md:100-101` — table rows `planner → sonnet`,
  `resourcer → sonnet`; (b) `docs/architecture.md:26` — box label
  `planner (sonnet)`, line 30 `resourcer (sonnet)`; (c)
  `docs/cost-model.md:23` — tier diagram `planner / resourcer / editor /
  qa (sonnet)`, line 60 — `¢ — sonnet, small input bounded by file size`.
  Additionally: `docs/for-analysts.md:94-95` still states "unchanged since
  2026-05-15 — last commit touching `.claude/agents/*.md` was `8570a9c`"
  (actual last commit: `2778316`, 2026-05-22); `docs/for-analysts.md:106-107`
  reads "A common analyst suggestion is 'downgrade subagents from opus to
  sonnet.' This is already done" — now factually reversed; `docs/for-analysts.md:109-111`
  suggests "a valid future move is downgrading `resourcer` to haiku" which
  implies resourcer is at sonnet, not the current opus.  `README.md` and
  `README.ko.md` are correct; three other docs were missed by commit `2778316`.
  Carries forward unresolved from `2026-05-22-contract.md`,
  `2026-05-22-all.md`, `2026-05-23-contract.md`, `2026-05-23-all.md`
  (4th consecutive unresolved cycle).
  Suggested fix: Update `docs/for-analysts.md` (table rows, provenance line,
  "already done" sentence, haiku-demotion note), `docs/architecture.md` box
  labels, and `docs/cost-model.md` tier diagram and subagent table row to
  reflect planner=opus, resourcer=opus, editor=sonnet, qa=sonnet.

- **[low]** §5 `Requested-by: user` footer absent — commit `fcc5fee` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" fcc5fee` returns only the subject line
  "abtest(planner+resourcer): flip to opus for A/B" with no body and no
  `Requested-by: user` trailer.  Commit is §5-scope, dated 2026-05-22
  (post-convention threshold 2026-05-17), reachable from `origin/main`
  (`git cat-file -e fcc5fee` exits 0).  Carry-forward from
  `2026-05-23-contract.md`.
  Suggested fix: Include the canonical `Requested-by: user` footer on all
  future §5-scope commits, including interim A/B flips.

- **[low]** §5 `Requested-by: user` footer absent — commit `f714a58` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" f714a58` includes the prose sentence
  "Operator §5 approval granted in-session via AskUserQuestion" — intent is
  clear, but no `Requested-by: user` trailer line is present.  Per §5, the
  footer is required to "make the approval mechanically verifiable in
  `git log`"; body prose is not parseable by `scripts/pre-merge-check.sh`.
  Carry-forward from `2026-05-23-contract.md`.
  Suggested fix: Include the literal `Requested-by: user` trailer in
  addition to prose approval citation.

- **[low]** §5 `Requested-by: user` footer absent — commit `2778316` —
  `.claude/agents/planner.md`, `.claude/agents/resourcer.md`
  Evidence: `git log --format="%B" 2778316` shows detailed operator
  rationale ("Operator decision 2026-05-22 ~17:50 KST: keep pair upgrade
  …") but no `Requested-by: user` trailer.  This is the production-
  configuration commit and the highest-stakes §5 commit of the three.
  Carry-forward from `2026-05-23-contract.md`.
  Suggested fix: Add `Requested-by: user` footer in any follow-up commit
  that references this decision arc.

- **[low]** §8 hardcoded machine path in operator guidance —
  `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7`
  Evidence: Line 7 reads `**Where to save downloaded mp3s:**
  \`/Users/melons/ai/assets/music/\`` — a machine-specific absolute path
  in a committed operator-facing file.  Not in the §8 exception registry;
  no `# §8 exception:` comment present.  File is git-tracked
  (`git ls-files` exits 0).  On a different clone path the instruction
  is wrong.  Carry-forward from `2026-05-23-contract.md`.
  Suggested fix: Replace with `$REPO_ROOT/assets/music/` and note that
  `$REPO_ROOT` is set by `agents/lib/env.sh`.

- **[low]** §8 hardcoded machine path in test-script comment —
  `scripts/test-lyric-extract.sh:10`
  Evidence: Line 10 is `# Uses /Users/melons/ai/assets/music/vocal-dreampop-blue-hours.mp3`
  — machine-specific absolute path in a comment.  Functional code on line 18
  correctly uses `$REPO_ROOT`; the comment-only violation has no runtime
  impact but §8 prohibits literal `/Users/...` in committed files regardless
  of executable vs comment context.  Carry-forward from
  `2026-05-23-contract.md`.
  Suggested fix: Replace comment path with `$REPO_ROOT/assets/music/...`
  to match the functional code.

- **[low]** Roadmap Done section 31 commits behind HEAD — `docs/roadmap.md`
  Evidence: `git log 4f3e717..HEAD --oneline | wc -l` = 31.  The most
  recent Done entry references commit `4f3e717` (2026-05-22 ~16:04 KST).
  31 commits have landed since then with no corresponding Done entries.
  Per §9, every commit needs a Done entry.  Was 24 at `2026-05-23-all.md`
  (7 new commits this cycle added to the gap).
  Suggested fix: Run `scripts/roadmap-done-sync.sh --apply` to auto-backfill.

- **[low]** 10 test scripts in `scripts/` not referenced in `docs/` or
  `README.md` — carry-forward from `2026-05-23-all.md`
  Evidence: The following scripts appear in the tracked `scripts/` tree but
  do not appear in any file under `docs/` (including `for-analysts.md`,
  `architecture.md`, `music-video-pipeline-reference.md`) or in
  `README.md` / `README.ko.md`: `test-first-touch.sh`,
  `test-lyric-extract.sh`, `test-music-video-batch.sh`,
  `test-music-video-trim.sh`, `test-music-video-upload-meta.sh`,
  `test-music-video-validate.sh`, `test-qa-anchor.sh`,
  `test-shader-gates.sh`, `test-shot-plan.sh`, `test-thumbnail.sh`.
  All are test harnesses for already-documented features; functional risk
  is low.
  Suggested fix: Add a single sentence or table row in `docs/for-analysts.md`
  under a "Test harnesses" subheading that enumerates the test scripts as
  companion regression suites for their feature counterparts.

## Drift between docs and code

| Doc location | Doc claim | Code / config reality |
|---|---|---|
| `docs/for-analysts.md:100` | `planner` → `sonnet` | `.claude/agents/planner.md:5` → `model: opus` |
| `docs/for-analysts.md:101` | `resourcer` → `sonnet` | `.claude/agents/resourcer.md:5` → `model: opus` |
| `docs/for-analysts.md:94-95` | "last commit touching `.claude/agents/*.md` was `8570a9c`" | last commit = `2778316` (2026-05-22) |
| `docs/for-analysts.md:106-107` | "downgrade to sonnet already done" | planner + resourcer are at opus; statement is reversed |
| `docs/for-analysts.md:109-111` | "valid future move: downgrade `resourcer` to haiku" | resourcer is at opus; framing implies sonnet baseline |
| `docs/architecture.md:26` | `planner (sonnet)` box label | `.claude/agents/planner.md` → `model: opus` |
| `docs/architecture.md:30` | `resourcer (sonnet)` box label | `.claude/agents/resourcer.md` → `model: opus` |
| `docs/cost-model.md:23` | `planner / resourcer / editor / qa (sonnet)` | planner + resourcer = opus since 2026-05-22 |
| `docs/cost-model.md:60` | `¢ — sonnet, small input bounded by file size` | planner + resourcer are opus (higher token cost) |

## Stale items

- **Roadmap Done section** is 31 commits behind HEAD `7edba96`.  The
  auto-reconciliation script (`scripts/roadmap-done-sync.sh`) exists and
  can close this gap idempotently; the gap has grown from 24 (prior audit)
  to 31 in this cycle without any reconciliation run.

- **Roadmap "Now"** continues to describe an operator-activation step for
  `job-hunt` v0.4.0 that has been the current item for 40+ commits.  The
  roadmap text explicitly acknowledges the structural nature of this stale
  ("this 'Now' subgoal remains the active operator-side step; no agent work
  queued").  All 5 most-recent commits are music/lyric work unrelated to
  this item.  Not actionable for the agent; carried forward as informational.

- **`docs/for-analysts.md:94-95` provenance claim** states "unchanged since
  2026-05-15 — last commit touching `.claude/agents/*.md` was `8570a9c`".
  The actual last commit is `2778316` (2026-05-22).  This stale provenance
  claim directly contradicts the high-severity model-drift finding and will
  mislead any analyst reading the file at face value.

## Compliance with operator-contract.md

| Rule | Status | Notes |
|------|--------|-------|
| §1 Agent does all the work | ✓ PASS | No user-recipe instructions found in committed files |
| §2 Never pause unless told | ✓ PASS | No pause-seeking patterns in committed docs or scripts |
| §3 Money firewall | ✓ PASS | All five missions route through local tools; highlight/summarize/shorts-batch through `agents/lib/ollama.sh`; music-video through local aubio; faceless-short defaults to ollama; no direct Anthropic SDK calls in mission code; `config/policies.yaml` `budget_usd_ceiling: 5.00` intact |
| §4 Auto-approve mode | ✓ PASS | `config/policies.yaml` intact; local-resource allow-list in `.claude/settings.json` template covers declared tools |
| §5 Logic changes require explicit user OK | ⚠ FAIL [low×3] | `fcc5fee` (no marker, no body), `f714a58` (prose approval present but no canonical `Requested-by: user` trailer), `2778316` (detailed rationale, no trailer); all three carry forward from prior audits |
| §6 Auto-commit + auto-push / branch strategy | ✓ PASS | `records/` gitignored; rendered plists gitignored; `.env` gitignored; `.claude/settings.json` gitignored; no output artifacts committed under `agents/` or `scripts/` |
| §7 Split commit and push | ✓ PASS | No `git commit ... && git push` compound found in any committed script |
| §8 Code/data separation + portability | ⚠ FAIL [low×2] | `assets/lyrics/2026-05-21-suno-vocal-prompts.md:7` — hardcoded `/Users/melons/ai/assets/music/`; `scripts/test-lyric-extract.sh:10` — hardcoded path in comment.  All §8-registry-listed files retain their `# §8 exception:` comments (verified via grep). |
| §9 Goal and roadmap as source of truth | ✓ PASS | Structural stale in "Now" explicitly acknowledged in-file; Done lag is a maintenance gap addressed by `roadmap-done-sync.sh` |
| §10 Session resume protocol | ✓ PASS (behavioral) | Not fully verifiable from static code; no committed violation patterns |
| §11 Shutdown protocol | ✓ PASS (behavioral) | Most recent daily report present at `docs/daily/` |
| §12 No PII or secrets | ✓ PASS | `git grep` secret scan returns only placeholder/comment/variable-name matches; `.env.example` is schema-only; no real names, emails, or credentials in committed files |

## Next audit hint

Resolve the model-assignment drift in `docs/for-analysts.md`, `docs/architecture.md`,
and `docs/cost-model.md` — this [high] finding has now persisted across
**four consecutive audits** (22-contract, 23-contract, 23-all, 24-all);
the doc update is a 5-minute fix that will return the verdict to CLEAN.
