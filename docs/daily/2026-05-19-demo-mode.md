# 2026-05-19 — feat/demo-mode session report

**Session window**: 2026-05-19 ~00:55 KST (after the operator's
"브랜치 전략 반드시 지킬것. 항상 유념해" reaffirmation) →
~01:30 KST (current).

## Headline

Roadmap Next #1 ("Zero-friction onboarding path") is essentially
complete and merge-ready on `feat/demo-mode`.  A brand-new clone
of the repo now produces a real 60-second 1080×1920 music-video
in ~2 minutes from `git clone` to `open short.mp4`, with **zero
external accounts** — no Pexels signup, no Suno round-trip, no
`.env` edit.

The path's correctness is proven by `scripts/test-demo-mode.sh`
running against a fresh clone (in this session, against the
local feat-branch via `FRESH_CLONE_REMOTE=$(pwd)`).  PASS line
appended to `docs/onboarding/demo-mode-log.txt`:

```
2026-05-19 01:25:59 +0900  PASS  remote=/Users/melons/ai
short=...music-video-demo-012441/outputs/short.mp4  size=81MB
duration=60s  sources_lines=3
```

## Commits on feat/demo-mode (most recent first)

```
d2e145a feat(demo-mode): scripts/test-demo-mode.sh + onboarding docs + first PASS log
a77af98 feat(demo-mode): bootstrap recommends demo path when no keys/music present
15a897b feat(demo-mode): wire MUSIC_VIDEO_DEMO_MODE into music-video mission
4aace92 feat(demo-mode): fetch-demo-music.sh + CC-BY-4.0 publish rule + incompetech allowlist
e3dd657 feat(demo-mode): fetch-demo-broll.sh — zero-friction CC-BY B-roll cache
```

Base: `a537018` on `main`.  Branch is 5 commits ahead.

## What's on disk

```
scripts/
├── fetch-demo-broll.sh        ← curated CC-BY-3.0 Blender CDN fetcher
├── fetch-demo-music.sh        ← curated CC-BY-4.0 Incompetech fetcher
└── test-demo-mode.sh          ← fresh-clone PASS/FAIL gate

agents/missions/music-video/run.sh   ← MUSIC_VIDEO_DEMO_MODE branch added
                                       (no logic change to non-demo path)

config/copyright-allowlist.yaml     ← + incompetech.com domain
                                      + CC-BY-4.0 publish_rule

scripts/bootstrap.sh                 ← demo-path-recommended UX rewrite

docs/onboarding/
├── demo-mode.md                ← user-facing demo guide
└── demo-mode-log.txt           ← reproducibility PASS log
```

## Empirically validated this session

| Check | Method | Result |
|---|---|---|
| Blender CDN URLs resolve | HEAD probe of 3 candidates | ✓ all 200 |
| Incompetech URLs resolve | HEAD probe of 5 tracks | ✓ all 200 |
| fetch-demo-broll smoke | single-clip fetch + idempotent re-run | ✓ pass |
| fetch-demo-music smoke | single-track fetch + sidecar JSON | ✓ valid 256kbps MPEG |
| Mission demo-mode end-to-end | MUSIC_VIDEO_DEMO_MODE=1 ./run.sh demo | ✓ 60.0s 96MB on local tree |
| Fresh-clone reproducibility | scripts/test-demo-mode.sh against local | ✓ 60s 81MB on freshly-cloned tree |
| SOURCES.txt dedup | 3 unique credit lines for 7 keyword slots | ✓ correctly deduped |

## What's NOT yet done

1. **Merge to main** — requires operator OK (Gate 4) per §6.
   Recommended order:
   - First merge `feat/skill-music-video` (it's older and has more
     work; tag `v0.2.0` upon merge for the milestone).
   - Then merge `feat/demo-mode` (or rebase on the new main, since
     `demo-mode` branched from `main` before any of feat/skill-music-video's
     commits landed — no actual conflict expected but the rebase
     keeps history linear).

2. **README.md EN + KO refresh** — deferred per
   `docs/operator-contract.md` README maintenance cadence rule.
   Best batched at the v0.2.0 merge, covering:
   - `skills/` layer (from feat/skill-music-video)
   - Demo-mode Quick Start section
   - Link to `docs/onboarding/demo-mode.md`
   - Code/Data separation table row for `skills/`
   - Architecture diagram if it touches surface area

3. **Drift to clean up at merge time**:
   - The audit on `feat/demo-mode` re-flags the `.claude/settings.json`
     hardcoded-path [medium] because `feat/skill-music-video`'s fix
     isn't in this branch's ancestry.  Resolved automatically once
     feat/skill-music-video merges to main, since `feat/demo-mode`
     can then rebase onto the fixed main.

## Branch hygiene notes (recorded for memory)

Operator twice flagged off-topic commits on feature branches this
session.  Saved as `feedback_branch_strategy_strict` in agent
memory.  Going forward, every commit on every branch is checked
against the branch's stated purpose via `git branch --show-current`
before staging.  Micro-fixes that don't fit go to main directly
(or a dedicated `fix/` branch); cross-cutting infrastructure
changes get their own feat branch.

The pre-merge regex fix (`0652092`) currently sits on
`feat/skill-music-video` despite being unrelated to Skill #1.
Decision (non-destructive): leave it there; it'll land at merge
and the impact is null (regex bug-fix has no Skill #1 conflict).
Force-push to clean would need explicit operator OK.

## What's autonomous-safe but NOT done

The autonomous-mode budget on this session was the demo-mode
work.  The following are listed for future-session pickup, not
attempted tonight without operator direction:

- **Layer 3 functional gate framework** (parked H in
  `docs/ideas.md`, ~3-4h scope) — needs architectural decisions
  about where per-skill `tests/functional.sh` lives.
- **Layer 4 periodic skill smoke** (parked H, ~2-3h scope) —
  needs launchd plist + status reporting + session-protocol
  extension.
- **Pre-merge regex relocation** (0652092 from feat/skill-music-video
  to main or `fix/` branch) — destructive, defer.
- **`docs/operator-contract.md` §6 list update** (`.claude/skills/*` →
  `skills/<name>/`) — contract edit, queue for operator OK.

## Where to pick up

Most natural next-session sequence:

1. Read this report + `docs/daily/2026-05-19.md` (the earlier
   morning brief that covers feat/skill-music-video).
2. Manually invoke `/music-video` in Claude Code (Step 1.3 of
   Skill #1 acceptance, gate 2 of pre-merge).
3. If output matches expectation → approve `feat/skill-music-video`
   merge → tag `v0.2.0`.
4. Rebase `feat/demo-mode` onto the new `main` → merge.
5. README EN + KO refresh in a single commit on main.
6. Decide on Skill #2 (job-hunt) design — that was the operator's
   stated next direction.

## Trust check

The work this session was **mechanical execution within the
roadmap's stated Next #1 scope** with explicit operator autonomy
delegation ("자율모드 맞지? ... 최대한 일하고 있어").  No new
direction invented; no decisions made on operator's behalf; no
logic-file (`.claude/agents/*.md`) edits.  All commits scope-
correct after the first-session correction was internalized to
memory.

🟢 Operator wakes up to: two feat branches both review-ready,
v0.2.0 a single Step-1.3 run + two merges away, clean Korean
brief possible from this and the morning report.

---

## Afternoon addendum (2026-05-19 ~14:00–17:30 KST, friend-meeting)

Operator brought repo to in-person meeting with the security-pro
friend.  Real first-time-user observation surfaced two clusters of
friction (now captured in `docs/ideas.md` 2026-05-19 ~17:30 entry):

**A. Permission prompts cluster** — friend's Claude Code hit ~30
per-tool approval dialogs during demo even with project settings
rendered.  Fix shipped on `feat/permission-bootstrap` (6 commits,
not yet merged):
- `scripts/install-claude-permissions.sh` — merges project allow
  list into user-level `~/.claude/settings.json`
- Template additions: `aubiotrack`/`aubioonset`/`cat`/`open`/
  `mktemp`/`date`/`dirname`/`basename`/`realpath`/`readlink`/
  `tee`/`sleep`/`seq`/`expr` (13 utilities missing)
- `scripts/test-claude-permissions.sh` — 11/11 PASS edge-case
  smoke
- Bug fix: jq `+` string concat → `\(…)` interpolation +
  strict non-empty validation
- Case study #6 EN+KO addendum capturing the field observation

**B. Demo-aesthetic constraint cluster** — operator asked for
cyberpunk-anime music video, "26년도같이" feel, no embedded text,
no copyrighted source.  Four-way constraint that no existing path
hits at once.  Solved with `feat/custom-broll-dir`:
- New `MUSIC_VIDEO_BROLL_DIR` env var on the music-video mission
  → pluggable B-roll directory (bypasses Pexels API)
- `scripts/fetch-ai-anime-broll.sh` — pollinations.ai Flux model
  → 8 cyberpunk-anime stills → ken-burns 7s mp4 each
- Demonstrated output:
  `records/missions/2026-05-19/music-video-cyberpunk-aigen-172446/
   outputs/short.mp4` (60s, 51 MB)
- Sequence of three demo iterations along the way: live-action
  Pexels (164501), trailers-with-text (anime-165615, anime-v2-170357),
  AI-gen final (aigen-172446)

**C. Operator's portability question** — "내 컴퓨터에만 있는
규칙들도 크게 문제가 없다면 repo로 가야하지 않을까?" — direct
restatement of own §8 principle 2.  Inventory + action plan
captured in `docs/ideas.md` 2026-05-19 ~17:30 entry.  Most rules
ARE already canonical in `operator-contract.md`; the gap is
bootstrap auto-installing the local affordances (hooks, launchd,
statusline).

## Open branches awaiting operator OK

```
feat/permission-bootstrap   6 commits  (per-tool prompts fix)
feat/custom-broll-dir       1 commit   (MUSIC_VIDEO_BROLL_DIR + AI-gen)
```

Both branches verified functional via the in-meeting demo session.
Neither merged to main pending operator's wake-up review.

## Where to pick up next

1. Read this section + `docs/ideas.md` 2026-05-19 ~17:30 entry.
2. Decide on permission-bootstrap merge — friend's test showed
   the fix is needed; merging into main would let any next user
   benefit.
3. Decide on custom-broll-dir merge — single commit, additive,
   adds AI-gen path as an option without disturbing existing
   demo / Pexels paths.
4. If both merged → optionally tag `v0.3.0` (permissions +
   pluggable B-roll = meaningful milestone over v0.2.0).
5. Optionally extend bootstrap.sh with hooks + scheduler
   auto-install per `docs/ideas.md` 2026-05-19 ~17:30 item 2.
